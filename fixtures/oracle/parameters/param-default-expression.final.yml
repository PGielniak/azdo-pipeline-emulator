parameters:
- name: p
  type: string
  default: x
stages:
- stage: __default
  jobs:
  - job: Job
    steps:
    - task: CmdLine@2
      env:
        OUT: '"x"'
      inputs:
        script: echo probe

