parameters:
- name: p
  type: boolean
  default: false
stages:
- stage: __default
  jobs:
  - job: Job
    steps:
    - task: CmdLine@2
      env:
        OUT: true
      inputs:
        script: echo probe

