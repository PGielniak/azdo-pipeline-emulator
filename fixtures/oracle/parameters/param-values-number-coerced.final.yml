parameters:
- name: p
  type: number
  default: '2'
  values:
  - 1
  - 2
stages:
- stage: __default
  jobs:
  - job: Job
    steps:
    - task: CmdLine@2
      env:
        OUT: 2
      inputs:
        script: echo probe

