variables:
- name: BASE
  value: base
- name: PICK
  value: from-elseif
parameters:
- name: a
  type: boolean
  default: false
- name: b
  type: boolean
  default: true
stages:
- stage: probe
  jobs:
  - job: probe
    steps:
    - task: CmdLine@2
      inputs:
        script: echo $(BASE)-$(PICK)

