steps:
- ${{ if false }}:
  - script: echo not-if
- ${{ elseif true }}:
  - script: echo selected-elseif
- ${{ elseif lt(1, 'not-a-number') }}:
  - script: echo not-later-elseif
- ${{ else }}:
  - script: echo not-else
