parameters:
- name: extra
  type: object
  default:
    MID_A: a
    MID_B: b
variables:
  BEFORE: before
  ${{ insert }}: ${{ parameters.extra }}
  AFTER: after
stages:
- stage: probe
  jobs:
  - job: probe
    steps:
      - task: CmdLine@2
        inputs:
          script: echo probe
