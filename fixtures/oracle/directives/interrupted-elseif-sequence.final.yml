stages:
- stage: __default
  jobs:
  - job: Job
    steps:
    - task: CmdLine@2
      inputs:
        script: echo interruption
    - task: CmdLine@2
      inputs:
        script: echo selected-elseif

