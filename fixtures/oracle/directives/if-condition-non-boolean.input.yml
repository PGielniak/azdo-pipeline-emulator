stages:
- stage: probe
  jobs:
  - job: probe
    steps:
      - task: CmdLine@2
        inputs:
          script: echo before
      - ${{ if 'text' }}:
        - task: CmdLine@2
          inputs:
            script: echo from-if
