stages:
- stage: __default
  jobs:
  - job: Job
    steps:
    - task: CmdLine@2
      env:
        BEFORE: before
        PICKED: else
        AFTER: after
      inputs:
        script: echo nested-mapping

