stages:
- stage: __default
  jobs:
  - job: Job
    steps:
    - task: CmdLine@2
      env:
        BEFORE: before
        PICKED: elseif
        ELSEIF_ONLY: yes
        AFTER: after
      inputs:
        script: echo mapping

