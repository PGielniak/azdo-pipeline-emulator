parameters:
- name: extra
  type: object
  default:
    script: echo merged
    displayName: Merged
stages:
- stage: probe
  jobs:
  - job: probe
    steps:
    - task: CmdLine@2
      displayName: Merged
      inputs:
        script: echo merged
    - task: CmdLine@2
      inputs:
        script: echo probe

