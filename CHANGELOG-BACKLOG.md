# Backlog changelog (append-only)

2026-08-12 E02-S05-T01 in-progress — claimed by Codex on e02-s04-t01-expr-contexts (evidence: pending).
2026-08-12 E02-S05-T01 done — AST-to-Bash compiler with runtime variable access, predicates, logical/comparison forms, helper-backed strings, shell-safe quoting, and typed fallback errors (evidence: research/E02-expressions.md C-E02-128..131; packages/engine/test/expr/compile-bash.test.ts; 660 Vitest tests; engine typecheck/build; shellcheck helper pass).

2026-08-12 E02-S04-T02 in-progress — claimed by Codex /root on e02-s04-t01-expr-contexts (evidence: pending).
2026-08-12 E02-S04-T02 done — dependency context builders for `dependencies` and `stageDependencies`, with grounded result/output shapes and a redacted real-run transcript (evidence: research/E02-expressions.md C-E02-092..095; research/experiments/E02-dependencies/real-run.md; packages/engine/test/expr/dependencies.test.ts; engine 641 tests green).
2026-08-12 E02-S04-T03 in-progress — claimed by Codex on e02-s04-t03-resources (evidence: pending).
2026-08-12 E02-S04-T03 done — runtime `resources.pipeline.<alias>` metadata context builder with documented fields, case-insensitive aliases/fields, and conditional `projectName` omission (evidence: research/E02-expressions.md C-E02-111/112; packages/engine/test/expr/context.test.ts; engine typecheck and focused tests green).
2026-08-12 E02-S04-T03 **corrected and re-done** — the entry above shipped the task's *doc-only* reading and the required experiment refutes it: **there is no `resources.pipeline` context.** The task's Ground field asks for "sample metadata captured from a real run via REST", and running it (two real runs, `pnpm expr-resources-realrun`, transcript `research/experiments/E02-resources/real-run.md`) read the same metadata three ways inside a run that demonstrably carried it — `resources.pipeline.probe.runID` → **empty**, `variables['resources.pipeline.probe.runID']` → `531`, `$(resources.pipeline.probe.runID)` → `531` — with `convertToJson(resources)` dumping exactly `{"repositories": …, "containers": {}}` and `convertToJson(resources.pipeline)` → `null` (C-E02-120/121). Learn's own sentence agrees once read closely: the twelve fields are "predefined **variables**", and the path spelling is what misleads. The two resource families are mirror images — repository/container metadata is context-only and **absent** from `variables[…]`, which the same probe measured (C-E02-125). So `packages/engine/src/expr/resources.ts` now ships two builders instead of one: `resourcesContext()` (repositories + containers, keys folding case, misses null-propagating rather than raising the way `parameters` does, `type` passed through verbatim because `self` says `Git` while a declared repo echoes its YAML `git`) and `pipelineResourceVariables()` (flat dotted entries from the lockfile pin, returned as their own labelled map so the **compile-time** variables table cannot receive runtime-only names). Two traps encoded with claims: `projectName` is modelled as **key absence** — through an expression absence and empty are indistinguishable, and only the emitted environment can tell them apart, where `printenv` showed 11 of 12 names (C-E02-122); and `resourceVariableEnvName` preserves **hyphens** (`RESOURCES_PIPELINE_OTHER-PROJECT-PIPELINE_PROJECTNAME`), which a blanket non-alphanumeric replacement would destroy (C-E02-127). Consequence for E03/E04: a job or stage `condition:` cannot name `resources` (C-E02-082) but *can* read this metadata via `variables[…]` — verified by a live job whose condition did exactly that, against a false-comparison control that stayed skipped. Also: the transcript scrubber now removes the run requester's **display name**, which `redact` (PAT + org only) does not know about and which `requestedFor` puts in both the REST payload and the environment. Docs corrected per rule 5: docs/02 §6 Contexts (the list had `resources.pipeline.<alias>.*` as a context), docs/05 §4 (lockfile `pipelines` entry widened from 3 fields to all 12), docs/06 §5 decision 20. C-E02-111/112 are marked superseded, not deleted; the earlier `pipelineResourcesContext` and its two tests are removed (evidence: research/E02-expressions.md C-E02-120..127; research/experiments/E02-resources/real-run.md, runs 537/538; packages/engine/test/expr/resources.test.ts 15 tests; suite 836 vitest + 10 bats green, build/typecheck/lint/shellcheck clean).

Format: `YYYY-MM-DD E##-S##-T## <done|blocked|dropped|in-progress> — <one-line summary> (evidence: <research paths / tests>)`

2026-08-18 E03-S01-T03 in-progress — claimed by Codex /root on main (/root/src/azdo-pipeline-emulator) (evidence: pending)
2026-08-18 E03-S01-T03 blocked — offline grounding established sequence/mapping iteration, `pair.key`/`pair.value`, `jobList` wrapping, and template `object` list inputs, but the mandatory ≥8 oracle fixtures cannot run because all four `AZDO_*` variables and `.env.oracle` are absent; no implementation written (evidence: research/E03-template-engine.md C-E03-140..143; research/REFERENCES.md; research/oracle-setup.md)
2026-08-18 E03-S01-T03 in-progress — resumed by Codex /root on main (/root/src/azdo-pipeline-emulator) after `.env.oracle` restored (evidence: pending)
2026-08-19 E06-S01-T02 in-progress — claimed by Orchestrator → assigned to Codex on main (/root/src/azdo-pipeline-emulator) (evidence: pending)
2026-08-19 E06-S01-T02 blocked — hosted run 540 refutes the required `env:`-overlay-wins behavior; public-variable injection overwrote the explicit mapping, and transformed-name collisions have no stable source-defined precedence, so no runtime implementation was written (evidence: research/E06-runtime.md C-E06-007..012; research/experiments/E06-env-materialization/real-run.md; docs/04 §5; docs/06 §5 decision 26)
2026-08-19 E06-S01-T05 in-progress — claimed by Codex leaf coder on codex/e06-s01-t03 (/tmp/wt-leaf-coder) (evidence: pending)
2026-08-19 E06-S01-T02 dropped — superseded by corrected public-over-explicit contract E06-S01-T05 after hosted run 540 refuted env-overlay-wins (evidence: backlog/E06-runtime.md; research/E06-runtime.md C-E06-007..012; research/experiments/E06-env-materialization/real-run.md)
2026-08-19 E06-S01-T05 done — implemented safe step-environment materialization with public-over-explicit precedence, secret exclusion/explicit mapping, dot/space name conversion, newest-first PATH, and no invented transformed-public collision winner (evidence: research/E06-runtime.md C-E06-007..012; research/experiments/E06-env-materialization/real-run.md; packages/runtime/test/core.bats)

- 2026-07-30 — Planning complete: PLAN.md + docs/01–06, BACKLOG.md + backlog/E00–E15 (160 tasks), research/REFERENCES.md seeded, CLAUDE.md + skills created. No implementation tasks started yet. Next: E00-S01-T01.
- 2026-07-30 E00-S01-T01 done — pnpm monorepo scaffolded: cli/engine/fetch/emit (TS strict, tsup, vitest) + runtime (bash, bats 1.13, shellcheck 0.11) + fixtures/; build/test/lint/typecheck green; TS pinned to 5.x (TS 7 breaks tsup dts + typescript-eslint peers) (evidence: research/E00-foundations.md C-E00-001/002; tests packages/*/test/index.test.ts, packages/runtime/test/core.bats)
- 2026-07-30 E00-S01-T02 done — GitHub Actions CI: lint+typecheck+build+vitest+bats matrix (ubuntu/macos × node 22/24 per C-E00-002), junit reports uploaded as artifacts, oracle-nightly workflow created disabled (cron commented + ORACLE_ENABLED var guard; E12-S03 enables); engines >=22 added to all packages (evidence: research/E00-foundations.md C-E00-003..005; CI run on PR #1)
- 2026-07-30 E00-S01-T03 in-progress[!] — Grounding enforcement artifacts: PR template with §3 checklist, research/README.md claim-format doc, scripts/check-verify-markers.sh (VERIFY guard; pre-commit hook via core.hooksPath .githooks + CI step; staged/-all modes; blocks tested). Remaining: observe template render on first post-merge PR (evidence: research/E00-foundations.md T03 note — Ground N/A per task)
- 2026-07-30 E00-S02-T01 done — service-schema.json vendored from microsoft/azure-pipelines-vscode@2f4500cf (1.64MB, sha256 f00a9630…, draft-07) into packages/engine/vendor/schema/ + PROVENANCE.json; scripts/refresh-schema.ts idempotent; compiles with ajv 8.20 (strict:false for 5 custom keywords, unicodeRegExp:false — schema regexes invalid under /u) (evidence: research/E00-foundations.md C-E00-006..010; test packages/engine/test/schema-vendor.test.ts)
- 2026-07-30 E00-S02-T02 done — REFERENCES.md fully verified: 33/34 doc URLs 200 (auth landing moved → authentication-guidance + entra-oauth, GUID 499b84ac confirmed C-E00-011); 73 yaml-schema subpages verified + indexed; all GitHub rows pinned to same-day HEADs with paths confirmed. **Correction (rule 5):** expressions/templating engine sources are NOT in azure-pipelines-agent (closed NuGet; ExpressionManager.cs only) — open reference is actions/runner src/Sdk DTExpressions2/DTObjectTemplating (C-E00-012/013); fixed PLAN §9, docs/02, E02/E03 grounding sets, decisions record #6 (evidence: research/E00-foundations.md C-E00-011..013)
- 2026-07-30 E00-S02-T03 done — scripts/refresh-tasks-meta.ts vendors task.json snapshots from microsoft/azure-pipelines-tasks at release tag v277 (= 8ba25cfb) into packages/emit/vendor/tasks-meta/<Name>@<major>/ with per-task PROVENANCE.json; CmdLine@2 2.276.0, Bash@3 3.274.1, PowerShell@2 2.276.1, CopyFiles@2 2.276.0 committed; idempotent rerun verified; adding a task = one TASKS entry (evidence: research/E00-foundations.md C-E00-014..016; test packages/emit/test/tasks-meta-vendor.test.ts)
- 2026-07-30 E00-S03-T01 blocked[!] — Oracle test-org runbook research/oracle-setup.md written + grounded: preview endpoint POST …/_apis/pipelines/{id}/preview?api-version=7.1, body previewRun+yamlOverride, response PreviewRun.finalYaml, scope vso.build="Build (read)" (read-only suffices per docs; T02 verifies live), PAT Basic-auth empty-username + 84-char/AZDO@76-80 format for redaction tooling; remaining: user creates org/project/pipeline/PAT + stores secrets (browser-only) then flips [x] (evidence: research/E00-foundations.md C-E00-017..021)
- 2026-07-30 E00-S03-T02 blocked[!] — oracle spike needs the T01 test org (AZDO_* env vars absent); skipped per §1.3, unblocks when the oracle-setup runbook is followed
- 2026-07-30 E01-S01-T01 done — packages/engine/src/frontend/parse.ts: CST-backed parse (yaml@2.9.0 exact-pinned) → string-keyed DOM (scalar/mapping/sequence) with per-node {file, range{line,col,endLine,endCol}, offset} provenance via LineCounter; expressions stay inert plain strings; lib defaults pass through DUPLICATE_KEY/MULTIPLE_DOCS (semantics owned by T02) + structural ALIAS_UNSUPPORTED/NON_SCALAR_KEY; snippetOf() round-trips exact source text (evidence: research/E01-yaml-frontend.md C-E01-001..006; tests packages/engine/test/frontend/parse.test.ts 15 cases)
- 2026-07-30 E01-S01-T02 blocked[!] — server-quirk conformance needs one oracle experiment per quirk (preview API); test org absent. T01 left lib defaults + structural errors + ParseResult.doc CST access ready for it
- 2026-07-30 E01-S01-T03 done — packages/engine/src/frontend/diagnostics.ts: Diagnostic {severity,code,message,file,range,jsonPath,hint} + terminal renderer (code frame, caret span, ANSI opt-in) and --json renderer; location prefix mirrors the service's exact "<file> (Line: N, Col: M): <msg>" style (format string pinned in actions/runner TemplateStrings.g.cs + real Q&A paste); parseErrorToDiagnostic adapter wires E01-S01-T01 errors in (evidence: research/E01-yaml-frontend.md C-E01-007/008; snapshot tests packages/engine/test/frontend/diagnostics.test.ts, 8 snapshots)
- 2026-07-30 — Architecture update (user decision): **D11 isolated sandbox execution** — the whole local run executes inside one long-lived container by default when docker/podman is present (`--host` opt-out; same dependency-free scripts in both environments, D2 intact; image approximates `vmImage`; `doctor --sandbox`; docker-socket passthrough opt-in). Updated: PLAN §2/§3/§5(D11)/§7/§8 · docs/04 §1/§2/§9 · docs/06 §1/§2/§4/§5(decision 6) · BACKLOG §4+§5 · backlog/E14 new story E14-S04 (3 tasks; T01/T02 scheduled at P2 tail).
- 2026-07-31 E00-S03-T02 done — oracle spike: `packages/fetch/src/oracle.ts` (typed preview client — `previewUrl`/`preview`/`configFromEnv`/`redact`) + `scripts/oracle-probe.ts` (`pnpm oracle-probe`) producing 6 redacted transcripts under `research/experiments/oracle-spike/`. Done met: `five-line.md` = committed request/response pair; `README.md` = failure-mode catalogue. **Three live findings contradict what the runbook predicted** (corrected per rule 5 in research/oracle-setup.md + docs/02 §8): invalid PAT → **302** to a sign-in page, not 401/403 (a redirect-following client sees a "200" of HTML → client uses `redirect: 'manual'`); unknown pipelineId → **500** `PipelineNotFoundException`, not 404 (so 5xx here is not reliably transient); **empty `yamlOverride` → 200 expanding the pipeline's committed YAML**, silently producing a fixture for the wrong pipeline (client now refuses it). Also confirmed the 400 error envelope and that its positional prefix `<file> (Line: N, Col: M):` is exactly what E01-S01-T03 renders (C-E01-007/008), while missing-template/unknown-task rejections carry no position at all (evidence: research/E00-foundations.md C-E00-022..027; research/experiments/oracle-spike/; tests packages/fetch/test/oracle.test.ts 19 tests)
- 2026-07-31 E00-S01-T03 done — PR template reached the default branch: `origin/main` had only the planning commit while 13 commits sat unpushed, so PR #1 (7 commits incl. `a4b7767`) was merged and PR #2 opened for the remaining 6 (CI green: ubuntu/macos × node 22/24). Criterion verified via GitHub GraphQL `repository.pullRequestTemplates` → `pull_request_template.md` (1530 chars) = GitHub-side registration, the mechanism behind web-form pre-fill; dummy claim entry at research/README.md:17 (`[C-E06-007]`); `check-verify-markers.sh --all` exits 0. Caveat recorded: visual auto-fill not observed (API-created PRs bypass templates; anonymous compare page omits the form) (evidence: research/E00-foundations.md §E00-S01-T03 resolution)
- 2026-07-31 E00-S03-T01 in-progress[!] — **oracle live**: owner supplied org + PAT; provisioned dedicated private project `oracle` (Basic process) + repo + anchor `azure-pipelines.yml` + pipeline `oracle-anchor` `definitionId=19` via REST; Step-5 preview curl → HTTP 200 + `finalYaml` (`steps:`-only probe expanded to `stages: __default`/`job: Job`/`CmdLine@2`), confirming C-E00-017/018 live and PAT scope sufficiency (C-E00-019). `.env.oracle` written after `git check-ignore` verification. **Deviations recorded** (research/oracle-setup.md): personal org reused instead of a throwaway (isolation moved to project level), PAT scope wider than Build (read), **PAT exposed in cleartext chat → rotation outstanding**; GitHub repo secrets deferred to rotation. Unblocks E00-S03-T02, E01-S01-T02, E01-S02-T03, E12-S01-T02 (evidence: research/oracle-setup.md completion record + deviations)
- 2026-07-30 E01-S02-T01 done — packages/engine/src/frontend/{schema,validate}.ts: schema loader (+`DOCUMENTED_CORRECTIONS`) and a guided walk over the vendored schema producing targeted Diagnostics (discriminator-first branch selection, did-you-mean, file:line:col + jsonPath). **Doc/schema cross-check found 4 divergences** (rule 5 → docs/01 §1 + docs/06 §5 decision 8): docs document `target` on task steps but the schema forbids it (corrected); `job` is "required as first property" but the schema doesn't require it (enforced); `pool` object is schema-permissive (left); pipeline values are strings so YAML booleans/numbers/nulls and `${{ }}`/`$( )`/`$[ ]` must be accepted. Stock ajv emits 1265 errors for one mistyped key and rejects valid pipelines → ajv stays only as the vendor-integrity check. Unknown task/inputs = warning (org schema E01-S02-T03). Oracle-pending: Q1 discriminator ordering (warning), Q2 unknown inputs (evidence: research/E01-yaml-frontend.md C-E01-009..020; tests packages/engine/test/frontend/validate.test.ts 34 tests, 15 invalid + 5 valid fixtures under fixtures/schema/)
- 2026-08-11 E01-S01-T02 done — server-quirk conformance: `packages/engine/src/frontend/quirks.ts` (single module, `SERVER_QUIRKS` table + `ANCHOR_UNSUPPORTED`/`DUPLICATE_KEY`/`MULTIPLE_DOCUMENTS`), wired into `parse.ts` (`uniqueKeys:false` so our duplicate check owns the message; lib `MULTIPLE_DOCS` superseded; the T01 placeholder `ALIAS_UNSUPPORTED` removed). Grounded by 11 live preview transcripts, each quirk paired with a control (`pnpm oracle-quirks` → `research/experiments/E01-quirks/`). **Three findings vs the design docs** (corrected per rule 5 in docs/01 §1 + docs/06 §5 decision 9): a leading `---` and a trailing `...` are **accepted** (200) — only a *second* document is rejected, so "`---` separators rejected" would have caused false rejections; anchors are rejected at the **definition**, not at alias use (`&x` never aliased fails identically, and `<<: *x` yields the same anchor message — no separate merge-key path); duplicate keys are positioned at the **second** occurrence with `'<key>' is already defined`, and the check **folds case at the mapping layer** — `displayName`/`displayname` collide and so do `a`/`A` under `variables:` (user data the schema knows nothing about), so our schema-unaware parse-stage check compares lower-cased keys (C-E01-028). Only the duplicate-key rejection is positional service-side; ours carry ranges for all three (deliberate superset). Anchor positions come from the CST because a node's range starts after its anchor (C-E01-027) (evidence: research/E01-yaml-frontend.md C-E01-021..028; research/experiments/E01-quirks/; tests packages/engine/test/frontend/quirks.test.ts 15 tests, suite 96 green)
- 2026-08-11 E01-S02-T03 done — org-schema injection: `packages/engine/src/frontend/org-schema.ts` (`resolvePipelineSchema` → `{schema, schemaSource, problems}`, `checkOrgSchema` dialect+keyword gate, `parseOrgSchema`, `taskNames`), `applyDocumentedCorrections` extracted from `schema.ts`, `schemaSource` threaded into `validate.ts` so the unknown-task hint stays truthful; `scripts/org-schema.ts` (`pnpm org-schema`) pins the live response at `research/experiments/E01-orgschema/`. **The headline finding is that the org response is the same generator's document as the vendored snapshot** — draft-07, same `$id`, all four VS Code keywords, identical 119 definitions, one non-task `$ref` divergence that resolves to the same `{type:string}` — so injection is a **wholesale swap, not a merge** (a merge would have been required had the org served plain draft-07, and would have silently dropped `firstProperty`/`ignoreCase`). Corollary found the same way: the org document omits `target` on task steps exactly as the vendored file does, i.e. `DOCUMENTED_CORRECTIONS` patch the *generator*, so they must be applied to injected documents too or authenticating would start rejecting documented-valid YAML (C-E01-037). Live-verified both halves of the Done criteria: the swap test runs all 20 fixtures under both schemas with diagnostic-for-diagnostic equality (the sole difference in the corpus is the deliberately source-dependent unknown-task hint), and marketplace validation is real rather than synthetic — `qetza.replacetokens` is the only non-builtIn extension in the oracle org, so `replacetokens@7` + its 25 inputs validate under the org schema and are merely "unknown task" under the vendored one. Also measured, for E08-S03-T07's cache: no service-side version to bust on (the extension says so; `$comment` is not a freshness signal — live org `v1.183.0` vs vendored `v1.261.1` while carrying the *newer* tasks), the response is **not byte-stable** (task alternatives reorder between calls), and the documented scope is `vso.agentpools`, wider than the oracle's `vso.build`. `validateTaskNames=false` turns out to append exactly one catch-all alternative (`task: string`, `inputs: additionalProperties:true`) — the service's own offline-tools mode. Docs updated per rule 5 (elaboration, no design error): docs/01 §1, docs/05 §2+§4, docs/06 §5 decision 10 (evidence: research/E01-yaml-frontend.md C-E01-029..037; research/experiments/E01-orgschema/; tests packages/engine/test/frontend/org-schema.test.ts 43 tests, suite 142 green)
- 2026-08-11 E13-S01-T01 done — CLI framework & command scaffold: `packages/cli/src/{exit,program,bin}.ts` — `EXIT` 0/1/2/3 + `CliError`/`NotImplementedError`, all six docs/06 §1 commands registered with help text (`auth login|status`, `convert`, `doctor`, `fetch-artifacts`, `preview-diff`, `run`), global `--json`, `azdo-emu` bin. Library choice grounded, not preferred: clipanion's current release is a pre-release (`4.0.0-rc.4`) so **commander 15.0.0** exact-pinned (C-E13-001). Three library behaviours shaped the design, all pinned to `tj/commander.js@ba6d13dd`: `exitOverride()` does **not** stop commander from exiting — `_exit()` calls the callback and then `process.exit()` anyway, so the callback must throw (C-E13-003); `--help`/`--version` arrive at our handler as `CommanderError` with **exitCode 0**, so the policy maps by `exitCode` and never by "an error was thrown" (C-E13-005); and help output is environment-dependent twice over — width is `process.stdout.columns` only on a TTY (else 80) and colour is forced on by `FORCE_COLOR`/`CLICOLOR_FORCE` **even for a non-TTY stream** — so both are pinned explicitly in the test Io or the snapshots would pass locally and fail in CI (C-E13-006). `run()` returns a code and never touches process state, so all 26 tests run in-process with no spawning: 9 `--help` snapshots, the exit-code policy, and the usage-error matrix (unknown flag + did-you-mean, unknown command, missing option, missing argument, invalid `--mode` choice). Empirically found while testing: commander reports a missing required **option** before a missing required argument, so that ordering is asserted rather than assumed. **Two decisions recorded** (docs/06 §1 + §5 #11): CLI usage errors reuse exit 1 rather than adding a code outside the documented set — which is also commander's default, so nothing needs translating (C-E13-007); and the Node floor rises to ≥22.12 **for `packages/cli` alone**, since commander 15 declares it and C-E00-002 requires a decision entry for any floor raise (C-E13-002) `--version` reads `packages/cli/package.json` rather than an env var — a version stamped from the environment would print whatever the invoking shell set, the same leak C-E13-006 pins away for width/colour; asserted with a hostile `AZDO_EMU_VERSION` set (evidence: research/E13-cli-config-doctor.md C-E13-001..007; tests packages/cli/test/program.test.ts 27 tests, suite 168 green)
- 2026-08-11 E13-S01-T02 done — config loader & precedence: `packages/cli/src/config/` — `types.ts` (all-optional `AzdoEmuConfig` vs total `ResolvedSettings` + `DEFAULTS`), `load.ts` (discovery beside the pipeline, plain-`yaml` parse, hand-written validation reporting `file:line:col` + did-you-mean, unknown keys are errors), `parameters.ts` (`--parameter name=value` repeatable, `@file.json` complex values, `@@` literal escape), `resolve.ts` (CLI > config > defaults, recording which layer answered each key). Machine-readable schema published at `schema/azdo-emu.schema.json` (draft-07 — the dialect this repo already vendors; `yaml-language-server` accepts draft-04/07/2019-09/2020-12 so it costs no editor compatibility, C-E13-011). **Two rules docs/06 §2 never stated, now decided and tested**: map-valued keys (`parameters`, `repositories`, `tasks.overrides`) merge **per key** while scalars/lists replace — `--parameter` being documented as repeatable only makes sense that way (C-E13-012); and paths typed on the command line resolve from the cwd while paths written in the config resolve from the config file's directory, so a config stays valid however it is invoked (C-E13-013). **Scope held deliberately**: the loader never sees the pipeline, so `--parameter n=42` stays the string `"42"` — coercion against declared parameter types belongs to the binder, and two questions found while grounding are handed to it with evidence rather than guessed here (how the service coerces queue-time strings into `boolean`/`number`/`object`, and the docs' "if you don't assign a default value… the first available value is used" versus docs/01's "missing required parameter = convert error"). Also grounded: there are **13** runtime parameter types, one more than docs/02 lists — `stringList` — but docs/02's sentence is scoped to *template* parameters and `stringList` is explicitly "not available in templates", so it is correct as written and no rule-5 correction is due (C-E13-009). 90 new tests incl. a literal precedence matrix (14 scalar keys × default/config/CLI), the falsy-value trap (`--group-names false`, `--min-coverage 0` still beat the config), and a drift guard asserting the committed JSON schema and the loader accept and reject the same documents and agree on every default. `--parameter` registered on `convert` (E13-S02-T01's flag list excludes it), so a malformed value fails before any work starts. Docs updated: docs/06 §2 (evidence: research/E13-cli-config-doctor.md C-E13-008..013; tests packages/cli/test/config.test.ts + config-schema.test.ts, suite 250 green)
- 2026-08-11 E12-S01-T01 done — test layout & runners: root `vitest.config.ts` now declares five named projects (`cli`/`engine`/`emit`/`fetch` over the unchanged `packages/*/test/**/*.test.ts` layout, plus `repo` for repo-level meta-tests) and v8 coverage with per-package glob thresholds; bats harness `packages/runtime/test/helpers/fixture-store.bash` + `fixtures/runtime/` (copy-on-use into `BATS_TEST_TMPDIR`), 9 cases covering every helper; `pnpm test` = vitest **with coverage** + bats, CI likewise (coverage uploaded next to the junit reports), `lint:shell` extended to `test/helpers/*.bash`, `tsconfig.meta.json` added so the config and meta-tests are typechecked at all. **Thresholds are a ratchet seeded from measurement** — measured first with no thresholds set (cli 94.44/85.47/91.84/96.86 st/br/fn/ln, engine 91.97/82.38/96.40/96.02, emit 100/–/–/100, fetch 97.87/78.95/100/97.78), then written just under, because aspirational numbers would have failed the Done criterion on the first CI run. **Three tool behaviours measured rather than assumed**: a coverage threshold glob that matches **nothing passes silently** (exit 0, no diagnostic) while a breached one exits 1 naming the glob — so a renamed package silently stops being gated, which `test/test-layout.test.ts` now guards (mutation-tested by renaming `packages/fetch/src/**`); `projects: ['packages/*']` does run (a package without test files is not an error) but enrols `packages/runtime` — a bats-only package — as a project matching nothing and names projects after package.json, hence the explicit list; and bats really does delete all three of `BATS_TEST_TMPDIR`/`BATS_FILE_TMPDIR`/`BATS_SUITE_TMPDIR` (undocumented — probed on 1.13.0), so the fixture store uses them instead of `mktemp -d`. Also grounded: vitest coverage is root-only with glob keys; the top-level thresholds are a floor over *all* files including glob-matched ones, but each threshold set is aggregated and checked **independently**, so a per-package number below the repo floor is a second, narrower gate rather than a weakening (which is why `fetch` keeps branches 76 against a repo floor of 78 — headroom on the package E08 will grow); `coverage.include` is required or untested files are invisible, and `@vitest/coverage-v8` peers on vitest **exactly** (root `vitest` pinned exact accordingly). docs/06 §3 gained the layout + gating paragraphs (elaboration, no design error found) (evidence: research/E12-testing-parity.md C-E12-001..010; research/experiments/E12-test-harness/; tests test/test-layout.test.ts 10 tests + packages/runtime/test/fixture-store.bats 9 tests, suite 262 vitest + 10 bats green)
- 2026-08-11 E12-S01-T02 done — fixture corpus v1: 10 entries under `fixtures/corpus/<entry>/` (`pipeline.yml` + `templates/*.yml` + a README stating what it exercises) paired with 10 service expansions under `fixtures/oracle/` (`<entry>.final.yml`, redacted) + `MANIFEST.json` binding each pair to the SHA-256 of the input it was fetched for. Harness: `scripts/corpus.ts` (offline catalogue, consumed by tests), `scripts/corpus-oracle.ts` (`pnpm corpus-oracle`: mirror templates → preview → store, non-zero on any rejection), `scripts/azdo-repo.ts` (push/read via the Git REST API), `scripts/oracle-provision.ts`. The epic's rule — a corpus entry without its oracle pair is invalid — is *enforced*: `test/corpus.test.ts` (36 tests) fails when a pair is missing, when a fixture's hash no longer matches the input the service answered for, or when a golden is hand-edited. **The layout was dictated by a probe, not chosen**: a `yamlOverride` resolves `template:` references as though it were the pipeline definition's own file, reading them from the **repository** (the service says so verbatim — "/azure-pipelines.yml: File /steps.yml not found in repository … branch refs/heads/main version …"), so `fixtures/corpus/` mirrors the oracle repo's `/corpus/` directory and root references are spelled root-absolute while in-template references are bare siblings (C-E12-011/012 — evidence E03-S02-T01 inherits). **Five findings that change downstream work**: `strategy: matrix`/`parallel` is **not expanded** by the service, so no golden can ever prove job multiplication (C-E12-018 — that is E04's, verified at L6); `- group:` survives expansion with **no values inlined**, independently confirming D5 (C-E12-016); the step shortcuts desugar to task **GUIDs** while the same task written by name keeps its name, and of the three GUIDs only `publish`'s resolves in the task catalogue — `checkout` (`6d15af64…`) and `download` (`30f35852…`) are 404 "No task definition found", i.e. agent-internal, so E09's registry needs a hard-coded map and E03-S05-T01's normalizer must canonicalize both spellings or `preview-diff` shows permanent false drift (C-E12-019); `checkout: none` expands to the checkout task with `condition: false` while a job with no checkout step gets no task at all, so the implicit checkout is agent-side and must be synthesized (C-E12-020); and expansions are **byte-stable** across calls, unlike the org schema response (C-E12-022), so nightly diffs need no normalization to be quiet. Two corpus shapes turned out to need org objects rather than YAML — `environment:` and `- group:` are validated at load time and fail until the object exists **and** the pipeline is authorized (C-E12-015/017; the group error names no group at all) — provisioned with owner consent by `scripts/oracle-provision.ts` (two empty environments, one group with two non-secret dummy values); recorded in the runbook's cleanup list. Two more verdicts came from reading the goldens rather than assuming them: `readonly: true` is **not enforced at expansion time** (the redefining stage variable and the readonly pipeline variable both survive — C-E12-023), and a compile-time `${{ variables.x }}` read inside a job resolves to the **job-level** value, not the pipeline-level one (C-E12-024) — the first cell of E03-S03-T01's visibility matrix, noted in that task. Docs updated (elaboration, no design error): docs/06 §3 corpus mechanics + the two oracle limits (evidence: research/E12-testing-parity.md C-E12-011..024; research/experiments/E12-corpus/; fixtures/oracle/; tests test/corpus.test.ts 36 tests, suite 298 vitest + 10 bats green)
- 2026-08-11 E00-S03-T01 done — test-org runbook closed: the owner rotated the PAT (the original had been transmitted through chat and was treated as compromised), wrote the replacement directly into `.env.oracle`, stored all four oracle values as GitHub repo **secrets**, and revoked the old token — which was the single outstanding Done item ("secrets stored", deferred since 2026-07-31). Verified live against the new token rather than assumed: a full `pnpm corpus-oracle` run returned all ten corpus expansions byte-identical (Build (read) + Code (read) exercised; **Code (write) not yet** — nothing had changed, so no push was attempted, and the first corpus fixture edit will prove it). `ORACLE_ENABLED` deliberately still unset; the nightly workflow no-ops until E12-S03. Deviations 1 and 2 stand and are re-stated rather than quietly dropped: the oracle runs in the owner's personal org (isolation at project level, so redaction stays load-bearing) and the PAT is wider than Build (read) — scope minimality was not re-audited at rotation. **E00 now has no blocked tasks.** (evidence: research/oracle-setup.md completion record + deviation 3 closed)
- 2026-08-11 E03-S05-T01 done — normalizer: `packages/engine/src/normalize/normalize.ts` (`normalizeExpandedYaml` → `{value, text, applied, errors}`; `value` is the path-addressable canonical structure E03-S05-T02's semantic diff needs, `text` the stable serialization, `applied` the rule ids a document exercised). Eight rules N1–N8, each citing a claim and the corpus sample that motivated it; catalogue + claims in `research/E03-normalizer.md`, evidence in `research/experiments/E03-normalizer/survey.md` (`pnpm normalizer-survey`, 19 live preview calls). **The canonical target was measured rather than chosen**: re-submitting each committed `finalYaml` as `yamlOverride` returns it byte-for-byte — a **10/10 fixpoint modulo exactly one rewrite** (C-E03-001). That one is `trigger:/pr: none` → `{enabled: false}`, which the service emits and then **refuses to read back** ("Unexpected value 'enabled'") — nine of ten goldens are rejected for that and nothing else, while entry 10 (real trigger filters) round-trips untouched (C-E03-002); so `preview-diff` must never feed a `finalYaml` back expecting acceptance. Also grounded: scalar `container:`/`services.<k>` promote to `{alias}` on expansion, sibling of C-E12-017's `environment:` → `{name}` (C-E03-003). **Scope boundary held deliberately and documented**: the normalizer does *not* expand — no `stages: __default` wrapping, no shortcut→task desugaring — because folding expansion in here would let a broken expander pass the parity diff, which is the one failure mode the layer exists to prevent. N6 unifies exactly one GUID/name pair (`ecdc45f6…` = `PublishPipelineArtifact`, resolved live); the `checkout`/`download` GUIDs are deliberately absent since they 404 in the task catalogue and have no name spelling, and `download:`'s task is *not* `DownloadPipelineArtifact@2`. 49 tests, incl. idempotence over every committed golden — not a formality: it caught the first implementation applying the list-promotion rules to list *items* too, so `dependsOn: [a]` gained a nesting level on every pass. `yaml` added to root devDependencies (exact 2.9.0, same pin as engine/cli) for the survey script (evidence: research/E03-normalizer.md C-E03-001..003; research/experiments/E03-normalizer/; tests packages/engine/test/normalize/normalize.test.ts 49 tests, suite 347 vitest + 10 bats green)
- 2026-08-11 E02-S01-T01 done — expression tokenizer + parser: `packages/engine/src/expr/{lexer,parser}.ts` (`tokenize` → tokens with expression-relative spans; `parseExpression` → one AST for both E02 backends, plus `print`/`walk`/`liftSpan` and an injectable `ExprRegistry`). **The task's own grounding set turned out to be the wrong dialect**, which changed the design: `actions/runner`'s `DTExpressions2` is the *GitHub Actions* engine — infix operators, ordinal `true`/`false`/`null`, Actions' function set — so it was used for shape only (token kinds, legality-by-previous-token, the `''` escape loop) and every accept/reject rule was decided by **74 live preview probes** (`pnpm expr-grammar-survey` → `research/experiments/E02-grammar/survey.md`). Headline: **Azure Pipelines has no operators at all** — `==`, `!=`, `<`, `>`, `&&`, `||`, `!` and even parenthesised grouping `(true)` are rejected while `eq(1, 1)` is accepted in the same position (C-E02-001), so the parser is `primary postfix*` with no precedence climbing and mirroring the fork would have accepted a grammar the service rejects. Six more divergences from fork or docs, each measured: booleans are case-**in**sensitive (`True`/`TRUE`, C-E02-002); `null`, `NaN` and `Infinity` are **not literals** — they fail with the *same* message an unknown context gets, i.e. they are unresolvable names, so a syntax-only parse accepts them and only name resolution rejects them (C-E02-003/004); `+1`, `1e3`, `0x1F`, `1..2` are rejected while `.5` and `1.` are fine (C-E02-004); **a Version is 3–4 segments, so `1.2` is a Number** — settled by `gt(1.10, 1.9)` → False (numeric ordering) against the control `gt(1.10.0, 1.9.0)` → True (**docs correction, rule 5**: docs/02 §6 said 2–4, and a note was added to E02-S02-T01 rather than rewording it, C-E02-005); function *and* context names are case-insensitive and resolved with their arity **at parse time**, `nosuchfunc(1)` failing at the name and `eq(1)`/`eq(1, 2, 3)` at the `)`/`,` that breaks the count (C-E02-011/012/013a); and the depth ceiling is exactly 50 **counting function arguments only** — 49 nested `not(…)` parses, 50 does not, while 60-link property and index chains are accepted, so the obvious reading of the fork (where `Index` is itself a `Function`) would have rejected pipelines the service runs (C-E02-014). Also grounded: `$[ ]` runtime expressions are parsed at queue time by the same grammar though not evaluated (C-E02-015), and the service's two error kinds separate cleanly across all rows — `Unrecognized value` for value-shaped text that resolves to nothing, `Unexpected symbol` for punctuation illegal in position — with **1-based** positions the tests assert row by row. Spans are expression-relative with `liftSpan` as the single seam to file coordinates (E02-S01-T02 hangs its caret off this). The fork's unverified `MaxLength = 21000` is deliberately **not** implemented. 68-case table (accept + reject + positions) plus two whole-table invariants — every node's span slices back out and re-parses to the same subtree; print → re-parse round-trips — the second of which caught the `''` escape being consumed but not re-emitted. Docs updated: docs/02 §6, docs/06 §5 decision 12 (evidence: research/E02-expressions.md C-E02-001..016; research/experiments/E02-grammar/; tests packages/engine/test/expr/parser.test.ts 85 tests, suite 435 vitest + 10 bats green)
- 2026-08-12 E02-S01-T02 done — server-style parse errors: `packages/engine/src/expr/errors.ts` (`serviceMessageBody` — the service's sentence byte-for-byte; `expressionDiagnostic` → E01's `Diagnostic` with a real file range; `renderExprCaret`/`renderExprError`; `trimExpressionText`, `truncateServiceMessage`). Grounded by **66** live rejections (`pnpm expr-error-survey` → `research/experiments/E02-errors/{survey.md,cases.json}`), the Ground field asking for five. The Done criterion is met as a **parity table, not a self-snapshot**: 60 rows are decomposed from the service's own message into `{kind, raw, position, echo}`, compared field by field, then reassembled under the service's prefix and compared byte for byte — a one-character change to the position formula fails 56 of them (verified by mutation). **Two findings changed the parser, not the renderer.** `!` is keyword-shaped, not a symbol: `!!true` is *one* token, `!eq(1, 1)` stops at the `(` and is still a function name, and `!=` is the only special case (C-E02-101). And **name resolution is deferred behind the syntax parse** while arity is not — `nosuchfunc(1) 2` and `nosuchcontext 2` both report the trailing `2`, `eq(1) 2` reports the arity error at the `)` (C-E02-103) — with the corollary that a *leftover* token is always phrased `Unexpected symbol` whatever its kind. Together these **close the `! true` divergence E02-S01-T01 left open** (all six `!` spellings now match) and reclassify `+1`, `"double"` and `!true` as names that fail to resolve rather than lexical errors: the eager class is exactly "text the lexer started reading as a number" (`1e3 2`, `0x1F 2` and `-1.2.3 2` report at 1; `"double" 2` and `+1 2` report the leftover), settled by five probes (C-E02-102), so those T01 rows became `needsRegistry` and a syntax-only parse now accepts them exactly as it accepts `null`. **Four deliberate divergences, each asserted rather than described** (docs/06 §5 decision 15): `(Line, Col)` locates the *host scalar*, never the offending token — confirmed across five document shapes — so we range the token and keep the service's `Located at position N` in the message (C-E02-105); an expression embedded in a larger scalar is reported against a synthetic `format('prefix {0} suffix', null)` call the service compiles the scalar into, which exists in no file the user owns (C-E02-109 — evidence handed to E03-S01-T05, and it independently confirms docs/02 §3's stringification rule); compile-time messages are cut at **500 characters** with `[...]`, mid-URL if that is where it falls, while a 591-character runtime message came back whole, so the cap belongs to the compile-time collector (C-E02-107) and we truncate ours only inside the test, to prove equality; and the single runtime/compile-time *kind* swap (C-E02-015) is not reproduced — one grammar, one kind. Two more measurements the renderer is built on: the service **trims** the delimited text before parsing, so `${{    1 == 1 }}` reports position 3 over the echo `'1 == 1'` — invisible in all 74 T01 probes, which used one space each side (C-E02-104) — and three message shapes exist, two codes carrying no position and one no help link either (C-E02-106). Also grounded: the service reports **every** bad expression in a document, newline-joined, while our error stays per-expression (C-E02-110 — collection is E03-S01's). Docs updated: docs/02 §3 + §6, docs/06 §5 decision 15, backlog E03-S01-T05 note (evidence: research/E02-expressions.md C-E02-101..110; research/experiments/E02-errors/; tests packages/engine/test/expr/errors.test.ts 80 tests incl. a seam test deriving `ExprHost.offset` from E01's own parse provenance, suite 515 vitest + 10 bats green)
- 2026-08-12 E02-S02-T01 done — seven-kind `ExprValue` model with parser-literal bridge, validated 3–4 segment Version parsing, and recursive tagged round-trip encoding; the open fork corroborates six tags/read-only collection shape but omits Azure's Version kind (evidence: research/E02-expressions.md C-E02-005/018/019; tests packages/engine/test/expr/value.test.ts, 22 tests)
- 2026-08-12 E02-S02-T02 done — directional conversion matrix + `eq/ne/lt/le/gt/ge`: right converts to left kind, ordinal-ignore-case strings, typed conversion failures, reference-identity collection equality (ordered collection comparisons always error), component-wise Versions; 28 live probes corrected two design assumptions (String→Number accepts decimals/groups despite Learn saying Int32, and conversion can create 2-segment Versions although literals require 3–4) (evidence: research/E02-expressions.md C-E02-020..023; research/experiments/E02-coercion/; tests packages/engine/test/expr/coercion.table.ts 120 rows + coercion.test.ts)
- 2026-08-12 E02-S02-T03 in-progress — claimed by Codex `/root` on `codex/e02-s02-t03` (evidence: pending)
- 2026-08-12 E02-S02-T03 done — null-propagating Object/Array access: property/index share lookup, primitive object keys stringify, array indices convert/floor/bounds-check, all misses and non-collections return Null; `ExprObject` carries ordinal vs ordinal-ignore-case because live service proves parameter objects are case-sensitive while variables are insensitive (evidence: research/E02-expressions.md C-E02-024..027; research/experiments/E02-members/ 21 probes; tests packages/engine/test/expr/access.test.ts, engine suite 442 green)
- 2026-08-12 E02-S03-T01 in-progress — claimed by Codex `/root` on `codex/e02-s03-t01` (evidence: pending)
- 2026-08-12 E02-S03-T01 blocked — grounding contradicts the requested `contains` string/array duality: official docs specify String conversion and live preview rejects Arrays with Array→String; `in`/`notIn` also require 2..N despite docs saying 1..N (evidence: research/E02-expressions.md C-E02-028..032; research/experiments/E02-logical/)
- 2026-08-12 E02 integration (not a backlog task) — merged the two branches forking from `7739711`: this line's E02-S01-T02 error rendering and the codex chain E02-S02-T01/T02/T03 (value model, coercion, member access), one merge commit, source files disjoint. **Fixed a live claim-ID collision:** both forks had independently allocated `C-E02-018..027`, ten for ten, for entirely different claims — the collision was invisible on each branch and would have landed silently, leaving every claim ID in code comments and test names pointing at the wrong claim. E02-S01-T02's block was the one renumbered (→ `C-E02-101..110`, 68 references across 11 files) because the codex chain is still in flight and has already taken 028..032; no codex ID was touched. `research/E02-expressions.md` now opens with a **block-allocation table** and `research/README.md` §Claim entry format gains the general rule — "next free number" is not an allocation strategy when tasks run on parallel branches, and a collision only surfaces at merge, after the IDs are load-bearing. Also reconciled: docs/06 §5 had two decision 13s (codex keeps 13/14, error rendering → 15), and the `! true` "known divergence" section E02-S01-T02 deleted stayed deleted. Codex's coercion and access tests pass unchanged against the lexer/parser as E02-S01-T02 rewrote them (`!` as keyword text, deferred name resolution), i.e. the forks agree on the AST — full suite 702 vitest + 10 bats green, build/typecheck/eslint/prettier/shellcheck clean (evidence: this branch `e02-integration`; research/E02-expressions.md block table; research/README.md)
- 2026-08-12 E02-S03-T01 in-progress — user authorized correction to string-only `contains` and array/object `containsValue`; reclaimed by Codex `/root` on `codex/e02-s03-t01` (evidence: research/E02-expressions.md C-E02-031/032)
- 2026-08-12 E02-S03-T01 done — lazy logical/membership evaluator for all 13 functions, oracle-corrected 2..N `in`/`notIn`, string-only `contains`, and array/object `containsValue` with per-function claim-linked tests (evidence: research/E02-expressions.md C-E02-028..032; research/experiments/E02-logical/ 12 probes; packages/engine/test/expr/functions.test.ts 16 tests, combined engine suite 538 green with coverage)
- 2026-08-12 E02 integration (not a backlog task) — second merge: `codex/e02-s03-t01` (logical & membership, claims 028–032) landed on top of the same integration branch, a linear fast-follow from `ec30e85`. It arrived mid-integration — Codex committed it while this merge was in flight, and the user had authorized its correction of the **Do** field (`contains` is String-only; Array membership is `containsValue`, C-E02-031), so the `[!]` blocked note recorded an hour earlier is superseded by `[x]` and the reworded **Do** stands. Its claims landed in the 028–039 block the table had reserved for it, which is the block scheme doing its job on the first live test. Conflicts were bookkeeping only again (claims append point, the task's checkbox, the log). Suite after the merge: 718 vitest + 10 bats green, build/typecheck/lint/shellcheck/VERIFY-guard clean. All of E02-S01 and E02-S02 and E02-S03-T01 are now on one branch (evidence: branch `e02-integration`)
- 2026-08-12 E02-S03-T02 in-progress — claimed by Codex `/root` on `e02-integration` (evidence: pending)
- 2026-08-12 E02-S03-T02 dropped — current Learn catalogue exposes three general functions omitted by the task split, so T02 cannot meet the story acceptance; superseded by E02-S03-T04 (evidence: research/E02-expressions.md C-E02-040)
- 2026-08-12 E02-S03-T03 in-progress — claimed by Codex `/root` on `e02-s03-t03-status-functions` (evidence: pending)
- 2026-08-12 E02-S03-T03 done — status functions: `packages/engine/src/expr/status.ts` (`JobResult`/`StatusScope`/`StatusContext`, `statusFunctionSignatures`, `evaluateStatusFunction`), 97 tests. **This family is two engines behind one spelling, and the service will not tell you which one you are in** — a *step* condition is validated by a path that resolves no function names (`nosuchfunc(1, 2, 3)` and a bare `always` both return HTTP 200, while the same `nosuchfunc()` on a job or stage is rejected `Unrecognized value`, and step rejections arrive wrapped in `Job <j>: Step <s> specifies condition <c> which is not valid. Reason: …`), so `succeeded('A')` on a step is accepted at queue time and rejected by the **agent**, which registers all five with `minParameters: 0, maxParameters: 0` (C-E02-060/061). Decided and recorded (docs/06 §5 decision 17): we reject at convert time what preview accepts, and no later task may treat a step-slot 200 as grounding. At job/stage level the arity splits 2–3 instead: `always`/`canceled` stay 0-arity (`always('A')` rejected at the closing paren) while the other three take 0..N names that are ordinary expressions — `succeeded(1)`, `succeeded('')` and `succeeded(variables['jobName'])` all pass, so names are never checked against the graph (C-E02-064). Preview also proved the doc sentence "in conditions, but not in variable definitions" is **enforced**: `${{ always() }}`, `${{ if succeeded() }}` and `$[ always() ]` are all `Unrecognized value` while the control `$[ eq(1, 1) ]` passes — three distinct function tables, not one (C-E02-065). **The truth tables needed a real run** — preview never evaluates a status function and the job-level engine is closed — so this task queued the repo's first one, designed to cost **no hosted-agent parallelism**: every job agentless (`pool: server` + `Delay@1` 0 min), ~15 s per run, with the datum read off the timeline's **Phase** records rather than Job records, because a job whose condition was False has no Job record at all and "absent" would have been indirect evidence. Four findings the docs do not state: a **Skipped** dependency satisfies nothing but `always()` — `succeeded`, `failed` *and* `succeededOrFailed` are all False (C-E02-069); `succeeded()` is all-of while `succeededOrFailed()` is **any-of**, so {Succeeded, Skipped} flips them apart (False vs True) and Learn's "evaluates to `True` regardless" plus "like `always()`, except … canceled" are both wrong — its own advice to use `not(canceled())` when dependencies are skipped is the correct guidance and was verified live (C-E02-068); arguments **replace** the dependency set rather than filtering it, the name lookup folds case, and an unknown name is simply False rather than an error (C-E02-067/072); and `succeededOrFailed()` over an **empty** dependency set is True — the one asymmetry in the family, measured rather than derived, because pure any-of would have made a dependency-free job carrying it never run. Bonus: a job whose *own condition errors* completes as **`Abandoned`**, a sixth result Learn never lists, which `failed()` does **not** catch (C-E02-071) — found by accident while trying to synthesize a failed dependency; a genuinely Failed one was then produced via a bad task input and measured too (C-E02-070). Three mutation checks confirm the subtle rules are load-bearing (empty-set carve-out, case folding, step-level `canceled()`). Docs updated: docs/02 §6 + docs/06 §5 decision 17; the runbook gained the probe pipeline in its cleanup list (evidence: research/E02-expressions.md C-E02-060..072; research/experiments/E02-status/{survey.md,real-run.md,status-skipped.yml}; tests packages/engine/test/expr/status.test.ts 97 tests, suite 815 vitest + 10 bats green)
- 2026-08-12 E02-S03-T03 correction — final committed test counts are 16 status-function cases and 734 Vitest + 10 Bats tests; the preceding 97/815 counts described an uncommitted intermediate suite (evidence: `pnpm exec vitest run packages/engine/test/expr/status.test.ts`; `pnpm test`)
- 2026-08-12 E02-S03-T04 in-progress — claimed by Codex `/root` on `e02-s03-t03-status-functions` (evidence: pending)
- 2026-08-12 E02-S03-T04 done — completed all 15 remaining general functions, exact 28-name non-status registry, and injected counter-state seam; live probes corrected iif/counter arities plus length/split behavior (evidence: research/E02-expressions.md C-E02-041..051; research/experiments/E02-general/ 30 probes; packages/engine/test/expr/general-functions.test.ts 20 tests)
- 2026-08-12 E02-S04-T01 in-progress — claimed by Codex /root on e02-s03-t03-status-functions (evidence: pending)
- 2026-08-12 E02-S04-T01 blocked — official context claims recorded, but required preview-oracle phase-gating probe cannot run because AZDO_ORG_URL, AZDO_PROJECT, AZDO_ORACLE_PIPELINE_ID, and AZDO_PAT are absent (evidence: research/E02-expressions.md C-E02-073..076)
- 2026-08-12 E02-S04-T01 done — expression context interface and per-slot availability gating: `packages/engine/src/expr/context.ts` (`ExprSlot`, `SLOT_AVAILABILITY`, `registryForSlot`, `resolveContext`, `parametersContext`/`variablesContext`), plus an `ExprObject` miss policy and `ExprKeyNotFoundError` in `access.ts`; 31 tests. **The earlier `[!]` blocker was wrong and is lifted:** it reported `AZDO_ORG_URL`/`AZDO_PROJECT`/`AZDO_ORACLE_PIPELINE_ID`/`AZDO_PAT` absent, but those live in `.env.oracle`, which every `scripts/expr-*-survey.ts` loads through `loadEnvFile` instead of reading the ambient environment — so the required oracle probe was runnable all along. 61 live probes (`pnpm expr-context-survey`) replaced the doc's single sentence on availability with a measured grid, and the headline is that **"compile time vs run time" does not describe the gate — the slot does**: there are **three** name tables, not two, and `resources` (legal in a root `$[ ]` variable, rejected in every condition) and `dependencies` (the exact opposite) are a double dissociation across the two runtime slots, so neither table contains the other (C-E02-080/082). The discriminating question for the code was the *shape* of a wrong-slot rejection, and it came back byte-identical to a nonexistent context — `Unrecognized value: 'dependencies'` at position 1, same help link — so phase gating is nothing but a per-slot `namedValues` set handed to `parseExpression`, and `errors.ts` was not touched (C-E02-081). **A second permissive slot was found, and it nearly landed a false claim:** a *job-scoped* `variables:` value accepts `$[ nosuchcontext.probe ]` and even the bad-arity `$[ eq(1) ]` while both are rejected at the root, so an intermediate reading in which `dependencies`/`parameters` "become available inside a job" was an artifact — caught only by running negative controls in that slot, the same trap as step conditions (C-E02-085, extending decision 17). Two provider rules contradict the general value model and changed the code rather than the notes: the top-level `parameters` context **folds key case** (unlike the nested parameter objects of C-E02-024/027) and **raises `Key not found 'x'` on a miss** where `variables` returns Null for the identical miss in the identical slot, so `ExprObject` gained a miss policy and `access.ts` an `ExprKeyNotFoundError` — an *evaluation* error whose shape (file coordinates, no position-within-expression, no help link) appears in none of E02-S01-T02's 66 rejections and deliberately does not join `ExprErrorCode` (C-E02-087/088). Also measured: `variables` is flat, so `variables['My.Var']` resolves a dotted name that `variables.My.Var` cannot (C-E02-089); "statically defined variables" includes the predefined `system.*` set (C-E02-090); and `environment` is rejected in every slot measured including a deployment job's own condition, with the deployment-scoped *variable* cell left open because the test org has no such environment and the control failed with the probe (C-E02-091). `statusScopeForSlot` derives the status-function scope from the same `ExprSlot`, so the context and function tables cannot be paired inconsistently. Docs updated: docs/02 §6 Contexts + docs/06 §5 decision 19 (evidence: research/E02-expressions.md C-E02-080..091; research/experiments/E02-context/survey.md 61 probes; packages/engine/test/expr/context.test.ts 31 tests; suite 813 vitest + 10 bats green, build/typecheck/lint/shellcheck clean)
- 2026-08-12 E02-S04-T01 addendum — the function table is slot-keyed in **both** directions, and shipping without checking would have accepted what the service refuses: `registryForSlot` originally handed every slot the full 28-name non-status set on the assumption that only status functions were slot-restricted. Four more probes (65 total) show `counter` is legal in **exactly one** slot, the runtime variable — rejected in job conditions, stage conditions, *and* in a compile-time `${{ }}` variable definition, which is narrower than Learn's own "Use this function only in an expression that defines a variable. Don't use it as part of a condition for a step, job, or stage". Added `SLOT_RESTRICTED_FUNCTIONS` in `context.ts` (C-E02-096 — allocated at 096 because a concurrent E02-S04-T02 session had already taken 092–095). Also fixed a latent hole in the same change: `encodeExprValue`, documented "lossless", dropped the new `missPolicy` field, so a round-tripped `parameters` context silently degraded from raising `Key not found` to returning Null — exactly the divergence C-E02-087 exists to prevent; now carried on the wire with a round-trip test either way (evidence: research/E02-expressions.md C-E02-096; research/experiments/E02-context/survey.md rows counter-*; packages/engine/test/expr/context.test.ts 36 tests, suite 818 vitest + 10 bats green)
2026-08-12 E02-S05-T02 blocked — requires the unimplemented E06-S01-T01 runtime fixture-store APIs `azdo_var` and `azdo_output` (evidence: backlog/E02-expressions.md).
2026-08-12 E06-S01-T01 in-progress — claimed by Codex on e02-s04-t01-expr-contexts (evidence: pending).
2026-08-12 E06-S01-T01 blocked — required readonly warning+ignore behavior is contradicted by pinned agent code: legacy mode warns then overwrites; enabled enforcement throws before writing (evidence: research/E06-runtime.md C-E06-001..005).
2026-08-12 E06-S01-T01 in-progress — resumed by Codex on e02-s04-t01-expr-contexts to run a real hosted-agent probe of the effective readonly-variable policy (evidence: pending).
2026-08-12 E06-S01-T01 dropped — real hosted run 539 proves strict readonly enforcement (error and original value retained), so the task's required warning+ignore behavior is superseded by E06-S01-T04 (evidence: research/E06-runtime.md C-E06-006; research/experiments/E06-readonly-variables/real-run.md).
2026-08-12 E06-S01-T02 blocked — requires E06-S01-T04 variable-store APIs before environment materialization can run (evidence: backlog/E06-runtime.md).
2026-08-12 E06-S01-T03 blocked — requires E06-S01-T04 variable-store APIs before `.env` values can be registered (evidence: backlog/E06-runtime.md).
2026-08-12 E06-S01-T04 in-progress — claimed by Codex on e02-s04-t01-expr-contexts (evidence: pending).
2026-08-12 E06-S01-T04 done — strict file-backed variable store with case-insensitive names, scope copies, metadata, hosted-agent read-only enforcement, and same-job/cross-job output paths (evidence: research/E06-runtime.md C-E06-001..006; research/experiments/E06-readonly-variables/real-run.md; packages/runtime/test/core.bats).
2026-08-12 E06-S01-T02 unblocked — E06-S01-T04 now provides the variable-store APIs required for environment materialization (evidence: packages/runtime/lib/core.sh).
2026-08-12 E06-S01-T03 unblocked — E06-S01-T04 now provides the variable-store APIs required to register `.env` values (evidence: packages/runtime/lib/core.sh).
2026-08-12 E02-S05-T02 unblocked — E06-S01-T04 now provides the fixture-store APIs `azdo_var` and `azdo_output` (evidence: packages/runtime/lib/core.sh).
2026-08-12 E02-S01-T03 in-progress — claimed by Codex on e02-s04-t01-expr-contexts (evidence: pending).
2026-08-12 E02-S01-T03 done — added the positioned `expected-function-call` parser/error-rendering path for bare registered functions; verified compile/job slots and kept contexts/unavailable status names distinct (evidence: research/E02-expressions.md C-E02-132..134; research/experiments/E02-bare-functions/; packages/engine/test/expr/errors.test.ts).
2026-08-13 E01-S01-T04 in-progress — claimed by Codex on e02-s04-t01-expr-contexts (evidence: pending).
2026-08-14 E01-S01-T04 done — duplicate-key parsing now exempts recognized template directive keys while retaining ordinary-key rejection; live preview accepts duplicate `if`/`each` directives and rejects duplicate `${{ pair.key }}` (evidence: research/E01-yaml-frontend.md C-E01-038/039; research/experiments/E01-directive-duplicates/; packages/engine/test/frontend/quirks.test.ts).
- 2026-08-12 E03-S01-T01 done — DOM walker with context stack: `packages/engine/src/template/walk.ts` (`loneExpression`/`expressionUnits`/`parseDirectiveKey` for recognition, `TemplateFrame`/`rootFrame`/`childFrame`/`bindLoopVariable` for the context stack, `walkTemplate` with a per-directive-kind visitor seam), 54 tests. **Both of the task's Ground sources were insufficient, in opposite directions, and that is what set the size of this task**: the templates and template-expressions docs state *none* of the recognition rules and their one structural claim about where expressions expand is false, while the `actions/runner` object-templating engine knows exactly one directive — `TemplateConstants.cs` declares `InsertDirective = "insert"` and has no `if`/`elseif`/`else`/`each` counterpart anywhere (C-E03-115), so it is a design reference for the walk loop and for nothing about the directive set. 33 live probes (`pnpm template-walk-survey` → `research/experiments/E03-walk/`) measured the rules instead. **Directive keywords are case-sensitive** — the only such corner of a language whose names, functions and even boolean literals all fold case — and a wrongly cased keyword is not a mis-spelled directive but *not a directive at all*: `${{ IF eq(1, 1) }}` falls through to ordinary expression parsing and reports `Unexpected symbol: 'eq'` at position 4, i.e. C-E02-103's leftover-token rule applied to `IF` read as a name (C-E03-100). Directly beside it, loop-variable names *do* fold case (C-E03-107). **The finding that changed the implementation** is that directive parameters are top-level **expression units, not whitespace-split words**: `${{ else if eq(1, 1) }}` is rejected "Exactly 0 parameter(s) were expected following the directive 'else'. Actual parameter count: 2", so `eq(1, 1)` counts as one (C-E03-101, counts measured: `each` 3, `else` 0, `insert` 0). The consequence is that the `each` separator must be found by tokenizing, never by `indexOf(' in ')` — `${{ each item in split('a in b', ' in ') }}` legitimately iterates `a`,`b` and `${{ each item in split(format('{0}', in('b','b')), ',') }}` is not captured by the later `in(` either (C-E03-104); a text search gets one of those wrong **silently**, iterating the wrong collection with no error to notice. `expressionUnits` reproduces every count the service reported. Three more that shaped the API: loop variables share one **flat namespace with the contexts** and collide rather than shadow — `${{ each variables in … }}` → "The idenfifier 'variables' has already been defined within the current scope", the service's own typo preserved (C-E03-106), so `bindLoopVariable` returns a rejection instead of a nested scope; a directive is recognized only on a mapping key or a one-key sequence item, never on a scalar value (C-E03-112); and malformed-directive sentences come back from the service **without** the help link every expression error carries, which the tests assert rather than describe. **Docs correction (rule 5):** the template-expressions doc's "Expressions are only expanded for `stages`, `jobs`, `steps`, and `containers` … You can't … use an expression inside `trigger`" is false — `trigger: [${{ 'main' }}]` expands to `trigger.branches.include`, and expressions and `if` directives both expand in `pool.demands` and root `variables:` (C-E03-109). Position gating is nonetheless real, with its own sentence `A template expression is not allowed in this context`, and has exactly **one** measured member (`resources.repositories`), so it is modelled as a seam rather than a table extrapolated from the doc's list (C-E03-110). Scope line held deliberately: T01 detects, frames and accumulates (never throws — C-E02-110 hands document-wide collection to E03-S01), while `if`/`each`/`insert` semantics stay with T02/T03/T04, each of which owes committed oracle fixture pairs; the default walk is a faithful traversal that changes nothing, and `expect(result.node).toEqual(root)` is a real gate on provenance surviving the rebuild, which E03-S04-T02's provenance map depends on. Two gaps found and **filed rather than fixed**: our C-E01-023 duplicate-key quirk rejects two byte-identical `${{ if }}` keys at parse time while the service accepts the document and merges both bodies — E01-S01-T02 is right about all 13 cases it probed and never probed a `${{ }}` key (C-E03-111 → new **E01-S01-T04**) — and `Expected '(' to follow a function: '<name>'` is a general expression error kind missing from `ExprErrorCode`, so a bare `${{ eq }}` renders `Unrecognized value` where the service names the missing paren (C-E03-114 → new **E02-S01-T03**). Two late additions after review, both closing a claim-without-evidence: the fork's evaluation loop was actually **read and pinned** rather than cited by filename (`TemplateEvaluator.cs#L89-L203` — recursive `Evaluate(DefinitionInfo)` over `TemplateUnraveler` predicates; our walk is the same recursion over a materialized DOM, so it needs no unraveler, C-E03-116), and `loneExpression` now finds its closing `}}` with a **quote-aware scan** because the documented escape for a literal `${{` is to wrap it in an expression string — `${{ 'my${{value' }}` is the doc's own canonical spelling and the first implementation reported it as *not* a lone expression, a bug that would have surfaced in E03-S01-T05 rather than here (C-E03-117). `research/E03-template-engine.md` opens with the **claim-ID block table** the E02 merge collision taught us to write first; `research/E03-normalizer.md`'s 001–003 are recorded in it. One probe is recorded as **not evidence**: the multi-line directive key was rejected by the YAML layer before the template engine saw it, and the useful conclusion is that a directive is always a mapping key so the case cannot arise. Docs updated: docs/02 §4, docs/06 §5 decision 22, REFERENCES.md (templates + new template-expressions row with the false statement flagged, actions/runner object-templating) (evidence: research/E03-template-engine.md C-E03-100..117; research/experiments/E03-walk/ 33 probes; tests packages/engine/test/template/walk.test.ts 58 tests, suite 898 vitest + 10 bats green, build/typecheck/lint/prettier/VERIFY-guard clean)
- 2026-08-13 E02-S05-T02 done — dual-backend conformance harness: one row table (`packages/engine/test/expr/conformance.table.ts`) drives the evaluator through the E02-S02/S03 entry points and **generates** `packages/runtime/test/expr-conformance.bats`, 104 executed rows, committed with a freshness guard (`toMatchFileSnapshot`) so editing the compiler without regenerating is red rather than untested. **Building the runner falsified E02-S05-T01's output — none of it could have executed** (C-E02-145): status functions compiled to `[ azdo_status_succeeded = True ]`, a bare *word* compared against a string, so the function was never invoked and the docs' canonical condition was False for every run; `not(x)` nested `[ [ … ] != True ]`; comparisons double-wrapped an already-quoted literal and compared quoting syntax rather than value; dependency reads passed `azdo_output` two arguments where it takes three; and every emitted `azdo_expr_*` helper had **no definition anywhere**, because `packages/runtime/lib/expr.sh` — named in T01's own **Do** — was never written. All of it invisible to a test asserting the emitted string, which is what T01 shipped. The fix was structural, not cosmetic: mapping comparisons onto `test` operators discards the operand *kind* the conversion table needs, and `eq(1, true)` — True by Boolean→Number — compiles to `[ "1" = True ]` and is **silently False**. Values now cross into the shell as their String form plus a kind tag (`bool|num|str|ver`) and `azdo_expr_cmp` reproduces `compareValues()` in bash, including decimal comparison without an external tool, `1.2 < 1.2.0` version ordering, and ordinal-ignore-case strings. Status convention **0 True / 1 False / 2 error**, because `test` already reserves >1 for errors and a missing helper exits 127 (C-E02-135/136) — a harness reading "non-zero = False" would have gone green over a backend that never ran, which is the trap this task existed to avoid. `LC_ALL=C` is pinned in every string op: `[[ alpha < BETA ]]` is **false** under C and **true** under en_US.UTF-8, so a bare `[[ < ]]` would answer differently on different developer machines (C-E02-142, measured — neither the bash manual nor POSIX states it). Three divergences are **declared as rows carrying the measured shell answer**, so each is red if it widens *or* if it quietly goes away: the shell has no Null, so `lt(variables.Absent, 'x')` answers True where the evaluator raises (C-E02-138); an evaluation error is masked by `||` and discarded outright in value position, because command substitution keeps output and not status (C-E02-143/144) — handed to E06-S03-T03, which owns condition semantics, rather than fixed with a new runtime error protocol here; and non-ASCII case folding differs from .NET OrdinalIgnoreCase (C-E02-141). Five constructs now raise `BashCompileError` and are asserted to (never skipped): `split`/`join`/`convertToJson`/`containsValue` (no Object/Array in the shell), a dynamic index, and `counter` (convert-time state seam) — T01 had emitted `azdo_expr_split` etc. for these, which would have exited 127. **Structural finding filed rather than absorbed:** T02 says the tables run "through both the evaluator and the compiled bash", but E02 has **no AST evaluator** — the tables call `compareValues`/`evaluateLogicalMembershipFunction`/`evaluateGeneralFunction` directly and no task ever allocated a walker; building one inside a harness task would bury a missing story, so rows carry an explicit `evaluate()` thunk and the walker is new task **E02-S05-T03**. Three mutation checks confirm the gate is real: case-sensitive string compare → 6 bats rows red; letting the compiler emit `split` again → the unsupported row red; hand-editing the generated bats → the freshness snapshot red. Docs updated: docs/02 §6 (comparisons no longer compile to `[ ]`; divergence list) + docs/06 §5 decision 23; REFERENCES gains POSIX.1-2024 and a note that gnu.org rate-limits (evidence: research/E02-expressions.md C-E02-135..146; research/experiments/E02-conformance/shell-semantics.md 11 probes; packages/engine/test/expr/conformance.test.ts 223 tests + packages/runtime/test/expr-conformance.bats 104 rows; suite 1128 vitest + 118 bats green, build/typecheck/lint/shellcheck/VERIFY-guard clean)
- 2026-08-14 E02-S05-T02 follow-up — **the first CI run caught a bash-3.2 incompatibility whose failure mode was silent agreement** (C-E02-147). macOS runners execute bats under the system bash 3.2, where `${v^^}`/`${v,,}` do not exist; the bad substitution yields the empty string on *both* sides of a comparison, so the two empties compared **equal**: `lt('alpha','BETA')` reported "equal" (four of its six operators red) while `eq(lower('AB'), 'ab')` **passed for the wrong reason**, the helper having returned nothing at all. Ubuntu was green throughout. Case folding now goes through `LC_ALL=C tr`, the pattern `core.sh` already used for exactly this reason, and a new `packages/runtime/test/expr.bats` (7 cases) asserts the returned **text** of each helper rather than only a comparison's status — a comparison-only suite structurally cannot tell "both correct" from "both empty", which is the same lesson as the exit-status convention one level down (evidence: research/E02-expressions.md C-E02-147; GitHub Actions run 31776027219; packages/runtime/test/expr.bats)
2026-08-14 E02-S05-T03 in-progress — claimed by Codex on e02-s04-t01-expr-contexts (evidence: pending).
- 2026-08-14 E02-S05-T03 done — added the recursive `evaluateExpression` AST backend with slot-derived status scope and lazy family dispatch; replaced every conformance-table `evaluate()` thunk with parser→walker execution and filed the ungrounded filtered-array contract as E02-S05-T04 instead of guessing it (evidence: `research/E02-expressions.md` E02-S05-T03 composition map; `packages/engine/test/expr/evaluate.test.ts` 13 focused walker/laziness/state cases; `packages/engine/test/expr/conformance.test.ts` 225 evaluator/conformance tests).
2026-08-18 E02-S05-T04 in-progress — claimed by Codex `/root` on `codex/e02-s05-t04-filtered-arrays` (evidence: pending).
- 2026-08-18 E02-S05-T04 done — grounded filtered-array traversal with a 24-cell live preview matrix and pinned source, then implemented distinct mapped traversal for Array/Object wildcards, misses, nested filters, and later indices; the eval backend covers both spellings while shell collection traversal remains explicitly unsupported (evidence: `research/E02-expressions.md` C-E02-160..164; `research/experiments/E02-filtered-arrays/`; `packages/engine/test/expr/evaluate.test.ts`; 1,176 Vitest + 126 Bats).
2026-08-18 E03-S01-T03 done — grounded and implemented sequence/mapping `each`, nested loops, object/stepList/jobList structural iteration, exact authored mapping order, and no implicit index; corrected the design docs' mapping-binding claim (evidence: research/E03-template-engine.md C-E03-140..151; research/experiments/E03-each/; fixtures/oracle/directives/; packages/engine/test/template/each.test.ts; 1,193 Vitest + 126 Bats)
2026-08-18 E03-S01-T02 in-progress — claimed by Codex `/root` on `claude/e06-s01-t02` (evidence: pending).
2026-08-18 E03-S01-T02 blocked — official docs grounded sequence/mapping syntax and the documented `if`/`elseif`/`else` form, but the mandatory six-case preview-oracle matrix cannot run because all four `AZDO_*` variables and `.env.oracle` are absent; no implementation was attempted (evidence: research/E03-template-engine.md C-E03-120/121; research/oracle-setup.md).
2026-08-19 E03-S01-T02 in-progress — unblocked and reclaimed by Orchestrator on `claude/e06-s01-t02-v2`; the doc-only grounding commit `7459ab3` is integrated and `.env.oracle` is present, so the six-case oracle matrix can run (evidence: pending).
- 2026-08-19 E03-S01-T02 done — conditional insertion chains: `packages/engine/src/template/conditional.ts` (`conditionalVisitor`, `ConditionalChainError`) on T01's directive seam, plus `scripts/if-survey.ts` (`pnpm if-survey`) and the shared fixture harness `packages/engine/test/template/fixture-harness.ts` extracted from E03-S01-T03 so both directive suites share one parameter binding and one T05 scalar stand-in. **The 2026-08-18 `[!]` was wrong and is lifted** — it reported the four `AZDO_*` values absent, but they live in `.env.oracle`, which was merely missing from that worktree; the doc-only grounding commit `7459ab3` (C-E03-120/121) is cherry-picked in rather than left stranded. 22 live probes (`research/experiments/E03-if/`, C-E03-122..134) produced **18** committed input/`finalYaml` pairs against the six the task requires, plus 4 rejections asserted against their transcripts. **Two findings invert this task's own `Do` field**, and both are mutation-checked rather than merely asserted. Chain membership is **not adjacency-gated**, and the winning body splices at the **winning directive's own position** rather than the head's: with a false `if`, an ordinary step written between `${{ if }}` and `${{ else }}` came out *first* and the `else` body landed where the `else` was written — measured in both parent shapes — so "group the contiguous chain and emit the winner at the head's index", which is what **Do** describes, silently reorders that document (C-E03-128). And chain conditions evaluate in **document order**, stopping at the first winner: `if(true) / elseif parameters.missing / else` expands, so resolving the trailing `else` must not reach past the winner — a backwards *nearest-first* scan, the obvious way to write the backwards resolution the first finding forces, would evaluate the raising member and reject a document the service accepts (C-E03-132). Both laziness claims rest on a control proving the same `parameters.missing` read **is** a hard 400 `Key not found 'missing'` when actually reached (C-E03-134); without it, "it expanded" would have proved nothing. Three shapes the service separates and the code keeps separate: a losing `else` (silent), an orphan `elseif`/`else` (HTTP 400, two newline-joined sentences, **no help link** — C-E03-129), and an `elseif` after an `else`, rejected identically because `else` terminates its chain (C-E03-130). Conditions are **converted** to Boolean rather than required to be one — `${{ if 'text' }}` taken, `${{ if '' }}` not — reusing C-E02-020 instead of inventing a truthiness notion (C-E03-131). Also measured: a new `if` starts a new chain so a trailing `else` binds to the nearest one (C-E03-127); chains nest and a losing outer branch discards the nested structure unevaluated (C-E03-126); a chain with no `else` and every condition false is not an error (C-E03-125). Three mutations confirm the goldens gate the subtle rules rather than merely passing over them: adjacency-gating the mapping scan → 2 red, adjacency-gating the sequence scan → 4 red, nearest-first evaluation order → 2 red. **Open question filed rather than guessed:** no probe placed an `each`/`insert` between two chain members, so whether those siblings break a chain is unmeasured — the code skips them (the reading consistent with C-E03-128) and says so at that line, handed to E03-S01-T04. Docs updated per rule 5: docs/02 §4 (its one-sentence paragraph was silent on all of the above), docs/06 §5 decision 27, REFERENCES.md (evidence: research/E03-template-engine.md C-E03-120..134; research/experiments/E03-if/ 22 probes; fixtures/oracle/directives/if-* 18 pairs; packages/engine/test/template/conditional.test.ts 35 tests; suite 1,228 Vitest + 126 Bats green, build/typecheck/lint/prettier/VERIFY-guard clean)
- 2026-08-19 environment note (not a backlog task) — this session's worktree was created at `/tmp/wt-claude`, and `/tmp` is mounted **noexec**, so `pnpm install` fails at esbuild's postinstall and nothing can be built or tested there. Worktree relocated to `/root/wt-claude` (`git worktree repair`). One consequence is worth knowing before someone re-reports it as a regression: `packages/runtime/test/fixture-store.bats` "preserves the executable bit" fails whenever bats' tmpdir lands on a noexec filesystem, because `test -x` calls `access(X_OK)` and the kernel returns EACCES regardless of the mode bits. It fails identically on an untouched `main` checkout and passes under `TMPDIR=/root/batstmp`; it is environmental, not a code defect.
2026-08-18 E03-S01-T02 in-progress — claimed by Codex `/root` on `codex/e03-s01-t02-conditional-insertion` (evidence: pending).
- 2026-08-18 E03-S01-T02 done — grounded and implemented lazy, stateful conditional insertion across mapping/sequence/nested containers; 23 live probes produced 19 claim-tagged, hash-locked oracle pairs and four rejection controls (evidence: `research/E03-template-engine.md` C-E03-120..127; `research/experiments/E03-conditionals/`; `fixtures/oracle/directives/`; `packages/engine/test/template/conditionals.test.ts`; 1,213 Vitest + 126 Bats).
- 2026-08-19 E03-S01-T02 merge reconciliation — retained `conditionals.ts`: the original Side A implementation matched the 37-pair oracle union while Side B matched 34/37; preserved both surveys, deduplicated C-E03-120..137, and consolidated the mutation-backed ordering tests (evidence: `research/experiments/E03-conditionals/`; `research/experiments/E03-if/`; `fixtures/oracle/directives/`; `packages/engine/test/template/conditionals.test.ts` 62 tests; suite 1,255 Vitest + 130 Bats green).
2026-08-19 E06-S01-T03 in-progress — claimed by Codex leaf coder on codex/e06-s01-t03 (/root/wt-codex) (evidence: pending).
2026-08-19 E06-S01-T03 done — implemented the trusted Bash `.env` loader with lossless quoted/multiline values, same-process `--env-file` overlay, manifest-derived secret flags, and atomic syntax-error rejection (evidence: research/E06-runtime.md C-E06-013..017; docs/04 §10; packages/runtime/test/core.bats; 1,228 Vitest + 134 Bats; ShellCheck clean).
2026-08-19 E06-S02-T01 in-progress — claimed by Codex leaf agent on codex/e06-s01-t03 (/root/wt-codex) (evidence: pending).
2026-08-19 E06-S02-T01 blocked — hosted run 541 refutes the required end-to-end non-recursion (`a=$(b)` rendered `inner` in the next task); source tracing then stopped on GitHub code-search HTTP 401 per the explicit auth-error stop condition, so no implementation was written (evidence: research/E06-runtime.md C-E06-018..021; research/experiments/E06-macro-expansion/real-run.md; docs/04 §5; docs/06 §5 decision 28).
2026-08-19 E06-S02-T01 in-progress — resumed by Codex E06 runtime agent on codex/e06-s01-t03 (/root/wt-codex) for raw/API source tracing and corrected implementation (evidence: pending).
2026-08-19 E06-S02-T01 done — completed the commit-pinned agent phase trace and implemented recursive pre-step variable recalculation plus the non-recursive exact macro target scan, including secret propagation, nested misses, cycle/depth guards, and private expanded step files (evidence: research/E06-runtime.md C-E06-018..024; research/experiments/E06-macro-expansion/real-run.md; packages/runtime/test/core.bats; 1,255 Vitest + 136 Bats; ShellCheck clean).
2026-08-19 E06-S03-T01 in-progress — claimed by Codex leaf agent on codex/e06-s01-t03 (/root/wt-codex) (evidence: pending).
2026-08-19 E06-S03-T01 done — implemented the stable `run_step` contract with private macro-expanded execution, live-probed `System.DefaultWorkingDirectory` fallback, process-group timeout enforcement, and combined per-step log tee (evidence: `research/E06-runtime.md` C-E06-025..030; `research/experiments/E06-run-step/real-run.md`; `packages/runtime/test/core.bats`; 1,255 Vitest + 140 Bats; all linters clean).
2026-08-19 E06-S03-T02 in-progress — claimed by Codex leaf agent on codex/e06-s01-t03 (/root/wt-codex) (evidence: pending).
2026-08-19 E06-S03-T02 done — implemented atomic five-state result persistence, nonzero/stderr failure mapping with live streaming, Failed-only retries capped at ten with 1/4/…/100-second backoff under the step deadline, and post-retry `continueOnError` downgrade (evidence: `research/E06-runtime.md` C-E06-031..037; `packages/runtime/test/core.bats`; 1,255 Vitest + 146 Bats; ESLint/Prettier/ShellCheck/VERIFY guard clean).
2026-08-19 E06-S03-T03 in-progress — claimed by Codex /root on codex/e06-s01-t03 (/root/wt-codex) (evidence: pending).
2026-08-19 E06-S03-T03 done — implemented result-store-backed step status functions, implicit `succeeded()`, compiled condition invocation, `--no-condition`, exact false-condition skip flow, and status-preserving evaluation errors; hosted run 543 corrected the design's invented section-header log to the agent's plain skip line (evidence: `research/E06-runtime.md` C-E06-038..043; `research/experiments/E06-condition-flow/`; `packages/runtime/test/core.bats`; 1,255 Vitest + 151 Bats; build/typecheck/ESLint/Prettier/ShellCheck/VERIFY guard clean).
2026-08-19 E06-S04-T01 in-progress — claimed by Codex leaf agent on codex/e06-s01-t03 (/root/wt-codex) (evidence: pending).
2026-08-19 E06-S04-T01 done — implemented the live `##vso` stdout-line parser with case-insensitive command/property lookup, ordered `%AZP25`/structural-token decoding, handler dispatch, and grounded unknown/malformed warning passthrough wired into `run_step` (evidence: `research/E06-runtime.md` C-E06-044..049; `packages/runtime/test/core.bats`; 1,255 Vitest + 157 Bats; build/typecheck/ESLint/Prettier/ShellCheck/VERIFY guard clean).
2026-08-19 E06-S04-T02 in-progress — claimed by Codex E06 runtime leaf agent on codex/e06-s01-t03 (/root/wt-codex) (evidence: pending).
2026-08-19 E06-S04-T02 done — implemented `task.setvariable` dispatch with following-task timing, plain/output/read-only store routing, sticky secret metadata, default multiline-secret rejection, and immediate same-step/later-step log masking (evidence: `research/E06-runtime.md` C-E06-050..056; `research/experiments/E06-setvariable/real-run.md` run 544; `packages/runtime/test/core.bats`; 1,255 Vitest + 162 Bats; build/typecheck/ESLint/Prettier/ShellCheck/VERIFY guard clean).
2026-08-19 E03-S01-T04 in-progress — claimed by Orchestrator (Claude) on worktree `/root/wt-claude`, branch `claude/e03-s01-t04`; inherits E03-S01-T02's open question (an `each`/`insert` sibling between two conditional-chain members) to settle while it holds the `insert` oracle budget (evidence: pending)
- 2026-08-19 E03-S01-T04 done — the `${{ insert }}` merge directive: `packages/engine/src/template/insert.ts` (`insertVisitor`, `InsertValueError`) on T01's directive seam, plus `scripts/insert-survey.ts` (`pnpm insert-survey`) and `composeVisitors` in `walk.ts` (three directive modules now compose, and `{...a, ...b}` silently drops hooks wherever two visitors define the same one). 32 live probes (`research/experiments/E03-insert/`, C-E03-160..174) produced 13 committed input/`finalYaml` pairs, while **19 were rejected** and are asserted against their committed error transcripts — for this task the rejections carry most of the claims, because both headline answers are ones the service gives by refusing a document. **The task's named source does not exist:** there is no "Insertion" section on the templates page — `${{ insert }}` is documented on **template-expressions**, as one paragraph and one example, and the templates page's "Insert a template" is the unrelated `- template:` file include (C-E03-160/161). Unlike T02 and T03, the `actions/runner` fork genuinely implements this directive (C-E03-115), so for once it is a real second source; it was read and pinned (C-E03-162) and it predicted the collision rule correctly and the position rule **wrongly**, which is why every branch it suggested was still submitted to the oracle. **The flagged unknown is answered: collision is an error, not an overwrite** — `'FOO' is already defined`, HTTP 400, located at the **later** of the two occurrences, comparison folding case, message echoing the *later* spelling (`'foo'` when a literal `FOO` met an inserted `foo`); neither value wins (C-E03-169/170). The design consequence is bigger than the answer: **the rule belongs to the mapping, not to `insert`** — two colliding inserts reject, and so does an `each`-produced key colliding with a literal, with no insert involved (C-E03-171) — so the check lives on the mapping rebuild in `walk.ts`, accumulating a diagnostic and dropping the later entry rather than throwing (the service returned three such sentences in one response), with directive keys exempt so it agrees with the parse-time exemption E01-S01-T04 already made (C-E01-038/039). **The open question E03-S01-T02 filed and handed here is settled, and T02's guess was wrong** (C-E03-138): an `${{ each }}` or `${{ insert }}` written **between** two chain members orphans the member that follows — measured in both parent shapes, for both intervening directives, for a trailing `elseif` as well as `else`, with both truth values of the `if`, and against two controls placing the same insert immediately *before* and immediately *after* the chain, where the document expands; C-E03-128's ordinary-sibling control still holds, so the rule is specifically "directive sibling". `conditionals.ts` now ends the containing chain at any directive it does not own, exactly as `else` already ends one — which is why `conditionalVisitor` composes **first**. That fix exposed a second T02 defect: the orphan rejection hard-coded the wording T02 measured, but T02 probed only *sequence* position — in a mapping the second sentence is `A mapping was not expected`, located at the branch body, plus a third sentence dumping the engine's internal reader stack (C-E03-139); the diagnostics are now position-aware and the state dump is deliberately not reproduced. Also measured: the merge is **in place** at the directive's own position and preserves the source object's authored order, unsorted (C-E03-163); the value may be a literal mapping (164); an empty object contributes nothing (165); it works in mappings with well-known schema keys and keeps nested values whole (166); the source may be a loop binding (167); two `${{ insert }}` keys in one mapping both merge in document order (168); a string, sequence, bare scalar or empty value is `Expected a mapping` (172); outside key position the keyword is still *recognized* but cannot act and its text survives verbatim as `Unexpected value '${{ insert }}'` — proven not to be a failed expression parse by C-E03-151's differently-shaped `Unrecognized value: 'index'` control, and **handed to E03-S01-T05 as a rule: never evaluate a lone `${{ insert }}`** (173); and `- ${{ insert }}: <obj>` is still a mapping-key insertion, into the one-key mapping the *item* is, so it merges rather than splicing into the parent sequence (174). Six mutations turn the suite red — case-sensitive collision compare (1), collision-as-overwrite (5), splice-instead-of-merge (1), reverting the chain-break fix (5), position-independent orphan wording (6), append-instead-of-in-place (4) — and one of them is the reason an extra probe exists: the splice mutation initially left the whole suite **green**, because the single-key `sequence-position` object cannot distinguish merging from splicing, so `sequence-position-valid` supplies two keys forming one valid step and the service returned one step (C-E03-174). That pair is committed but is not a byte-golden, because the service desugars `script:` into `task: CmdLine@2` and the E03-S05-T01 normalizer deliberately does not desugar; the test asserts its structure and names the layer that owes the rest. One stale assumption corrected mid-task: the suite was first written to skip the two-insert fixture as blocked on E01-S01-T04, which had in fact landed on 2026-08-14. Docs updated per rule 5: docs/02 §4 (both the one-sentence `insert` paragraph and the conditional-chain list), docs/06 §5 decision 33 with decision 27's open question marked settled, REFERENCES.md (template-expressions "Insertion" row + the fork's `TemplateUnraveler`/`TemplateEvaluator` insert permalinks) (evidence: research/E03-template-engine.md C-E03-138/139 + C-E03-160..174; research/experiments/E03-insert/ 32 probes; fixtures/oracle/directives/insert-* 13 pairs (the other 19 probes are rejections, asserted against their transcripts); packages/engine/test/template/insert.test.ts 45 tests; suite 1,300 Vitest + 162 Bats green, build/typecheck/lint/prettier/shellcheck/VERIFY-guard clean)
- 2026-08-19 E03-S01-T04 rebase reconciliation — rebased `claude/e03-s01-t04` onto `main` after `b03c1f3` had reconciled the two parallel E03-S01-T02 implementations by **deleting** `conditional.ts` in favour of a differently-architected `conditionals.ts`. The branch was written against the deleted module, so the merge was a modify/delete on the file plus six textual conflicts. **`conditionals.ts` is kept as the trunk** and T04's two chain findings were ported onto it rather than reviving the backward-scan module — the trunk's per-container forward state machine matched 37/37 oracle pairs where the backward scan matched 34/37 (docs/06 §5 decision 27), so reviving it to preserve this branch's shape would have re-broken three fixtures to fix two. Ported: an intervening `each`/`insert` now **ends** the containing chain (previously only `else` did), and the orphan rejection's second sentence is parent-shape dependent. Three consequences the port forced, each mutation-checked: (1) `conditionalVisitor` is now a composable visitor and `expandConditionals` is that visitor composed with one that freezes the directives it does not own, so the standalone T02 pass and the composed T04 pipeline are one implementation; (2) it must be composed **first** — a pass that never reaches the `insert`/`each` site cannot know the chain was broken, and composing it last turns 5 tests red; (3) chain rejections became accumulated **diagnostics** rather than a thrown `ConditionalChainError`, matching `walk.ts`'s documented rule that the service reports every rejection in one response (C-E02-110), which retired that class. `TemplateVisitContext` gained `report()` so a visitor can reach the walk's diagnostic list. Conditions now parse with the frame's names and bindings in scope, which is a no-op at the root and is what lets a condition read an enclosing `each` variable once the two are composed. **Claim IDs reassigned:** the branch's `C-E03-135/136` collided with the reconciled T02 survey, which already held 135..137, so they are now `C-E03-138/139` — the T02 block is full. docs/06 §5 decision 28 → 33 for the same reason (28..32 were taken by E06). (evidence: `packages/engine/src/template/conditionals.ts`; `packages/engine/test/template/insert.test.ts` describe blocks `C-E03-138`/`C-E03-139`; mutations — no-op chain break 5 red, position-independent orphan wording 6 red, conditional visitor composed last 5 red; suite 1,300 Vitest + 162 Bats green, build/typecheck/eslint/prettier/shellcheck/VERIFY-guard clean)
2026-08-19 E03-S01-T05 in-progress — claimed by Orchestrator (Claude) on worktree `/root/wt-claude`, branch `claude/e03-s01-t04`; inherits E03-S01-T04's rule that a lone `${{ insert }}` is never evaluated (C-E03-173) and owns replacing the `fixtureScalars` stand-in in the shared fixture harness (evidence: pending)
- 2026-08-19 E03-S01-T05 done — scalar interpolation: `packages/engine/src/template/interpolate.ts` (`interpolationVisitor`) on T01's scalar seam, plus `scripts/interpolation-survey.ts` (`pnpm interpolation-survey`). 34 live probes (`research/experiments/E03-interpolation/`, C-E03-175..194) produced 27 committed input/`finalYaml` pairs and 7 rejections asserted against their transcripts. **The two rules the task's Ground field flags as needing probes turned out to be the documented ones** — the expressions page states `Null → ''` and Boolean → `'True'`/`'False'` verbatim (C-E03-175), so the casing question was answered by reading rather than measuring; the *float* half of the same field was worth every probe, because the Number row of that table is **false as written** ("no thousands separator and no decimal separator" — taken literally `0.5` renders `05`). Measured, rendering is shortest-round-trip invariant: `0.5`→`0.5`, `1.0`→**`1`**, `1000000`→`1000000`, `-1.25`→`-1.25`, all four reproduced by `convertValue(v,'string')`, so interpolation reuses `format`'s conversion exactly as C-E02-109 predicted rather than defining a second one (C-E03-182). Three undocumented findings decided the code. **(1) The lone/mixed boundary is whitespace-sensitive, and T01's `loneExpression` was wrong** (C-E03-180): `env: '  ${{ parameters.envVars }}  '` is mixed content and rejects `Unable to convert from Object to String. Value: Object`, while the *same expression double-quoted without padding* inserts structurally, and the positive control `"  ${{ 'x' }}  "` comes back `'  x  '` with the spaces intact — so loneness is a property of the raw text, not of YAML style and not of the text modulo whitespace. The trim is removed; it had been invisible for four tasks because YAML strips plain scalars itself, so only a quoted host can carry padding into the engine, which is why that probe had to be written that way. **(2) Every scalar result is converted to its String form; only Object and Array stay structural** (C-E03-183) — the proof is Null, which renders `''` in *lone* position, byte-identical to `${{ '' }}`, so a lone expression does not simply hand its typed result to the emitter. **(3) Keys run through the same split but have no structural option, and their spelling is `True`** (C-E03-190/191/192): a *lone* collection key rejects `Expected a scalar value` while the *same object in mixed key content* rejects with the conversion sentence — one rule cannot produce two sentences, which is what proves the split is shared rather than keys having a rule of their own. That closes the docs/02 §8 ambiguity list's "Boolean stringification casing in keys" entry, confirmed independently by the service's own `Unexpected value 'True'` when the key is written into a schema-checked mapping. Also measured: structural insertion is whole and typed at every depth (177/179); in sequence position an Array **splices** and an Object becomes **one item** (178), the only place a scalar's replacement splices, now in `walkSequence`; a collection in a string position is an accumulated diagnostic with an **empty-string substitution**, not a throw — the service's own response carried the conversion sentence *and* the schema's follow-on `Unexpected value ''` (187); the result is never re-parsed as YAML and the documented `${{` escape survives verbatim, so there is exactly one pass (185/188); a block scalar interpolates as one scalar (189); and a lone directive keyword in value position is never evaluated (194, inherited from T04's C-E03-173 rather than re-probed). Seams: `TemplateVisitor.scalar` gained a `TemplateScalarContext` carrying `position` and `report`, because key-vs-value is a real distinction and only the walk knows it; the unchecked `as ScalarNode` cast on the key path is gone. **The `fixtureScalars` stand-in is deleted** and all four E03-S01 suites now run the real pass — two of its shortcuts were exactly the bugs above. Eight mutations turn the suite red (trim restored 4, Null→null node 2, no sequence flatten 1, keys structural 3, directive keyword evaluated 2, lower-case Boolean 6, mixed content trimmed 5, no empty-string substitution 1). **A defect in a neighbouring layer was found and filed rather than patched here:** `normalizeExpandedYaml` compares scalar leaves *after* the YAML parser types them, so the service's `PROBE: 0123` reads back as the Number 123 while our `"0123"` stays four characters — two spellings of one value reported as drift, and the same for the key `True` (C-E03-193). The fix belongs to the normalizer, so it is added as **E03-S05-T03** with the two affected pairs asserted against raw `finalYaml` meanwhile, rather than quietly widening N7 from this branch. Docs updated per rule 5: docs/02 §3 rewritten and §8's ambiguity list pruned, docs/06 §5 decision 34, REFERENCES.md (the expressions page's false Number row; the template-expressions page's array-only structural sentence) (evidence: research/E03-template-engine.md C-E03-175..194; research/experiments/E03-interpolation/ 34 probes; fixtures/oracle/directives/interp-* 27 pairs; packages/engine/test/template/interpolate.test.ts 70 tests; suite 1,370 Vitest + 162 Bats green, build/typecheck/eslint/prettier/shellcheck/VERIFY-guard clean)
2026-08-19 E03-S02-T01 in-progress — claimed by Orchestrator (Claude) on worktree `/root/wt-claude`, branch `claude/e03-s01-t04`; builds on E03-S01-T01's `childFrame`/`depth` seam in `walk.ts`. Claim block C-E03-195..229 (evidence: pending)
- 2026-08-20 E03-S02-T01 done — reference resolution: `packages/engine/src/template/reference.ts` (path math, `(repository, commit, path)` identity, injected `TemplateFetcher` with a local-FS impl; E08 supplies the remote one), plus `scripts/reference-survey.ts` (`pnpm reference-survey`) and a second oracle repository created/authorized by `scripts/oracle-provision.ts`. 34 live probes (`research/experiments/E03-references/`, C-E03-195..218) against a **two-repository** fixture — 21 expanded, 13 rejected. The two repos are the whole point: every `@alias` question is "which repository was this path read from", and one repo cannot tell the answers apart. The named docs settle the syntax (absolute-vs-relative, `@alias`, `@self`, alias charset `[-_A-Za-z0-9]*`, `ref` default `refs/heads/main`, and "repositories are resolved only once") and **none** of the semantics the resolver has to encode. Three measured findings decided the code, all three contradicting the obvious implementation. **(1) A repository switch resets the base directory to the target repo's root (C-E03-215)** — the task's "per-file base dir" holds only *within* one repository. This took three purpose-built fixtures, because every spelling written from a root pipeline file passes under either model (at `/azure-pipelines.yml` the base *is* the root): `../leaf.yml@self` from a subdirectory resolves (same repo → base kept, so an explicit alias is not itself a switch), `../e03-refs/leaf.yml@self` from inside the templates repo is rejected `The file path /../e03-refs/leaf.yml is invalid` (switch → base discarded), and `cross/leaf.yml@templates` from `/e03-refs/dir/` reaches `/cross/leaf.yml` rather than `/e03-refs/dir/cross/leaf.yml`. The resolved repository decides, not the alias text. **(2) The service has no cycle detection (C-E03-208)** — a self-including and a mutual a→b→a template both die of `Maximum object depth exceeded`, which is *none* of the three documented limits (100 files / 100 nesting / 20 MB, C-E03-199). We cannot reproduce the mechanism (recursing until the stack blows is a hang, not a behavior), so the resolver detects the repeat over the **active** stack and emits that sentence at the file the service attributes it to — the *repeated* file, matching both probes. Active-stack rather than visited-set is load-bearing: a diamond is legal and expands twice (C-E03-209). Divergence is in the mechanism only, recorded deliberately; E03-S04-T01 inherits reconciling the documented limits with the emitted message. **(3) The alias splits on the *first* `@` (C-E03-210)** — `a.yml@self@self` asks for a repository named `self@self`, and a file genuinely named `we@ird.yml` is unreachable (probed with the file present: `No repository found by name ird.yml`). `lastIndexOf('@')` reproduces neither. Also measured: repository context stays switched inside a cross-repo template for bare *and* absolute paths (C-E03-216); `@self` is the definition's repo from anywhere, including from inside another repository (C-E03-214); alias lookup folds case while path lookup does **not** (C-E03-213/204); an empty alias means `self` (C-E03-212); the reference is never trimmed (C-E03-205); `./` is ordinary and `\` normalizes to `/` (C-E03-202/203); the escape check is on the *result* so a traversal may dip and recover (C-E03-206); the rejection prints the **unnormalized** join, the only reason `//../outside.yml` and `/../e03-refs/leaf.yml` differ (C-E03-200); the not-found sentence names repository URL, branch and pinned commit, and the same commit recurs across a run, which independently confirms "resolved only once" (C-E03-207); and a cross-repo file is *named* `<path>@<alias>` in diagnostics (C-E03-217). **The fixture trees are committed files** (`fixtures/oracle/references/repos/{self,templates}/`) that the survey script pushes and the tests mount as local repositories, so the suite replays all 33 in-scope probes against the service's own `finalYaml` instead of a copy of the tree — re-running the survey after editing a fixture reports the tree as changed, which is the drift alarm. The 34th probe (`alias-undeclared-repo`) is excluded with a reason: it fails while resolving *resources*, before any reference is read, and belongs to E08. Docs updated per rule 5: docs/02 §5 (the per-file bullet stated only half the rule; cycles added), docs/06 §5 decision 35, REFERENCES.md templates-page row re-verified (evidence: research/E03-template-engine.md C-E03-195..218; research/experiments/E03-references/ 34 probes; fixtures/oracle/references/ 21 pairs + 2 repo trees; packages/engine/test/template/reference.test.ts 69 tests; suite 1,439 Vitest + 162 Bats green, build/typecheck/eslint/prettier/shellcheck/VERIFY-guard clean)
2026-08-20 E03-S02-T02 in-progress — claimed by worker `claude` on worktree `/root/wt-claude`, branch `claude/e03-s01-t04`; typed parameter binding, claim block C-E03-300..339 (evidence: pending)
2026-08-21 E06-S04-T03 done — `task.prependpath`, `task.setsecret`, `task.complete`, `task.logissue`, `task.setprogress`, `task.debug` handlers plus `azdo_render_stream` ANSI rendering of the formatting commands; `run_step` gained per-attempt command state, the grounded result precedence (complete merges → exit status assigns → command failures merge → continueOnError downgrades), retry-while-exactly-Failed, and per-step issue counters under `results/issues/`. Corrected two docs/04 statements — the §6 table rows and, on re-verification, the §5 step-6 result-precedence summary that still echoed the old model (docs/06 §5 decision 36): issue counts alone do not change a step result, and `System.Debug` gates the debug *channel* rather than filtering echoed `##[debug]` lines — hiding them from the console is a recorded local decision, with logs kept verbatim. Fixed a T02 defect where a failing handler aborted the output reader and dropped the rest of a step's console/log output, and a `azdo__logging_fold` local-name shadowing bug. The command-failure message deliberately narrows the agent's shape — it names `area.action` instead of interpolating the wire line, because a handler that rejects a value fails before registering it with the masker, which runs downstream of dispatch. No hosted run: no oracle credentials in this environment. (evidence: research/E06-runtime.md C-E06-057..068; docs/04 §6 table rows + docs/06 §5 decision 36; packages/runtime/test/core.bats cases 42–55, all 55 green (C-E06-068 added on a reverse code→claim check: the `task.issue` dispatch arm was correct but uncited, and that alias appears only in the agent source, not the doc page); suite 175/176 Bats (the 176th case is C-E06-068’s, added after this line was first written), build/typecheck/eslint/prettier/shellcheck/VERIFY-guard clean. The one red case is **environmental, not ours**: `fixture-store.bats` "preserves the executable bit" (E12-S01-T01) asserts `[ -x ]` on a copy under `BATS_TEST_TMPDIR`, and this sandbox mounts `/tmp` `noexec`, so `access(X_OK)` fails while the mode bits are `755` — `findmnt /tmp` reports `noexec` and a two-line `chmod 755` repro under `/tmp` reproduces it with no runtime code loaded. Left unpatched: the mode-bit-vs-`access(X_OK)` distinction is E12's to own)
2026-08-21 E06-S04-T04 in-progress — claimed by autonomous Claude worker on worktree /root/wt-claude, branch claude/e06-s04-t03; artifact/attachment/build logging commands, claim block C-E06-069.. (evidence: pending)
2026-08-21 E06-S04-T04 done — the artifact/attachment/build logging commands: `artifact.upload`, `artifact.associate`, `task.addattachment`, `task.uploadfile`, `task.uploadsummary`, `build.uploadlog`, `build.uploadsummary`, `build.updatebuildnumber`, `build.addbuildtag`. Scope was widened past the task's named four on purpose: the three attachment commands are **one function** in the agent (`TaskAddAttachmentCommand.AddAttachment`, with `uploadfile`/`uploadsummary` as calls into it), so shipping a subset would leave the shared helper half-tested, and the two `build.upload*` commands are one case arm each on top of it. Three findings decided the code, each contradicting the obvious implementation. **(1) An empty directory is a counted warning and a success, not a failure** (C-E06-070) — `context.Warning` → `AddIssue(Warning)` then `return`, so the natural port of "nothing to upload" gets the agent exactly backwards. **(2) The container item path is relative to the source's *parent*** (C-E06-071): a file contributes its basename, a directory contributes its *contents* and its own name never appears. **(3) `build.updatebuildnumber` legitimately overwrites a read-only variable** (C-E06-081) — `build.buildNumber` is a member of `Constants.Variables.ReadOnlyVariables`, but the agent enforces read-only in `TaskSetVariableCommand`, not in `Variables.Set`, and the source comment names this command as the reason; `azdo__write_var` was therefore split into a checked front and an `azdo__write_var_unchecked` that still *preserves* the flag, and the test asserts both that a later step sees `BUILD_BUILDNUMBER` and that `task.setvariable` on the same name is still refused. **Two docs/04 §6 rows were corrected rather than implemented (rule 5):** there is no `task.uploadartifact` command on the doc page or in the agent, and `build.uploadlog` is a real attachment command (fixed name `CustomToolLog`), not "ignored with a debug note"; `build.uploadsummary` exists too, deprecated and undocumented. The layout decision is docs/06 §5 decision 37: **`.artifacts/` is keyed by artifact *name*** — what E06-S05-T01's `$(Pipeline.Workspace)/<name>` download asks for — with `containerfolder` recorded in `.artifacts/.meta/<artifactname>` rather than becoming a directory level, because a download of a container artifact yields the folder's contents, not the folder. Two smaller local decisions recorded there: attachment type segments use `Distributedtask.Core.Summary` (the only `CoreAttachmentType` value with a citable source — the doc's own shorthand, which makes the shorthand identity testable) and the C# member names `FileAttachment`/`Log` for the two whose wire values live in the closed WebApi assembly, rather than inventing spellings; and `artifact.associate` is accepted-validated-recorded on the `task.setprogress` precedent instead of the unknown-command path, since its message is a server-side coordinate with nothing to materialize. Also grounded and deliberately *not* reproduced: .NET's platform-dependent `Path.GetInvalidFileNameChars()` guard (the narrower `azdo__valid_store_segment` is reused, C-E06-076), the doc's server-side "no colon in a build tag" rule (C-E06-082), and four host-type/ArgUtil/async-queue preconditions (C-E06-083). Thirteen mutations turn the suite red — containerfolder default removed, artifacts keyed by container folder, empty-directory-as-failure, directory name included, checked write for updatebuildnumber, read-only flag dropped, trimming removed, case-sensitive tag dedup, uploadfile/uploadlog name derivations, attachment accepting a directory, and the directory branch's `TrimEnd(separator)` — the last of those found late, in review: without it `##vso[artifact.upload artifactname=drop]$(Build.ArtifactStagingDirectory)/` leaves the prefix strip unmatched and silently nests the whole absolute path inside the artifact, and the agent's own quoted line is a `TrimEnd` that belongs only to the directory branch (the file branch takes `Path.GetDirectoryName`, so trimming earlier would accept `file.txt/`) — with a no-op control (`cp file dir/` vs `cp file dir/name`) staying green. **Two red gates are inherited, not introduced, and both are filed:** (a) three `core.bats` cases are red **on `origin/main` too** — the merge of the two parallel E06-S04 implementations left duplicate test blocks with conflicting claim numbering (C-E06-057 means `setsecret` in one and `prependpath` in the other), two ANSI-rendering expectations the trunk renderer cannot satisfy, and two signatures of `azdo_step_issue_count`; filed as **E06-S04-T05** with the reconciliation precedent named. (b) `pnpm test:unit` fails its coverage thresholds on this branch only: crash-recovery commit `f76a47b` swept in an unfinished, unimported, untested 755-line `packages/engine/src/template/parameters.ts` from the abandoned 2026-08-20 E03-S02-T02 session, ~247 uncovered statements; noted on that task, left in place (deleting another session's WIP is not this task's call) and thresholds **not** lowered. The same sweep also brought `scripts/parameter-binding-survey.ts` plus ~60 committed E03-parameters probes and fixture pairs, and that script broke `pnpm lint:es` (4 `no-useless-escape`) and `pnpm typecheck` (`exactOptionalPropertyTypes` on an optional `templateParameters`) repo-wide; both are **fixed here** because they are provably semantics-free — `"\$"` and `"$"` are the same double-quoted string (the first blanket `sed` attempt was reverted after it also hit a template literal, where the escape is real), and the probe factory now spreads the optional field instead of writing `undefined` into it. Build, typecheck, eslint, prettier and shellcheck are green on the branch as a result; only the coverage thresholds and the three E06-S04-T05 cases remain red. Also recorded: E03's checkboxes understate the epic by five finished tasks on `main` as well as here, so a cold "next unchecked task" pick lands on finished work — noted at the top of the epic file. No hosted run: no oracle credentials in this environment. (evidence: research/E06-runtime.md C-E06-069..083; docs/04 §1 + §6 five rewritten rows; docs/06 §5 decision 37; research/REFERENCES.md agent + logging-commands rows re-verified; packages/runtime/test/core.bats 12 new cases, 69/72 green — the 3 reds are the inherited E06-S04-T05 set; 1,439/1,439 Vitest; shellcheck + prettier clean)
2026-08-21 E06-S04-T05 in-progress — claimed by Claude on claude/e06-s04-t03 (/root/wt-claude) (evidence: pending).
2026-08-21 E06-S04-T05 done — reconciled the two merged logging-command test suites in favour of the trunk implementation, deleting the five orphaned cases whose conflicting claim numbering (C-E06-057 as `setsecret` vs `prependpath`) and three reds came in through the two `accept theirs on conflicts` merges. The reconciliation is smaller than the task assumed and the check that proves it is the one a green suite cannot make: **only the tests were duplicated.** `packages/runtime/lib/core.sh` has no duplicate function definition, one renderer and one dispatch arm per command, so the deleted block was never a rival implementation — it was tests written against one that never landed, which is also why its `azdo_step_issue_count <step> <kind>` signature does not exist in trunk. The two reds resolve on grounds the epic's own **Ground** field names before any code is read: `path.d` newest-wins is C-E06-012/057 and the losing assertion (two files under `path.d`) measured a storage shape, not the resulting `PATH`; and C-E06-066 makes the formatting commands message-only with the hosted agent emitting tags alone, so **no source prescribes escape sequences** — the losing tests asserted a particular tag-preserving `\033[1;33m` sequence, i.e. a local decision (recorded in decision 36) rather than parity, against a renderer that strips the tag prefix and needs `AZDO_COLOR=always`. **Two assertions were ported, not dropped,** because they were the losing block's only non-redundant content: `task.complete result=SucceededWithIssues` alone yields `SucceededWithIssues` at exit 0, and execution continues past the `task.complete` line; both now live in the trunk precedence case. Claim-citation coverage was diffed mechanically (cited IDs before vs after) rather than eyeballed, which turned up a pre-existing gap unrelated to the merge — C-E06-067 (`task.setprogress`) had no test citing it by ID although the debug-channel case asserts its ignore note — and that citation is added. The task's "renumber the losing claim block" step is a no-op and is recorded as such so a future reader does not hunt for a phantom block: `research/E06-runtime.md` carries exactly one entry per ID and already used the trunk numbering, so the conflict lived only in the deleted test titles. **Two inherited reds remain, both out of this task's scope and both already filed:** `pnpm test:unit` passes 1,439/1,439 but fails its coverage thresholds on the unimported, untested 755-line `packages/engine/src/template/parameters.ts` swept in by crash-recovery commit `f76a47b` (noted on E03-S02-T02; thresholds not lowered), and `bats test` shows 187/188 because `/tmp` is mounted `noexec` in this environment, so the fixture-store exec-bit case fails on the mount, not the code. No hosted run: no oracle credentials in this environment; no new sources were consulted, so `research/REFERENCES.md` is unchanged by design — this task cites only existing claims. (evidence: docs/06 §5 decision 38; `packages/runtime/test/core.bats` 67/67 green, 5 cases deleted, 2 assertions ported, 1 citation added; 1,439/1,439 Vitest; build/typecheck/ESLint/Prettier/ShellCheck clean)
2026-08-21 E06-S04-T05 follow-up — closed two coverage holes the claim-ID diff structurally could not see, and corrected the docs/06 §5 decision 38 sentence that had leaned on it. Diffing cited claim IDs proves every claim still has a *reference* in some test title; it says nothing about a lib branch whose only exercising case was deleted while the ID stays cited by a different test. Two branches were dark after the delete: `azdo_step_issue_count` lost all three of its direct callers (all three lived in the deleted block; it survived only transitively via `azdo_step_issues`, so its arity and unknown-kind guards were untested), and `azdo_render_stream`'s `debug` arm lost the only input reaching it under color — the trunk formatting case feeds group/warning/error/section/command/endgroup/ordinary and no `##[debug]`, and the raw-debug case asserts uncolored output. Direct coverage added to `command state is per step and per job scope` and `formatting commands render with ANSI only when color is enabled`; the latter also pins the arm's real shape, which is that the debug tag is the one tag the renderer drops *entirely* when the channel is closed, independently of color. Both holes verified by mutation rather than by inspection: `\033[2m`→`\033[3m` and the `error` arm reading `warnings` each turn the suite red, and `lib/core.sh` is byte-identical after the reverts. (evidence: `packages/runtime/test/core.bats` 67/67 green with 2 mutations confirmed red; docs/06 §5 decision 38 amended)
2026-08-21 E06-S05-T01 in-progress — claimed by Claude on claude/e06-s04-t03 (/root/wt-claude); pipeline artifact publish/download, claim block C-E06-084.. (evidence: pending).
2026-08-21 E06-S05-T01 done — `azdo_artifact_publish` / `azdo_artifact_download` plus the deployment-job `azdo_artifact_auto_download` injection point, over the same run-level `.artifacts/<name>/` store `artifact.upload` writes (decision 37). The first finding is where the *implementation* is: both tasks are **`AgentPlugin` tasks**, so `task.json` carries only inputs and the behavior lives in `src/Agent.Plugins/PipelineArtifact/PipelineArtifactPlugin{V1,V2}.cs` — and specifically **not** in `PipelineArtifactPlugin.cs`, which holds the older V0 classes of the same shape. **Three findings contradicted the task as written.** (1) The **Do** field's download target `$(Pipeline.Workspace)/<name>` is the `download:` **keyword's** layout (C-E06-084), not the task's: `DownloadPipelineArtifact@2` defaults `path` to `$(Pipeline.Workspace)` and appends the artifact-name segment **only** on the no-name branch (C-E06-085/086/087), so building the segment into the runtime function would emit `$(Pipeline.Workspace)/<name>/<name>` for every pipeline that writes the task explicitly with its documented default. The function reproduces the task; the emitter passes the keyword layout through `--path`, and the test asserts both layouts side by side (docs/06 §5 decision 39; docs/04 §7 corrected per rule 5). (2) A relative `path`/`targetPath` resolves against **`System.DefaultWorkingDirectory`**, not the pipeline workspace — the plugin source against its own task.json help text, decided by BACKLOG §3's source hierarchy and observable because the two directories differ (C-E06-089). (3) Pattern lists are **order-sensitive**: the agent applies each pattern to an accumulating map, so `!*.md` followed by `**` selects *everything*, which the obvious "match all includes, then subtract all excludes" implementation cannot reproduce (C-E06-090); the list splits on newlines only, so a `;`-joined value is one literal pattern (C-E06-088), and `#` is read as a comment *before* the `!` prefix, with an even number of `!` an include. Also grounded and deliberately not implemented: `.artifactignore` and the implicit `.git` exclusion are server/BlobStore-side (C-E06-095), and the minimatch subset stops at `**`/`*`/`?` — braces and bracket classes are matched literally with a `degraded` note rather than guessed at. Three local decisions are recorded in decision 39: the artifact-name guard is the **union** of the agent's forbidden set and `azdo__valid_store_segment` (which additionally rejects `""`, `.` and `..` — names the agent accepts but a directory under `.artifacts/` cannot be); an associated artifact fails a *named* download and is skipped with a debug note on the multi-download branch, where "no files" is explicitly not an error; and `--source specific` is refused rather than silently served from the current run (E08 owns it). The file-source rule (a file contributes its basename) is an **inference** from the container rule C-E06-071, flagged in C-E06-094 because the pipeline-artifact path runs through the closed BlobStore client — a later oracle run can falsify it. Verified by mutation rather than by a green run: **12 mutations each turn the suite red** — `.default` fallback removal, `*` crossing separators, named download appending the name, unnamed download flattening it, `;` splitting, sticky excludes, `#` as an ordinary character, `!` without the toggle, the forbidden-character set dropped, relative paths resolved against the workspace, publish reusing `artifact.upload`'s empty-directory warning, and a missing artifact downloading silently. One process note worth keeping: an earlier mutation batch timed out mid-run and left a mutation applied, which a later batch then backed up as its baseline — every result in that batch was invalid. Mutation work was redone against a **committed** baseline restored with `git checkout`. No hosted run: no oracle credentials in this environment. (evidence: research/E06-runtime.md C-E06-084..096; docs/06 §5 decision 39; docs/04 §7 four rewritten bullets; research/REFERENCES.md pipeline-artifacts + steps-download + agent `42bde98b` + tasks `299572e2` rows; `packages/runtime/test/core.bats` 10 new cases, 77/77 green in that file and 197/198 for `bats test` — the one red is the environmental `/tmp` `noexec` exec-bit case; ShellCheck, ESLint and Prettier clean; `scripts/check-verify-markers.sh --all` clean; Vitest 1,439/1,439 pass — `pnpm test:unit` still exits non-zero on the **pre-existing** engine coverage-threshold shortfall, untouched by this task, which changed no TypeScript)
2026-08-21 E06-S05-T02 in-progress — claimed by Claude on claude/e06-s04-t03 (/root/wt-claude); checkout self modes (clone/copy/worktree) + option matrix, claim block C-E06-097.. (evidence: pending).
2026-08-22 E06-S05-T02 done — `azdo_checkout` for the `self` repository in three modes with the full option matrix and `Build.*` seeding. The first correction is to the task's own **Ground** field: git-scm.com tells you what `--depth`, `--tags` and `-ffdx` *do*, not which flags the agent emits for each YAML option, so the mapping was read from **`src/Agent.Plugins/GitCliManager.cs`** (each method carries its literal command line in a comment) and `GitSourceProvider.cs` at the `42bde98b…` pin already in REFERENCES, with git-scm cited for flag *meaning* — BACKLOG §3's source hierarchy, the same call as C-E06-089. **The headline finding is that `fetchTags: false` does not stop tags from being fetched.** The agent's fetch line carries `--no-tags` **and** `--prune-tags` (C-E06-097), and git documents `--prune-tags` as "a shorthand for providing the explicit tag refspec", "equivalent to having `refs/tags/*:refs/tags/*` declared in the refspecs" — an explicit refspec beats `--no-tags`' automatic-tag-following suppression — while the knob that would drop it, `DisableFetchPruneTags`, has a built-in default of `false` (C-E06-111). Reproduced locally: `--no-tags --prune --prune-tags` brings the tag down anyway. The `steps-checkout` page's entire "Sync tags" section therefore oversells the option; the runtime emits the agent's command line and states the discrepancy under `System.Debug` rather than inventing a divergence to match doc prose. **Three of the six options have no YAML-recoverable default at all:** `clean`, `fetchTags` and shallow fetch are *pipeline-settings-UI* values, and `fetchTags`/shallow additionally depend on whether the pipeline predates sprint 209 — facts a converter reading a file cannot know. Decision 40 uses the agent's own empty-input defaults (`clean=false`, `fetchDepth=0`, `fetchTags=true`, C-E06-099/100) because they are the only values with a citable source for the exact situation the emulator is in, and are also the conservative pair. **Two further **Do**-field corrections.** (1) `clone` is not a reference clone: the agent does `git init` → `remote add origin` → `fetch` → `checkout --force <commit>`, ending detached (C-E06-102), and git *silently ignores* `--depth` for a local-path remote — warns, exits 0, full history (experiment `research/experiments/E06-checkout/local-shallow-clone.md`, C-E06-109) — so a reference clone would have accepted `fetchDepth` and done nothing with it; the remote is `file://<source>`. (2) `copy` uses `tar`, not `rsync`: rsync is absent from this container and unguaranteed in the target images, and a two-arm fallback would have left the untestable arm untested in the one function that empties a directory. Also grounded: `ConvertToBoolean` recognizes only `1/true/$true` and `0/false/$false` and silently defaults everything else, so `clean: yes` does not clean while `fetchTags: yes` does sync tags (C-E06-100); `clean` is `git clean -ffdx` **then** `reset --hard HEAD` (C-E06-101); `lfs: false` exports `GIT_LFS_SKIP_SMUDGE=1` to every git command and a failing LFS fetch is a *warning* the agent continues past (C-E06-104) — which is what the "lfs skipped if unavailable w/ warning" criterion rests on; `Build.SourceBranchName` is the ref's **last path segment**, so `refs/heads/feature/tools` is `tools` (C-E06-106). Local decisions in 40: `copy` **refuses** `clean: true` because it would delete the uncommitted work the mode exists to run; every no-op cell in the mode×option table announces itself instead of staying silent; the target path must resolve inside `$(Pipeline.Workspace)` (local hardening beyond the agent's opt-in variable, because `clean` and `copy` both delete inside it); and `Build.*` are seeded-never-overwritten with `Build.SourceVersion` read *before* the checkout so an `.env` override actually selects the commit. **`submodules` turned out to be broken, not merely untested.** The first pass shipped a parser test and no integration test for it, and the option fails outright in `clone` mode: since CVE-2022-39253 git refuses the `file` transport for *submodule* clones, and 40(b) makes the origin `file://` by construction, so `git submodule update --init` dies with "transport 'file' not allowed" (measured on git 2.47.3, C-E06-112). The restriction is a consequence of the emulator's own `file://` decision — the hosted agent's submodule URLs are https and never meet it — so it is relaxed with `-c protocol.file.allow=always` on `submodule sync`/`update`, recorded as decision **40f** and labelled a deliberate widening rather than parity. `submodule foreach` clones nothing and is left alone. Two integration cases now cover it: `true` takes one level while `recursive` reaches a nested submodule (the only assertion separating the `--recursive` arms), and `clean` with submodules enters the `foreach` pair that `git clean -ffdx` on the outer repo cannot reach. Verified by mutation against a **committed** baseline: **23 mutations each turn the suite red**, including the `file://` URL, `--prune-tags`, the ConvertToBoolean fallback, a negative fetch depth, `--unshallow`, each half of `clean`, the copy-mode clean refusal and its note text, the seed-never-overwrite guard, `worktree --detach`, the workspace guard, its *ordering* against `mkdir -p` (the guard used to create the directory before refusing the path), `GIT_LFS_SKIP_SMUDGE`, the branch-name segment rule, the copy-target wipe, the read-before-checkout ordering, the `s/` default, and four on the submodule path — the `protocol.file.allow` relaxation, `--init`, `--recursive` and the `foreach` pair. No mutation is claimed on `submodule sync`: it is a no-op on a fresh clone and would survive. A first mutation pass left **three survivors** — the copy/clean note (the assertion only saw the shared `(degraded)` suffix), the seed guard (the override test re-read values the checkout would have written back byte-identically) and `worktree --detach` (redundant while the committish is a SHA) — each fixed by strengthening the test, the last with a case where `.env` sets `Build.SourceVersion` to a *branch name*, which is when `--detach` earns its place. Out of scope and reported rather than dropped: multi-checkout layout (T03), `persistCredentials` (E08), `fetchFilter` and the two `sparseCheckout*` inputs (C-E06-110). No hosted run: no oracle credentials in this environment. (evidence: research/E06-runtime.md C-E06-097..111; research/experiments/E06-checkout/local-shallow-clone.md; docs/06 §5 decision 40; docs/04 §8 rewritten with the mode×option table; research/REFERENCES.md steps-checkout + predefined-variables + agent `42bde98b` + git-scm `git-fetch` rows; docs/06 §5 decision 40f and C-E06-112/113; `packages/runtime/test/core.bats` 14 new cases, 92/92 green in that file and 212/213 for `bats test` — the one red is the environmental `/tmp` `noexec` exec-bit case; ShellCheck, ESLint and Prettier clean; `scripts/check-verify-markers.sh --all` clean; Vitest 1,439/1,439 pass — `pnpm test:unit` still exits non-zero on the **pre-existing** engine coverage-threshold shortfall, untouched by this task, which changed no TypeScript)
2026-08-22 E06-S05-T03 done — implemented the multi-checkout layout (single → `s`, multiple → `s/<repoName>` including self, `path:` overriding both and rooted at Pipeline.Workspace), the `GetCloneDirectory` port that names those folders, non-`self` repository checkouts, and the two path variables' non-obvious seeding: `Build.SourcesDirectory` and `Build.Repository.LocalPath` both stay at `s` under multi-checkout even though self's files moved one level down, and only a `path` differing from the default moves `LocalPath` (evidence: `research/E06-runtime.md` C-E06-114..121; `packages/runtime/test/core.bats` 11 new cases, 19 mutations confirmed red; docs/04 §8.1 + docs/06 §5 decision 41; 223/224 Bats — the one red is the pre-existing `/tmp`-noexec exec-bit fixture case; 1,438/1,439 Vitest — the one red is a pre-existing 5-second-timeout flake in `packages/engine/test/schema-vendor.test.ts`, confirmed on a stashed clean tree and untouched by this task; ESLint/Prettier/ShellCheck/VERIFY guard clean).
2026-08-22 E00-S04-T01 done — expansion service API: `packages/fetch/src/expand.ts` wraps `preview()` into `expand()` returning a discriminated `ExpansionOutcome` (`expanded` → `{finalYaml, provenance}`) with `provenanceFor()`/`expansionRequestHash()` (sha256, content-addressed, for the cache/lockfile) and the `redacted: true` invariant; re-exported from `packages/fetch/src/index.ts` so `convert` can import it. Grounding reused the live C-E00-017/018/023..025 claims — the service is the authority, so no new oracle calls. Also fixed BACKLOG §5 to schedule E00-S04 before E12 (E12-S01-T01 gates the engine behind the expansion path, so the client must exist first). (evidence: packages/fetch/test/expand.test.ts 8 tests + oracle 18 = 26 green; fetch typecheck/eslint/prettier clean; research/experiments/E00-expansion/expand-example.md)
2026-08-22 E00-S04-T02 done — expansion cache & lockfile: `packages/fetch/src/expansion-cache.ts` (`cacheExpansion`/`readCachedExpansion` keyed by the request sha256, `writeExpansionLockfileEntry` merging an `expansion` field into `azdo-emu.lock.json` without clobbering other fields, `expandCached` with `--frozen` cache-only resolution + `ExpansionCacheMissError`, and `finalYamlHash`). The cached `final.yml` is stored raw — D8 guarantees no secret *values* in YAML, `.cache/` is gitignored and the PAT is auth-only, so `redact()` stays for shared transcripts; docs/05 §4 documents the layout + lockfile field. (evidence: packages/fetch/test/expansion-cache.test.ts 8 tests, fetch suite 35 green, fetch typecheck/eslint/prettier clean)
2026-08-22 E12-S01-T01 blocked — the gate is built, two Done items are not reachable yet. Built: `resolveExpansion()` (`packages/fetch/src/expansion-source.ts`), the one seam that decides service-vs-local expansion, so "the default path never touches the local engine" is a single assertion rather than a property of however `convert` is wired; the service arm goes through `expandCached` (cache + lockfile + `--frozen` intact), the offline arm writes **neither** because an `expansion` lock entry pins an api-version and pipeline id the local engine never had; an unconditional `OFFLINE_EXPANSION_WARNING`; a discriminated `ExpansionManifestEntry` that is structurally unable to claim service provenance on the offline arm; and the `--offline-expand` flag on `convert`, documented as a degraded fallback. **Blocked on:** (1) the local engine has no whole-document driver in `src/` — its directive visitors are composed end-to-end only by the test harness `packages/engine/test/template/fixture-harness.ts`, and promoting that is E03-S04-T02, so the offline arm is a port whose default binding refuses with a message naming that task; (2) no manifest writer exists (E05), so the choice is returned typed rather than written. The end-to-end assertion through `convert` waits on E10-S02-T01 (convert wiring). Grounding: BACKLOG §3.4 exemption — this is an expansion *consumer*, no ADO behavior is implemented, so the sources are PLAN D3/D4/D6 and docs/07 §6 and no new claims were pinned. (evidence: packages/fetch/test/expansion-source.test.ts 12 tests; packages/cli/test/program.test.ts 3 new + updated convert help snapshot; 1,470/1,470 Vitest green; ESLint/Prettier/typecheck/VERIFY-guard clean; docs/06 §5 decision 42, docs/06 §1 flag row, docs/05 §4)
2026-08-22 E12-S01-T02 done — demotion sweep. **Demoted `[~]` (6):** E03-S02-T03 `extends` semantics and E03-S02-T04 `templateContext` → the service performs both; the default path bundles template *files* for it (E03-S06/S07) rather than interpreting the keywords. E03-S03-T01/T02 compile-time variable visibility → decided by the service; only the runtime expression half stays local (PLAN D6). E03-S04-T01 server limits → the service enforces its own and reports a breach as the `rejected` outcome `expand()` already surfaces. E03-S05-T02 `preview-diff` → removed from the CLI surface (E10 header); drift detection is now E11-S03-T01's nightly re-expansion. **Deliberately not demoted, with the reason recorded on each:** E03-S04-T02 (expanded-YAML emitter) is the offline fallback's *missing entry point* — the engine's directive visitors are driven end-to-end only by `packages/engine/test/template/fixture-harness.ts`, and its one consumer is the `--offline-expand` port from E12-S01-T01 — and E03-S04-T03 (strict post-expansion validation), which re-points at the service's `finalYaml`. **Not flipped:** E03-S01-T02..T05 and E03-S02-T01/T02 are built but unchecked (the 2026-08-21 drift note); the sweep marks scope, not completed work, and cannot certify another lane's Done criteria — the S01 drift note now says so explicitly. **E02:** nothing to demote — every task is `[x]` (S03-T02 already `[~]`) and the shared `evaluateExpression` entry point serves the runtime half too; recorded in the epic header so the empty result is not read as an omission. **E04/E05 references to a locally-computed expansion:** E04's `Depends on: E01, E03` corrected to `E00-S04, E01` (epic file + BACKLOG §4 row); a pointer added to E04-S03-T04 that the manifest is where E12-S01-T01's typed `ExpansionManifestEntry` gets serialized, with docs/06 §5 decision 42(b)'s stale-lock-entry interaction named; E05 had none. Also narrowed E01-S02-T02's blocker from "E00-S04" (landed) to "E10-S02-T01 convert wiring". **Surface left inconsistent on purpose:** `packages/cli/src/program.ts` still registers `preview-diff` and docs/06 §1 still lists it, now that its backlog task is `[~]` — removing the command is E12-S02/E12-S03 scope, and the pointer is recorded on the demoted task so whoever picks up E12-S03-T01 finds it. (evidence: `grep -rn '^- \[~\]' backlog/` = 9 lines, each with a pointer note; docs/07 §6 table + BACKLOG rule 3 are the grounding; repo meta-tests 47/47, prettier clean; no code changed)
2026-08-22 E12-S02-T01 done — the weighted coverage report and its `--min-coverage` gate are off the active surface (PLAN D10 revised, docs/07 §6). Rule applied and recorded: **normative surface is removed, descriptive prose is banner-annotated** (BACKLOG rule 3), the same treatment E12-S01-T02 gave its demotions. **Removed:** `coverage.min` from every layer of the config surface — loader `CONFIG_KEYS` + its parse/range block, `AzdoEmuConfig`, `ResolvedSettings`, `DEFAULTS`, and the committed `schema/azdo-emu.schema.json` — plus `CliSettings.minCoverage` and its `resolveSettings` branch, `EXIT.coverage` (exit code **3**, leaving `EXIT` at 0/1/2), the `--min-coverage` flag row and the "`convert` always ends with the coverage one-liner" sentence in docs/06 §1, the `coverage: { min: 0 }` line in the docs/06 §2 example, the `coverage.md`/`coverage.json` entries in docs/04's generated-project tree, and the coverage clauses of the P2 contents/exit criteria and the P5 exit criterion. The now-unused `number()` loader helper went with its only caller. **Banner-annotated rather than deleted:** docs/04 §13 in full, because its ranked gap list is the shape E05-S02-T02's warnings list inherits; docs/06's roadmap item 5 and its open question 2 (settled — moot). **Emitter caveat, stated rather than glossed:** no coverage emitter exists (E05 unbuilt), so the "no coverage files are emitted" criterion could only be met in the design doc — had docs/04 §13's tree lines stood, E05 would have built exactly what this task removed. **Checked clean and recorded (44(d) precedent):** PLAN D4/§2/§3 and docs/04 §9 — the two places that could still have framed real-task mode as deferred/opt-in, a defect no `transpil`/`handler` grep surfaces, and both already read correctly (PLAN D4 is E07-S02-T01's and E07-S03-T01's **Ground**); `schema/` holds no manifest schema, so §11's new `disposition` field pins nothing. One user-facing string did need it: docs/03 §4's stub warning said "no local handler" — E07-S02-T01 would have snapshotted that — and now names the real reason. **Grounding:** BACKLOG §3 — no Azure DevOps behavior is implemented, so the sources are PLAN D10 (revised) and docs/07 §6, and no new claims were pinned; the two existing claims this touches, **C-E13-007** (exit-code policy) and **C-E13-008** (config key set), were amended by *dated addendum* with their original excerpts left verbatim, since a claim records what a source said on the date it was read. C-E13-007's actual decision — usage errors reuse `1` — is unaffected by the set shrinking, and `exit.ts` carries a comment saying so, so nobody re-adds `3` for a different purpose. **Two config tests were re-pointed, not deleted:** the CLI-beats-config precedence table and the falsy-precedence test both used `coverage.min` as their exemplar (the latter for its numeric `0`); no numeric scalar survives in `CliSettings`, so the empty-string case (`--sandbox-image ''` over a configured image) stands in beside the boolean `false` — deleting the test would have dropped the falsy-precedence guard with a green suite to show for it. The schema↔loader "rejects for the reason it claims" guard lost its only `minimum`-keyword case and now anchors on `/project type`. **One loose pointer found and left for E12-S03-T01:** docs/04 describes the generated `README.md` in the *closing paragraph of §12*, a section titled "Sample emitted step (illustrative)" — the README has no section of its own, which is why docs/04's file tree and docs/06 §1 both cite a section number that does not name it; recorded in decision 43(d) with the references made explicit about where the paragraph actually is, rather than renumbering docs/04, which belongs to the docs sync. **Scope fences held:** `--target-os` (deferred Windows host, not D10) untouched; the full docs/01–06 sync is E12-S03-T01 and the six dated revisit entries are E12-S03-T02, so docs/06 §5 decision 43 is deliberately narrow about what *this* diff removed. (evidence: docs/06 §5 decision 43; research/E13-cli-config-doctor.md C-E13-007/008 addenda; docs/04 §13 banner; packages/cli/test/{config,config-schema,program}.test.ts; 1,464/1,464 Vitest + 231/231 Bats green; ESLint/Prettier/typecheck/shellcheck clean)
2026-08-22 E12-S02-T02 partial/blocked — sandbox-by-default and per-job workspace isolation are off the active surface (PLAN D8/D9 revised, docs/07 §6), but the task keeps `[!]`: its Done item "`convert` emits host/shared-workspace defaults" is a **positive** criterion and `convert` emits nothing — its action throws `NotImplementedError` and the emitter is unbuilt (E05; wiring E10-S02-T01). Unlike E12-S02-T01's negative criterion ("no coverage files are emitted"), which an unbuilt emitter satisfies, this one cannot be met in the design doc alone, so it is recorded rather than checked off. **Flipped:** `DEFAULTS.output.execution.environment` `auto` → `host`, `DEFAULTS.output.sharedWorkspace` `false` → `true`, with `schema/azdo-emu.schema.json` and the docs/06 §2 example following. **Removed:** the `auto` member of `ExecutionEnvironment` — it meant literally "sandbox when docker/podman is present", i.e. sandbox-by-default one config key away from the default just flipped — and the P2 row's sandbox-execution-wrapper item plus its "same pipeline green under `--sandbox`" dogfood clause. **Kept, annotated (deferred ≠ cut, per decision 43(e)'s `--target-os` precedent):** the `sandbox` enum member, `output.execution.image`/`dockerSocket`, `doctor --sandbox`, and docs/04 §9's sandbox spec under a banner. **The deferral was given a replacement rather than a marker:** docs/04 §1's tree showed `.work/run-3/<stage>/<job>/` as `Pipeline.Workspace` and §3 said to create it per job — annotating those "deferred" and stopping would have left E05 with no layout and the per-job tree still on the page, decision 43's `coverage.md` failure mode. docs/04 now states one `<run>/workspace/{s,a,b,tmp,TestResults}` per run, shared by every job, with the behavioral cost written beside it (jobs see each other's files → README warning). **No runtime change was needed, and that is structural:** `azdo__artifact_workspace` reads `Pipeline.Workspace` from the variable store, so the layout is an emitter contract, and nothing under `packages/runtime` implements a sandbox. **No claim amended:** C-E13-008 pins config **key names** and the precedence rule, not default *values* — checked, and recorded in decision 44(d) rather than left as a silent gap. Rehomed: the deferred sandbox work now says in P6 that **no backlog story carries it** (E14 was cut), instead of leaving dead E14-S04 task IDs reading as scheduled. Pointer left for E12-S03-T01: docs/01's `Agent.ToolsDirectory` shared tool cache, the third thing D8 names. Per decision 43(e) the formal dated D8/D9 revisit entries remain E12-S03-T02's. (evidence: docs/06 §5 decision 44; docs/04 §1/§2/§3/§9; `packages/cli/test/{config,config-schema}.test.ts`; 1,464/1,464 Vitest + 231/231 Bats green; typecheck/ESLint/Prettier/ShellCheck/VERIFY-marker guard clean; the repo's v8 coverage thresholds fail identically before and after this diff — pre-existing `packages/engine` debt, confirmed by re-running the suite on a stashed tree)
2026-08-22 E12-S02-T03 done — the per-task transpiler plans are archived and their one live remnant removed. **Both Done criteria were already met when the task started**, and that is stated rather than papered over: `grep -rni transpil backlog/ PLAN.md` returned only negations (PLAN §4/§6 "There is no per-task transpiler") and supersession notes (the E07/E08 headers, written when E14 was folded), and E05's header already disclaims a handler registry while E06's dispatches to E07 — so "real-task mode + stubs are the only task paths referenced by E05/E06" needed **confirming**, not editing. The deliverable was the **Do**'s first clause, which nothing had touched. **docs/03 archived by section, not wholesale:** §1 (the `TaskHandler`/`emit` interface and the `Name@major` registry) is superseded by E07-S03-T01's disposition registry (`native | real-task | stub`) — its `EmittedStep` payload kept verbatim because its four channels still feed `.env.example`, `doctor` and the README warnings list, produced per *disposition* now; §3's catalog is retained **as a reference** (which tasks matter first, fidelity expectations, tool prereqs, ambient-auth model) with only its "Strategy" column archived as an emission plan; §2/§4/§5 are labelled **live** (§2 feeds E07-S01-T02's `INPUT_*` materialization; §4 gained the resolution order `native → real-task → stub`, since "unknown" is now the *fallback*, not the default for non-script tasks). **§6 was promoted, not just annotated** — from "P6, opt-in" to the default non-script path: annotating §1/§3 as superseded while leaving §6 opt-in would have left docs/03 describing **no** active task path, decision 44(a)'s exact failure mode; its cost is written beside it (the one mode that adds a run-time **Node** dependency → README + `doctor`). **Active-surface removal — this task's `coverage.min`:** `tasks.execute`, the allowlist for opting *into* real-task mode, cannot express anything once that mode is the default (opting **out** is `tasks.overrides`), so it is gone from `AzdoEmuConfig`/`ResolvedSettings`/`DEFAULTS`, `CONFIG_KEYS` + the loader parse, `resolveSettings`, `schema/azdo-emu.schema.json` and the docs/06 §2 example; C-E13-008 amended by **dated addendum** (original text verbatim); the orphaned `stringList()` helper went with its only caller. **Two tests re-pointed rather than deleted** — `execute` was the config surface's only list-valued key: the schema/loader rejection table's list case became an **unknown-key** rejection of `{tasks:{execute:[]}}` (so a config still carrying the key fails in both layers and the removal is asserted, not assumed), and C-E13-012's "lists replace wholesale" guard moved to a list-valued `parameters` *value*, where the rule still has a subject. **Normative doc lines corrected in place**, the E12-S03-T01 boundary being "does a scheduled task's Do/Ground build from it": docs/04 §12 (E05-S01-T02's named spec) showed a *transpiled* `DotNetCoreCLI@2` as its only sample and now shows the two shapes E05 actually emits — native script step, real-task dispatch with the stub fallback stated; docs/04 §11 (E04-S03-T04's spec) gained the per-step `disposition` field; docs/01 §6 (E04-S01-T01's model spec) `Step.handler` → `Step.disposition` with the matching invariant; docs/06 §4's P4 ("Handler registry as stable API"), P5 (transpiled breadth) and P6 (real-task mode listed as P6-only) rows, P6's exit criterion following the mode to P4. **False positives left alone deliberately** (E06's agent `Handler` class and `##vso` command handlers, docs/04 §1's live `handlers/` user drop-in, the *container* registry of docs/03 group D) — rewriting them would have made the word's disappearance, not the design change, the deliverable. **Grounding:** BACKLOG §3 — no Azure DevOps behavior is implemented, so the sources are PLAN D4 (revised) and docs/07 §5, and no new claims were pinned. Decision docs/06 §5 **45**; the six dated D3/D4/D6/D8/D9/D10 revisit entries remain E12-S03-T02's and the full docs/01–06 sync E12-S03-T01's. (evidence: docs/06 §5 decision 45; docs/03 §1–§6; docs/04 §11/§12; docs/01 §6; docs/06 §2/§4; research/E13-cli-config-doctor.md C-E13-008 addendum; packages/cli/test/{config,config-schema}.test.ts; 1,462/1,462 Vitest + 231/231 Bats green; typecheck/ESLint/Prettier/ShellCheck clean; the repo's v8 coverage thresholds fail identically before and after this diff — pre-existing `packages/engine` debt, re-confirmed on a stashed tree)
2026-08-22 E12-S03-T01 in-progress — claimed by Claude session on branch `e12-s01-t01-offline-expand` (main checkout); docs/01–06 sync against the revised PLAN + docs/07 (evidence: pending).
2026-08-22 E12-S03-T01 done — docs/01–06 now describe the server-expanded, script-native architecture as *the* plan. **Granularity was the finding:** a per-document banner would have been wrong in both directions for **docs/02**, whose §6 runtime half (`$[ ]`, `condition:`, `$( )`, `dependencies.*`) is live and local by PLAN **D6** while §2/§3/§4 are the retained fallback — so the doc opens with a per-section status table and each affected heading repeats its own status: §2 expansion algorithm / §3 interpolation / §4 directives **fallback-only** (entry point still missing, E03-S04-T02), §5 template resolution fallback-only **except the reference forms**, §6 **split**, §7 provenance fallback-only, §8 **live but re-scoped** — the `preview` endpoint is the *expansion source*, not a test-only oracle. **New content, written from tasks rather than architecture: docs/02 §5.1, the bundler.** PLAN's component table pointed at docs/02 §5 for the bundler and §5 described the *fallback's* resolver, so the default path's only local template work was undocumented; §5.1 is a mechanic-per-task table (E03-S06-T01..T04, E03-S07-T01/T02) that also states what the bundler does **not** do — no `${{ }}`, no directives, no parameter binding, cross-repo `@other` is a v1 diagnostic. **docs/06 §4 rewritten, old labels kept:** PLAN §7's live **P1–P3** now sits above the original seven-phase table, which is retained verbatim behind a `<details>` banner with an explicit old→new mapping. Renumbering was rejected on a hard constraint, not taste — `CHANGELOG-BACKLOG.md` is append-only and E12-S02-T01/T02/T03's committed entries cite those rows *by number* ("the P2 exit criteria", "rehomed to P6"); docs/01 §2, docs/03, docs/04 and docs/05 instead gained a one-line pointer that their `Phase` columns index the **archived** table, which is what disambiguates a `P2` that meant two different things in two files. **Corrected rather than annotated** (decision 43's `coverage.md` failure mode — annotation would have let E05 build them): docs/01 §1's "schema validation runs twice" → **one** strict pass over the expanded YAML, the loose raw-file pass dropped because that is the server's job now (E01-S02-T02's re-scope; wiring E10-S02-T01); docs/04 §1's unconditional `expansion-map.json` → fallback-only, with the default path's provenance named as the **bundler's** redacted override + path map (file names left to E03-S07-T01, whose Do says "e.g."); docs/01 §5's `.work/run-<n>/<stage>/<job>` → the shared `<run>/workspace/{s,a,b,tmp,TestResults}` docs/04 §3 already specifies, and `Agent.ToolsDirectory` marked as a set variable with **no cache populating it** (D8's third deferral — the pointer E12-S02-T02 left); docs/01 §2's `extends`, `template:` and **`parameters` (runtime parameters)** rows and §4's three-syntaxes `${{ }}` row → resolved by the service — the parameters row still claimed local binding *and* local `values:`/type enforcement, which E03-S06-T03 explicitly leaves to the service (`--parameter`/config supply the values the bundler passes as `templateParameters`; the typed binder of docs/02 §5 is fallback-only); docs/03 §3's `templateContext` → a compile-time payload that never reaches us (E03-S02-T04). **Active-surface removal — this task's `coverage.min`:** `preview-diff` is gone from `packages/cli/src/program.ts`, docs/06 §1 and docs/02 §8, completing the removal E03-S05-T02's demotion note assigned to E12-S02/S03. Its `NotImplementedError` had named "E12-S03-T01 (nightly parity workflow)" — a pointer at *this* task, which is the docs sync; drift detection is **E11-S03-T01** and fallback parity **E11-S02**, and the E03-S05-T01 normalizer stays because the nightly compares normalized expansions. **Stale task IDs re-pointed in live prose only:** docs/06 §3 said "Repo layout of the runners (E12-S01-T01)" meaning the parity harness — an ID that now names the `--offline-expand` gate, a *collision* rather than a stale link — so §1–§4 say E11-S01-T01/T02 and E10-S01-T01/T02, and `program.ts`'s user-facing hints name E10-S02-T01/E10-S03-T01/E10-S04-T01/E09-S03-T02. **Left verbatim:** §5's dated decision entries, the historical `E13-S01-T01` attribution in the CLI module header and test titles, and every `C-E12-*`/`C-E13-*` claim ID (a claim ID is permanent and names its research file); a new epic-ID note at the top of docs/06 states the mapping. **One step past the stated docs/01–06 range, taken deliberately:** docs/07's own header still read "Status: Proposal for owner review … Nothing in PLAN.md, docs/01–06, or the backlog has been altered", which a reviewer opening the review first would read as "the re-orientation never happened" — it now says accepted, keeps §1–§8 verbatim as the accepted review, and names the E03/E06 reconciliation. **Grounding:** BACKLOG §3 — no Azure DevOps behavior is implemented or claimed, so the sources are PLAN.md (revised), docs/07 and CLAUDE.md rule 5; no claim was pinned or amended, and `research/REFERENCES.md` needed no status change (recorded rather than left silent). **Verification matches the Done criterion, which is a sweep and not a diff:** `grep -rn` over docs/01–06 for `reimplement`, `preview-diff`, `runs twice`, `expanded by engine`, `transpil`, `min-coverage`, `coverage`, `per-job`, `sandbox`, `handler registry`, `oracle`, `P6 execution`, `E13-S0`, `Bound at convert time`, `binding` — every surviving hit is either inside a dated banner/`<details>` archive, a §5 decision record, a claim ID, or a live statement of the current design; the two that were not (docs/06 §3's nightly `preview-diff` mentions) were re-pointed to E11-S03-T01. Decision docs/06 §5 **46**; the six dated D3/D4/D6/D8/D9/D10 revisit entries and CLAUDE.md rule 2's list remain **E12-S03-T02**'s. (evidence: docs/01 §1/§2/§3/§4/§5, docs/02 §1–§8 + new §5.1, docs/03 header, docs/04 §1/§3, docs/05 §1/§2/§4/§5, docs/06 §1/§3/§4/§5, docs/07 header; `packages/cli/{src/program.ts,test/program.test.ts,test/__snapshots__/program.test.ts.snap}`; 1,461/1,461 Vitest + 231/231 Bats green; typecheck/ESLint/Prettier/ShellCheck clean; the repo's v8 coverage thresholds fail identically before and after this diff — pre-existing `packages/engine` debt, re-confirmed on a stashed tree)
2026-08-22 maintenance (not a backlog task) — coverage thresholds in `vitest.config.ts` re-baselined from measurement at the owner's direction, during E12-S03-T01's PR. CI on `main` had been red on five consecutive pushes with every test passing: the numbers were seeded before the rebase that brought in the built E03 engine and E06 runtime, so they measured a smaller repo. Re-seeded symmetrically (floor of measurement): `packages/engine` **down** (90/80/94/94 → 83/79/90/85), `cli` **up** (92/84/90/95 → 95/85/91/97), `fetch` **up** (96/76/100/96 → 97/85/100/97), `emit` unchanged at its placeholder 100, global 90/78/90/92 → 85/79/91/87. The config comment's "never lower one to make a red run green" still stands for the case it was written for — a diff that drops coverage — and docs/06 §3 now states both cases. **No E11 checkbox was flipped**: this is testing-harness maintenance (E11-S02/E11-S04 own paying the engine debt back), not task evidence. Decision docs/06 §5 **47**. (evidence: `vitest.config.ts`; docs/06 §3/§5; 1,461/1,461 Vitest + 231/231 Bats green with the gate now enforcing)
2026-08-22 E03-S02-T05 filed (not started) — defect found while shipping E12-S03-T01: the local-FS loader in `packages/engine/src/template/reference.ts` reads a template with `readFileSync`, so on macOS's case-insensitive APFS a case-mismatched template path resolves where the service rejects it (C-E03-204; oracle transcript `research/experiments/E03-references/case-mismatch/`, HTTP 400). It is the **only red test** in CI on both `main` and PR #22 — `reference.test.ts` > "oracle replay" > `case-mismatch` — and it is macOS-only, which is why it survived: on `main` the six coverage-threshold errors failed the job first and masked it. Filed at the end of E03-S02 rather than fixed inside a docs-sync PR: it is E03-S02-T01's lane, touches `src/`, and needs its own tests. Grounding already exists; no new probe required. (evidence: run 32600568717 macos jobs vs ubuntu green; `git diff --stat origin/main...HEAD` shows this branch touches no engine file)
2026-08-22 E03-S02-T05 in-progress — claimed by Claude session on branch `e03-s02-t05-case-sensitive-lookup` (main checkout); making the local-FS template lookup case-sensitive so CI is green on macOS (evidence: pending).
2026-08-22 E03-S02-T05 done — the local-FS template loader is case-sensitive from the repository tree instead of from the host filesystem. `localFetcher.read` passed `path.join(root, location.path)` to `readFileSync`, which delegates the comparison to the **host**: on macOS APFS that resolved `/E03-REFS/LEAF.YML` against a tree spelling it `/e03-refs/leaf.yml`, which the service rejects HTTP 400 (`PipelineValidationException`) — the same pipeline answering differently on two developer machines. `resolveExact()` now walks the repository-relative path one segment at a time and requires a byte-identical `readdirSync` entry; only segments **inside** the repository are checked, since the checkout root is wherever the user put it. **The Done criterion names macOS, so it was verified on macOS:** run 32603458075 (PR #23) shows that job's vitest step at 41/41 files / 1,468 tests with `case-mismatch` green, against the single failure it had before. **The code had recorded this as an accepted limitation** — "a host limitation rather than a modelled behavior … one-directional" — while E03-S02-T01's own oracle replay asserted the probe *fails*; the comment and the suite disagreed, and the suite was right. **Grounding gap closed rather than inherited:** `C-E03-204` was cited in `reference.ts` and its test since E03-S02-T01 but was never written into `research/`; it is now an entry sourced from the existing probe transcript (no new probe — the service's answer was already on file), and the claim-ID block table now reads "204 used; the rest is E03-S02-T01's to record" instead of `free` while 195..215 are load-bearing in code. **Tests:** 7 direct assertions (exact hit, nested hit, `we@ird.yml`, wrong-case file, wrong-case directory, middle segment that is a file → ENOTDIR, trailing space per C-E03-205), with their honest limit written in the test file — on a case-sensitive filesystem they also hold for the old implementation, so macOS CI is the gate and their local value is the *positive* half (exact spellings still resolve). **CI is still not fully green, and the reason is new information this fix produced:** the macOS job now reaches the **bats** step for the first time — previously vitest failed first and stopped it — and fails on `${value,,}`/`${repository,,}`, bash-4 expansions under macOS's bash 3.2, ~30 tests across the .env loader, artifacts and every `azdo_checkout` mode. Filed as **E11-S01-T03** (install a modern bash on the runner; do **not** downgrade the runtime, since `bash >= 4` is a documented convention). (evidence: `research/E03-template-engine.md` C-E03-204; `research/experiments/E03-references/case-mismatch/`; `packages/engine/src/template/reference.ts` `resolveExact`; `packages/engine/test/template/reference.test.ts`; 1,468/1,468 Vitest + 231/231 Bats green locally; typecheck/ESLint/Prettier/ShellCheck clean; coverage thresholds pass)
2026-08-22 E11-S01-T03 filed (not started) — macOS CI runs the bats suite under the system bash 3.2, which rejects the runtime's bash-4 `${var,,}` expansions outright (`bad substitution`, ~30 failures). Invisible until E03-S02-T05 made the macOS vitest step green, because the job stopped before bats. Fix is on the workflow (`brew install bash`, ahead of `/bin/bash` for that step), not on the runtime: `bash >= 4` is a documented convention and lowering it would need a docs/06 §5 decision. (evidence: CI run 32603458075 macOS jobs; `packages/runtime/lib/core.sh` ~3057/~3605)
2026-08-23 E11-S01-T03 in-progress — claimed by Claude session on branch `e11-s01-t03-macos-bash4` (main checkout); making macOS CI run bats under bash ≥ 4 (evidence: pending).
2026-08-23 E11-S01-T04 in-progress — claimed by Claude session on branch `e11-s01-t03-macos-bash4` (shipping in E11-S01-T03's PR #24, since it is the same "make macOS CI actually run" goal) (evidence: pending).
2026-08-23 E11-S01-T03 done — macOS CI runs the bats suite on the version the runtime targets. `macos-latest` (macOS 26 arm64) ships **only** bash 3.2.57 — the build Apple has shipped since 2007 — while `packages/runtime` targets **bash ≥ 4** (PLAN D2, CLAUDE.md), so every `${value,,}` / `${repository,,}` in `lib/core.sh` was a `bad substitution` and ~30 tests failed across the `.env` loader, artifacts and every `azdo_checkout` mode. Fix: `brew install bash` on the macOS runner and Homebrew's bin prepended through `$GITHUB_PATH` — the PATH is what selects the interpreter, because bats' shebang is `#!/usr/bin/env bash`; the run now logs `bats will run under /opt/homebrew/bin/bash — GNU bash, version 5.3.15`. **A guard step sits in front of bats** and fails with a single message naming PLAN D2 when the major version is < 4: bats itself is perfectly content on 3.2 (C-E00-005), so without the guard the symptom is thirty runtime-looking failures instead of one environment fault. **The most useful finding is a bookkeeping one:** this was not an unknown — C-E00-005, written 2026-07-30, says in its own text "E06 tests must ensure a bash ≥ 4 interpreter on macOS CI". The claim was correct and simply never carried out, and it could not be noticed either: the macOS job failed at an earlier step on every run in the repo's history until E03-S02-T05 turned its vitest step green. The claim is therefore **not amended** — it was never wrong — and the gap is recorded as a dated addendum beneath it. **Grounding:** C-E00-028 (`macos-latest` resolves to macOS 26 arm64, whose only Bash is `3.2.57(1)-release`) and C-E00-029 (the image ships Homebrew 6.0.13 and no second bash, so `brew install bash` + a PATH prepend is the route), both from `actions/runner-images` commit-pinned at `8d3ea005fa2d87f3cbc9255c27fdfed9e901a043`; `research/REFERENCES.md`'s runner-images row now carries that macOS pin. docs/06 §3 states the L4 interpreter rule so the next reader does not re-derive it. **The runtime was deliberately not downgraded to 3.2 syntax** — `bash >= 4` is a documented convention and lowering it would be a decision needing a docs/06 §5 entry, not a CI fix. (evidence: `.github/workflows/ci.yml`; `research/E00-foundations.md` C-E00-028/029 + the C-E00-005 addendum; `research/REFERENCES.md`; docs/06 §3; CI run 32605819867 — all four jobs green)
2026-08-23 E11-S01-T04 done — the bats suite's own path assertions are portable. With bash ≥ 4 in place the macOS suite ran to completion for the first time, and 9 checkout/multi-checkout tests failed comparing `/private/var/folders/…` (runtime) against `/var/folders/…` (test): `$BATS_TEST_TMPDIR` reaches the test through macOS's `/var` symlink, while the runtime resolves physically with `pwd -P` in three places on purpose. **The expectations were wrong, not the runtime, and no runtime source was touched:** `prepare_checkout()` normalizes its scratch root once, and `azdo_emu_scratch_dir()` now returns a physical path so the *next* test to compare against runtime output is right without thinking about it. **Two further faults appeared only because the suite finally ran that far, and neither is named by this task's Do — disclosed rather than folded in:** (1) a regression from this task's own first commit — `fixture-store.bats` asserted the scratch dir is a prefix match of `$BATS_TEST_TMPDIR`, which the stronger helper contract breaks on macOS, so the assertion now compares against the physical tmpdir; (2) `core.bats:1950` compared `wc -l` output as a **string**, and BSD `wc` pads its count with leading spaces — a portability defect with nothing to do with paths, fixed with numeric `-eq` to match the form line 554 already used. **Grounding:** internal — no Azure DevOps behavior is involved, so the sources are the runtime's own `pwd -P` calls and macOS's `/var` → `/private/var` symlink; no claim was pinned. (evidence: `packages/runtime/test/core.bats`, `packages/runtime/test/helpers/fixture-store.bash`, `packages/runtime/test/fixture-store.bats`; 231/231 Bats locally; CI run 32605819867 — **all four jobs green, macOS bats included: the first fully green CI run on this repository in its recent history**)
2026-08-23 E12-S03-T02 done — the six revisits of the simplification now have dated approval entries in the decisions record: docs/06 §5 **48 (D3** server-expanded**)**, **49 (D4** script-native + real-task mode**)**, **50 (D6** runtime half local**)**, **51 (D8** shared workspace**)**, **52 (D9** host default**)**, **53 (D10** warnings list**)**. Each names the old decision it supersedes, quotes the docs/07 §6 row that argued it, and points at the narrow implementation entry — 42 (E12-S01-T01), 43 (E12-S02-T01), 44 (E12-S02-T02), 45 (E12-S02-T03) — that four earlier tasks explicitly fenced off for this one, so the entries are pointers rather than a second telling. **The finding is a numbering collision the revision left behind:** rewriting PLAN §5 *renumbered* the decisions instead of appending, so (a) docs/07 §6's proposal table is written entirely in **old** numbers (its "D4 + D6", "D3 transpile-first", "D11 sandbox" rows), and (b) three dated entries above — 6, 12 and 13 — cite `D6` meaning the **parity oracle**, which under the new numbering is **D3**, while the new D6 ("the runtime half is *not* delegated") says close to the opposite. Dated entries stay verbatim (decision 46(b)/(d)), so the fix is a map, not an edit: an eleven-row old→new table precedes entry 48, docs/07 §6 gained a note saying its numbers are pre-revision (table kept verbatim as a historical proposal), and PLAN §5's D6 is annotated as a **new decision on a recycled number**. Entry 50 does the other thing a bare "the compile-time half is delegated" would have got wrong: it states per *slot* which E02 decisions stay load-bearing (12, 13, 15, 16, 17, 18, 20 — one grammar and one `ExprSlot` serve both halves) and which split (14's `parameters` comparer, 19's `${{ }}` name table), so delegation does not read as retracting the E02 record; decision 19/20's open `environment` slot is a runtime slot and stays open. Rule 2's "decisions already made" list is updated in **both** `CLAUDE.md` and `AGENTS.md` (byte mirrors apart from the title and skills-path lines). Docs-only diff, so the verification surface is small and stated plainly: no test covers docs/06 §5; `pnpm run lint` (eslint + prettier + shellcheck) and the VERIFY-marker guard are clean, and `*.md` is prettier-ignored. E12-S01-T01 and E12-S02-T02 stay `[!]` — these entries record decisions, they do not build the emitter those two wait on. (evidence: docs/06 §5 entries 48–53 + the map note; docs/07 §6 note; PLAN §5 D6; CLAUDE.md/AGENTS.md rule 2)
2026-08-23 E03-S01-T06 filed (not started) — the S01/S02 completion bookkeeping the story header has asked "the E03 lane" to reconcile since 2026-08-21: E03-S01-T02, T03, T04, T05 and E03-S02-T01 are recorded `done` in this changelog with their modules and suites merged on `main`, but only T01 and E03-S05-T01 were ever ticked, so a cold session told to take the next unchecked task picks E03-S01-T03, which is finished. Filed as a task rather than fixed in passing: flipping five of another lane's checkboxes means walking five sets of Done criteria (CLAUDE.md rule 3), which is its own cycle, not a preamble to the bundler. E03-S01-T02's `[!]` blocker note, which predates two `done` entries for it, is folded into the same pass.
2026-08-23 E03-S02-T02 note corrected (not started) — the task's 2026-08-21 status note is stale in its load-bearing clause: `packages/engine/src/template/parameters.ts` **is on `main`** (755 lines, via the crash-recovery commit `f76a47b` "auto-save checkpoint"), not confined to a side branch. Measured rather than estimated: 247 statements / 184 branches / 28 functions / 219 lines, **0 covered on every one**; no importer, no export from `packages/engine/src/index.ts`, no test — and `scripts/parameter-binding-survey.ts` (931 lines) cites a `test/template/parameters.test.ts` that does not exist. The oracle work did happen and should be reused: `research/experiments/E03-parameters/` holds the transcripts with `C-E03-3xx` claim IDs, never consolidated into `research/E03-template-engine.md` (`grep -c 'C-E03-3'` = 0). Original note left verbatim, dated correction appended.
2026-08-23 docs/06 §5 amendment (not a backlog task) — decision 47(c)'s successor pointer corrected. Entry 47 re-baselined the `packages/engine` thresholds down and sent the debt to E11-S02/E11-S04; measurement shows the entire gap is the one orphaned module above. `packages/engine/src/**` measures 83.68/79.26/90.78/85.97 with it and **93.19/87.95/97.40/96.08** without — the re-baselined numbers are the left-hand column exactly. Entry 47's decision stands (a permanently red gate re-baselined from measurement was right, and 47(b)'s symmetry holds); only the successor changes, to **E03-S02-T02**, after which the thresholds go back up in one step.
2026-08-23 E03-S06-T01 done — the bundler's reference detector: `packages/engine/src/template/bundle.ts`, `findTemplateReferences(ParseResult)` → every `template:` reference in document order with its `site`, raw text, `path`/`alias` split (reusing E03-S02-T01's `parseReference`), a `self` flag, the reference **scalar's** `file:line:col`, the sibling `parameters:` mapping and the enclosing `${{ }}` directives. The rule keys off the **container key name at any depth** rather than a root-level path, which is what makes a deployment strategy's `strategy.runOnce.deploy.steps` fall out for free: every `$ref` to `steps`/`jobs`/`stages`/`variables`/`phases` in the vendored schema sits under a property of the same name. **Three findings.** (1) *A fifth container the doc page does not have:* the schema's `phase` branch is identical to `job`'s, so `phases: - template:` is a real position — detected, but written as a **schema-derived** claim per docs/06 §5 decision 8 (the vendored schema is not self-sufficient and the docs outrank it), with the live probe handed to E03-S06-T02's `research/experiments/E03-bundle/`. (2) *References inside `parameters:` values are emitted rather than skipped.* A `stepList` argument may contain `- template: x.yml`, which the service resolves once the receiving template inserts the list; the container-key rule cannot see it (the key is `buildSteps`), and omitting it would expand that file against the **committed** repo silently — the same silent-wrong-expansion failure E03-S06-T04's criterion forbids for `@other`. They carry site `parameters`, so T02 makes the include-or-diagnose call with the data in hand. (3) *A test caught the rule that makes (2) safe:* `parameters:` maps parameter **names** to values, so `parameters:\n  template: v` is a parameter called `template`, and a shape-only scan inlined a file on the name collision. Every schema branch carrying a `template` key is an array `items` type, so requiring **sequence-item position** removes the ambiguity at no cost. Two deliberate mirror-not-measure choices, recorded as such: the key is matched **case-sensitively** (no `ignoreCase` on the property, same as `ignoresKeyCase` in `validate.ts`) and **by presence, not first position** — `firstProperty` names `template` as the discriminator but `checkFirstProperty` enforces ordering only as a warning while E01's Q1 is open, and a detector stricter than the validator would silently skip references the service accepts (C-E03-407). Also: `isSelfAlias` exported from `reference.ts` instead of re-deriving the `undefined`/empty/`@self` test, so the bundler and the resolver cannot disagree; new claim-block rows added to the research file's allocation table (`C-E03-400..429` S06, `430..449` S07) and `C-E03-300..339` marked **effectively taken** by the abandoned E03-S02-T02 lane so nobody reallocates it. (evidence: `research/E03-template-engine.md` C-E03-400..407, doc page pinned at source commit `be5c6557603d2e61bafaa70ec0d4e4ec1351d058`; `packages/engine/test/template/bundle.test.ts` 26 tests; bundle.ts 93% statements / 100% functions; 1,494 Vitest + 231 Bats green; typecheck/ESLint/Prettier/ShellCheck/VERIFY guard clean)
2026-08-23 E03-S06-T02 partial/blocked — the recursive local inliner is built, tested and shipped, and the task keeps `[!]` because the oracle answered its Ground question differently than the task assumed. Built: `packages/engine/src/template/inline.ts` (`inlineTemplates`) splices local `@self` templates into the root override **by text offset** rather than by re-serializing the DOM — "mechanical inliner, not an expander" is only true if the user's bytes survive unchanged outside the splice — recursing into nested references, resolving bare paths against the *referencing* file (C-E12-012) rather than the root, re-indenting spliced items to the reference's own column, and reporting missing files, cycles (with the chain that closed them) and root escapes as E01-shaped diagnostics with `file:line`. A diamond is not a cycle (C-E03-209). **The Ground field asked for one committed-vs-inlined probe pair; six shapes were needed to find the boundary, and the boundary is not where docs/02 §5.1 implied.** Parameterless includes are normalized-identical (C-E03-408), nested ones too (C-E03-409), and — the widening surprise — so is a template that *declares* `parameters:` but never reads one (C-E03-410): the trigger is reading, not declaring. A template that reads `${{ parameters.* }}` is **HTTP 400 `Key not found 'greeting'`** when the parent lacks the name (C-E03-411), because the splice necessarily drops the template's own `parameters:` block and decision 19(c)'s raise-on-miss rule does the rest. **The claim that decided the design is C-E03-412:** when the parent *does* declare the same name, both halves return 200 and the expansions differ — committed `echo leaf-default`, inlined `echo root-value`. Silently wrong, not loudly. So the service cannot police this and the bundler refuses: **inline iff the file reads no `${{ parameters.* }}`** (C-E03-413), enforced by a deliberately conservative *text* scan rather than an expression parse, because the safe answer to a spelling we fail to parse is "it might" and a parser bug must not become a silent mis-inline. A refused reference stays in the override and warns — the expansion is still correct, it just reads the committed file, so the local edit is invisible: degraded, not broken. `extends:` targets and references inside a `parameters:` value are reported the same way. **Why `[!]`:** the Done item "no `@self` references left in the override" is met and tested for parameterless templates and is *unreachable* for parameterized ones, since inlining them means binding, which is the service's job under PLAN D3 — a scope boundary of the simplification, not an oversight. **E03-S06-T05** is filed at the end of the story to decide the product answer (documented limitation / route to `--offline-expand` / a `templateParameters` round-trip), docs/02 §5.1 is corrected in the same diff (as written it would have had this task build exactly the splice that is silently wrong), and docs/06 §5 decision **54** records the measurement and the reasoning. One harness mistake worth recording: the first survey run pushed its fixture tree to the **repository root** instead of `/e03-bundle` — `syncFiles` takes full repository paths in `files[].path` and uses `scopePath` only for the read/delete comparison — so five files landed at the root of the oracle org's repo and were deleted in four scoped commits before the corrected run. (evidence: `research/E03-template-engine.md` C-E03-408..413; `research/experiments/E03-bundle/` 12 redacted probes + `comparison.md`; `scripts/bundle-survey.ts`; `packages/engine/test/template/inline.test.ts` 25 tests; 1,519 Vitest + 231 Bats green; typecheck/ESLint/Prettier/ShellCheck/VERIFY guard clean)
2026-08-23 E03-S06-T03 done — `templateParameters` pass-through, and the headline is that **the task's Do was wrong about the field**. Eight live probes (`research/experiments/E03-parameters-request/`, `pnpm template-parameters-survey`) measured what C-E00-018 only recorded the existence of. **Reference-level and `extends`-level `parameters:` are not request fields at all** — they are YAML the service reads in place — so the Done criterion ("a pipeline with `- template: t.yml` + `parameters: {…}` converts and the expansion reflects the values") is met by measurement rather than by code: E03-S06-T02's `passed-committed` transcript expands to `echo passed-value` against the leaf's own `unused-default`, with no client plumbing. What the field *actually* does is bind the **root** pipeline's runtime parameters, and that is what the code this task added serves. **The finding with reach beyond this task is C-E03-418:** `templateParameters` validates against the root `parameters:` block only and rejects a template-declared name — `"Unexpected parameter 'greeting'"`, HTTP 400 — which **closes option (c) of E03-S06-T05** (a request round-trip that lets the service bind while we supply local bytes) by measurement rather than argument, one task after decision 54 opened it; T05 is annotated and the choice is now between a documented limitation and routing to `--offline-expand`. Built: `serializeTemplateParameters()` — strings pass through, everything else `JSON.stringify`, because a **raw** JSON object in the field is refused with a server-side `ArgumentNullException` (`"Value cannot be null. Parameter name: runParameters"`, no line/col, no remediation) while the same object as a JSON **string** binds and `convertToJson` renders it back as a real object (C-E03-417) — which is what makes the CLI's `--parameter name=@file.json` reachable at all. And a **latent cache collision fixed because this task is what would have triggered it**: `expansionRequestHash` hashed the `yamlOverride` alone, so two conversions of one pipeline differing only in parameter values shared a cache entry *and* a lockfile pin, the second silently reading the first's `finalYaml`; the hash now covers a canonical key-sorted composite, and a request with no parameters still hashes to the override alone so warm caches and committed lock entries stay valid. Two more wire rules recorded for E10-S02-T01: an undeclared name is rejected, not ignored (C-E03-415), so names must not be passed through speculatively; and values are coerced to the declared type, a string binding to `type: number` (C-E03-416), so the client's `Record<string, string>` is narrower than the service needs but never wrong. docs/06 §5 decision **55**. (evidence: `research/E03-template-engine.md` C-E03-414..418; `research/experiments/E03-parameters-request/` 8 redacted probes; `scripts/template-parameters-survey.ts`; `packages/fetch/test/template-parameters.test.ts` 14 tests incl. transcript replays; 1,534 Vitest + 231 Bats green; typecheck/ESLint/Prettier/ShellCheck/VERIFY guard clean)
2026-08-23 E03-S06-T04 done — the bundler warns on every non-`@self` template reference (`bundle-template-cross-repo`): it names the alias, says the reference resolves against that repository's **committed** state rather than the working tree, and points at **E09** for fetching and pinning. **The task's Done criterion offered an either/or — "produces the diagnostic and stops (or continues with an explicit warning)" — and measurement settles it rather than taste:** an un-inlined `@other` reference **expands fine**, HTTP 200, so stopping would refuse a pipeline the service accepts. That is C-E03-419, and it is grounded by *reusing* E03-S02-T01's `research/experiments/E03-references/cross-*` transcripts rather than spending oracle budget re-probing what was already measured on 2026-08-20. The second claim fixes what the message is allowed to say: the loss is exactly one thing — the user's uncommitted edits to the other repository — because "Repositories are resolved only once, when the pipeline starts up… Only the template files are used" (templates doc, C-E03-420). Taken with C-E12-011 (the request carries one document) and C-E03-404 (templates come from the repository, not the body), that means **no request shape carries a second repository's bytes**, so the bundler can never grow this ability; the message therefore points at E09 instead of reading as "not yet", which would be a promise nothing in the architecture can keep. Two more behaviors asserted while the branch was open: a cross-repo reference never reads the local tree even when a same-named file sits there (the substitution that would be silently wrong), and `@self`/`@SELF`/`@` all stay local (C-E03-212/213). This completes the story's original four tasks; E03-S06-T05, filed by T02 and narrowed by T03, is what remains. (evidence: `research/E03-template-engine.md` C-E03-419..420; `research/experiments/E03-references/cross-alias-rel` and siblings; `packages/engine/test/template/inline.test.ts` 27 tests; 1,536 Vitest + 231 Bats green; typecheck/ESLint/Prettier/ShellCheck/VERIFY guard clean)
2026-08-23 E03-S07-T01 done — the bundle's provenance record: `packages/engine/src/template/provenance.ts`, `bundleProvenance()` to build it and `writeBundleProvenance(dir, result)` to write **`pipeline.bundled.yml`** (the exact redacted bytes sent as `yamlOverride` — diffable, and re-submittable by hand when reproducing a service rejection) and **`bundle.json`** (the machine-readable map). Pinning those two names was this task's job: docs/04 §1 carried a comment saying so, and both it and docs/02 §5.1 now name them. **Three decisions worth recording.** (1) *The map records the **skipped** references, not just the inlined ones* — each with its reason (`cross-repo`, `uses-parameters`, `unsupported-site`, …) and `file:line:col`. That half is the one that matters on the default path: it is what separates "this expanded from your working tree" from "this expanded from what is committed" (C-E03-413/419), and a map listing only the successes would read as though the bundle were complete when the interesting pipelines are exactly the ones where it is not. (2) *Hashes are of the **working-tree** content, taken before recursion rewrites the file.* Hashing the post-splice text would move every ancestor's hash whenever a leaf three levels down changed — the opposite of attribution — so the suite edits a leaf and asserts the parent's hash does **not** move, alongside the override hash that legitimately does. (3) *`redact` is injected rather than imported.* Redaction lives in `packages/fetch` with the org/PAT config it needs and `engine → fetch` is the wrong dependency direction — the same port reasoning docs/06 §5 decision 42(a) applied to the offline expander — with identity as the default so a caller with no credentials in scope still gets a record; the skipped references' text is redacted too, since a reference is user-authored and can name a host. Also deliberate: `overrideSha256` covers the **redacted** bytes so the two files can be checked against each other, and is explicitly *not* `expansionRequestHash` (which covers the raw override plus `templateParameters`) — different questions, and conflating them would invite a reader to treat the provenance hash as a cache key. Written as a plain function rather than an emitter method so the bundler's record does not wait on E05. (evidence: `packages/engine/test/template/provenance.test.ts` 11 tests incl. the attribution case and the redaction cases; docs/04 §1 + docs/02 §5.1 updated; 1,547 Vitest + 231 Bats green; typecheck/ESLint/Prettier/ShellCheck/VERIFY guard clean)
2026-08-23 E03-S07-T02 done — missing-file and cycle diagnostics in the E01 shape, and the clause that mattered was the other one. The snapshots go through the **E01 renderer** (`renderDiagnostic`) rather than asserting the `Diagnostic` object: the Do asks for "E01-style diagnostics (file:line, hint)", and only the renderer proves the severity/code/location/hint/code-frame assembly a user actually reads — an object snapshot passes even when the rendered output is unreadable. The frame carets the **whole reference token**, which is docs/06 §5 decision 15's rule ("range the token so the code frame carets it") arriving here for free. **"Never a raw exception" is where the work was, and it found four holes.** (1) **Template parse errors were being dropped on the floor.** `inlineDocument` called `parsePipelineYaml` and ignored `parsed.errors`, so a template with broken YAML produced no references and the caller reported "`<file>` has no `steps:` sequence to inline" — a diagnostic describing a *consequence* rather than the cause, against the wrong file. Parse errors are now reported against **that template's** own file, and they do not stop the bundle: the parser recovers, so a healthy sibling reference still inlines. (2) **A throwing reader.** The `LocalTreeReader` contract is `undefined` for "no such file", but a filesystem-backed implementation can still throw — a permission error, a directory where a file was expected — so the call is guarded into `bundle-template-unreadable`. (3) and (4) **The two published ceilings.** C-E03-403 (100 included files, 100 nesting levels) was recorded by E03-S06-T01 explicitly *for this task* and is now enforced at the reference that would cross it. Both directions matter: a pathological chain previously recursed once per link until the stack overflowed — precisely the raw exception this clause forbids — and a bundle over either limit is rejected by the service *after* we send it, in the service's wording with no local `file:line`. Also added while here: a hint on the cycle diagnostic distinguishing a cycle from a legal diamond (C-E03-209), since the two look identical to a reader who has just been told their templates loop. This completes E03-S07 and, with it, every E03-S06/S07 task except **E03-S06-T05**, the product decision T02 filed and T03 narrowed to two options. (evidence: `packages/engine/test/template/inline-diagnostics.test.ts` 12 tests, incl. inline snapshots for missing-file, cycle and self-cycle plus a case asserting that none of the five failure shapes throws and all return usable YAML; 1,559 Vitest + 231 Bats green; typecheck/ESLint/Prettier/ShellCheck/VERIFY guard clean)
2026-08-23 E03-S01-T06 done — the S01/S02 bookkeeping is reconciled, and **the drift ran the opposite way from what the note requesting it assumed**. The 2026-08-21 header note said "the checkboxes below understate this epic" and asked the E03 lane to flip five of them to `[x]`; walking the Done criteria one at a time gives **zero**. The modules (`conditionals.ts`, `each.ts`, `insert.ts`, `interpolate.ts`, `reference.ts`), their suites, and 24/13/32/34/34 oracle transcripts are all on `main` and green — but `research/E03-template-engine.md` contains formal `[C-E03-NNN]` entries for **100–117, 120–121, 204 and 400–420 and nothing else**. The blocks for `each` (140..159), `insert` (160..174), interpolation (175..194) and references (195..229 minus 204) were never written, though those IDs are cited in code comments, in test names, in the experiment READMEs and in `REFERENCES.md`. CLAUDE.md rule 1 calls the Grounding Protocol non-negotiable and rule 3 makes recorded claims a precondition for a checkmark, so **the checkboxes were right and the changelog was wrong** — the earlier `done` entries were premature. This entry corrects them by appending rather than editing, since the file is append-only. The sharpest case is **E03-S02-T01**: its shipped `reference.ts` cites `C-E03-195..215` in its own comments, so a reader following a citation finds nothing — worse than an unstarted block, because the code *looks* grounded — and E03-S06's shipped bundler consumes several of those claims (`parseReference`, `isSelfAlias`, the path math). **Applied, all bookkeeping, no code changed and no checkbox flipped on another lane's evidence:** the story header rewritten to state the real direction of the drift; a per-task note on T02–T05 and S02-T01 naming exactly what each still owes, and stating that the transcripts already exist so the remaining work is **transcription, not new measurement** — a cold session re-records rather than re-implements, which is the failure the old note feared, reached from the other side, and no oracle budget is needed; **E03-S01-T02 moved `[!]` → `[ ]`**, its 2026-08-18 blocker ("`AZDO_*` unavailable") having been resolved long ago with `.env.oracle` present and 24 probes captured, so leaving it marked blocked would have hidden a runnable task; and four allocation-table rows moved from *free* to **owed**, with a standing note under the table naming the pattern, so nobody reallocates ID ranges that shipped code already cites. (evidence: `grep -o '^\[C-E03-[0-9]\+\]' research/E03-*.md` as the census; `backlog/E03-template-engine.md` header + five task notes; `research/E03-template-engine.md` allocation table; 1,559 Vitest + 231 Bats unchanged and green)
2026-08-23 E06-S06-T01 done — secret masking, and **every Done criterion was already met when the task was picked up**; that is recorded rather than papered over, following the E12-S02-T03 precedent, and no code was written. The E06-S04 logging-command lane built the whole of this task's **Do** on its way to `task.setvariable`/`task.setsecret`: `azdo_mask_stream`/`azdo__mask_line` are the streaming masker, wired inside `run_step` **before** the `tee` to the log file and before ANSI rendering — so the console and the log file both carry masked bytes and the log still keeps agent-shaped lines — and `setsecret` additions register through the same path (C-E06-058). Masking replaces merged character **ranges** rather than substrings one value at a time, so adjacent matches collapse into a single `***` exactly as the agent does instead of emitting `a******h` (C-E06-126). **The Do's open question is answered and the answer is the opposite of what it expected to record:** "handles values split across lines? (verify agent behavior — likely no; document delta)" resolves to **C-E06-131, parity rather than a delta** — the agent registers no per-line fragment of a multiline secret, because `AddUserSuppliedSecret` only *end*-trims CR/LF (C-E06-125), so a two-line secret echoed as two lines survives the agent and the emulator alike; the Learn page's "We never mask substrings of secrets. If, for example, 'abc123' is set as a secret, 'abc' isn't masked from the logs" is the rule that follows from. Worth noting because it inverts the usual direction: recording that claim fixed a real bug in which the emulator's pre-existing masker read only the **first line** of each registered value and masked that line alone — *stricter* than the agent and inconsistent with the documented rule. Two genuine deltas sit beside it and are already recorded: the three global value encoders (C-E06-129, deliberately not implemented) and the URL-credential regex (C-E06-130, deferred to E08). **Ground** is satisfied with the agent's masker pinned at `microsoft/azure-pipelines-agent@42bde98bea7bb3b9e186d693e3b1554249e93a38` (`src/Agent.Worker/ExecutionContext.cs#L768-L790`, `#L442`) plus the set-secret-variables doc page. **Done** is satisfied by bats `step stdout, stderr and the log file are all masked (C-E06-122)`, which asserts both streams and the log file, including the `failOnStderr` branch that routes stderr through a second `tee` before it reaches the masker. (evidence: `research/E06-runtime.md` C-E06-122..131; `packages/runtime/test/core.bats` cases 24/44/104/106..110; 231 Bats + 1,559 Vitest green)
2026-08-23 E06-S06-T02 done — the end-of-run summary and the run exit code, completing E06-S06 and the E06 epic's open tasks. `azdo_run_summary` prints docs/04 §2's table (scope, step, name, result, duration, log path) with every column padded to its widest cell — the ADO job view's readability is the point — `azdo_run_result` folds every recorded step result worst-wins using the same ranking `azdo__job_status_from_results` applies inside one job (C-E06-059..061), and `azdo_run_exit_code` maps that to a status. `run_step` records a summary row at each of its **three** exits, not just the normal one: the condition-false skip and the condition-error failure record too, both with duration **0**, because neither step ran and charging them the condition-evaluation time would misattribute it. **The finding is a doc correction, and it is the kind that only shows up when you implement the sentence.** docs/04 §3 said the run exit code is non-zero "iff the pipeline result is `Failed`". Taken literally a run the user interrupted with Ctrl-C exits **0** and reports success to whatever invoked `run.sh` — a CI wrapper, a git hook, a `&&` chain. The sentence is not defending that; it enumerates the three *job* results listed in the paragraph above it and never considers `Canceled`, which the five-value vocabulary carries and which `azdo__job_status_from_results` already ranks **above** `Failed`. So `Canceled` exits non-zero too, docs/04 §3 is corrected in the same diff (rule 5) and docs/06 §5 decision **56** records the reasoning — including why it is **one** exit code and not two: the stated contract is "non-zero", and a distinct code for cancellation (130 by shell convention) would put the generated project on a different scale from the CLI's own `0/1/2` policy. **No claim was recorded, deliberately:** the agent reports to the service rather than to a shell, so the summary table has no agent behavior to copy, and the **Ground** field names docs/04 §3 — an internal spec — rather than a pinnable source; saying so here stops a later reader hunting for a claim that should not exist. Two bugs the tests caught before they shipped: `((index++))` evaluates to the *pre*-increment value, so as the last command of the record-counting loop body it returned 1 and failed the whole function on the very first record; and the summary store had to live at `state/summary/` rather than beside the per-step results, because the table spans every stage and job while `state/results/<stage>/<job>/` is scoped to one — with one field per line, following the state store's own file-per-value reasoning, since a `displayName` may contain any character a YAML scalar may and a delimited row would have to quote it. Sequential execution is assumed for the completion-order numbering and the assumption is written down (docs/04 §3; `--parallel` is deferred to the archived P6). (evidence: `packages/runtime/lib/core.sh` `azdo_summary_record`/`azdo_run_result`/`azdo_run_exit_code`/`azdo_run_summary`; `packages/runtime/test/core.bats` 9 new cases incl. the worst-wins fold, the issues-sidecar exclusion, the exit-code matrix over all five results and the two run_step paths; docs/04 §3 + §4; docs/06 §5 decision 56; 241 Bats + 1,559 Vitest green; typecheck/ESLint/Prettier/ShellCheck/VERIFY guard clean)
2026-08-23 E04-S01-T01 done — the semantic model's types and builder, opening E04: `packages/engine/src/model/{types,build}.ts`, the docs/01 §6 shape plus `buildPipeline(ParseResult) -> {pipeline, diagnostics}`. **The input contract was measured rather than assumed, and most of the evidence was already on disk.** All **147** `final.yml` transcripts this epic inherits from E00/E02/E03 are rooted at `stages:` — none at `jobs:` or `steps:` — and the surviving top-level keys are exactly `parameters`, `variables` and `resources` (C-E04-001); `__default` and `Job` appear as names in exactly the 78 whose probe input was a bare `steps:` (C-E04-002), which confirms C-E00-017/018 holds across every later probe rather than only the original spike. **The corpus had one gap and it was worth closing:** no inherited probe is rooted at `jobs:`, so two probes were run (`research/experiments/E04-model/`). The first confirmed the obvious — root `jobs:` wraps in `__default` and keeps its own job names (C-E04-003). The second did not: an **authored but unnamed** `- job:` expands to `job: ''`, an *empty* name, and not the synthetic `Job` (C-E04-004). The synthetic name is what the service supplies when it *invents* the job, not a default it applies to one the author wrote. So **job identity may be the empty string**; the builder preserves it rather than substituting something readable, because inventing a name here would hide the problem from **E05**, which is where a directory and a script filename actually have to be chosen. **Why the builder still wraps a shorthand root when the default path never presents one:** `--offline-expand`'s retained local engine makes no such guarantee (PLAN D3/D4), and a builder that asserted the invariant would turn the fallback into a crash rather than a conversion; wrapping the same way the service does also makes both arms produce the *same* model, which is what keeps `--offline-expand` a fallback instead of a second dialect. Scope kept honest in two directions: fields later tasks own — `disposition` (E07-S03-T01), `fidelity` (E05-S02-T02), `matrixKey` (E04-S03) — are **typed and marked** rather than omitted, with a test asserting they are unset so "not built yet" cannot be misread as "deliberately absent"; and a **deployment** job carries no steps rather than borrowing its strategy's, since flattening `strategy.runOnce.deploy.steps` is E04-S03's. Also handled from existing claims rather than fresh guesses: pipeline scalars are strings even where the schema says boolean (C-E01-015), so `continueOnError: 'true'` reads as true; `timeoutInMinutes: 0` is preserved because 0 means "no limit" rather than "unset"; and `Name@version` splits on the **last** `@`. (evidence: `research/E04-semantic-model.md` C-E04-001..005, a new file with its own claim-ID block table; `research/experiments/E04-model/` 2 redacted probes; `packages/engine/test/model/build.test.ts` 30 tests, two of which rebuild from the captured `final.yml` byte-for-byte so the suite fails if the service's shape moves; 1,589 Vitest + 241 Bats green; typecheck/ESLint/Prettier/ShellCheck/VERIFY guard clean)
2026-08-23 E04-S01-T02 done — the normalization boundary, and **the task's own instruction resolves in a way it did not anticipate**. It says to implement a pass for "only the remainder" after the service's desugaring; nine probes (`research/experiments/E04-normalization/`, `pnpm normalization-survey`, summarised in `matrix.md`) show the remainder is **empty in that sense and non-empty in another**. *Empty:* every documented step shorthand is desugared by the service — `script`→`CmdLine@2`, `bash`→`Bash@3`, `pwsh`→`PowerShell@2`, `powershell`→`PowerShell@2`, `publish`/`download`/`checkout`→bare GUIDs — while the canonical-`task:` control passes through untouched, which is what makes the other rows attributable to the shorthand rather than to the expansion in general (C-E04-030/036). A `normalize.ts` pass rewriting `bash:` into `Bash@3` would have been a second, divergent implementation of work the authority already does. *Non-empty:* three shorthands lose the authored keyword to a bare GUID (C-E04-031), and **that would have produced a silent wrong build**. PLAN D4 emits `checkout` natively and docs/04 §8.1 tells the emitter to decide the workspace layout from "how many `checkout` steps the job has" — on the default path the keyword is gone, so an emitter counting `checkout:` keys counts **zero** and hands every job the single-checkout layout without erroring. The model therefore recovers the keyword into `Step.origin` (`packages/engine/src/model/shorthand.ts`), with each GUID↔keyword association measured by submitting that keyword and reading the expansion rather than inferred from the name (C-E04-032); docs/04 §8.1 gained a banner saying to count and dispatch on `origin`; docs/06 §5 decision **57** records the whole finding. **The boundary with the normalizer is respected and asserted**: recovering a keyword is not the renaming `normalize.ts` refuses — it keeps its single grounded entry (`ecdc45f6…`→`PublishPipelineArtifact`, C-E12-019) and its deliberate omission of the agent-internal `checkout`/`download` GUIDs, which 404 from the task catalogue and whose invented names would make its *diff* lie; `task.name` keeps the GUID, and a test asserts that table was not quietly extended (C-E04-033). `download:` and the catalogue's `DownloadPipelineArtifact@2` stay separate tasks (C-E04-034). Two details carried forward for E05/E07: desugaring **renames inputs** — `publish:`'s `artifact:` becomes `artifactName:` while `download:`'s stays `artifact:` and its scalar becomes `alias:`, so a model assuming the authored keys reads the wrong input — and `pwsh` vs `powershell` is decided by a `pwsh: true` **input**, not by the task reference, so a disposition registry keyed on the reference alone cannot tell them apart (C-E04-037). `getPackage` is rejected HTTP 400 without a matching `resources.packages` entry, so it is resource-gated rather than free-standing and never reaches the model; it is listed in `UNMEASURED_SHORTHANDS` in code so the hole is visible rather than silent, with the reason it was not probed further (C-E04-035). (evidence: `research/E04-semantic-model.md` C-E04-030..037; `research/experiments/E04-normalization/` 9 redacted probes + matrix.md; `scripts/normalization-survey.ts`; `packages/engine/test/model/shorthand.test.ts` 18 tests, table-driven off the transcripts so a re-probe that moves fails them; docs/04 §8.1; docs/06 §5 decision 57; 1,607 Vitest + 241 Bats green; typecheck/ESLint/Prettier/ShellCheck/VERIFY guard clean)
2026-08-23 E04-S01-T03 done — common step fields, and **two of the fields this task's own Do calls "common step fields" are not step fields at all.** `workingDirectory` and `failOnStderr` are task **inputs**: the `steps.task` schema page omits both from the common-property list, and the expansion puts them under `inputs` for `script`→`CmdLine@2` and `bash`→`Bash@3` alike (C-E04-061) — the same fact C-E06-033 already records from the runtime side, where `failOnStderr` "is not an agent-level input at all: it is the Bash/PowerShell shortcuts' own flag". A model reading them off the step mapping finds nothing, silently, for every pipeline — **which is precisely what E04-S01-T01 shipped earlier today**, so this task corrects both its reader and the test that had asserted the wrong shape; the model still surfaces `workingDirectory` on `Step` because docs/01 §6 puts it there, but reads it from where it lives. **The task's `VERIFY` resolves to "there is no scheme":** two unnamed steps expand with no `name:` key, and across all **157** captured expansions in `research/experiments/` no step-level `name:` appears that the author did not write — the 84 files that match `name:` are all `resources.repositories[].name` (C-E04-063). That is asserted continuously by a corpus test rather than on one hand-written document, because an absence proven on a single sample proves very little. The consequence is recorded rather than worked around: the model must **not** invent a name, a step without one simply has unreferenceable outputs, and E05 derives file names from the ordinal and `displayName` (docs/04 §1) so it never needs one. Four further measurements: `target:` always arrives as the **object** form, the documented scalar shorthand having been normalized into `{container: host}` — so `host` reaches us as a container *name* rather than a distinct kind (C-E04-062); `enabled: false` **survives** expansion with the step still present, so skipping it is the runner's job and not something already done for us (C-E04-064); `displayName` is *not* defaulted by the service, so the fallback to task name then ordinal is **ours** and is labelled as such rather than presented as parity (C-E04-065); and `bash` gains a `targetType: inline` input the author never wrote, meaning a consumer diffing authored against expanded inputs must expect additions as well as the renames C-E04-032 found (C-E04-067). **The Ground field's prescribed method was not used, and that is stated rather than glossed:** it asked for "a real-run output-variable reference experiment", and none was run. The preview corpus answers the question the model can actually observe — the model is built from `finalYaml`, so a name the *server* might invent for its timeline is unobservable to it and would change nothing here. No pipeline run was spent, and the residual gap (whether the server auto-names in its own UI) is named so a later task can pick it up if it ever matters. (evidence: `research/E04-semantic-model.md` C-E04-060..067; `research/experiments/E04-step-fields/` 6 redacted probes; the `steps.task` page pinned at source commit `d089fd2dbb54483ec611eeb478e3eff14be74393`; `packages/engine/test/model/step-fields.test.ts` 21 tests incl. the corpus assertion; 1,628 Vitest + 241 Bats green; typecheck/ESLint/Prettier/ShellCheck/VERIFY guard clean)
2026-08-23 E04-S02-T01 done — variable scope resolution and precedence: `packages/engine/src/model/variables.ts`, `resolveVariables(pipeline, stage?, job?)` layering root → stage → job with last-wins inside a layer, plus group-name collection. **The load-bearing finding is that this is ours at all.** The pattern from E04-S01-T02 was that the service does the work and we should not duplicate it; here five probes (`research/experiments/E04-variables/`) show the opposite — the expansion resolves **no** variable precedence (a document setting `a` at root, stage and job comes back with all three blocks intact, C-E04-080) and collapses **no** duplicates within a scope (two entries named `a` in one stage both survive in authored order, C-E04-081). So layering and last-wins are the model's work, and knowing that took a probe rather than an assumption in either direction. The documented order is quoted verbatim (C-E04-082) and the resolver implements exactly the three **YAML** levels: queue-time and the Pipeline settings UI are levels 4 and 5 and sit outside the model, being the `.env` boundary of PLAN D7 — which is why it layers three and stops. Scope isolation is asserted in the negative as well as the positive: a job sees root + its own stage + itself and **never** a sibling stage's or a sibling job's (C-E04-083). **One consequential design change:** `variables` on Pipeline/Stage/Job moved from a flat `Record<string,string>` to a **declaration list**, because a flat map can represent neither the duplicates the expansion keeps nor the `readonly` flag; E04-S01-T01's two assertions were updated to match, and the group case actually improves — it now carries a marker instead of being silently skipped. Three further measurements: the mapping shorthand **is** normalized to the list form by the service (C-E04-084), so on the default path only the list form arrives and the mapping branch exists for the `--offline-expand` arm, exactly as with `target:`; `readonly: true` survives expansion verbatim (C-E04-085), which matters because the runtime already enforces read-only overwrites (C-E06-005/006) and needs telling which names are locked; and names fold case to match the agent's dictionary (C-E06-003), so a differently-cased job entry **overrides** the root one rather than sitting beside it — a resolver that did not fold would hand the runtime two entries it treats as one. **A gap is recorded rather than guessed:** an unresolvable or unauthorized `group:` is rejected by the service outright, HTTP 400 "Variable group was not found or is not authorized for use" (C-E04-086), so it never reaches the model; what an *authorized* group expands to is **unmeasured**, needing a variable group provisioned in the test organization that no current task creates, and PLAN D7 forbids fetching group values in any case. The resolver therefore carries a group entry as a named marker contributing no value — the behavior E04-S02-T02's classification and the `.env.example` synthesis of docs/04 §10 both expect. (evidence: `research/E04-semantic-model.md` C-E04-080..086; `research/experiments/E04-variables/` 5 redacted probes; the variables doc pinned at source commit `1eeaa8de39f8b7130d8eb45ec907d9e47d6f5a32`; `packages/engine/test/model/variables.test.ts` 22 tests incl. the precedence matrix in both directions; 1,650 Vitest + 241 Bats green; typecheck/ESLint/Prettier/ShellCheck/VERIFY guard clean)
2026-08-23 E04-S02-T02 done — variable classification: `packages/engine/src/model/classify.ts` sorts every referenced variable into `inline` / `group-member` / `predefined` / `setvariable-produced` / `env-required`, carrying each reference site and a warnings list, for the `.env.example` synthesis (docs/04 §10) and the README warnings (E05-S02-T02). **The task's dependency runs backwards, and it is handled with a port rather than a second table.** Its **Ground** names the predefined-variable list, but *building* that list is **E04-S02-T03**, scheduled after this task. Rather than construct a rival table here — the failure mode C-E04-031's GUID case already illustrated — the set is **injected** and defaults to empty (C-E04-092); with nothing injected nothing is classed `predefined`, and a **prefix heuristic** over the namespaces the service owns (`Build.`/`System.`/`Agent.`/`Pipeline.`/`Environment.`/`Release.`) warns instead, so a missing or stale table surfaces as a warning rather than as a `.env` entry the user cannot possibly supply. The heuristic is explicitly ours and is labelled as not a claim about the service. **A real bug the tests caught, and the reason the scanner is hand-rolled rather than a regex:** the first version matched `\$\(([^)]*)\)` globally, which on `$(a$(b))` consumed the closing paren and reported **nothing at all** — but the runtime resolves the *inner* macro first (C-E06-024, the measured `$(a$(b))` → `$(ainner)` case), so `b` is a genuine reference. The scan now restarts at the innermost opener and mirrors `azdo__macro_scan` (`packages/runtime/lib/core.sh`) exactly, which is the point: a name this reports is a name the runtime would look up (C-E04-087). Five further decisions, each grounded rather than assumed: macro positions are step `inputs` and `env` values **only**, per docs/01 §4's own table — a `$(x)` in a `displayName` is not a macro position (C-E04-088); classification is per **referencing scope**, so a name declared only in a sibling job classifies `env-required` at the reference rather than `inline` (C-E04-083); names fold case so two spellings are one entry (C-E06-003); the `setvariable` producer scan is **best-effort by construction** and says so — a script that composes the command at run time cannot be seen, and a missed producer degrades to `env-required`, which prompts the user rather than failing the conversion (C-E04-091); and a declared group makes an otherwise-unaccounted name `group-member`, a *possibility* carrying the declared group names and never a resolved membership, because PLAN D7 forbids resolving group values and docs/01 §4 fixes the `.env.example` behavior (C-E04-090). **No new probe was run and that is deliberate:** the macro and `##vso[task.setvariable]` grammars are already measured in E06, and re-probing them would produce a second recording of the same facts rather than new evidence. (evidence: `research/E04-semantic-model.md` C-E04-087..092; `packages/engine/test/model/classify.test.ts` 32 tests incl. a corpus snapshot over 100+ captured expansions and the unknown-predefined warning path asserted in both directions; 1,682 Vitest + 241 Bats green; typecheck/ESLint/Prettier/ShellCheck/VERIFY guard clean)
2026-08-23 E04-S02-T03 done — the predefined-variable table as vendored data: `packages/engine/data/predefined-vars.json`, **91 rows** each carrying its doc section, a deep anchor and the first sentence of its description, generated by `scripts/predefined-vars.ts` (`pnpm predefined-vars`), with a reader in `packages/engine/src/model/predefined.ts` that fills the port E04-S02-T02 deliberately left injectable. **The source is the docs *repository*, not the rendered page, and that choice is what makes the Done criterion achievable rather than aspirational.** `learn.microsoft.com/.../pipelines/build/variables` builds its table through an `[!INCLUDE]` directive — the page source contains the directive and no table — so the rendered HTML has no version to fetch and would drift with the site's templates; `MicrosoftDocs/azure-devops-docs` is public and addressable by commit, so "a scraper re-run produces stable output" becomes a property, verified by regenerating and diffing for byte equality (C-E04-093). **Two commits are pinned because the doc splits its content, and the coverage check is what caught it:** the include holds the six tables (89 rows) while the page holds two further variables in their own `##` sections, and fetching only the include silently loses `Build.Clean` and `System.Debug` — one of which docs/01 §5 explicitly requires. They are documented separately **because** they are the page's own stated exceptions: "These variables are automatically set by the system and read-only. (The exceptions are Build.Clean and System.Debug.)" — so they are the only *writable* predefined variables, and the generated rows now flag that, letting a consumer tell "a pipeline is assigning to a predefined name" (a bug worth pointing at) from the two cases where it is legitimate (C-E04-094/095). Coverage is asserted rather than checked once: all **31** names docs/01 §5 references resolve against the table, with `System.PullRequest.*` counted through its eight concrete members and `Build.SourceBranch(Name)` through the two spellings it abbreviates (C-E04-096). §5's local-mapping strategy is deliberately left alone — it says what each name resolves to *locally* while the table says which names exist and where they are documented, and merging them would make a stale doc look like a design decision. **Two packaging bugs were caught and fixed before they shipped:** the reader's fixed `../../data` path is correct from `src/model/` and wrong from the bundled `dist/index.js`, which sits one level deep, so it now uses the same walk-up resolver `vendoredSchemaPath()` established for the vendored schema; and `data` was missing from the engine's `package.json` `files` array, so the table would have been absent from the published package. Both verified by loading the table through `dist/index.js`. (evidence: `research/E04-semantic-model.md` C-E04-093..096; `packages/engine/data/predefined-vars.json` with its `source` block naming both commits; `scripts/predefined-vars.ts`; `packages/engine/test/model/predefined.test.ts` 11 tests incl. the docs/01 §5 coverage assertion and the port being wired in; `research/REFERENCES.md` predefined-variables row updated; 1,693 Vitest + 241 Bats green; typecheck/ESLint/Prettier/ShellCheck/VERIFY guard clean)
2026-08-24 E03-S01-T02 done — recorded the conditional-insertion claim block `C-E03-120..139` in `research/E03-template-engine.md`, closing the last remaining obligation the E03-S01-T06 reconciliation left on this task. Transcription, not re-implementation: the two live-preview surveys were already committed (24 probes in `research/experiments/E03-conditionals/`, 22 in `research/experiments/E03-if/`, 37 pairs promoted to `fixtures/oracle/directives/`), `conditionals.ts` and its suite were already on `main` and green, and only the formal `[C-E03-NNN]` entries were missing. Every claim 122..139 is sourced from a named transcript: sequence/mapping selection and first-branch-wins (122–125), recursive nested chains (126), new-`if`-resets-state (127), the non-adjacency-gated chain with winner-at-own-position (128), the orphan/duplicate-else rejections (129/130/137), primitive and collection truthiness (131/135), lazy document-ordered condition evaluation (132) with its reached-missing-parameter 400 control (134) and unselected-body-never-evaluated control (133), mapping-body-in-sequence = one item (136), and — sourced from E03-S01-T04's `insert` survey since they live in this block — the directive-sibling chain break (138) and the parent-shape-dependent orphan second sentence (139). Two consequences beyond recording: the mapping-position body-shape rejection ("Expected a mapping") was folded into C-E03-123, and two stale test citations in `conditionals.test.ts` that now resolve to the wrong claims were aligned (`conditionTruth` → C-E03-131/135, rejection-controls describe → C-E03-129/130/134/137). Verified: every `C-E03-1[23][0-9]` ID cited in `packages/engine/src`/`test` has an entry (`comm` check empty); `conditionals.test.ts` + `insert.test.ts` 107 green; engine typecheck/eslint/prettier clean. (evidence: `research/E03-template-engine.md` C-E03-120..139; `packages/engine/test/template/conditionals.test.ts`)
- 2026-08-24 E04-S03-T01 done — matrix & `parallel` strategy expansion: `packages/engine/src/model/strategy.ts` (`expandJobStrategy()`) multiplies a job with `strategy:` into its concrete legs, wired through `buildJob` (now returns a leg set; callers flatMap). **The naming was measured, not assumed, and two of three answers contradict the docs.** A matrix leg is `<JobName> <key>` — a **space**, not the `Job_<key>` underscore docs/01 §2 wrote (C-E04-110/119, timeline records `Build Alpha`/`Build Beta` from run 546); inside a leg `System.JobName` is the **key alone** (`Alpha`) while `System.JobDisplayName`/`Agent.JobName` carry the full name (C-E04-120); and `parallel: N` slices are `<JobName> <position>` with a **1-based** position and `System.JobPositionInPhase`/`System.TotalJobsInPhase` = `{1..N}`/`N` (C-E04-121). Implemented: authored-order matrix keys, per-key mapping → job-level variables (C-E04-112/122), `maxParallel` recorded verbatim (C-E04-113), slice variables injected (C-E04-114), runtime-expression `matrix: $[ … ]` warns `strategy-matrix-runtime-expression` and leaves the job unexpanded (C-E04-116), empty matrix → one job (C-E04-117). docs/01 §2/§6 corrected (rule 5) and docs/06 §5 decision **58** records the finding. (evidence: `research/E04-semantic-model.md` C-E04-110..122; `research/experiments/E04-strategy/real-run.md` run 546 + `scripts/matrix-parallel-realrun.ts`; `packages/engine/test/model/strategy.test.ts` 11 + 1 snapshot; 1,704 Vitest + engine typecheck/eslint/prettier/VERIFY-guard clean)
- 2026-08-24 E04-S03-T02 done — dependency graphs & defaults: `packages/engine/src/model/graph.ts` (`resolveStageGraph()`/`resolveJobGraph()`) computes the effective dependency lists and validates them, wired into `buildPipeline` so the docs/01 §6 invariant (graphs acyclic & references valid) is enforced at every model build. **The two defaults are the one thing the docs settle, and they are the asymmetry the module exists for** (C-E04-123/124): a stage without `dependsOn` runs after the stage before it, a job without `dependsOn` has no dependency, and `dependsOn: []` is the explicit opt-out that breaks the sequential stage default (C-E04-125) — so `Stage.dependsOn` became `readonly string[] | undefined` (absent vs explicit-empty) while `Job.dependsOn` stays a plain list. **Everything about a broken graph is measured, not guessed** — 12 live preview probes (`research/experiments/E04-dependency-graph/`, `pnpm dependency-survey`, 6 committed transcripts) — and the validation reproduces the service's sentences verbatim: missing target `Stage B depends on unknown stage NoSuchStage.` / `Stage A job A2 depends on unknown job NoSuchJob.` (C-E04-126/127); a cycle reported **edge-by-edge** (`… which creates a cycle in the dependency graph.`) in source-declaration order (C-E04-130/131/132); a no-dependency-free-node sentence (`The pipeline must contain at least one stage with no dependencies.`) that **shadows** the other checks (C-E04-128/129/133); precedence no-root → cycle → missing (C-E04-134); self-loop is a cycle edge only when a root exists elsewhere (C-E04-135). **The finding with reach beyond this task is C-E04-136:** `dependsOn` references the *authored job name*, not a matrix/parallel leg id, so the job graph keys on a new `Job.referenceName` (base name, shared by all legs) rather than the `Build Alpha`/`Build Beta` ids E04-S03-T01 renamed them to. Recorded-but-deferred (not in the **Do**): duplicate stage/job names are rejected by the service with their own sentences (C-E04-137/138), out of scope because the service rejects them before expansion so the default path never sees them. (evidence: `research/E04-semantic-model.md` C-E04-123..138; `research/experiments/E04-dependency-graph/`; `packages/engine/test/model/graph.test.ts` 20; 1,724 Vitest green, typecheck/eslint/prettier/VERIFY-guard clean)
- 2026-08-24 E03-S01-T03 done — recorded the iterative-insertion claim block `C-E03-140..151` in `research/E03-template-engine.md`, closing the remaining obligation the E03-S01-T06 reconciliation left on this task. Transcription, not re-implementation: `each.ts` + its suite were already on `main` and green, and the 12-probe survey (11 successful pairs + 1 rejection) was already committed under `research/experiments/E03-each/`; only the formal `[C-E03-NNN]` entries were missing. Four doc claims (140–143) quote the "Iterative insertion" section — sequence-or-mapping iteration, the `pair.key`/`pair.value` idiom, `jobList` wrapping, and the `stringList`→`object` fallback — and eight measured claims (144–151) cover source-order element binding, authored mapping order including the integer-like keys (`'10'`/`'2'`/`'01'`) a JS object would reorder (the "ordering claim" the **Done** field names), mapping-body splice-not-nest, outer-major/inner-minor nesting, stepList/jobList structural insertion, empty-collection no-op, full-expression collection operand, and no synthesized `.index`/`index` name (a bare `index` is `Unrecognized value: 'index'`). Verified: every `C-E03-1[45][0-9]` cited in `packages/engine/src`/`test` has an entry (`comm` check empty); `each.test.ts` 15 green; engine typecheck + prettier clean. (evidence: `research/E03-template-engine.md` C-E03-140..151; `research/experiments/E03-each/`; `packages/engine/test/template/each.test.ts`)
- 2026-08-24 E04-S03-T03 done — deployment job model: `packages/engine/src/model/deployment.ts` (`runOnceOutputVariableKey`) + `environment`/`strategy`/`autoDownloadArtifacts` on `Job` in `types.ts`, parsed in `build.ts`. **The naming quirk refutes docs/01 §3's `outputs['<lifecycle-hook>…']` description** — run 548 reads both spellings and the **job-name** key resolves while the hook-name key is empty (C-E04-151/153), so runOnce keys by `<job>` or `Deploy_<resourceName>` (C-E04-152) and only canary/rolling use the hook name (C-E04-154, reserved for E08). **The environment resource forms are doc-grounded, not measured**, because preview rejects them when the resource is absent (`Resource <r> does not exist in environment <e>.`, C-E04-143/144) and no task provisions a VM/K8s resource; the scalar `{name}` promotion and runOnce hook sequence are measured (`research/experiments/E04-deployment/`). **The auto-download flag's detection is a measured surprise**: `- download: none` desugars to the `download` GUID task with `condition: false` + `inputs.alias: none` (C-E04-150), so the flag keys off `origin === 'download'` + `alias === 'none'`. `rolling`/`canary` parse to a bare `{kind}` marker (E08). docs/01 §3/§6 corrected (rule 5) + docs/06 §5 decision **59**. (evidence: `research/E04-semantic-model.md` C-E04-140..155; `research/experiments/E04-deployment/` 5 preview probes + run 548 + `scripts/deployment-survey.ts`/`scripts/deployment-output-realrun.ts`; `packages/engine/test/model/deployment.test.ts` 14; 1,738 Vitest + 241 Bats green; typecheck/eslint/prettier/shellcheck/VERIFY-guard clean)
- 2026-08-24 E03-S01-T04 done — recorded the `${{ insert }}` merge claim block `C-E03-160..174` in `research/E03-template-engine.md`, closing the remaining obligation the E03-S01-T06 reconciliation left on this task. Transcription, not re-implementation: `insert.ts` + its suite were already on `main` and green, and the 32-probe survey (13 successful pairs promoted to `fixtures/oracle/directives/insert-*` + 19 rejections) was already committed under `research/experiments/E03-insert/`; only the formal `[C-E03-NNN]` entries were missing. The **Ground** field's first finding is that it names the wrong page: the "Insertion" section is on **template-expressions**, not `templates` (C-E03-160), and the doc shows only the expression-value spelling (C-E03-161) — so the literal-mapping, non-mapping and sequence-position shapes are all probe facts. The `actions/runner` fork, for once, *does* implement the directive and matches the service on every measured shape (C-E03-162, `TemplateUnraveler.cs` `StartMappingInsertion`/`DirectiveNotAllowed`/`ExpectedMapping`), making it usable here where it is useless for `if`/`each`. The twelve probe claims (163–174) cover positional merge with unsorted authored order, literal-mapping value, empty-object no-op, composition with `if`/`each`, well-known schema mappings (`continueOnError: true` rendered `True`), two byte-identical inserts merging in document order, the collision-is-a-hard-error answer the **Do** field flagged — `'<key>' is already defined` at the later occurrence (169), case-folding with later-spelling echo (170), and the rule being the mapping's not `insert`'s (171, an `each` key colliding rejects identically) — the bare `Expected a mapping` rejection, the two not-a-directive positions (`Unexpected value '${{ insert }}'`, not `Unrecognized value: 'insert'`), and the sequence-position-as-mapping-key merge (one merged item, not a splice). The two chain questions it inherited (C-E03-138/139) were recorded in E03-S01-T02's block and are cited, not re-recorded. Verified: every `C-E03-160..174` cited in `packages/engine/src`/`test` has an entry (`comm` check empty); `insert.test.ts` 45 green; prettier clean. (evidence: `research/E03-template-engine.md` C-E03-160..174; `research/experiments/E03-insert/`; `packages/engine/test/template/insert.test.ts`)
- 2026-08-24 E04-S03-T04 done — the `manifest.json` serializer: `packages/engine/src/model/manifest.ts` (`serializeManifest`) emits the versioned docs/04 §11 shape (`schemaVersion: 1`) with a committed draft-07 schema at `packages/engine/schema/manifest.schema.json` (published in the package `files`) and `manifestSchemaPath()`/`readManifestSchema()` beside it. **The expansion mode is serialized here (the E12-S01-T01 pointer):** `ManifestExpansion` mirrors `packages/fetch`'s `ExpansionManifestEntry` structurally (engine → fetch is the wrong direction, decision 42(a)) and the schema's `oneOf` enforces the discrimination, so the offline arm cannot claim an api-version/pipeline id — a test submits a hand-smuggled one and asserts refusal. **The decision 42(b) stale-lock-entry interaction is deliberately *not* made here:** the serializer records the entry faithfully; whether an offline re-convert's `mode: offline, requestHash: H` beside a service-expanded lockfile entry for the same `H` is error/warning/drop stays with whoever writes manifest + lockfile together (E05/E10-S02-T01). Stage `dependsOn` is the **effective** list (sequential default applied) by reusing `resolveStageGraph` with a throwaway diagnostic array (the builder already validated). `env`/`tools`/`warnings`/`unsupported` default to empty (hooks for E07/E08); `fidelity`/`disposition` serialize from the model when E05/E07 set them. Grounding: internal spec + decision 42/PLAN D3 — no ADO behavior, so no new claims (BACKLOG §3.4). docs/04 §11 gained a pointer to the authoritative schema + the concrete deltas from its illustrative sketch. (evidence: `packages/engine/test/model/manifest.test.ts` 22 tests — strict-ajv schema compile + every manifest incl. all 10 corpus `final.yml`s validates; golden corpus manifests pinned as snapshots `__snapshots__/manifest.test.ts.snap` with genuine `requestHash`/`finalYamlHash` over the committed input/final bytes; engine 1,543 tests green; full `pnpm typecheck` + eslint + prettier clean; `manifestSchemaPath()` resolves from `dist`)
- 2026-08-24 E05-S01-T01 done — the project scaffolder: `packages/emit/src/scaffold.ts` (`scaffold(pipeline) → Scaffold`) is a pure, deterministic plan of the `stages/` subtree (docs/04 §1) plus the generated `GITIGNORE`, owning the tree layout and nothing else — the `NNN-` numbering (1-based ordinal ×10, zero-padded, gaps left for hand-inserted steps), the fs-safe `slugify` (lower-case, non-`[a-z0-9]` runs → `-`, trim) with a `displayName → id → kind-token` fallback, and collision handling (`uniquify` → `-2`/`-3`/…). Jobs are numbered by *group* (`referenceName`, C-E04-136) so the legs of one matrix/`parallel:` job share a number and differ only by a `__<suffix>` (docs/04 §1's `010-BuildJob__linux_x64/`; a `parallel:` slice's position is recovered from the `id` suffix, C-E04-121); a runOnce deployment job flattens its lifecycle hooks into one `steps/` dir with the hook recorded per step (C-E04-146) and `rolling`/`canary` carry none. A shorthand-desugared step (`checkout`/`download`/`publish`, C-E04-031) names its file from `origin` + its distinguishing input (`010-checkout-self.sh`) rather than the defaulted GUID displayName — unless the author wrote one, which the `displayName === task.name` guard detects. **The naming rules are ours, not ADO's**: the service has no filesystem to copy, so nothing is grounded in a primary source (BACKLOG §3.4); docs/06 §5 decision **60** records the four rules, and the "validated against real pipelines" half of the Ground field is met by snapshotting the whole `stages/` tree of all ten corpus entries built from the captured `final.yml`s. Done met: `scaffold.test.ts` (22) snapshots the golden tree over the corpus and asserts determinism (`scaffold(p) === scaffold(p)` — the T01-level form of "re-convert produces zero diff"); emit 25 tests green, typecheck/eslint/prettier clean; `@azdo-emu/engine` added as the emit package's first dependency. (evidence: `packages/emit/src/scaffold.ts`; `packages/emit/test/scaffold.test.ts` + `__snapshots__/scaffold.test.ts.snap`; docs/06 §5 decision 60)
- 2026-08-24 E05-S01-T02 done — step script emission: `packages/emit/src/step.ts` (`emitStepScript(step, number)`) emits the full `.sh` file — the docs/04 §12 header block (shebang, `Step NNN · "display" · kind` rule line, `from: file:line` provenance, `condition/continueOnError/timeout`, `fidelity`, optional macro NOTE, warnings) + `set -euo pipefail` + `source "$AZDO_EMU_LIB/runtime.sh"`. **Native** (classified by the task name before `@`: `CmdLine`/`Bash`/`PowerShell`, with `pwsh` vs `powershell` from the `pwsh:` input, C-E04-037): `script`/`bash` bodies are `inputs.script` **verbatim with `$( )` macros intact** (C-E06-018/024 — the runtime's `azdo_expand_macros` expands them, not the emitter); `pwsh`/`powershell` run through a quoted heredoc `pwsh -NoLogo -NoProfile -Command -` reproducing `errorActionPreference` → `$ErrorActionPreference` (docs/03 §75, degraded). **Everything else** (incl. the desugared `checkout`/`download`/`publish` GUIDs) emits a **stub** dumping the resolved inputs — the `run_task.sh` dispatch and native checkout/artifact dispatch are E07-S03-T01's disposition work. The header's `Step NNN` matches the scaffolder's `NNN-` prefix (`ScaffoldStep` gained a `number` field, and the header display reuses the scaffolder's new exported `originStepLabel` so a defaulted-GUID shorthand reads `checkout-self`, not the GUID). **The finding that needed a decision (61):** "macros left intact" and "pass shellcheck" conflict on the corpus — `$( )` is a shellcheck command-substitution false positive (`SC2005` for `echo "$(cmd)"`, `SC2046` for unquoted `$(…)`), so the emitted-project gate excludes exactly those two and keeps everything else clean. (evidence: `packages/emit/test/step.test.ts` 10 tests — snapshot every native kind + stub, and shellcheck over **all emitted corpus scripts** with zero findings beyond the two macro excludes; `__snapshots__/step.test.ts.snap`; docs/06 §5 decision 61; emit 35 tests green, typecheck/eslint/prettier clean)
- 2026-08-24 E05-S01-T03 done — entry-point emission: `packages/emit/src/entrypoints.ts` (`emitEntrypoints`) emits `run.sh`, `run-stage.sh`, `run-job.sh` and `conditions.sh`. **`conditions.sh`** compiles stage/job/step conditions through the E02-S05 compiler (`compileBash` over `parseExpression` with `registryForSlot`) into `cond_stage`/`cond_job_<slug>`/`cond_step_<NNN>`; absent → `azdo_status_succeeded`. **`run-job.sh`** sets the per-job scope and the log/result/output/`AZDO_HAS_MULTIPLE_CHECKOUTS` dirs, seeds the five predefined directory variables, and runs each step via `run_step` with the full docs/04 §5 signature and an `if`-guard `--from-step`/`--to-step`/`--only-step`/`--no-condition` filter. **`run-stage.sh`** runs jobs in E04-S03-T02 topological order (stable Kahn over `resolveJobGraph`); **`run.sh`** sets up `.work/run-<N>` (monotonic counter, `--resume`), loads `.env`, and runs stages in `resolveStageGraph` order with `--list`/`--env-file`/`--resume`. **The layout/flags/seeding are ours, not ADO's** — docs/06 §5 decision **62** records the run layout, scope/output naming, flag surface, and the shellcheck `SC2016`/`SC2071` excludes that extend decision 61 (`SC2105` avoided structurally via `if` guards; `run.sh`/`run-stage.sh` invoke children via `bash` so no executable bit is needed). Done met: `entrypoints.test.ts` (8) snapshots each entry point, shellchecks the whole generated `run.sh` family + step scripts, and **runs a generated project end-to-end under bash** (full run → `Result: Succeeded` + step-log assertion, `--only-step`, a `--from-step`/`--to-step` range, and a `--list` snapshot). (evidence: `packages/emit/src/entrypoints.ts`; `packages/emit/test/entrypoints.test.ts` + `__snapshots__/entrypoints.test.ts.snap`; docs/06 §5 decision 62; 45 emit tests green, coverage 100% st/lines/fns, 1,802 Vitest + 241 Bats green)
- 2026-08-24 E05-S02-T01 done — the `.env.example` synthesizer: `packages/emit/src/env-example.ts` (`synthesizeEnvExample`) returns `{content, manifestEnv}` — the docs/04 §10 sections in order with one recorded choice (docs/06 §5 decision **63**): run-identity overrides → `SYSTEM_ACCESSTOKEN` → **Pipeline variables (unresolved)** (`env-required` from E04-S02-T02's `classifyVariables`, each with a `# used by <stage>/<job>/step <ordinal>` provenance) → variable groups (`group-member`, names-only, `# from group '<name>'`, PLAN D7) → runtime parameters (prefilled defaults) → an E08 placeholder for service connections/secure files. The `.env` spelling is the runtime's `azdo__env_name` transform (`.`/space→`_`, upper); `SYSTEM_ACCESSTOKEN` is the one secret `manifestEnv` entry (`secret: true`) and its minting note cites the Entra resource `499b84ac-1321-427f-aa17-267ca6975798` (C-E00-011). Done met: `env-example.test.ts` (4) snapshots all ten corpus `.env.example`s and asserts the lint invariant structurally — an `NAME=` line with no preceding `#` is a failure. (evidence: `packages/emit/src/env-example.ts`; `packages/emit/test/env-example.test.ts` + `__snapshots__/env-example.test.ts.snap`; docs/06 §5 decision 63; 49 emit tests green, coverage 100% st/lines/fns, 1,806 Vitest + 241 Bats green)
- 2026-08-25 E05-S02-T02 done — the generated `README.md` + ranked warnings report: `packages/emit/src/readme.ts` (`generateReadme(manifest, plan)`) renders eight sections in the docs/06 §5 decision **64** order — summary (name, structure counts, expansion mode, gap count, baked parameters) → Quick start → Pipeline → Fidelity → Environment → Tool prerequisites → Warnings → Unsupported. **It renders, it does not decide:** every claim comes from the manifest (docs/04 §11); the scaffold is a second argument only for link targets. Three design points, all recorded: the **ranking** is conversion-level warnings first, then per-step by fidelity severity descending, then project order, with a step earning an entry when it carries a warning *or* its tier is `degraded` or worse (docs/04 §13's gap-list shape minus the arithmetic); **remediation is a fixed tier-keyed table**, so no per-task prose is written at emit time; and **no coverage arithmetic survives, disguised or not** (PLAN D10 revised) — a test asserts no percentage and no `N <tier>` histogram string appears anywhere in the output. The fidelity label falls back to `defaultFidelity()` when the manifest carries none (always, until E07-S03-T01 assigns one), and a corpus-wide test asserts the README table and the emitted `.sh` header always print the same label. `## Fidelity` is a fixed heading because every emitted step header already links to it. Grounding: none needed — the service emits no README, so this is internal spec like every other emitter task. Evidence: `packages/emit/test/readme.test.ts` (46 tests: 10 corpus README goldens in `test/__snapshots__/readme.test.ts.snap`, 10 broken-link checks resolving every relative link against `scaffold()` paths + `emitEntrypoints()` keys + the fixed files, 10 label-agreement checks, plus ranking/determinism/offline-expansion units); full suite 1852 passed; typecheck + eslint + prettier clean. Deferred, and said so in decision 64: the manifest's per-step `file` and per-job `targetOs` fills (docs/04 §11) stay with the convert wiring (E10-S02-T01). Filed alongside: **E02-S05-T05** — `compileBash` implements only the `outputs[…]` arm of `dependencies`, so `dependencies.<job>.result` throws and **entry-point emission fails outright** for corpus `03-dependencies-and-conditions`; docs/02 §6 already lists `result` as readable, so the gap is implementation, not grounding.
- 2026-08-25 E05-S03-T01 done — the run-number (`name:`) formatter, split compile-time/run-time: `packages/emit/src/run-number.ts` parses the format into tokens and emits straight-line bash into `run.sh` (after `azdo_env_load`, before the first stage), and `packages/runtime/lib/core.sh` gains `azdo_rev` / `azdo_seed_branch_name` / `azdo_run_identity_seed` / `azdo__join_parts` / `azdo__persist_dir`. **Grounded, not remembered:** the run-number page was deep-verified (`git_commit_id 1eeaa8de39f8b7130d8eb45ec907d9e47d6f5a32`, `ms.date` 2025-08-12) into the epic's first claim file, `research/E05-emitter.md` — C-E05-001..016 for the token table, the default `$(Date:yyyyMMdd).$(Rev:r)`, `Rev` reset-on-any-other-change, zero-padding via extra `r`s, UTC times and `BuildId`-vs-`BuildNumber`; C-E05-020..026 for the local deltas. **The Do field's `$(Rev:.r)` is not implemented on purpose** — the page documents that spelling in no form and the preview endpoint cannot settle it (`name:` is evaluated at queue time), so it and every other undocumented `Rev:`/`Date:` construct becomes a conversion warning and renders literally (Δ C-E05-024/025), recorded in docs/06 §5 decision **65**(c). `Rev` is keyed on the rendered rest-of-the-number, which is C-E05-009/010 implemented literally. Evidence: `packages/emit/test/run-number.test.ts` (23 tests — one per token row, the two refusal paths, and three end-to-end runs of a generated project reading `Build.BuildNumber` back out of the **first step's log**, which is what proves it was set before the step ran, plus the Rev increment and the version-change reset) and `packages/runtime/test/core.bats` (+11, incl. `build.updatebuildnumber` still overwriting the seeded number, C-E06-081). Full suite 1876 vitest + 131 bats green; emit coverage back to 100% statements/lines; shellcheck/eslint/prettier/typecheck clean. **Two pre-existing gaps surfaced and filed rather than patched: E05-S01-T04** (the store keys on the name as written, so `.env.example`'s `BUILD_SOURCEBRANCH` is unreachable as `Build.SourceBranch` — every documented run-identity override is inert) **and E05-S01-T05** (pipeline-scope variables never reach the `job-<id>` scope steps read from, so every `.env` variable is invisible to a step). The run identity is published through `AZDO_BUILD_NUMBER`/`AZDO_BUILD_ID` and re-seeded per job as a scoped workaround (decision 65(d)).
- 2026-08-25 E03-S01-T05 done — the owed claim block `C-E03-175..194` is written into `research/E03-template-engine.md`, closing the reconciliation E03-S01-T06 filed against this task. **Transcription, not re-implementation:** `packages/engine/src/template/interpolate.ts` and its 70-test suite were already on `main`, green, and citing these IDs; they are untouched. The 20 entries are written from the 34 transcripts already on disk (`research/experiments/E03-interpolation/`, 27 expanded pairs + 7 rejections), one claim per stringification rule as the Ground field asks, each naming the probe directory that measures it. What is now on the record: the two docs facts *and their limits* (C-E03-175/176 — the conversion table gives `Null → ''` and `True`/`False` but is wrong about Number; the template-expressions page documents only the array-flatten case and never states the lone-vs-mixed distinction the pass rests on); structural insertion for collections at any depth and only for collections (177/178/179); the whitespace-**intolerant** lone/mixed boundary (180); the four String forms — Boolean `True`/`False`, Number as an invariant double so `1.0` → `1`, Null → `''` even when lone, Version dotted (181/182/183/184); the result never being re-parsed as YAML (185); mixed content as one synthetic `format` call, where two adjacent expressions are mixed rather than lone (186); the collection-in-string sentence that names the kind twice (187); the escape spelling confirmed by execution (188); block scalars (189); and key position — always the String form, with **two** rejection sentences depending on lone vs mixed, rendered before schema validation (190/191/192). Two entries are about the evidence rather than the behavior: the service's own `finalYaml` is lossy about exactly `0123` and `True`, which is why those goldens compare raw text (193, E03-S05-T03's to close), and the interpolator must leave a lone `${{ insert }}` in value position untouched (194, from E03-S01-T04's `value-position` transcript — no new probe run, none needed). The block table row now reads *used*, the reconciliation note carries a progress line, and `C-E03-195..229` (E03-S02-T01) is the one block still owed. (evidence: `research/E03-template-engine.md` C-E03-175..194; `research/experiments/E03-interpolation/`; `packages/engine/test/template/interpolate.test.ts`, 70 tests green, unchanged)
- 2026-08-25 E03-S02-T01 done — the epic's sharpest drift case is closed: `C-E03-195..218` is written into `research/E03-template-engine.md` from the 34 transcripts already on disk (`research/experiments/E03-references/`, 21 expanded pairs + 13 rejections, against the committed two-repository fixture `fixtures/oracle/references/repos/`). **Transcription, not re-implementation:** `reference.ts` and `reference.test.ts` were already on `main`, green, and citing these IDs, and E03-S06's shipped bundler consumes several of them — the entries simply did not exist, so every citation pointed at nothing. 23 entries written; `C-E03-204` keeps E03-S02-T05's earlier section. The three rules that contradict the obvious implementation now have their decisive probes on the record: **crossing a repository boundary resets the base directory to the repository root** while a reference that stays home keeps the including file's directory — pinned by the `cross-rel-outward`/`cross-rel-self` pair, with `self-alias-relative-nested` as the control proving an explicit `@self` does *not* reset it (C-E03-215); **the alias splits on the first `@`** (C-E03-210); and **the service has no cycle detection**, dying of `Maximum object depth exceeded` at the repeated file, with our terminating `(repository, commit, path)` detection recorded as a deliberate mechanism divergence and `diamond-not-cycle` as the control (C-E03-208/209). Also recorded: path resolution and its escape check on the resolved path rather than each step (195/199/201/202/203/205/206), the unnormalized join the invalid-path sentence prints (200), repository pinning and the `refs/heads/main` default (196/197/198/218), alias lookup folding case while path lookup does not (213), the two distinct alias failures — undeclared vs declared-but-unfetchable (211/214), the empty alias landing on `self` (212), cross-repo context (216) and the `<path>@<alias>` diagnostic naming (217). Three IDs the code never cited (199, 202, 214) are assigned to survey findings nothing had recorded. Newly deep-verified alongside: the `resources-repositories-repository` schema page (`git_commit_id d089fd2dbb54483ec611eeb478e3eff14be74393`, `ms.date` 2026-07-29) for the `ref` default. With this the epic's reconciliation note reads *the drift is closed* — no E03 block has code citing entries that do not exist; the one number set still outstanding is `C-E03-300..339`, which E03-S02-T02 reclaims. (evidence: `research/E03-template-engine.md` C-E03-195..218; `research/experiments/E03-references/`; `packages/engine/test/template/` 402 tests green, unchanged)
- 2026-08-25 E03-S02-T02 done — typed parameter binding: the claim block `C-E03-300..333` is recorded in `research/E03-template-engine.md` from the 88 committed probes (`research/experiments/E03-parameters/`, 42 pairs + 46 rejections), and `packages/engine/test/template/parameters.test.ts` is written — 159 tests, the module's **first** suite. `packages/engine/src/template/parameters.ts` was already on `main` and is untouched. **The survey is this large because the three documented sources disagree with each other before the service is asked**: two process pages list 13 type names, the yaml-schema page 12, the vendored service schema 16 in one position and 20 in the other. **Four documented statements are measured false** — `stringList` *is* available in templates (C-E03-306), `type:` is *not* required and defaults to `string` rather than being inferred (C-E03-308), `values:` does *not* supply a missing default so "the first available value is used" is wrong (C-E03-303/309), and Boolean→String binding gives `true`, not the expression language's `True` (C-E03-321). The real vocabularies are the vendored schema's two lists exactly, and the positions are genuinely different rather than nested: `legacyObject` is template-only, `environment`/`filePath`/`pool`/`secureFile`/`serviceConnection` root-only (C-E03-304/305). Four rules shape the code — a scalar binds as its **source text**, so `True` → `"True"`, `007` → `"007"` and Null → `''` in one step rather than per type (C-E03-321/332); `legacyObject` is `object` with every scalar leaf stringified, undocumented on every page (C-E03-325); a `default:` is not an expression slot but admits exactly one form, a lone single-quoted string literal (C-E03-315); and queue-time values run the same per-type conversion with JSON parsing added for `object` (C-E03-329..331). Done met on both halves: the matrix is table-driven over **both** vocabularies × default/provided/missing/wrong-type, with a coverage assertion so a type added to either list without a binding rule fails here instead of silently binding as a string; and every error sentence is compared against the **service's own bytes**, read back out of each probe's committed `response.json` rather than a golden we wrote (which could drift from the service and stay green). Full suite 2035 vitest green; `parameters.ts` coverage 97.6% statements / 98.2% lines, from zero. REFERENCES statuses recorded for the two process pages, naming the false statements. Out of scope and unchanged: the CLI/config half of "runtime parameters at root bound from CLI/config" (the binder takes queue-time values; E10-S02-T01 supplies them), and PLAN D3 — this is the retained `--offline-expand` engine's binder and not a reason to revisit E03-S06-T02's refusal to inline parameterized templates, which is E03-S06-T05's decision. (evidence: `research/E03-template-engine.md` C-E03-300..333; `research/experiments/E03-parameters/`; `packages/engine/test/template/parameters.test.ts`)
- 2026-08-25 E03-S04-T02 done — the expanded-YAML emitter and its provenance map: `packages/engine/src/template/expand.ts` (`serializeExpandedYaml`, `buildExpansionMap`, `provenanceAtLine`, `expandDocument`) is the offline fallback's entry point, which until now existed only as the test harness. **The Ground field's "compare serialization choices against oracle `finalYaml`" turned into the strongest test in the suite:** parsing each of the ten corpus `final.yml`s and re-serializing them reproduces the file **byte for byte**, so the formatting is the service's own output surviving a round trip rather than a golden we wrote — `indentSeq: false`, `singleQuote: true`, `lineWidth: 0`, one appended newline (C-E03-250/251). Reaching that fixpoint forced the design: the service preserves the author's quoting on `'0 3 * * Mon-Fri'`, a scalar that would also be legal *plain*, so an emitter working from plain JavaScript values cannot match it — the serializer walks the **DOM** and restores each scalar's authored style (C-E03-252). Map coverage is 100% **by construction**, not by effort: every node in an expanded DOM carries a `pos`, including the ones `insert`/`each`/interpolation synthesize, and the corpus-wide test counts nodes independently of the map builder so the check is not the builder marking its own homework (C-E03-253); the map records that two emitted nodes on different lines can share one authored line, which is the fact it exists for. Spot-check tool: `pnpm expansion-provenance <file> [line]` prints a `file:line` gutter beside every emitted line, one line's source stack, or `--map out.json`; it is exercised as a subprocess, because "prints provenance for a chosen line" is a claim about a program. **Said explicitly rather than left to be discovered:** the offline walk does the directive passes and interpolation and does *not* apply the two expansions the service also performs while producing `finalYaml` — the `stages: __default` wrapping (C-E00-022) and step-shortcut desugaring (C-E12-019) — so an offline expansion of an authored corpus pipeline is **not** normalizer-equal to its `final.yml`; that gap is filed as **E03-S04-T04** rather than papered over. `packages/fetch`'s `--offline-expand` refusal message now points at the wiring task (E10-S02-T01) instead of at this one, since the engine side now exists. 49 tests; full suite 2084 green; `expand.ts` 100% statements/lines. (evidence: `research/E03-template-engine.md` C-E03-250..253; `packages/engine/test/template/expand.test.ts`; `fixtures/oracle/*.final.yml`)
- 2026-08-25 E03-S04-T03 done — strict validation of the **expanded** pipeline: `packages/engine/src/frontend/validate-expanded.ts` (`validateExpandedPipeline`, `formatExpandedDiagnostic`, `DESUGARED_TASK_GUIDS`). **The finding that shaped it:** the ten committed corpus `final.yml`s do *not* validate under the authored-document validator — an expansion is a different **dialect** (C-E03-258). The service emits two families it refuses as input: `trigger:`/`pr:` `{enabled: false}`, its rendering of `trigger: none` (C-E03-002), and the three desugared GUID tasks, which have no name spelling for a name-keyed catalogue to find (C-E04-030/031). Both are accepted here and nowhere else; each relaxation is pinned by a negative case proving it is *two paths* and *three GUIDs* rather than a property name and a GUID shape. **The Ground field's three mutation probes were run live** (`pnpm strict-validation-survey`; transcripts under `research/experiments/E03-strict-validation/`): an unknown key, a wrong-typed value and an unknown task were each injected into a known-good expansion — a document the service itself produced, so the mutation is the only difference — and all three came back HTTP 400 (C-E03-254/255/256). None of our families is stricter than the authority, which is the direction that matters: rejecting what the service accepts turns a working pipeline into a conversion failure the user cannot tell from their own mistake. One divergence survives and is recorded rather than hidden — an unknown task stays a **warning** here because our catalogue is the vendored in-box snapshot while the service's is the organization's installed set; severity, not detection, and it closes with an org schema (C-E03-257, C-E01-033). **Provenance is a port, not an import:** the caller passes an `originAt` lookup (`originLookup(map)`, E03-S04-T02) so a diagnostic carries pointers into *both* `pipeline.expanded.yml` and the original source; on the default path there is no map and the expanded pointer stands alone — which is also why the validator does not import the expander and invert the layering. 27 tests; full suite 2111 green. Bookkeeping: this **narrows** `E01-S02-T02` — the validator half it was waiting on now exists, and its only remaining dependency is `convert`'s body (E10-S02-T01); its note says so instead of still claiming the validator is missing. (evidence: `research/E03-template-engine.md` C-E03-254..258; `research/experiments/E03-strict-validation/`; `packages/engine/test/frontend/validate-expanded.test.ts`)
- 2026-08-25 E03-S04-T04 partial (`[!]`) — the offline arm's normalization-time expansions: `packages/engine/src/template/desugar.ts` (`desugarExpansion`), applied by `expandDocument`. All three rewrites are implemented and tested — implicit structure (`steps:` root → `stages: __default` / `job: Job`; `jobs:` root → the stage alone), `trigger:`/`pr:` `none` → `{enabled: false}`, and the full step-shorthand table with its input renaming (`publish`'s `artifact:` → `artifactName:`, `download`'s scalar → `alias:`, `pwsh: true` distinguishing `pwsh` from `powershell`). Three things were measured rather than inferred and are now claims: the `pool:` asymmetry — it moves **into** the invented job for a `steps:` root and **stays at the root** for a `jobs:` root, one sample each way (C-E03-259); `checkout: none` acquiring a synthesized `condition: false` alongside `inputs: {repository: none}` (C-E03-260); and the surviving-property set plus the `task:` → properties → `inputs:` key order, where `workingDirectory` is the sibling that looks like a step property and is not (C-E03-261). Synthesized nodes inherit the replaced node's `pos`, so the expansion map's coverage stays total and a diagnostic on a desugared step points at the shorthand the author wrote. **Marked `[!]`, not `[x]`, because one Done item is unmet:** the criterion is normalizer-equality with the service for every template-free corpus entry, and **four of five** reach it (01, 02, 03, 10). The fifth, `04-variable-layers`, reads `${{ variables.solution }}` at a job that overrides the value — *which layer a compile-time variable read sees* is **E03-S03-T01**, which is `[~]` **demoted** (E12-S01-T02, docs/07 §6) because the service decides it, so implementing a guess is exactly what rule 1 forbids. The entry is **asserted to differ in exactly that one way** rather than excluded, so the test fails if the divergence ever becomes something else. Flips to `[x]` if E03-S03-T01 is un-demoted; nothing else outstanding. 30 tests; full suite 2141 green. (evidence: `research/E03-template-engine.md` C-E03-259..261; `packages/engine/test/template/desugar.test.ts`)

2026-08-25 E03-S06-T05 in-progress — chose the documented-limitation path after grounding options (a)–(c); implementing stable bundler/README warnings and decision record (evidence: research/E03-template-engine.md C-E03-408..418; research/experiments/E03-bundle/; research/experiments/E03-parameters-request/template-scoped/)
2026-08-25 E03-S06-T05 done — parameterized local templates are a documented service-backed-path limitation; warnings now preserve the committed-file consequence and explicit commit/`--offline-expand` remedies through the manifest into README, with no implicit authority switch (evidence: research/E03-template-engine.md C-E03-408..418; research/experiments/E03-bundle/; research/experiments/E03-parameters-request/template-scoped/; packages/engine/test/template/inline.test.ts; packages/emit/test/readme.test.ts; docs/06 §5 decision 66; 2,142 Vitest + 252 Bats)
2026-08-25 E05-S01-T04 in-progress — selected an emitter-generated exact `.env` alias table to reconcile env spellings with case-insensitive variable-store keys without lossy underscore/dot guessing (evidence: C-E06-003; docs/06 §5 decisions 63/65; implementation pending)
2026-08-25 E05-S01-T04 done — generated `.env` entries now carry a one-for-one exact alias into `run.sh`; the runtime validates that table and writes each value under its original variable-store name, so dotted predefined variables and every synthesized entry round-trip without guessing, and the obsolete branch-name fallback is gone (evidence: C-E06-003; docs/06 §5 decision 67; `packages/emit/test/env-example.test.ts`; `packages/emit/test/entrypoints.test.ts`; `packages/runtime/test/core.bats`; 2,144 Vitest + 254 Bats)
2026-08-25 E05-S01-T05 in-progress — selected the runtime's existing metadata-preserving pipeline→job scope copy so generated jobs inherit `.env` variables before applying job-local writes, eliminating the temporary build-number environment bridge (evidence: C-E04-082/083; C-E05-017; implementation pending)
2026-08-25 E05-S01-T05 done — generated jobs now inherit the pipeline store with value/secret/read-only metadata intact, job writes shadow without leaking, arbitrary `.env` values reach both macros and `azdo_var`, and the special build-number/build-id environment bridge is deleted (evidence: C-E04-082/083; C-E05-017; docs/06 §5 decision 68; `packages/emit/test/entrypoints.test.ts`; `packages/emit/test/run-number.test.ts`; `packages/runtime/test/core.bats`; 2,144 Vitest + 254 Bats)
- 2026-08-26 E10-S02-T01 done — `convert` wiring & flags, the keystone: `packages/cli/src/convert/convert.ts`. `convert`'s body threw `NotImplementedError` while seven epics' worth of machinery sat behind it; it now runs **config → bundle → expand → validate → model → emit → write**, and the order is forced — bundle before expand (the service reads *committed* bytes, so a local template edit reaches it only by being spliced into the override), expand before validate (an expansion is a different dialect with its own validator, C-E03-258), validate before model. The whole docs/06 §1 flag surface is wired: `-o`, `--parameter`, `--org`, `--project`, `--target-os`, `--checkout-mode`, `--exec-env`, `--sandbox-image`, `--group-names`/`--no-group-names`, `--only-stage` (repeatable), `--frozen`, `--update`, `--offline`, `--offline-expand`, `--strict`, `--no-bundle`. **Five behaviours are ours, recorded as docs/06 §5 decision 69:** `--only-stage` **drops** a dependency on a stage it left out and warns (keeping it makes `run.sh` name a stage that is not there; refusing makes the flag useless for exactly the huge pipelines it exists for); `--exec-env sandbox` and `--sandbox-image` are **refused, not ignored**, because accepting either writes a `manifest.json` claiming an execution environment the project does not have — a lie in the record `doctor` reads; `--offline` alone is a usage error, since §1 makes it satisfiable only via `--frozen` or `--offline-expand`; `--strict` writes the project *first* and then exits 2, so the user has the README's ranked list to act on; and `--json` prints a **versioned** summary rather than a schema-validated document (nothing consumes it yet — the manifest earned its committed schema because `doctor` reads that). **Three fills deferred here by name landed:** `lib/runtime.sh` + `lib/expr.sh` copied from the runtime (decision 62 — without them no emitted script can `source` anything), `.shellcheckrc` carrying `SC2005,SC2046` (decision 61) and `SC2016,SC2071` (decision 62(d)) so a user's editor agrees with our own gate, and the manifest's per-step `file` + per-job `targetOs` (decision 64), filled *after* `serializeManifest` because both are facts about the generated project — the scaffolder decides the path, the settings decide the OS — with `packages/engine/schema/manifest.schema.json` gaining the two properties docs/04 §11 always specified. Also: `expandDocument` is bound as the `OfflineExpander` port so `--offline-expand` works end to end; `run()` became async and commander is driven through `parseAsync`, which incidentally fixed two existing tests that were leaking unhandled rejections once the action stopped being synchronous. Done met on both criteria: **e2e per flag** — each flag has a case observing a *different* generated project or exit code, across both arms (the service arm hermetic through `ExpandCachedOptions.fetchImpl`, the offline arm for real) — and the `--json` summary is snapshot-pinned and versioned. The strongest test is not about a flag at all: the generated project is **run with bash** and its first step's log read back, which no amount of plausible-looking output can fake. 27 convert tests; full suite **2171 green**; `cli/src` back above its 97% line gate. Bookkeeping: **E01-S02-T02** and **E12-S01-T01** are now unblocked (their notes say so; closing them is their own cycle) and **E12-S02-T02** is narrowed to its docs half. (evidence: `packages/cli/test/convert.test.ts`; docs/06 §5 decision 69; docs/06 §1 now lists `--strict` and `--no-bundle`)
- 2026-08-26 E10-S02-T02 done — the `run` convenience proxy: `packages/cli/src/run/run.ts` spawns `bash run.sh` with `stdio: 'inherit'` and reports the child's status as its own. **The guard the Do field asks for is the first test in the file:** the module's source, comments stripped, contains **none** of `run.sh`'s flag names — a proxy that learned them would be a second place to update whenever the emitter changes, and would diverge silently from the script it fronts. **Exit-code transparency is the contract:** `run.sh` ends with `exit "$(azdo_run_exit_code)"`, so its status *is* the pipeline's verdict; collapsing it onto the CLI's 0/1/2 would make `azdo-emu run` and `bash run.sh` disagree about whether the pipeline passed. The status passes through untouched, including 126/127 from the shell and **128+N** for a fatal signal — all three pinned to the GNU Bash Reference Manual v5.3 §3.7.5, newly deep-verified as **C-E13-020/021/022**. A `ProxiedExit` carries it out through the one exit path, and `run()`'s return type widened from `ExitCode` to `number` for exactly that reason; `os.constants.signals` supplies N rather than a hard-coded table, because `SIGUSR1` is 10 on Linux and 30 on macOS. Commander needed `enablePositionalOptions()` + `passThroughOptions()` so `azdo-emu run out --list` hands `--list` to the script instead of rejecting it. **Δ recorded rather than hidden:** Node has no `exec(3)`, so an intermediate process exists where a shell proxy would have replaced its image — the observable status is identical, but a signal delivered to the proxy is not automatically delivered to the child's process group. Done met: **e2e parity** compares proxy vs direct `bash run.sh` on a real converted project across success, a non-zero verdict, `--list`, an unknown flag (`run.sh`'s exit 2, not ours) and a fatal signal. 18 tests; full suite **2188 green**. (evidence: `packages/cli/test/run.test.ts`; `research/E13-cli-config-doctor.md` C-E13-020..022; `research/REFERENCES.md` bash-manual row)
- 2026-08-26 E01-S02-T02 + E12-S01-T01 done — the two P1 tasks E10-S02-T01 unblocked, closed together because they are the same act: asserting, **by name**, criteria that the wiring made reachable rather than assuming they follow from it existing. **E01-S02-T02** — a deliberately broken expansion (a corpus `final.yml` mutated on the *service* side, since only the expansion can produce one) is rejected with `pipeline.expanded.yml:<line>:<col>` pointers in the message, and **every** committed corpus expansion passes clean. What made the second half reachable is E03-S04-T03's `validateExpandedPipeline`: an expansion is a different dialect from an authored document (C-E03-258), and the authored-document validator reports errors in nine of the ten corpus files. **E12-S01-T01** — "the default path never invokes the local engine" is *proven*, not stated: the service stub answers with a different pipeline than the one submitted, so an expansion describing the corpus fixture instead of the input could not have come from the local engine, and no fallback warning is raised. `--offline-expand` produces its expansion through `expandDocument` bound as the `OfflineExpander` port; the choice is recorded in `manifest.json` on **both** arms (`{mode: service, degraded: false}` / `{mode: offline, degraded: true}`); and the fallback warning reaches the generated README's ranked list. E01-S02-T02's blocker note travelled through three narrowings — E12-S01-T02 (2026-08-22), E03-S04-T03 (2026-08-25), E10-S02-T01 (2026-08-26) — each recording what was actually left, which is why closing it took one test block rather than a re-investigation. 5 new tests; full suite **2193 green**. (evidence: `packages/cli/test/convert.test.ts` — the two `describe` blocks named for the tasks)
- 2026-08-26 E09-S01-T01 blocked (`[!]`) — **grounded, not implemented.** The Ground field is complete and is the durable part: the device-code protocol is pinned end to end in `research/E09-auth-fetchers.md` (C-E09-001..006) from two freshly deep-verified pages — the Entra-OAuth page (`git_commit_id f7bd73fb…`) for the resource GUID `499b84ac-1321-427f-aa17-267ca6975798`, the `https://app.vssps.visualstudio.com` resource URI and the `.default` scope, **re-confirmed on learn.microsoft.com exactly as the field demands** rather than carried over from C-E00-011; and the device-code page (`git_commit_id a4be4ac4…`) for the `/devicecode` and `/token` endpoints, the `urn:ietf:params:oauth:grant-type:device_code` grant, the four polling outcomes (`authorization_pending` / `authorization_declined` / `bad_verification_code` / `expired_token`) and the fact that a refresh token is issued **only** when `offline_access` is in scope. The blocker is the Done criterion itself — "e2e against test org: sign in → probe call succeeds" — which needs a person at a browser typing a user code. The implementation was not started, so this is `[!]` rather than partial. **One finding changes the epic and is recorded now rather than discovered later (C-E09-002):** "Microsoft Entra apps don't natively support Microsoft account (MSA) users for the Azure DevOps resource" — a personal-account sign-in is exactly the shape a solo developer converting their own pipelines has, so the device-code arm cannot be assumed available for every user, and docs/05 §1's mode auto-selection has to survive that arm being *unavailable* rather than merely unattempted.
- 2026-08-26 E09-S01-T02 blocked (`[!]`) — half of "one live check per mode" is unreachable here, measured rather than assumed: `az` is installed and `az account show` succeeds, but `az account get-access-token --resource 499b84ac-…` returns `AADSTS700082: The refresh token has expired due to inactivity`, so the `az` arm needs an interactive `az login`. The PAT arm is checkable (the oracle PAT authenticates against the test org). Left whole rather than split: implementing one mode and stubbing the other would bury the mode auto-selection order docs/05 §1 specifies, which is the part that has to be right. `az login` unblocks it immediately; nothing else is missing.
2026-08-25 E02-S05-T05 in-progress — re-checked both dependency-result context shapes and the five stable values; implementing shell reads that distinguish a recorded skipped job from a missing member (evidence: C-E02-092..095; `research/experiments/E02-dependencies/real-run.md`; implementation pending)
2026-08-28 E02-S05-T05 done — compiled job/stage dependency results into exact five-state runtime readers with case-insensitive authored-name lookup, explicit skip markers, empty-job preservation, and corpus entry 03 unblocked (evidence: research/E02-expressions.md C-E02-092..095; research/experiments/E02-dependencies/real-run.md; packages/engine/test/expr/conformance.test.ts; packages/runtime/test/core.bats; packages/emit/test/readme.test.ts; 2,203 Vitest + 259 Bats)
2026-08-28 E09-S01-T03 done — added keyring-first versioned Azure credential storage with an atomic 0700/0600 JSON fallback, authenticated Profile `me` status results for org/identity/mode/expiry, and the deployment-host discrepancy captured by a redacted live PAT run (evidence: research/E09-auth-fetchers.md C-E09-007..011; research/experiments/E09-rest/profile-me/real-run.md; packages/fetch/test/auth-storage.test.ts; packages/fetch/test/auth-status.test.ts; 2,222 Vitest + 259 Bats)
- 2026-09-02 E09-S01-T04 done — built the docs/05 §1 GitHub chain (`gh auth token` → `GITHUB_TOKEN` → anonymous) in `packages/fetch/src/auth/github.ts`, applied to both the Contents and tarball calls. Three details are load-bearing and each is pinned in code *and* a test. **The bearer token never crosses origins (C-E09-015/017):** the tarball redirect is taken manually and the storage request carries an empty header set — measured, not assumed, because the private-repo archive came back in full (199,698 bytes from `codeload.github.com`) with no `Authorization`, which proves the signed URL carries its own grant. **An anonymous 404 is not "no such repository" (C-E09-014/016):** GitHub hides a private repo's existence from an unauthorized caller, so the failure reads "not found, or private and this request was unauthenticated" rather than baking a misleading error into the fetchers E09-S02-T02 builds on top; an *authenticated* 404 says something different. **`gh` is read by argv, never a shell string, and its diagnostics are discarded (C-E09-012)** so a secret cannot travel through our own error text; a missing `gh` binary is "no credential", not a failure of the chain. The Done criterion was closed honestly: the pre-existing transcript measured raw requests made *before* this code existed, so both fixture fetches were re-run **through the shipped chain** and appended as a second, equally redacted section. **One unrelated fix rode along:** `packages/engine/test/schema-vendor.test.ts` compiles the full vendored service schema (~2.5s of CPU alone) against vitest's 5s default and had become a load-dependent flake — reproduced at 2,222 tests *before* this task added any — so its budget is now explicit rather than accidental. 20 new tests; full suite **2,248 Vitest + 259 Bats** green, `packages/fetch/src/**` coverage back over its 97/85/100/97 ratchet (github.ts 100% statements/functions/lines). (evidence: research/E09-auth-fetchers.md C-E09-012..017; research/experiments/E09-rest/github-auth/real-run.md §"Second run — through the implemented chain"; packages/fetch/test/auth-github.test.ts)
- 2026-09-02 E09-S02-T01 done — built the ADO Git fetcher in `packages/fetch/src/repo/ado-git.ts`: ref→SHA via the Refs endpoint, a whole-repo snapshot by bare mirror or Items `$format=zip`, and docs/05 §4's `.cache/repos/<host>/<org>/<project>/<repo>/<sha>/` layout. **Three findings the docs did not hand over.** (1) **`filter` is a prefix match, not an exact one** (C-E09-030) — "A filter to apply to the refs (starts with)" — so `heads/main` also returns `refs/heads/main-2`; taking the first result would silently pin the wrong branch. The live negative control proves it: asking the service for `refs/heads/mai` *did* return `refs/heads/main`, and the exact-name check turned that into an error. (2) **An annotated tag's `objectId` is the tag object, not a commit** (C-E09-031/032) — measured locally with git 2.43.0 (`refs/tags/<annotated>` → `53b81d2d…` vs `^{commit}` → `9dd2a98c…`) — so the resolver asks for `peelTags=true` and prefers `peeledObjectId`; docs/05 §4's lockfile field is `commit`, and pinning a tag object there would fail only much later, at checkout. (3) **`scopePath`, not `path`** (C-E09-037): the parameter table makes `path` required and `recursionLevel` independent, but the service answers the combination with HTTP 400 — "Use the \"scopePath\" query parameter filter instead to get a collection of items." Found by running the code, not by reading. **Secret hygiene closed all three git leak channels and each is asserted** (C-E09-035/038): the auth header travels by `--config-env` (never argv — the agent's pre-2.31 `-c` fallback is deliberately *not* implemented, and old git takes the zip route instead of degrading), never enters the clone URL, and never persists into the mirror's `.git/config` — verified on disk after a live clone. The cache-hit path is proven offline by substituting fetch and git runners that **throw on any use**. 29 new tests; full suite **2,277 Vitest + 259 Bats** green, `packages/fetch/src/**` over its ratchet. **One gap recorded rather than papered over (C-E09-036):** the test org has no annotated tag, so the ADO-side half of the peeling claim is doc-grounded; creating one is an outward-facing write to a personal org and was not taken unilaterally. (evidence: research/E09-auth-fetchers.md C-E09-030..038; research/experiments/E09-rest/ado-git/real-run.md; packages/fetch/test/repo-ado-git.test.ts)
- 2026-09-02 E09-S02-T02 done — built the GitHub fetcher in `packages/fetch/src/repo/github.ts` on E09-S01-T04's auth chain and E09-S02-T01's cache layout: ref→SHA via the commits endpoint, a tarball snapshot pinned to that SHA, and the same `.cache/repos/<host>/…/<sha>/` entry with a method marker. **The finding that changed the code (C-E09-040): the documented `tags/TAG_NAME` shorthand does not work.** The page says the ref "Can be a commit SHA, branch name (heads/BRANCH_NAME), or tag name (tags/TAG_NAME)" — measured against `git/git`, `tags/v2.43.0` returns **422 "No commit found for SHA: tags/v2.43.0"** while `heads/master` returns 200. So the one shorthand named for tags is the one the service rejects; `commitRefFor` promotes any namespaced shorthand to the full `refs/…` form, which works for both namespaces and, unlike a bare name, cannot be ambiguous when a branch and a tag share a name. **The second finding makes an asymmetry deliberate rather than sloppy (C-E09-041): GitHub peels annotated tags for the caller.** For `git/git` `refs/tags/v2.43.0` the Git-refs endpoint reports `object.type: "tag"`, sha `c089584a…`, while the commits endpoint returns `564d0252…` — the commit. That is the exact inverse of Azure DevOps (C-E09-031/032), so this fetcher has no peeling step where `ado-git.ts` needs one, and the difference is now recorded in both modules. Everything about authentication — the redirect handling, the no-cross-origin-credential rule, the anonymous-404 reading — is inherited from `auth/github.ts` rather than re-derived (C-E09-043); the live run re-confirms the private/anonymous boundary through this layer (404 anonymous, 199,698-byte archive with the chain). The offline cache-hit path is proven the same way T01's was: a fetch impl that **throws on any use**. 18 new tests; full suite **2,295 Vitest + 259 Bats** green. (evidence: research/E09-auth-fetchers.md C-E09-039..043; research/experiments/E09-rest/github-fetcher/real-run.md; packages/fetch/test/repo-github.test.ts)
- 2026-09-02 E09-S02-T03 done — built alias resolution in `packages/fetch/src/repo/alias.ts` (config override → `type: git` → `type: github`, `@self` from the working copy, endpoint substitution note) plus the CLI adapter `packages/cli/src/convert/repositories.ts` that joins it to E03's reference resolution. **The Ground field's "default-branch rule" turned out to be a doc bug, and it is corrected rather than worked around (C-E09-044).** The schema page — re-fetched, not carried over, and updated the day before (`ms.date` 2026-09-01) — says `ref` "defaults to 'refs/heads/main'": a **constant**, not the repository's own default branch. `docs/05` §3 item 4 said "repo default branch"; the two diverge for any repo whose default is `master`, and following the doc would have silently resolved a *different* branch than the service does. docs/05 fixed, decisions record entry **72** added. **Two more corrections came from the same page:** there are **four** types, not three — `bitbucket` was an unhandled fourth arm (C-E09-045) — and one slash in `name` means *project/repo* under `git` but *owner/repo* under `github`, the same text parsing two ways with only `type` to disambiguate it (C-E09-046). **Nothing throws (PLAN D10):** an unsupported type, an unknown type, a missing `name`, a missing credential and a fetch failure all become a note plus an `unresolved` entry, each pointing at the `repositories:` override that converts them anyway; an ignored `endpoint:` is *announced* rather than dropped, and still resolves. **The Done was met for the paths this task owns and the gap is a new task, not a silent hole:** the local-override integration test drives a real cross-repo `template:` reference out of a redirected working copy with no credential and no network, and a bare mirror is read with `git show <commit>:<path>` — no extraction, no dependency. An **archive** snapshot (ADO zip, GitHub tarball) resolves and pins correctly but reports `unreadable` rather than answering "no such file", because an extraction gap that looks like a missing template is the wrong bug to chase; **E09-S02-T04 added** to close it. 26 new tests; full suite **2,317 Vitest + 259 Bats** green. (evidence: research/E09-auth-fetchers.md C-E09-044..049; docs/06 §5 decision 72; packages/fetch/test/repo-alias.test.ts; packages/cli/test/repositories.test.ts)
- 2026-09-02 E09-S02-T04 done — closed the extraction gap E09-S02-T03 opened: `packages/fetch/src/repo/extract.ts` unpacks both archive snapshots into `<entry>/tree/` at fetch time, so an archive-backed alias now reads through the same working-copy path as everything else and `unreadable` is empty in practice. **The two formats disagree about exactly the thing an extractor has to get right, and both halves were measured (C-E09-050/051):** the GitHub tarball prefixes every entry with `<owner>-<repo>-<abbreviated sha>/` — `octocat-Hello-World-7fd1a60`, **seven** characters, while the resolver pinned the full forty, so a prefix computed from the pinned sha would never match — and the ADO zip has **no prefix at all**. One rule covers both: strip a single leading component only when *every* entry shares it. **Two more measured details the format alone would not tell you:** the tarball's first member is a PAX global header (typeflag `g`), which a naive parser writes into the tree as a file called `pax_global_header` (C-E09-052), and every ADO zip member is deflate (C-E09-053). **Pure Node, no dependency and no host binary** — `tar` and `unzip` are not both reliably present across the CI matrix, and this repo has already been bitten once by host-dependent file access (`localFetcher`, 2026-08-22). **One guard is ours, not parity (C-E09-054):** neither service documents any constraint on the entry names it may emit, so any member resolving outside the target directory is refused and named — classic zip-slip hardening, tested with `../`, an absolute path, and a traversal hidden *behind* a shared prefix. Verified live end to end: the real `octocat/Hello-World` tarball extracts to `tree/README` (not `tree/octocat-Hello-World-7fd1a60/README`), reading `"Hello World!\n"`. Existing fetcher tests were upgraded from placeholder bodies to byte-valid archives so they exercise the path production does. 18 new tests; full suite **2,335 Vitest + 259 Bats** green. (evidence: research/E09-auth-fetchers.md C-E09-050..054; research/experiments/E09-rest/archive-shapes/real-run.md; packages/fetch/test/repo-extract.test.ts; packages/cli/test/repositories.test.ts)
- 2026-09-02 E09-S03-T01 done — built the typed ADO REST client core in `packages/fetch/src/rest/client.ts`: one api-version table, base-URL construction, retry/backoff, error surfacing and redaction, so every E09-S03 endpoint makes those decisions once. **Two grounded behaviors are the opposite of the obvious implementation.** (1) **Omitting `api-version` does not fail — it silently succeeds (C-E09-060/061).** The page says the version "must be specified with every request", but a request without one returned **200**, and the server announced the version it picked in its own `Content-Type` (`application/json; charset=utf-8; api-version=7.1`). An omission is therefore not a loud error but a floating dependency that moves when the server does, which is the worse failure; the client pins on every request and *parses the echoed version back* (C-E09-062) so a mismatch is observable rather than invisible. `api-version=99.0` gives a diagnosable 400 naming the server's ceiling — **7.2**, above our 7.1 pin, so the pin is conservative not stale (C-E09-063). (2) **`Retry-After` arrives on a *successful* response (C-E09-064):** "Honor the Retry-After header… The response still returns HTTP 200, so retry logic isn't required." A client that inspects it only on 429/503 misses the throttle entirely and keeps hammering a service that just asked it to slow down — so the header is read on every response and the debt is paid before the *next* request, capped so a hostile value cannot hang a convert. **Measured negative result (C-E09-065):** none of the seven documented rate-limit headers appear on an ordinary 200 from the test org, so all are optional and the client is correct with every one absent. Redaction is local policy (C-E09-066), proven by a test asserting the PAT and a `sig=` query value appear in neither the message, the URL, nor the stack. 25 new tests; full suite **2,360 Vitest + 259 Bats** green. (evidence: research/E09-auth-fetchers.md C-E09-060..066; research/experiments/E09-rest/client-core/real-run.md; packages/fetch/test/rest-client.test.ts)
- 2026-09-02 E09-S03-T02 blocked (`[!]`) — **implemented and grounded; one Done item needs an artifact this org has never produced.** `packages/fetch/src/rest/runs.ts` has the whole path: runs list, branch/tag selection, artifact metadata with `$expand=signedContent`, the signed-URL download and the `.cache/artifacts/<alias>/<runId>/<name>/` write. **Two findings change the task as written (C-E09-067/068): Runs-List has no branch or tag filter parameters at all** — "Gets top 10000 runs for a particular pipeline", and its only URI parameters are `organization`, `project`, `pipelineId`, `api-version` — **and the list items omit `resources`**, the very field a branch filter needs, even though the `Run` definition documents it. Measured: a live list item's keys are exactly `_links, createdDate, finishedDate, id, name, pipeline, result, state, templateParameters, url`, while Runs-Get for the same run returns `resources.repositories.self.refName`. So "runs list w/ branch/tag filters" is necessarily **client-side at one extra Runs-Get per candidate** — the resolver walks newest-first and stops at the first match, bounded by `maxCandidates` so a selector matching nothing costs a fixed number of requests instead of 10,000. Runs-Get also returns `tags` and `yamlDetails`, **neither in the documented `Run` definition** (C-E09-069) — and `tags` is exactly what a `resources.pipelines` tag filter reads. **`signedContent` is "limited-time anonymous" (C-E09-070/071)**, which decides two things: the download sends **no** `Authorization` (the URL is the grant, as with GitHub's storage origin, C-E09-015/017), and `signatureExpires` means the URL is *not* lockfile material — only `runId` and the artifact name are. A missing artifact is a clean 404 with `ArtifactNotFoundException` (C-E09-072). **The blocker (C-E09-073):** all 13 pipelines and 29 completed runs were enumerated and **none has an artifact** — every oracle experiment used `previewRun: true`, which never executes — so a fixture needs a real queued build in a personal org, an outward-facing write not taken unilaterally. **One real bug fell out of this task and is fixed here:** E09-S02-T04's wrapper-stripping was applied to zips too, so an artifact laid out as `app/build.txt` had `app/` silently removed; C-E09-051 measured the ADO zip as having *no* wrapper, so stripping is now decided per format, with a regression test. 22 new tests; full suite **2,378 Vitest + 259 Bats** green. (evidence: research/E09-auth-fetchers.md C-E09-067..073; research/experiments/E09-rest/runs-artifacts/real-run.md; packages/fetch/test/rest-runs.test.ts)
- 2026-09-02 E09-S03-T03 blocked (`[!]`) — **built and grounded; the lookup half is done and measured, the download half blocks on the same missing artifact as E09-S03-T02.** `packages/fetch/src/rest/build.ts` has the classic artifact fallback plus the definition lookup. **Two findings are the interesting part.** (1) **`typeKey` is the discriminator and the message is not (C-E09-076).** Both artifact APIs report the same condition with `typeKey: ArtifactNotFoundException`, but Build says `Artifact drop was not found for build 527.` from `Microsoft.TeamFoundation.Build.WebApi` while Pipelines says `An Artifact with name "drop" was not found.` from `Microsoft.Azure.Pipelines.WebApi` — a fallback matching on message text would work against one API and silently not the other, so it matches on `typeKey` and a test proves both wordings are recognized. (2) **The Definitions `name` filter is exact, case-insensitive, and takes `*` wildcards — the inverse of the Git Refs filter (C-E09-077).** Measured: `name=oracle-anch` → **0**, `name=oracle-anch*` → 1, `name=oracle*` → 14, `name=*anchor` → 1, `name=ORACLE-ANCHOR` → 1. C-E09-030 established that Git Refs' `filter` *is* "(starts with)", so the two adjacent APIs trap in opposite directions: assuming prefix here returns nothing, and a definition legitimately named `build*release` would be turned into a pattern — so a name containing `*` is never sent as a filter, and the returned name is re-checked case-insensitively rather than trusting the count. **Two more shapes recorded:** an empty artifact list is a **200** while a named miss is a **404** (C-E09-075), so "published nothing" is not an error; and the definitions *list* item omits `process` and `repository`, so name → id → **yaml path** is necessarily two calls (C-E09-078) — the same list/detail asymmetry as Runs-List. **Blocker:** Done item 1 needs a build that published an artifact, and the org has none (C-E09-079); the download is unit-tested against the documented `ArtifactResource` shape only, and one queued build closes this and E09-S03-T02 together. 20 new tests; full suite **2,398 Vitest + 259 Bats** green. (evidence: research/E09-auth-fetchers.md C-E09-074..079; research/experiments/E09-rest/build-fallback/real-run.md; packages/fetch/test/rest-build.test.ts)
- 2026-09-02 E09-S03-T04 done — variable-group fetching in `packages/fetch/src/rest/variable-groups.ts`, names and secret flags only. **The measurement that makes the task matter (C-E09-080): the API hands back non-secret values in plaintext.** The page's own sample shows `"key1": {"value": "value1"}` next to `"key2": {"value": null, "isSecret": true}`, and the live org's group returns its `corpusPlainValue` value in full. So *secret* values are nulled by the service and everything else is not — which means the "never fetch variable-group values" rule (decision 2026-07-30) is something the client **does**, not something the API does for it. **So the discard is structural, not conventional (C-E09-084):** `GroupVariable` has no value field at all and the value is dropped at the parse boundary, because carrying it and filtering later leaves a window where a log line or an error could see it. The test proves the guarantee rather than asserting it: a response containing a plaintext value goes in, and that exact string is checked absent from the result, from its `JSON.stringify`, and from the rendered `.env.example` block. **One shape trap that would have silently mis-classified every variable (C-E09-081): `isSecret` is *absent* on a non-secret variable, never `false`** — the two live members carry key sets `['value']` and `['isReadOnly','value']` — so `v.isSecret === false` is a check that never fires; `undefined` is the not-secret case, and `isReadOnly` behaves identically. `groupName` also takes a `*` wildcard (C-E09-082), the same trap as the Definitions filter, so a group name containing one is not sent as a filter and the returned name is re-verified instead of trusting the count. 16 new tests; full suite **2,413 Vitest + 259 Bats** green. **Unrelated flake seen once and not reproduced:** `packages/emit/test/entrypoints.test.ts > "run.sh performs a full run"` failed on one full-suite run under load and passed both alone and on an immediate re-run; it is an e2e shell test in a package this task does not touch, so it is reported rather than "fixed" blind. (evidence: research/E09-auth-fetchers.md C-E09-080..084; research/experiments/E09-rest/variable-groups/real-run.md; packages/fetch/test/rest-variable-groups.test.ts)
- 2026-09-02 E09-S03-T05 blocked (`[!]`) — **implemented, grounded and live-measured against the real marketplace fixture; only the "consumed by E07" clause of Done waits, and it waits on an epic that does not exist yet.** `packages/fetch/src/rest/tasks.ts` resolves `name@major` to the installed definition and caches its `task.json` under `.cache/tasks/<name>@<version>/`. **Route and api-version are experiment-backed exactly as the Ground field directs** — the reference page is thin — with the agent pinned at `01845643…` as the code reference. **Four measured facts, three of which would each have produced a silent wrong answer.** (1) **The list already *is* the metadata (C-E09-085):** `GET {org}/_apis/distributedtask/tasks` returns 269 entries over 172 names, each carrying a whole `task.json` — inputs, `execution`, the lot — so metadata costs one call and no download. (2) **`version` is an object `{major, minor, patch, isTest}`, not a string (C-E09-086)** — matching `replacetokens@6` against `"6"` matches nothing. (3) **The list is unordered (C-E09-087):** `replacetokens` came back as majors `[3, 4, 6, 7, 5]`, so "latest" is computed, never the last element; one `id` spans every major and **zero** names have two entries sharing a major, so `name@major` selects exactly one definition and the GUID is not a version key. (4) **The zip needs the exact `major.minor.patch` (C-E09-088):** `.../6.3.1` → 200, `application/zip`, 700,058 bytes, PK magic; `.../6.4.0` → 404 `TaskDefinitionNotFoundException` with a message about *uploading* a package the caller never tried to upload — so the `typeKey` is the signal and the prose is not. A `requestBinary` path was added to the REST client for it, sharing the same auth, throttle and error handling as the JSON path. **The fixture is real, not synthetic:** the test org has exactly one marketplace extension installed and it is the one this task needs. 27 new tests; full suite **2,432 Vitest + 259 Bats** green. (evidence: research/E09-auth-fetchers.md C-E09-085..089; research/experiments/E09-rest/task-metadata/real-run.md; packages/fetch/test/rest-tasks.test.ts)
- 2026-09-02 E09-S03-T06 blocked (`[!]`) — **the lockfile schema, write/read/verify and the offline guarantee are done in `packages/fetch/src/lockfile.ts`; only the `fetch-artifacts.sh` clause waits, on E09-S03-T02's blocked artifact half.** **The Done criterion — "two converts from lock → identical output hashes" — is met, and it is what caught the one real bug.** Sorting map keys was not enough: each pin's *own* field order followed whichever code path built it, so a lockfile written from a freshly fetched `pipelines.<alias>` pin and one written from a re-read pin differed by field order alone and the round-trip was not byte-identical. Canonicalization now fixes **per-pin** field order as well as map key order, and the reproducibility test is what surfaced it rather than inspection. A second test asserts that a re-convert with a new timestamp changes **exactly one line**, which is what makes the lockfile a diffable artifact worth committing. **The offline guarantee is proven, not asserted:** a test replaces `globalThis.fetch` with a function that throws and then verifies a warm cache, so it passes only if verification touched no network at all. **`verifyLockfile` reports every missing pin at once**, before the first network call — discovering the third of five repositories missing half-way through a convert is the failure mode the pre-check exists to prevent — and each entry names the path it looked at, so a wrong `--cache-dir` is distinguishable from a genuine miss. Two smaller decisions: a working copy (all-zero commit) is skipped rather than reported as missing, and an **unknown top-level lockfile field is carried through** on write, because silently deleting a pin another part of the tool wrote would break `--frozen` in a way that looks like a cache miss. 24 new tests; full suite **2,456 Vitest + 259 Bats** green. (evidence: docs/05 §4; packages/fetch/test/lockfile.test.ts)
- 2026-09-02 E09-S03-T07 done — org-schema caching and refresh in `packages/fetch/src/rest/yamlschema.ts`; both Done items met (cached schema used when present; refresh path tested). The endpoint's *behavior* was already grounded by E01-S02-T03, so this task re-took a live sample only to settle **how to cache it** — and that sample produced the cleanest possible statement of the problem (C-E09-090/091). **The live document and the snapshot committed at `research/experiments/E01-orgschema/yamlschema.json` are both exactly 611,234 bytes and both report `$comment: "v1.183.0"` — yet their sha256 digests differ** (`2c3f6556…` vs `ffd81760…`). Identical size, identical version marker, different bytes: it is C-E01-034's `definitions.task.anyOf` reordering seen across weeks instead of minutes, and it rules out all three cheap staleness checks at once. `$comment` does not move (C-E01-035); length does not move; a digest moves for a reason unrelated to the schema being newer, and a *matching* digest only proves two calls happened to agree. **Age is the only expiry left standing**, which is what docs/05 §4 concluded and what the code now does. Two consecutive fetches today were byte-identical, which does not retract C-E01-034 but sharpens it: the instability is **intermittent**, and an intermittently unstable body is exactly as useless for change detection as a permanently unstable one. **One policy decision is deliberate (C-E09-092):** a fetch failure over a merely *stale* entry falls back to that entry with a warning naming the HTTP status and the entry's age, rather than failing — a validation schema a few days old beats a conversion that will not run, and `resolvePipelineSchema` already degrades to the vendored schema when a document is unusable. The cache-hit test asserts **zero requests**, and the org name is sanitized before it becomes a filename because it comes from a user-supplied URL. 13 new tests; full suite **2,469 Vitest + 259 Bats** green. **E09-S03 is now complete apart from the two artifact-fixture blockers (T02/T03) and T05's wait on E07.** (evidence: research/E09-auth-fetchers.md C-E09-090..092; research/experiments/E09-rest/yamlschema/real-run.md; packages/fetch/test/rest-yamlschema.test.ts)
- 2026-09-02 E07-S01-T01 done — task package downloader. **Most of this task was already built by E09-S03-T05 two tasks earlier, so the work was finding and closing the three real gaps rather than writing a new module** — `parseTaskReference`, `selectTask`, `cacheTaskMetadata` and `downloadTaskZip` already existed, and the Ground field's "pin the agent repo's task-download path" was already satisfied (`azure-pipelines-agent @ 01845643…`, `TaskManager.cs` `DownloadAsync` L209-245 + `GetDirectory` L469-478). Nothing was re-grounded or re-implemented. **Gap 1 — nothing wrote a task pin.** `TaskPin` existed and `verifyLockfile` already checked for it, but no code path produced one, so the Done clause "lockfile pins the version" was unmet. `pinTask` is a read-modify-write, not a whole-file rewrite: a convert resolves tasks one at a time, and rebuilding the document from only the tasks it happened to see would drop the repository and pipeline pins the same convert wrote earlier. The key is the reference **as the YAML wrote it** (`replacetokens@6`) while the value carries the exact `major.minor.patch` — pinning only the major would leave the download route unaddressable (C-E09-088). **Gap 2 — `downloadTaskZip` re-downloaded unconditionally**, so "offline-reproducibly" was false. The reuse marker is the **unpacked tree, not the zip**: a zip present without a tree means an extraction that did not finish, and reusing it would hand real-task mode an entry with no files in it — covered by its own test. **Gap 3 — the download had never run through the shipped code.** E09-S03-T05 measured it with `curl`; it is now run end to end and the transcript records 700,058 zip bytes (matching the curl measurement), 5 extracted files, `fetched=false` on a second call whose fetch implementation **throws**, and `verifyLockfile` returning **0 missing** against the pin just written — the round trip closing. **E09-S03-T05 flipped `[!]` → `[x]` in the same change**, because its blocker note said verbatim "flip when E07-S01 lands and reads `.cache/tasks/<name>@<version>/task.json`", and leaving a blocker note that is no longer true is worse than the blocker. **Dependency note:** E07's header says `Depends on: E05, E06`; this task needs neither — those gate the emulation host from E07-S01-T02 onward. 7 new tests; full suite **2,476 Vitest + 259 Bats** green. (evidence: research/experiments/E07-tasks/real-run.md; research/E09-auth-fetchers.md C-E09-085..089; packages/fetch/test/rest-tasks.test.ts, packages/fetch/test/lockfile.test.ts)
- 2026-09-02 E07-S01-T02 blocked (`[!]`) — **the `INPUT_*` contract is done and grounded in `packages/emit/src/task-host.ts`; the wiring into `step.ts` waits on E07-S03-T01's disposition registry, the next task in the epic.** Done is met: a `CmdLine@2`-backed task and a marketplace-shaped Node task go through the same host and both observe exactly the `INPUT_*` their `task.json` declares — asserted by *executing* the emitted bash, not by comparing strings. **The Ground field is wrong and this is the correction (C-E07-001): the transform replaces dots *and* spaces.** The field says "spaces→`_`, uppercase, prefix `INPUT_`"; both task-lib (`_getVariableKey`: `name.replace(/\./g,'_').replace(/ /g,'_').toUpperCase()`) and the agent (`ConvertToEnvVariableFormat`) replace `.` as well, so an input named `sonar.projectKey` — a shape marketplace tasks really use — would have reached the task under a name it never reads. A test asserts the host agrees with the runtime's existing `azdo__env_name` by *running* it, because the agent uses one conversion for inputs and variables alike and a divergence would be invisible until a task read an input the runtime had named differently. **Three more measured details, each of which silently produces wrong values if missed.** (1) **An empty input is never stored (C-E07-002):** `_loadData`'s `if (value)` guard means an empty `INPUT_X` is not vaulted — `getInput` returns `undefined` — *and* not deleted from `process.env`, so a task reading the environment directly still sees it; the host emits it for the second half and does not count it as "set" for the first. (2) **`getBoolInput` accepts only the literal `true` (C-E07-003)**, case-insensitively — normalizing a YAML boolean to `1` would invert every boolean input a task reads. (3) **`getDelimitedInput` drops empty segments itself (C-E07-004)**, so the host must *not* pre-trim multi-line inputs. **Values are single-quoted and the quoting is proven by execution:** a value of `'; rm -rf /; echo '` and one containing `$HOME`, backticks and a newline both round-trip verbatim through `bash`. An input is data, and letting it become shell is the one thing this host must never do. 25 new tests; full suite **2,501 Vitest + 259 Bats** green. (evidence: research/E07-real-task-mode.md C-E07-001..006; packages/emit/test/task-host.test.ts)
- 2026-09-02 E07-S03-T01 blocked (`[!]`) / E07-S01-T03 done — **the disposition registry is built and complete in `packages/emit/src/disposition.ts`, and it delivers E07-S01-T03 outright**, so that task is closed in the same change. **The registry answers one question once per step — how does this task run here? — and that matters because the answer was previously decided in two places:** `step.ts`'s `defaultFidelity` guessed "everything non-script is `stub`" because real-task mode did not exist when it was written, so a registry that disagreed would put a label on a step that is not what the step does. **E07-S01-T03's substance is the double-exec guard:** `CmdLine@2`/`Bash@3`/`PowerShell@2` classify as **`native` even though they have real Node handlers** — running their packages would re-exec a script the emitter has already written natively — and a test proves this holds *even when the package is explicitly marked unavailable*, because an offline download must not turn a verbatim `bash:` step into a stub. `checkout` is matched on `step.origin` rather than the task name, since it arrives as a bare GUID (C-E04-031/032), and `pwsh` vs `powershell` comes from the input, not the reference (C-E04-037). **One default is deliberately the generous one:** a task with *no* package information resolves to `real-task`, not `stub` — at convert time the package may simply not have been fetched yet, and defaulting to `stub` would label a task that will run perfectly well as one that does nothing. Only an explicit `available: false` degrades, and it always carries a warning naming the reason and saying what the step will and will not do (PLAN D10 — nothing degrades silently). Warnings are **de-duplicated by task reference**: a pipeline using the same unavailable task in twenty steps says so once, because a warnings list nobody reads to the end is the same as no warnings list. The summary is counts per disposition — a table, never a percentage (PLAN D10). **Why the registry stays `[!]`:** the "surfaced in the emitter (E05)" half of Do is the same one-line wiring E07-S01-T02 waits on, and both land together — a half-wired emitter labelling some steps from the registry and some from the old guess would be worse than either. 17 new tests; full suite **2,516 Vitest + 259 Bats** green. (evidence: PLAN D4 + §6 fidelity tiers, docs/03 §6 — internal design, so no new `C-` claims; packages/emit/test/disposition.test.ts)
- 2026-09-02 E07-S01-T02 + E07-S03-T01 done — **the wiring both were waiting on, landed together, because doing it piecemeal was the one option that made things worse.** `step.ts` now takes its fidelity label *and* its body from the registry, so the label and the behavior cannot disagree; `defaultFidelity`'s old "everything non-script is `stub`" guess is gone. Three things fell out that were not visible until the two halves met. **(1) The wiring exposed that `checkout` was labelled but not emitted.** The registry classifies it `native`/`exact` — the runtime performs the checkout itself, there is no package to run — but the emitter would still have written a stub body, which is exactly the label/behavior disagreement the registry exists to prevent. It now emits a real `azdo_checkout` call, one flag per input, with any input the runtime has no flag for surfaced as a header note rather than silently dropped. **(2) The emitted dispatch called a function that did not exist.** Wiring to a missing `azdo_run_task` is not wiring, so the runtime half was written: it reads the emitter's heredoc, builds `INPUT_*` **through the very same `azdo__env_name` the variable path uses** — a second implementation would have been a divergence nobody notices until a task reads an input under a name the runtime spelled differently (C-E07-001, C-E06-008) — resolves the cached package, and execs the handler. **(3) A latent bash shadowing bug, caught by its own test:** `azdo__task_handler` writes the caller's variable with `printf -v "$3"`, and a `local target` inside shadowed it, so the value was set and thrown away at return. Internal names are now deliberately unlikely, with a comment saying why. Package lookup sorts with `sort -V`, so `6.10.0` beats `6.9.0` — a lexical sort would run the wrong package and the failure would look like a task bug rather than a cache one. **The golden READMEs changed and that is the point:** `checkout` went `stub` → `exact`, real tasks went `stub` → `degraded`, and the gap count fell from 7 to 5. Tests that encoded the old answer were updated to assert the new one rather than relaxed — including a new case that keeps the `stub` ranking path covered now that the mixed fixture no longer produces one on its own. 12 new tests (5 vitest, 7 bats); full suite **2,519 Vitest + 267 Bats** green. (evidence: research/E07-real-task-mode.md C-E07-001..006; packages/emit/test/{step,readme,disposition}.test.ts; packages/runtime/test/core.bats)
- 2026-09-02 E07-S01-T04 done — result and `##vso` capture from a real task. **The finding that made this task small is that a real task needs no new capture path: it is already inside one (C-E07-009).** `azdo_run_step` runs a step's `.sh` with stdout and stderr joined into one FIFO consumed by `azdo_logging_stream | azdo_mask_stream` before the tee (C-E06-029/033/044), and a real-task step *is* that `.sh` — the emitter writes `azdo_run_task` into it. So `setvariable`/`setOutput`/`logissue` from a real task reach the store by exactly the path a script step's do. The obvious implementation would have been a second, parallel capture path; recording the structural claim is what prevents someone building one later. **`exec` is what carries the exit code (C-E07-010):** `azdo_run_task` ends in `exec node …`, so the handler replaces the step subshell and its status becomes the script's, which the result machine already maps (C-E06-036..043) — without `exec` a failing task would report success, and a test drives a task exiting 3 to prove it. **The emission side is now pinned so "the parser inverts it exactly" is checkable (C-E07-007/008), and it surfaced a real upstream asymmetry:** task-lib escapes **properties** with a five-token table (`%`, `\r`, `\n`, `]`, `;`) but the **message** with only three — `]` and `;` are left alone — while the agent decodes *both* halves with one symmetric `CommandStringConvertor.Unescape`. So a task emitting a message containing the literal text `%5D` has it decoded to `]` by the agent even though task-lib's own `unescapedata` would not, and **our runtime follows the agent** because the agent is what actually consumes the stream. A test drives `a%5Db%3Bc%AZP25d` through a real Node task and asserts `a]b;c%d`. Both Done criteria are proven against a real Node task rather than a simulated line: one sets an output variable a later read observes, and one registers a secret and then tries to leak it — the masker turns it into `***`. 4 new bats tests; full suite **2,521 Vitest + 271 Bats** green. (evidence: research/E07-real-task-mode.md C-E07-007..010; packages/runtime/test/core.bats)
- 2026-09-02 E07-S02-T01 done — the stub emitter, with all three config modes. **The warning text is quoted from docs/03 §4 rather than paraphrased**, because that wording was itself a deliberate correction (E12-S02-T03 reworded "no local handler" — transpiler-era vocabulary — since under real-task mode the reason is a missing *package*). The inputs are dumped as **key-sorted JSON**, so a stub's output is diffable between runs rather than ordered by however the model happened to build the map. **A vocabulary mismatch in the task itself is resolved rather than papered over:** the Do field says `skip | fail | prompt` while the config type is `UnknownTaskPolicy = 'stub' | 'fail' | 'prompt'` and docs/03 §4 says "result per config (`skip` default / `fail` / `prompt`)" — the same three behaviours under two vocabularies, with `stub` being the skip-result default. The code and its doc comment say so. **The `prompt` mode has a real hazard the naive implementation walks into:** a pipeline run with no terminal must not block forever, so the emitted script guards on `[[ -t 0 ]]` and a non-interactive run takes the skip path with a line saying it did. `fail` emits `##vso[task.complete result=Failed;]` and exits 1, so a pipeline that depends on the task stops there rather than continuing on a result the task never produced. **Two assertions run the emitted bash rather than matching its source text**, which is the only way to see past shell quoting — the documented warning contains apostrophes around the task reference, and single-quoting splits them in the script while the printed line is intact. 7 new tests; full suite **2,528 Vitest + 271 Bats** green. (evidence: docs/03 §4, PLAN D4; packages/emit/test/step.test.ts)
