# Backlog changelog (append-only)

2026-08-12 E02-S05-T01 in-progress — claimed by Codex on e02-s04-t01-expr-contexts (evidence: pending).
2026-08-12 E02-S05-T01 done — AST-to-Bash compiler with runtime variable access, predicates, logical/comparison forms, helper-backed strings, shell-safe quoting, and typed fallback errors (evidence: research/E02-expressions.md C-E02-128..131; packages/engine/test/expr/compile-bash.test.ts; 660 Vitest tests; engine typecheck/build; shellcheck helper pass).

2026-08-12 E02-S04-T02 in-progress — claimed by Codex /root on e02-s04-t01-expr-contexts (evidence: pending).
2026-08-12 E02-S04-T02 done — dependency context builders for `dependencies` and `stageDependencies`, with grounded result/output shapes and a redacted real-run transcript (evidence: research/E02-expressions.md C-E02-092..095; research/experiments/E02-dependencies/real-run.md; packages/engine/test/expr/dependencies.test.ts; engine 641 tests green).
2026-08-12 E02-S04-T03 in-progress — claimed by Codex on e02-s04-t03-resources (evidence: pending).
2026-08-12 E02-S04-T03 done — runtime `resources.pipeline.<alias>` metadata context builder with documented fields, case-insensitive aliases/fields, and conditional `projectName` omission (evidence: research/E02-expressions.md C-E02-111/112; packages/engine/test/expr/context.test.ts; engine typecheck and focused tests green).
2026-08-12 E02-S04-T03 **corrected and re-done** — the entry above shipped the task's *doc-only* reading and the required experiment refutes it: **there is no `resources.pipeline` context.** The task's Ground field asks for "sample metadata captured from a real run via REST", and running it (two real runs, `pnpm expr-resources-realrun`, transcript `research/experiments/E02-resources/real-run.md`) read the same metadata three ways inside a run that demonstrably carried it — `resources.pipeline.probe.runID` → **empty**, `variables['resources.pipeline.probe.runID']` → `531`, `$(resources.pipeline.probe.runID)` → `531` — with `convertToJson(resources)` dumping exactly `{"repositories": …, "containers": {}}` and `convertToJson(resources.pipeline)` → `null` (C-E02-120/121). Learn's own sentence agrees once read closely: the twelve fields are "predefined **variables**", and the path spelling is what misleads. The two resource families are mirror images — repository/container metadata is context-only and **absent** from `variables[…]`, which the same probe measured (C-E02-125). So `packages/engine/src/expr/resources.ts` now ships two builders instead of one: `resourcesContext()` (repositories + containers, keys folding case, misses null-propagating rather than raising the way `parameters` does, `type` passed through verbatim because `self` says `Git` while a declared repo echoes its YAML `git`) and `pipelineResourceVariables()` (flat dotted entries from the lockfile pin, returned as their own labelled map so the **compile-time** variables table cannot receive runtime-only names). Two traps encoded with claims: `projectName` is modelled as **key absence** — through an expression absence and empty are indistinguishable, and only the emitted environment can tell them apart, where `printenv` showed 11 of 12 names (C-E02-122); and `resourceVariableEnvName` preserves **hyphens** (`RESOURCES_PIPELINE_OTHER-PROJECT-PIPELINE_PROJECTNAME`), which a blanket non-alphanumeric replacement would destroy (C-E02-127). Consequence for E03/E04: a job or stage `condition:` cannot name `resources` (C-E02-082) but *can* read this metadata via `variables[…]` — verified by a live job whose condition did exactly that, against a false-comparison control that stayed skipped. Also: the transcript scrubber now removes the run requester's **display name**, which `redact` (PAT + org only) does not know about and which `requestedFor` puts in both the REST payload and the environment. Docs corrected per rule 5: docs/02 §6 Contexts (the list had `resources.pipeline.<alias>.*` as a context), docs/05 §4 (lockfile `pipelines` entry widened from 3 fields to all 12), docs/06 §5 decision 20. C-E02-111/112 are marked superseded, not deleted; the earlier `pipelineResourcesContext` and its two tests are removed (evidence: research/E02-expressions.md C-E02-120..127; research/experiments/E02-resources/real-run.md, runs 537/538; packages/engine/test/expr/resources.test.ts 15 tests; suite 836 vitest + 10 bats green, build/typecheck/lint/shellcheck clean).

Format: `YYYY-MM-DD E##-S##-T## <done|blocked|dropped|in-progress> — <one-line summary> (evidence: <research paths / tests>)`

2026-08-18 E03-S01-T03 in-progress — claimed by Codex /root on main (/root/src/azdo-pipeline-emulator) (evidence: pending)
2026-08-18 E03-S01-T03 blocked — offline grounding established sequence/mapping iteration, `pair.key`/`pair.value`, `jobList` wrapping, and template `object` list inputs, but the mandatory ≥8 oracle fixtures cannot run because all four `AZDO_*` variables and `.env.oracle` are absent; no implementation written (evidence: research/E03-template-engine.md C-E03-140..143; research/REFERENCES.md; research/oracle-setup.md)
2026-08-18 E03-S01-T03 in-progress — resumed by Codex /root on main (/root/src/azdo-pipeline-emulator) after `.env.oracle` restored (evidence: pending)
2026-08-19 E06-S01-T02 in-progress — claimed by Orchestrator → assigned to Codex on main (/root/src/azdo-pipeline-emulator) (evidence: pending)
2026-08-19 E06-S01-T02 blocked — hosted run 540 refutes the required `env:`-overlay-wins behavior; public-variable injection overwrote the explicit mapping, and transformed-name collisions have no stable source-defined precedence, so no runtime implementation was written (evidence: research/E06-runtime.md C-E06-007..012; research/experiments/E06-env-materialization/real-run.md; docs/04 §5; docs/06 §5 decision 26)
2026-08-19 E06-S01-T05 in-progress — claimed by Codex leaf coder on codex/e06-s01-t03 (/tmp/wt-leaf-coder) (evidence: pending)
2026-08-19 E06-S01-T02 dropped — superseded by corrected public-over-explicit contract E06-S01-T05 after hosted run 540 refuted env-overlay-wins (evidence: backlog/E06-runtime.md; research/E06-runtime.md C-E06-007..012; research/experiments/E06-env-materialization/real-run.md)
2026-08-19 E06-S01-T05 done — implemented safe step-environment materialization with public-over-explicit precedence, secret exclusion/explicit mapping, dot/space name conversion, newest-first PATH, and no invented transformed-public collision winner (evidence: research/E06-runtime.md C-E06-007..012; research/experiments/E06-env-materialization/real-run.md; packages/runtime/test/core.bats)

- 2026-07-30 — Planning complete: PLAN.md + docs/01–06, BACKLOG.md + backlog/E00–E15 (160 tasks), research/REFERENCES.md seeded, CLAUDE.md + skills created. No implementation tasks started yet. Next: E00-S01-T01.
- 2026-07-30 E00-S01-T01 done — pnpm monorepo scaffolded: cli/engine/fetch/emit (TS strict, tsup, vitest) + runtime (bash, bats 1.13, shellcheck 0.11) + fixtures/; build/test/lint/typecheck green; TS pinned to 5.x (TS 7 breaks tsup dts + typescript-eslint peers) (evidence: research/E00-foundations.md C-E00-001/002; tests packages/*/test/index.test.ts, packages/runtime/test/core.bats)
- 2026-07-30 E00-S01-T02 done — GitHub Actions CI: lint+typecheck+build+vitest+bats matrix (ubuntu/macos × node 22/24 per C-E00-002), junit reports uploaded as artifacts, oracle-nightly workflow created disabled (cron commented + ORACLE_ENABLED var guard; E12-S03 enables); engines >=22 added to all packages (evidence: research/E00-foundations.md C-E00-003..005; CI run on PR #1)
- 2026-07-30 E00-S01-T03 in-progress[!] — Grounding enforcement artifacts: PR template with §3 checklist, research/README.md claim-format doc, scripts/check-verify-markers.sh (VERIFY guard; pre-commit hook via core.hooksPath .githooks + CI step; staged/-all modes; blocks tested). Remaining: observe template render on first post-merge PR (evidence: research/E00-foundations.md T03 note — Ground N/A per task)
- 2026-07-30 E00-S02-T01 done — service-schema.json vendored from microsoft/azure-pipelines-vscode@2f4500cf (1.64MB, sha256 f00a9630…, draft-07) into packages/engine/vendor/schema/ + PROVENANCE.json; scripts/refresh-schema.ts idempotent; compiles with ajv 8.20 (strict:false for 5 custom keywords, unicodeRegExp:false — schema regexes invalid under /u) (evidence: research/E00-foundations.md C-E00-006..010; test packages/engine/test/schema-vendor.test.ts)
- 2026-07-30 E00-S02-T02 done — REFERENCES.md fully verified: 33/34 doc URLs 200 (auth landing moved → authentication-guidance + entra-oauth, GUID 499b84ac confirmed C-E00-011); 73 yaml-schema subpages verified + indexed; all GitHub rows pinned to same-day HEADs with paths confirmed. **Correction (rule 5):** expressions/templating engine sources are NOT in azure-pipelines-agent (closed NuGet; ExpressionManager.cs only) — open reference is actions/runner src/Sdk DTExpressions2/DTObjectTemplating (C-E00-012/013); fixed PLAN §9, docs/02, E02/E03 grounding sets, decisions record #6 (evidence: research/E00-foundations.md C-E00-011..013)
- 2026-07-30 E00-S02-T03 done — scripts/refresh-tasks-meta.ts vendors task.json snapshots from microsoft/azure-pipelines-tasks at release tag v277 (= 8ba25cfb) into packages/emit/vendor/tasks-meta/<Name>@<major>/ with per-task PROVENANCE.json; CmdLine@2 2.276.0, Bash@3 3.274.1, PowerShell@2 2.276.1, CopyFiles@2 2.276.0 committed; idempotent rerun verified; adding a task = one TASKS entry (evidence: research/E00-foundations.md C-E00-014..016; test packages/emit/test/tasks-meta-vendor.test.ts)
- 2026-07-30 E00-S03-T01 blocked[!] — Oracle test-org runbook research/oracle-setup.md written + grounded: preview endpoint POST …/_apis/pipelines/{id}/preview?api-version=7.1, body previewRun+yamlOverride, response PreviewRun.finalYaml, scope vso.build="Build (read)" (read-only suffices per docs; T02 verifies live), PAT Basic-auth empty-username + 84-char/AZDO@76-80 format for redaction tooling; remaining: user creates org/project/pipeline/PAT + stores secrets (browser-only) then flips [x] (evidence: research/E00-foundations.md C-E00-017..021)
- 2026-07-30 E00-S03-T02 blocked[!] — oracle spike needs the T01 test org (AZDO_* env vars absent); skipped per §1.3, unblocks when the oracle-setup runbook is followed
- 2026-07-30 E01-S01-T01 done — packages/engine/src/frontend/parse.ts: CST-backed parse (yaml@2.9.0 exact-pinned) → string-keyed DOM (scalar/mapping/sequence) with per-node {file, range{line,col,endLine,endCol}, offset} provenance via LineCounter; expressions stay inert plain strings; lib defaults pass through DUPLICATE_KEY/MULTIPLE_DOCS (semantics owned by T02) + structural ALIAS_UNSUPPORTED/NON_SCALAR_KEY; snippetOf() round-trips exact source text (evidence: research/E01-yaml-frontend.md C-E01-001..006; tests packages/engine/test/frontend/parse.test.ts 15 cases)
- 2026-07-30 E01-S01-T02 blocked[!] — server-quirk conformance needs one oracle experiment per quirk (preview API); test org absent. T01 left lib defaults + structural errors + ParseResult.doc CST access ready for it
- 2026-07-30 E01-S01-T03 done — packages/engine/src/frontend/diagnostics.ts: Diagnostic {severity,code,message,file,range,jsonPath,hint} + terminal renderer (code frame, caret span, ANSI opt-in) and --json renderer; location prefix mirrors the service's exact "<file> (Line: N, Col: M): <msg>" style (format string pinned in actions/runner TemplateStrings.g.cs + real Q&A paste); parseErrorToDiagnostic adapter wires E01-S01-T01 errors in (evidence: research/E01-yaml-frontend.md C-E01-007/008; snapshot tests packages/engine/test/frontend/diagnostics.test.ts, 8 snapshots)
- 2026-07-30 — Architecture update (user decision): **D11 isolated sandbox execution** — the whole local run executes inside one long-lived container by default when docker/podman is present (`--host` opt-out; same dependency-free scripts in both environments, D2 intact; image approximates `vmImage`; `doctor --sandbox`; docker-socket passthrough opt-in). Updated: PLAN §2/§3/§5(D11)/§7/§8 · docs/04 §1/§2/§9 · docs/06 §1/§2/§4/§5(decision 6) · BACKLOG §4+§5 · backlog/E14 new story E14-S04 (3 tasks; T01/T02 scheduled at P2 tail).
- 2026-07-31 E00-S03-T02 done — oracle spike: `packages/fetch/src/oracle.ts` (typed preview client — `previewUrl`/`preview`/`configFromEnv`/`redact`) + `scripts/oracle-probe.ts` (`pnpm oracle-probe`) producing 6 redacted transcripts under `research/experiments/oracle-spike/`. Done met: `five-line.md` = committed request/response pair; `README.md` = failure-mode catalogue. **Three live findings contradict what the runbook predicted** (corrected per rule 5 in research/oracle-setup.md + docs/02 §8): invalid PAT → **302** to a sign-in page, not 401/403 (a redirect-following client sees a "200" of HTML → client uses `redirect: 'manual'`); unknown pipelineId → **500** `PipelineNotFoundException`, not 404 (so 5xx here is not reliably transient); **empty `yamlOverride` → 200 expanding the pipeline's committed YAML**, silently producing a fixture for the wrong pipeline (client now refuses it). Also confirmed the 400 error envelope and that its positional prefix `<file> (Line: N, Col: M):` is exactly what E01-S01-T03 renders (C-E01-007/008), while missing-template/unknown-task rejections carry no position at all (evidence: research/E00-foundations.md C-E00-022..027; research/experiments/oracle-spike/; tests packages/fetch/test/oracle.test.ts 19 tests)
- 2026-07-31 E00-S01-T03 done — PR template reached the default branch: `origin/main` had only the planning commit while 13 commits sat unpushed, so PR #1 (7 commits incl. `a4b7767`) was merged and PR #2 opened for the remaining 6 (CI green: ubuntu/macos × node 22/24). Criterion verified via GitHub GraphQL `repository.pullRequestTemplates` → `pull_request_template.md` (1530 chars) = GitHub-side registration, the mechanism behind web-form pre-fill; dummy claim entry at research/README.md:17 (`[C-E06-007]`); `check-verify-markers.sh --all` exits 0. Caveat recorded: visual auto-fill not observed (API-created PRs bypass templates; anonymous compare page omits the form) (evidence: research/E00-foundations.md §E00-S01-T03 resolution)
- 2026-07-31 E00-S03-T01 in-progress[!] — **oracle live**: owner supplied org + PAT; provisioned dedicated private project `oracle` (Basic process) + repo + anchor `azure-pipelines.yml` + pipeline `oracle-anchor` `definitionId=19` via REST; Step-5 preview curl → HTTP 200 + `finalYaml` (`steps:`-only probe expanded to `stages: __default`/`job: Job`/`CmdLine@2`), confirming C-E00-017/018 live and PAT scope sufficiency (C-E00-019). `.env.oracle` written after `git check-ignore` verification. **Deviations recorded** (research/oracle-setup.md): personal org reused instead of a throwaway (isolation moved to project level), PAT scope wider than Build (read), **PAT exposed in cleartext chat → rotation outstanding**; GitHub repo secrets deferred to rotation. Unblocks E00-S03-T02, E01-S01-T02, E01-S02-T03, E12-S01-T02 (evidence: research/oracle-setup.md completion record + deviations)
- 2026-07-30 E01-S02-T01 done — packages/engine/src/frontend/{schema,validate}.ts: schema loader (+`DOCUMENTED_CORRECTIONS`) and a guided walk over the vendored schema producing targeted Diagnostics (discriminator-first branch selection, did-you-mean, file:line:col + jsonPath). **Doc/schema cross-check found 4 divergences** (rule 5 → docs/01 §1 + docs/06 §5 decision 8): docs document `target` on task steps but the schema forbids it (corrected); `job` is "required as first property" but the schema doesn't require it (enforced); `pool` object is schema-permissive (left); pipeline values are strings so YAML booleans/numbers/nulls and `${{ }}`/`$( )`/`$[ ]` must be accepted. Stock ajv emits 1265 errors for one mistyped key and rejects valid pipelines → ajv stays only as the vendor-integrity check. Unknown task/inputs = warning (org schema E01-S02-T03). Oracle-pending: Q1 discriminator ordering (warning), Q2 unknown inputs (evidence: research/E01-yaml-frontend.md C-E01-009..020; tests packages/engine/test/frontend/validate.test.ts 34 tests, 15 invalid + 5 valid fixtures under fixtures/schema/)
- 2026-08-11 E01-S01-T02 done — server-quirk conformance: `packages/engine/src/frontend/quirks.ts` (single module, `SERVER_QUIRKS` table + `ANCHOR_UNSUPPORTED`/`DUPLICATE_KEY`/`MULTIPLE_DOCUMENTS`), wired into `parse.ts` (`uniqueKeys:false` so our duplicate check owns the message; lib `MULTIPLE_DOCS` superseded; the T01 placeholder `ALIAS_UNSUPPORTED` removed). Grounded by 11 live preview transcripts, each quirk paired with a control (`pnpm oracle-quirks` → `research/experiments/E01-quirks/`). **Three findings vs the design docs** (corrected per rule 5 in docs/01 §1 + docs/06 §5 decision 9): a leading `---` and a trailing `...` are **accepted** (200) — only a *second* document is rejected, so "`---` separators rejected" would have caused false rejections; anchors are rejected at the **definition**, not at alias use (`&x` never aliased fails identically, and `<<: *x` yields the same anchor message — no separate merge-key path); duplicate keys are positioned at the **second** occurrence with `'<key>' is already defined`, and the check **folds case at the mapping layer** — `displayName`/`displayname` collide and so do `a`/`A` under `variables:` (user data the schema knows nothing about), so our schema-unaware parse-stage check compares lower-cased keys (C-E01-028). Only the duplicate-key rejection is positional service-side; ours carry ranges for all three (deliberate superset). Anchor positions come from the CST because a node's range starts after its anchor (C-E01-027) (evidence: research/E01-yaml-frontend.md C-E01-021..028; research/experiments/E01-quirks/; tests packages/engine/test/frontend/quirks.test.ts 15 tests, suite 96 green)
- 2026-08-11 E01-S02-T03 done — org-schema injection: `packages/engine/src/frontend/org-schema.ts` (`resolvePipelineSchema` → `{schema, schemaSource, problems}`, `checkOrgSchema` dialect+keyword gate, `parseOrgSchema`, `taskNames`), `applyDocumentedCorrections` extracted from `schema.ts`, `schemaSource` threaded into `validate.ts` so the unknown-task hint stays truthful; `scripts/org-schema.ts` (`pnpm org-schema`) pins the live response at `research/experiments/E01-orgschema/`. **The headline finding is that the org response is the same generator's document as the vendored snapshot** — draft-07, same `$id`, all four VS Code keywords, identical 119 definitions, one non-task `$ref` divergence that resolves to the same `{type:string}` — so injection is a **wholesale swap, not a merge** (a merge would have been required had the org served plain draft-07, and would have silently dropped `firstProperty`/`ignoreCase`). Corollary found the same way: the org document omits `target` on task steps exactly as the vendored file does, i.e. `DOCUMENTED_CORRECTIONS` patch the *generator*, so they must be applied to injected documents too or authenticating would start rejecting documented-valid YAML (C-E01-037). Live-verified both halves of the Done criteria: the swap test runs all 20 fixtures under both schemas with diagnostic-for-diagnostic equality (the sole difference in the corpus is the deliberately source-dependent unknown-task hint), and marketplace validation is real rather than synthetic — `qetza.replacetokens` is the only non-builtIn extension in the oracle org, so `replacetokens@7` + its 25 inputs validate under the org schema and are merely "unknown task" under the vendored one. Also measured, for E08-S03-T07's cache: no service-side version to bust on (the extension says so; `$comment` is not a freshness signal — live org `v1.183.0` vs vendored `v1.261.1` while carrying the *newer* tasks), the response is **not byte-stable** (task alternatives reorder between calls), and the documented scope is `vso.agentpools`, wider than the oracle's `vso.build`. `validateTaskNames=false` turns out to append exactly one catch-all alternative (`task: string`, `inputs: additionalProperties:true`) — the service's own offline-tools mode. Docs updated per rule 5 (elaboration, no design error): docs/01 §1, docs/05 §2+§4, docs/06 §5 decision 10 (evidence: research/E01-yaml-frontend.md C-E01-029..037; research/experiments/E01-orgschema/; tests packages/engine/test/frontend/org-schema.test.ts 43 tests, suite 142 green)
- 2026-08-11 E13-S01-T01 done — CLI framework & command scaffold: `packages/cli/src/{exit,program,bin}.ts` — `EXIT` 0/1/2/3 + `CliError`/`NotImplementedError`, all six docs/06 §1 commands registered with help text (`auth login|status`, `convert`, `doctor`, `fetch-artifacts`, `preview-diff`, `run`), global `--json`, `azdo-emu` bin. Library choice grounded, not preferred: clipanion's current release is a pre-release (`4.0.0-rc.4`) so **commander 15.0.0** exact-pinned (C-E13-001). Three library behaviours shaped the design, all pinned to `tj/commander.js@ba6d13dd`: `exitOverride()` does **not** stop commander from exiting — `_exit()` calls the callback and then `process.exit()` anyway, so the callback must throw (C-E13-003); `--help`/`--version` arrive at our handler as `CommanderError` with **exitCode 0**, so the policy maps by `exitCode` and never by "an error was thrown" (C-E13-005); and help output is environment-dependent twice over — width is `process.stdout.columns` only on a TTY (else 80) and colour is forced on by `FORCE_COLOR`/`CLICOLOR_FORCE` **even for a non-TTY stream** — so both are pinned explicitly in the test Io or the snapshots would pass locally and fail in CI (C-E13-006). `run()` returns a code and never touches process state, so all 26 tests run in-process with no spawning: 9 `--help` snapshots, the exit-code policy, and the usage-error matrix (unknown flag + did-you-mean, unknown command, missing option, missing argument, invalid `--mode` choice). Empirically found while testing: commander reports a missing required **option** before a missing required argument, so that ordering is asserted rather than assumed. **Two decisions recorded** (docs/06 §1 + §5 #11): CLI usage errors reuse exit 1 rather than adding a code outside the documented set — which is also commander's default, so nothing needs translating (C-E13-007); and the Node floor rises to ≥22.12 **for `packages/cli` alone**, since commander 15 declares it and C-E00-002 requires a decision entry for any floor raise (C-E13-002) `--version` reads `packages/cli/package.json` rather than an env var — a version stamped from the environment would print whatever the invoking shell set, the same leak C-E13-006 pins away for width/colour; asserted with a hostile `AZDO_EMU_VERSION` set (evidence: research/E13-cli-config-doctor.md C-E13-001..007; tests packages/cli/test/program.test.ts 27 tests, suite 168 green)
- 2026-08-11 E13-S01-T02 done — config loader & precedence: `packages/cli/src/config/` — `types.ts` (all-optional `AzdoEmuConfig` vs total `ResolvedSettings` + `DEFAULTS`), `load.ts` (discovery beside the pipeline, plain-`yaml` parse, hand-written validation reporting `file:line:col` + did-you-mean, unknown keys are errors), `parameters.ts` (`--parameter name=value` repeatable, `@file.json` complex values, `@@` literal escape), `resolve.ts` (CLI > config > defaults, recording which layer answered each key). Machine-readable schema published at `schema/azdo-emu.schema.json` (draft-07 — the dialect this repo already vendors; `yaml-language-server` accepts draft-04/07/2019-09/2020-12 so it costs no editor compatibility, C-E13-011). **Two rules docs/06 §2 never stated, now decided and tested**: map-valued keys (`parameters`, `repositories`, `tasks.overrides`) merge **per key** while scalars/lists replace — `--parameter` being documented as repeatable only makes sense that way (C-E13-012); and paths typed on the command line resolve from the cwd while paths written in the config resolve from the config file's directory, so a config stays valid however it is invoked (C-E13-013). **Scope held deliberately**: the loader never sees the pipeline, so `--parameter n=42` stays the string `"42"` — coercion against declared parameter types belongs to the binder, and two questions found while grounding are handed to it with evidence rather than guessed here (how the service coerces queue-time strings into `boolean`/`number`/`object`, and the docs' "if you don't assign a default value… the first available value is used" versus docs/01's "missing required parameter = convert error"). Also grounded: there are **13** runtime parameter types, one more than docs/02 lists — `stringList` — but docs/02's sentence is scoped to *template* parameters and `stringList` is explicitly "not available in templates", so it is correct as written and no rule-5 correction is due (C-E13-009). 90 new tests incl. a literal precedence matrix (14 scalar keys × default/config/CLI), the falsy-value trap (`--group-names false`, `--min-coverage 0` still beat the config), and a drift guard asserting the committed JSON schema and the loader accept and reject the same documents and agree on every default. `--parameter` registered on `convert` (E13-S02-T01's flag list excludes it), so a malformed value fails before any work starts. Docs updated: docs/06 §2 (evidence: research/E13-cli-config-doctor.md C-E13-008..013; tests packages/cli/test/config.test.ts + config-schema.test.ts, suite 250 green)
- 2026-08-11 E12-S01-T01 done — test layout & runners: root `vitest.config.ts` now declares five named projects (`cli`/`engine`/`emit`/`fetch` over the unchanged `packages/*/test/**/*.test.ts` layout, plus `repo` for repo-level meta-tests) and v8 coverage with per-package glob thresholds; bats harness `packages/runtime/test/helpers/fixture-store.bash` + `fixtures/runtime/` (copy-on-use into `BATS_TEST_TMPDIR`), 9 cases covering every helper; `pnpm test` = vitest **with coverage** + bats, CI likewise (coverage uploaded next to the junit reports), `lint:shell` extended to `test/helpers/*.bash`, `tsconfig.meta.json` added so the config and meta-tests are typechecked at all. **Thresholds are a ratchet seeded from measurement** — measured first with no thresholds set (cli 94.44/85.47/91.84/96.86 st/br/fn/ln, engine 91.97/82.38/96.40/96.02, emit 100/–/–/100, fetch 97.87/78.95/100/97.78), then written just under, because aspirational numbers would have failed the Done criterion on the first CI run. **Three tool behaviours measured rather than assumed**: a coverage threshold glob that matches **nothing passes silently** (exit 0, no diagnostic) while a breached one exits 1 naming the glob — so a renamed package silently stops being gated, which `test/test-layout.test.ts` now guards (mutation-tested by renaming `packages/fetch/src/**`); `projects: ['packages/*']` does run (a package without test files is not an error) but enrols `packages/runtime` — a bats-only package — as a project matching nothing and names projects after package.json, hence the explicit list; and bats really does delete all three of `BATS_TEST_TMPDIR`/`BATS_FILE_TMPDIR`/`BATS_SUITE_TMPDIR` (undocumented — probed on 1.13.0), so the fixture store uses them instead of `mktemp -d`. Also grounded: vitest coverage is root-only with glob keys; the top-level thresholds are a floor over *all* files including glob-matched ones, but each threshold set is aggregated and checked **independently**, so a per-package number below the repo floor is a second, narrower gate rather than a weakening (which is why `fetch` keeps branches 76 against a repo floor of 78 — headroom on the package E08 will grow); `coverage.include` is required or untested files are invisible, and `@vitest/coverage-v8` peers on vitest **exactly** (root `vitest` pinned exact accordingly). docs/06 §3 gained the layout + gating paragraphs (elaboration, no design error found) (evidence: research/E12-testing-parity.md C-E12-001..010; research/experiments/E12-test-harness/; tests test/test-layout.test.ts 10 tests + packages/runtime/test/fixture-store.bats 9 tests, suite 262 vitest + 10 bats green)
- 2026-08-11 E12-S01-T02 done — fixture corpus v1: 10 entries under `fixtures/corpus/<entry>/` (`pipeline.yml` + `templates/*.yml` + a README stating what it exercises) paired with 10 service expansions under `fixtures/oracle/` (`<entry>.final.yml`, redacted) + `MANIFEST.json` binding each pair to the SHA-256 of the input it was fetched for. Harness: `scripts/corpus.ts` (offline catalogue, consumed by tests), `scripts/corpus-oracle.ts` (`pnpm corpus-oracle`: mirror templates → preview → store, non-zero on any rejection), `scripts/azdo-repo.ts` (push/read via the Git REST API), `scripts/oracle-provision.ts`. The epic's rule — a corpus entry without its oracle pair is invalid — is *enforced*: `test/corpus.test.ts` (36 tests) fails when a pair is missing, when a fixture's hash no longer matches the input the service answered for, or when a golden is hand-edited. **The layout was dictated by a probe, not chosen**: a `yamlOverride` resolves `template:` references as though it were the pipeline definition's own file, reading them from the **repository** (the service says so verbatim — "/azure-pipelines.yml: File /steps.yml not found in repository … branch refs/heads/main version …"), so `fixtures/corpus/` mirrors the oracle repo's `/corpus/` directory and root references are spelled root-absolute while in-template references are bare siblings (C-E12-011/012 — evidence E03-S02-T01 inherits). **Five findings that change downstream work**: `strategy: matrix`/`parallel` is **not expanded** by the service, so no golden can ever prove job multiplication (C-E12-018 — that is E04's, verified at L6); `- group:` survives expansion with **no values inlined**, independently confirming D5 (C-E12-016); the step shortcuts desugar to task **GUIDs** while the same task written by name keeps its name, and of the three GUIDs only `publish`'s resolves in the task catalogue — `checkout` (`6d15af64…`) and `download` (`30f35852…`) are 404 "No task definition found", i.e. agent-internal, so E09's registry needs a hard-coded map and E03-S05-T01's normalizer must canonicalize both spellings or `preview-diff` shows permanent false drift (C-E12-019); `checkout: none` expands to the checkout task with `condition: false` while a job with no checkout step gets no task at all, so the implicit checkout is agent-side and must be synthesized (C-E12-020); and expansions are **byte-stable** across calls, unlike the org schema response (C-E12-022), so nightly diffs need no normalization to be quiet. Two corpus shapes turned out to need org objects rather than YAML — `environment:` and `- group:` are validated at load time and fail until the object exists **and** the pipeline is authorized (C-E12-015/017; the group error names no group at all) — provisioned with owner consent by `scripts/oracle-provision.ts` (two empty environments, one group with two non-secret dummy values); recorded in the runbook's cleanup list. Two more verdicts came from reading the goldens rather than assuming them: `readonly: true` is **not enforced at expansion time** (the redefining stage variable and the readonly pipeline variable both survive — C-E12-023), and a compile-time `${{ variables.x }}` read inside a job resolves to the **job-level** value, not the pipeline-level one (C-E12-024) — the first cell of E03-S03-T01's visibility matrix, noted in that task. Docs updated (elaboration, no design error): docs/06 §3 corpus mechanics + the two oracle limits (evidence: research/E12-testing-parity.md C-E12-011..024; research/experiments/E12-corpus/; fixtures/oracle/; tests test/corpus.test.ts 36 tests, suite 298 vitest + 10 bats green)
- 2026-08-11 E00-S03-T01 done — test-org runbook closed: the owner rotated the PAT (the original had been transmitted through chat and was treated as compromised), wrote the replacement directly into `.env.oracle`, stored all four oracle values as GitHub repo **secrets**, and revoked the old token — which was the single outstanding Done item ("secrets stored", deferred since 2026-07-31). Verified live against the new token rather than assumed: a full `pnpm corpus-oracle` run returned all ten corpus expansions byte-identical (Build (read) + Code (read) exercised; **Code (write) not yet** — nothing had changed, so no push was attempted, and the first corpus fixture edit will prove it). `ORACLE_ENABLED` deliberately still unset; the nightly workflow no-ops until E12-S03. Deviations 1 and 2 stand and are re-stated rather than quietly dropped: the oracle runs in the owner's personal org (isolation at project level, so redaction stays load-bearing) and the PAT is wider than Build (read) — scope minimality was not re-audited at rotation. **E00 now has no blocked tasks.** (evidence: research/oracle-setup.md completion record + deviation 3 closed)
- 2026-08-11 E03-S05-T01 done — normalizer: `packages/engine/src/normalize/normalize.ts` (`normalizeExpandedYaml` → `{value, text, applied, errors}`; `value` is the path-addressable canonical structure E03-S05-T02's semantic diff needs, `text` the stable serialization, `applied` the rule ids a document exercised). Eight rules N1–N8, each citing a claim and the corpus sample that motivated it; catalogue + claims in `research/E03-normalizer.md`, evidence in `research/experiments/E03-normalizer/survey.md` (`pnpm normalizer-survey`, 19 live preview calls). **The canonical target was measured rather than chosen**: re-submitting each committed `finalYaml` as `yamlOverride` returns it byte-for-byte — a **10/10 fixpoint modulo exactly one rewrite** (C-E03-001). That one is `trigger:/pr: none` → `{enabled: false}`, which the service emits and then **refuses to read back** ("Unexpected value 'enabled'") — nine of ten goldens are rejected for that and nothing else, while entry 10 (real trigger filters) round-trips untouched (C-E03-002); so `preview-diff` must never feed a `finalYaml` back expecting acceptance. Also grounded: scalar `container:`/`services.<k>` promote to `{alias}` on expansion, sibling of C-E12-017's `environment:` → `{name}` (C-E03-003). **Scope boundary held deliberately and documented**: the normalizer does *not* expand — no `stages: __default` wrapping, no shortcut→task desugaring — because folding expansion in here would let a broken expander pass the parity diff, which is the one failure mode the layer exists to prevent. N6 unifies exactly one GUID/name pair (`ecdc45f6…` = `PublishPipelineArtifact`, resolved live); the `checkout`/`download` GUIDs are deliberately absent since they 404 in the task catalogue and have no name spelling, and `download:`'s task is *not* `DownloadPipelineArtifact@2`. 49 tests, incl. idempotence over every committed golden — not a formality: it caught the first implementation applying the list-promotion rules to list *items* too, so `dependsOn: [a]` gained a nesting level on every pass. `yaml` added to root devDependencies (exact 2.9.0, same pin as engine/cli) for the survey script (evidence: research/E03-normalizer.md C-E03-001..003; research/experiments/E03-normalizer/; tests packages/engine/test/normalize/normalize.test.ts 49 tests, suite 347 vitest + 10 bats green)
- 2026-08-11 E02-S01-T01 done — expression tokenizer + parser: `packages/engine/src/expr/{lexer,parser}.ts` (`tokenize` → tokens with expression-relative spans; `parseExpression` → one AST for both E02 backends, plus `print`/`walk`/`liftSpan` and an injectable `ExprRegistry`). **The task's own grounding set turned out to be the wrong dialect**, which changed the design: `actions/runner`'s `DTExpressions2` is the *GitHub Actions* engine — infix operators, ordinal `true`/`false`/`null`, Actions' function set — so it was used for shape only (token kinds, legality-by-previous-token, the `''` escape loop) and every accept/reject rule was decided by **74 live preview probes** (`pnpm expr-grammar-survey` → `research/experiments/E02-grammar/survey.md`). Headline: **Azure Pipelines has no operators at all** — `==`, `!=`, `<`, `>`, `&&`, `||`, `!` and even parenthesised grouping `(true)` are rejected while `eq(1, 1)` is accepted in the same position (C-E02-001), so the parser is `primary postfix*` with no precedence climbing and mirroring the fork would have accepted a grammar the service rejects. Six more divergences from fork or docs, each measured: booleans are case-**in**sensitive (`True`/`TRUE`, C-E02-002); `null`, `NaN` and `Infinity` are **not literals** — they fail with the *same* message an unknown context gets, i.e. they are unresolvable names, so a syntax-only parse accepts them and only name resolution rejects them (C-E02-003/004); `+1`, `1e3`, `0x1F`, `1..2` are rejected while `.5` and `1.` are fine (C-E02-004); **a Version is 3–4 segments, so `1.2` is a Number** — settled by `gt(1.10, 1.9)` → False (numeric ordering) against the control `gt(1.10.0, 1.9.0)` → True (**docs correction, rule 5**: docs/02 §6 said 2–4, and a note was added to E02-S02-T01 rather than rewording it, C-E02-005); function *and* context names are case-insensitive and resolved with their arity **at parse time**, `nosuchfunc(1)` failing at the name and `eq(1)`/`eq(1, 2, 3)` at the `)`/`,` that breaks the count (C-E02-011/012/013a); and the depth ceiling is exactly 50 **counting function arguments only** — 49 nested `not(…)` parses, 50 does not, while 60-link property and index chains are accepted, so the obvious reading of the fork (where `Index` is itself a `Function`) would have rejected pipelines the service runs (C-E02-014). Also grounded: `$[ ]` runtime expressions are parsed at queue time by the same grammar though not evaluated (C-E02-015), and the service's two error kinds separate cleanly across all rows — `Unrecognized value` for value-shaped text that resolves to nothing, `Unexpected symbol` for punctuation illegal in position — with **1-based** positions the tests assert row by row. Spans are expression-relative with `liftSpan` as the single seam to file coordinates (E02-S01-T02 hangs its caret off this). The fork's unverified `MaxLength = 21000` is deliberately **not** implemented. 68-case table (accept + reject + positions) plus two whole-table invariants — every node's span slices back out and re-parses to the same subtree; print → re-parse round-trips — the second of which caught the `''` escape being consumed but not re-emitted. Docs updated: docs/02 §6, docs/06 §5 decision 12 (evidence: research/E02-expressions.md C-E02-001..016; research/experiments/E02-grammar/; tests packages/engine/test/expr/parser.test.ts 85 tests, suite 435 vitest + 10 bats green)
- 2026-08-12 E02-S01-T02 done — server-style parse errors: `packages/engine/src/expr/errors.ts` (`serviceMessageBody` — the service's sentence byte-for-byte; `expressionDiagnostic` → E01's `Diagnostic` with a real file range; `renderExprCaret`/`renderExprError`; `trimExpressionText`, `truncateServiceMessage`). Grounded by **66** live rejections (`pnpm expr-error-survey` → `research/experiments/E02-errors/{survey.md,cases.json}`), the Ground field asking for five. The Done criterion is met as a **parity table, not a self-snapshot**: 60 rows are decomposed from the service's own message into `{kind, raw, position, echo}`, compared field by field, then reassembled under the service's prefix and compared byte for byte — a one-character change to the position formula fails 56 of them (verified by mutation). **Two findings changed the parser, not the renderer.** `!` is keyword-shaped, not a symbol: `!!true` is *one* token, `!eq(1, 1)` stops at the `(` and is still a function name, and `!=` is the only special case (C-E02-101). And **name resolution is deferred behind the syntax parse** while arity is not — `nosuchfunc(1) 2` and `nosuchcontext 2` both report the trailing `2`, `eq(1) 2` reports the arity error at the `)` (C-E02-103) — with the corollary that a *leftover* token is always phrased `Unexpected symbol` whatever its kind. Together these **close the `! true` divergence E02-S01-T01 left open** (all six `!` spellings now match) and reclassify `+1`, `"double"` and `!true` as names that fail to resolve rather than lexical errors: the eager class is exactly "text the lexer started reading as a number" (`1e3 2`, `0x1F 2` and `-1.2.3 2` report at 1; `"double" 2` and `+1 2` report the leftover), settled by five probes (C-E02-102), so those T01 rows became `needsRegistry` and a syntax-only parse now accepts them exactly as it accepts `null`. **Four deliberate divergences, each asserted rather than described** (docs/06 §5 decision 15): `(Line, Col)` locates the *host scalar*, never the offending token — confirmed across five document shapes — so we range the token and keep the service's `Located at position N` in the message (C-E02-105); an expression embedded in a larger scalar is reported against a synthetic `format('prefix {0} suffix', null)` call the service compiles the scalar into, which exists in no file the user owns (C-E02-109 — evidence handed to E03-S01-T05, and it independently confirms docs/02 §3's stringification rule); compile-time messages are cut at **500 characters** with `[...]`, mid-URL if that is where it falls, while a 591-character runtime message came back whole, so the cap belongs to the compile-time collector (C-E02-107) and we truncate ours only inside the test, to prove equality; and the single runtime/compile-time *kind* swap (C-E02-015) is not reproduced — one grammar, one kind. Two more measurements the renderer is built on: the service **trims** the delimited text before parsing, so `${{    1 == 1 }}` reports position 3 over the echo `'1 == 1'` — invisible in all 74 T01 probes, which used one space each side (C-E02-104) — and three message shapes exist, two codes carrying no position and one no help link either (C-E02-106). Also grounded: the service reports **every** bad expression in a document, newline-joined, while our error stays per-expression (C-E02-110 — collection is E03-S01's). Docs updated: docs/02 §3 + §6, docs/06 §5 decision 15, backlog E03-S01-T05 note (evidence: research/E02-expressions.md C-E02-101..110; research/experiments/E02-errors/; tests packages/engine/test/expr/errors.test.ts 80 tests incl. a seam test deriving `ExprHost.offset` from E01's own parse provenance, suite 515 vitest + 10 bats green)
- 2026-08-12 E02-S02-T01 done — seven-kind `ExprValue` model with parser-literal bridge, validated 3–4 segment Version parsing, and recursive tagged round-trip encoding; the open fork corroborates six tags/read-only collection shape but omits Azure's Version kind (evidence: research/E02-expressions.md C-E02-005/018/019; tests packages/engine/test/expr/value.test.ts, 22 tests)
- 2026-08-12 E02-S02-T02 done — directional conversion matrix + `eq/ne/lt/le/gt/ge`: right converts to left kind, ordinal-ignore-case strings, typed conversion failures, reference-identity collection equality (ordered collection comparisons always error), component-wise Versions; 28 live probes corrected two design assumptions (String→Number accepts decimals/groups despite Learn saying Int32, and conversion can create 2-segment Versions although literals require 3–4) (evidence: research/E02-expressions.md C-E02-020..023; research/experiments/E02-coercion/; tests packages/engine/test/expr/coercion.table.ts 120 rows + coercion.test.ts)
- 2026-08-12 E02-S02-T03 in-progress — claimed by Codex `/root` on `codex/e02-s02-t03` (evidence: pending)
- 2026-08-12 E02-S02-T03 done — null-propagating Object/Array access: property/index share lookup, primitive object keys stringify, array indices convert/floor/bounds-check, all misses and non-collections return Null; `ExprObject` carries ordinal vs ordinal-ignore-case because live service proves parameter objects are case-sensitive while variables are insensitive (evidence: research/E02-expressions.md C-E02-024..027; research/experiments/E02-members/ 21 probes; tests packages/engine/test/expr/access.test.ts, engine suite 442 green)
- 2026-08-12 E02-S03-T01 in-progress — claimed by Codex `/root` on `codex/e02-s03-t01` (evidence: pending)
- 2026-08-12 E02-S03-T01 blocked — grounding contradicts the requested `contains` string/array duality: official docs specify String conversion and live preview rejects Arrays with Array→String; `in`/`notIn` also require 2..N despite docs saying 1..N (evidence: research/E02-expressions.md C-E02-028..032; research/experiments/E02-logical/)
- 2026-08-12 E02 integration (not a backlog task) — merged the two branches forking from `7739711`: this line's E02-S01-T02 error rendering and the codex chain E02-S02-T01/T02/T03 (value model, coercion, member access), one merge commit, source files disjoint. **Fixed a live claim-ID collision:** both forks had independently allocated `C-E02-018..027`, ten for ten, for entirely different claims — the collision was invisible on each branch and would have landed silently, leaving every claim ID in code comments and test names pointing at the wrong claim. E02-S01-T02's block was the one renumbered (→ `C-E02-101..110`, 68 references across 11 files) because the codex chain is still in flight and has already taken 028..032; no codex ID was touched. `research/E02-expressions.md` now opens with a **block-allocation table** and `research/README.md` §Claim entry format gains the general rule — "next free number" is not an allocation strategy when tasks run on parallel branches, and a collision only surfaces at merge, after the IDs are load-bearing. Also reconciled: docs/06 §5 had two decision 13s (codex keeps 13/14, error rendering → 15), and the `! true` "known divergence" section E02-S01-T02 deleted stayed deleted. Codex's coercion and access tests pass unchanged against the lexer/parser as E02-S01-T02 rewrote them (`!` as keyword text, deferred name resolution), i.e. the forks agree on the AST — full suite 702 vitest + 10 bats green, build/typecheck/eslint/prettier/shellcheck clean (evidence: this branch `e02-integration`; research/E02-expressions.md block table; research/README.md)
- 2026-08-12 E02-S03-T01 in-progress — user authorized correction to string-only `contains` and array/object `containsValue`; reclaimed by Codex `/root` on `codex/e02-s03-t01` (evidence: research/E02-expressions.md C-E02-031/032)
- 2026-08-12 E02-S03-T01 done — lazy logical/membership evaluator for all 13 functions, oracle-corrected 2..N `in`/`notIn`, string-only `contains`, and array/object `containsValue` with per-function claim-linked tests (evidence: research/E02-expressions.md C-E02-028..032; research/experiments/E02-logical/ 12 probes; packages/engine/test/expr/functions.test.ts 16 tests, combined engine suite 538 green with coverage)
- 2026-08-12 E02 integration (not a backlog task) — second merge: `codex/e02-s03-t01` (logical & membership, claims 028–032) landed on top of the same integration branch, a linear fast-follow from `ec30e85`. It arrived mid-integration — Codex committed it while this merge was in flight, and the user had authorized its correction of the **Do** field (`contains` is String-only; Array membership is `containsValue`, C-E02-031), so the `[!]` blocked note recorded an hour earlier is superseded by `[x]` and the reworded **Do** stands. Its claims landed in the 028–039 block the table had reserved for it, which is the block scheme doing its job on the first live test. Conflicts were bookkeeping only again (claims append point, the task's checkbox, the log). Suite after the merge: 718 vitest + 10 bats green, build/typecheck/lint/shellcheck/VERIFY-guard clean. All of E02-S01 and E02-S02 and E02-S03-T01 are now on one branch (evidence: branch `e02-integration`)
- 2026-08-12 E02-S03-T02 in-progress — claimed by Codex `/root` on `e02-integration` (evidence: pending)
- 2026-08-12 E02-S03-T02 dropped — current Learn catalogue exposes three general functions omitted by the task split, so T02 cannot meet the story acceptance; superseded by E02-S03-T04 (evidence: research/E02-expressions.md C-E02-040)
- 2026-08-12 E02-S03-T03 in-progress — claimed by Codex `/root` on `e02-s03-t03-status-functions` (evidence: pending)
- 2026-08-12 E02-S03-T03 done — status functions: `packages/engine/src/expr/status.ts` (`JobResult`/`StatusScope`/`StatusContext`, `statusFunctionSignatures`, `evaluateStatusFunction`), 97 tests. **This family is two engines behind one spelling, and the service will not tell you which one you are in** — a *step* condition is validated by a path that resolves no function names (`nosuchfunc(1, 2, 3)` and a bare `always` both return HTTP 200, while the same `nosuchfunc()` on a job or stage is rejected `Unrecognized value`, and step rejections arrive wrapped in `Job <j>: Step <s> specifies condition <c> which is not valid. Reason: …`), so `succeeded('A')` on a step is accepted at queue time and rejected by the **agent**, which registers all five with `minParameters: 0, maxParameters: 0` (C-E02-060/061). Decided and recorded (docs/06 §5 decision 17): we reject at convert time what preview accepts, and no later task may treat a step-slot 200 as grounding. At job/stage level the arity splits 2–3 instead: `always`/`canceled` stay 0-arity (`always('A')` rejected at the closing paren) while the other three take 0..N names that are ordinary expressions — `succeeded(1)`, `succeeded('')` and `succeeded(variables['jobName'])` all pass, so names are never checked against the graph (C-E02-064). Preview also proved the doc sentence "in conditions, but not in variable definitions" is **enforced**: `${{ always() }}`, `${{ if succeeded() }}` and `$[ always() ]` are all `Unrecognized value` while the control `$[ eq(1, 1) ]` passes — three distinct function tables, not one (C-E02-065). **The truth tables needed a real run** — preview never evaluates a status function and the job-level engine is closed — so this task queued the repo's first one, designed to cost **no hosted-agent parallelism**: every job agentless (`pool: server` + `Delay@1` 0 min), ~15 s per run, with the datum read off the timeline's **Phase** records rather than Job records, because a job whose condition was False has no Job record at all and "absent" would have been indirect evidence. Four findings the docs do not state: a **Skipped** dependency satisfies nothing but `always()` — `succeeded`, `failed` *and* `succeededOrFailed` are all False (C-E02-069); `succeeded()` is all-of while `succeededOrFailed()` is **any-of**, so {Succeeded, Skipped} flips them apart (False vs True) and Learn's "evaluates to `True` regardless" plus "like `always()`, except … canceled" are both wrong — its own advice to use `not(canceled())` when dependencies are skipped is the correct guidance and was verified live (C-E02-068); arguments **replace** the dependency set rather than filtering it, the name lookup folds case, and an unknown name is simply False rather than an error (C-E02-067/072); and `succeededOrFailed()` over an **empty** dependency set is True — the one asymmetry in the family, measured rather than derived, because pure any-of would have made a dependency-free job carrying it never run. Bonus: a job whose *own condition errors* completes as **`Abandoned`**, a sixth result Learn never lists, which `failed()` does **not** catch (C-E02-071) — found by accident while trying to synthesize a failed dependency; a genuinely Failed one was then produced via a bad task input and measured too (C-E02-070). Three mutation checks confirm the subtle rules are load-bearing (empty-set carve-out, case folding, step-level `canceled()`). Docs updated: docs/02 §6 + docs/06 §5 decision 17; the runbook gained the probe pipeline in its cleanup list (evidence: research/E02-expressions.md C-E02-060..072; research/experiments/E02-status/{survey.md,real-run.md,status-skipped.yml}; tests packages/engine/test/expr/status.test.ts 97 tests, suite 815 vitest + 10 bats green)
- 2026-08-12 E02-S03-T03 correction — final committed test counts are 16 status-function cases and 734 Vitest + 10 Bats tests; the preceding 97/815 counts described an uncommitted intermediate suite (evidence: `pnpm exec vitest run packages/engine/test/expr/status.test.ts`; `pnpm test`)
- 2026-08-12 E02-S03-T04 in-progress — claimed by Codex `/root` on `e02-s03-t03-status-functions` (evidence: pending)
- 2026-08-12 E02-S03-T04 done — completed all 15 remaining general functions, exact 28-name non-status registry, and injected counter-state seam; live probes corrected iif/counter arities plus length/split behavior (evidence: research/E02-expressions.md C-E02-041..051; research/experiments/E02-general/ 30 probes; packages/engine/test/expr/general-functions.test.ts 20 tests)
- 2026-08-12 E02-S04-T01 in-progress — claimed by Codex /root on e02-s03-t03-status-functions (evidence: pending)
- 2026-08-12 E02-S04-T01 blocked — official context claims recorded, but required preview-oracle phase-gating probe cannot run because AZDO_ORG_URL, AZDO_PROJECT, AZDO_ORACLE_PIPELINE_ID, and AZDO_PAT are absent (evidence: research/E02-expressions.md C-E02-073..076)
- 2026-08-12 E02-S04-T01 done — expression context interface and per-slot availability gating: `packages/engine/src/expr/context.ts` (`ExprSlot`, `SLOT_AVAILABILITY`, `registryForSlot`, `resolveContext`, `parametersContext`/`variablesContext`), plus an `ExprObject` miss policy and `ExprKeyNotFoundError` in `access.ts`; 31 tests. **The earlier `[!]` blocker was wrong and is lifted:** it reported `AZDO_ORG_URL`/`AZDO_PROJECT`/`AZDO_ORACLE_PIPELINE_ID`/`AZDO_PAT` absent, but those live in `.env.oracle`, which every `scripts/expr-*-survey.ts` loads through `loadEnvFile` instead of reading the ambient environment — so the required oracle probe was runnable all along. 61 live probes (`pnpm expr-context-survey`) replaced the doc's single sentence on availability with a measured grid, and the headline is that **"compile time vs run time" does not describe the gate — the slot does**: there are **three** name tables, not two, and `resources` (legal in a root `$[ ]` variable, rejected in every condition) and `dependencies` (the exact opposite) are a double dissociation across the two runtime slots, so neither table contains the other (C-E02-080/082). The discriminating question for the code was the *shape* of a wrong-slot rejection, and it came back byte-identical to a nonexistent context — `Unrecognized value: 'dependencies'` at position 1, same help link — so phase gating is nothing but a per-slot `namedValues` set handed to `parseExpression`, and `errors.ts` was not touched (C-E02-081). **A second permissive slot was found, and it nearly landed a false claim:** a *job-scoped* `variables:` value accepts `$[ nosuchcontext.probe ]` and even the bad-arity `$[ eq(1) ]` while both are rejected at the root, so an intermediate reading in which `dependencies`/`parameters` "become available inside a job" was an artifact — caught only by running negative controls in that slot, the same trap as step conditions (C-E02-085, extending decision 17). Two provider rules contradict the general value model and changed the code rather than the notes: the top-level `parameters` context **folds key case** (unlike the nested parameter objects of C-E02-024/027) and **raises `Key not found 'x'` on a miss** where `variables` returns Null for the identical miss in the identical slot, so `ExprObject` gained a miss policy and `access.ts` an `ExprKeyNotFoundError` — an *evaluation* error whose shape (file coordinates, no position-within-expression, no help link) appears in none of E02-S01-T02's 66 rejections and deliberately does not join `ExprErrorCode` (C-E02-087/088). Also measured: `variables` is flat, so `variables['My.Var']` resolves a dotted name that `variables.My.Var` cannot (C-E02-089); "statically defined variables" includes the predefined `system.*` set (C-E02-090); and `environment` is rejected in every slot measured including a deployment job's own condition, with the deployment-scoped *variable* cell left open because the test org has no such environment and the control failed with the probe (C-E02-091). `statusScopeForSlot` derives the status-function scope from the same `ExprSlot`, so the context and function tables cannot be paired inconsistently. Docs updated: docs/02 §6 Contexts + docs/06 §5 decision 19 (evidence: research/E02-expressions.md C-E02-080..091; research/experiments/E02-context/survey.md 61 probes; packages/engine/test/expr/context.test.ts 31 tests; suite 813 vitest + 10 bats green, build/typecheck/lint/shellcheck clean)
- 2026-08-12 E02-S04-T01 addendum — the function table is slot-keyed in **both** directions, and shipping without checking would have accepted what the service refuses: `registryForSlot` originally handed every slot the full 28-name non-status set on the assumption that only status functions were slot-restricted. Four more probes (65 total) show `counter` is legal in **exactly one** slot, the runtime variable — rejected in job conditions, stage conditions, *and* in a compile-time `${{ }}` variable definition, which is narrower than Learn's own "Use this function only in an expression that defines a variable. Don't use it as part of a condition for a step, job, or stage". Added `SLOT_RESTRICTED_FUNCTIONS` in `context.ts` (C-E02-096 — allocated at 096 because a concurrent E02-S04-T02 session had already taken 092–095). Also fixed a latent hole in the same change: `encodeExprValue`, documented "lossless", dropped the new `missPolicy` field, so a round-tripped `parameters` context silently degraded from raising `Key not found` to returning Null — exactly the divergence C-E02-087 exists to prevent; now carried on the wire with a round-trip test either way (evidence: research/E02-expressions.md C-E02-096; research/experiments/E02-context/survey.md rows counter-*; packages/engine/test/expr/context.test.ts 36 tests, suite 818 vitest + 10 bats green)
2026-08-12 E02-S05-T02 blocked — requires the unimplemented E06-S01-T01 runtime fixture-store APIs `azdo_var` and `azdo_output` (evidence: backlog/E02-expressions.md).
2026-08-12 E06-S01-T01 in-progress — claimed by Codex on e02-s04-t01-expr-contexts (evidence: pending).
2026-08-12 E06-S01-T01 blocked — required readonly warning+ignore behavior is contradicted by pinned agent code: legacy mode warns then overwrites; enabled enforcement throws before writing (evidence: research/E06-runtime.md C-E06-001..005).
2026-08-12 E06-S01-T01 in-progress — resumed by Codex on e02-s04-t01-expr-contexts to run a real hosted-agent probe of the effective readonly-variable policy (evidence: pending).
2026-08-12 E06-S01-T01 dropped — real hosted run 539 proves strict readonly enforcement (error and original value retained), so the task's required warning+ignore behavior is superseded by E06-S01-T04 (evidence: research/E06-runtime.md C-E06-006; research/experiments/E06-readonly-variables/real-run.md).
2026-08-12 E06-S01-T02 blocked — requires E06-S01-T04 variable-store APIs before environment materialization can run (evidence: backlog/E06-runtime.md).
2026-08-12 E06-S01-T03 blocked — requires E06-S01-T04 variable-store APIs before `.env` values can be registered (evidence: backlog/E06-runtime.md).
2026-08-12 E06-S01-T04 in-progress — claimed by Codex on e02-s04-t01-expr-contexts (evidence: pending).
2026-08-12 E06-S01-T04 done — strict file-backed variable store with case-insensitive names, scope copies, metadata, hosted-agent read-only enforcement, and same-job/cross-job output paths (evidence: research/E06-runtime.md C-E06-001..006; research/experiments/E06-readonly-variables/real-run.md; packages/runtime/test/core.bats).
2026-08-12 E06-S01-T02 unblocked — E06-S01-T04 now provides the variable-store APIs required for environment materialization (evidence: packages/runtime/lib/core.sh).
2026-08-12 E06-S01-T03 unblocked — E06-S01-T04 now provides the variable-store APIs required to register `.env` values (evidence: packages/runtime/lib/core.sh).
2026-08-12 E02-S05-T02 unblocked — E06-S01-T04 now provides the fixture-store APIs `azdo_var` and `azdo_output` (evidence: packages/runtime/lib/core.sh).
2026-08-12 E02-S01-T03 in-progress — claimed by Codex on e02-s04-t01-expr-contexts (evidence: pending).
2026-08-12 E02-S01-T03 done — added the positioned `expected-function-call` parser/error-rendering path for bare registered functions; verified compile/job slots and kept contexts/unavailable status names distinct (evidence: research/E02-expressions.md C-E02-132..134; research/experiments/E02-bare-functions/; packages/engine/test/expr/errors.test.ts).
2026-08-13 E01-S01-T04 in-progress — claimed by Codex on e02-s04-t01-expr-contexts (evidence: pending).
2026-08-14 E01-S01-T04 done — duplicate-key parsing now exempts recognized template directive keys while retaining ordinary-key rejection; live preview accepts duplicate `if`/`each` directives and rejects duplicate `${{ pair.key }}` (evidence: research/E01-yaml-frontend.md C-E01-038/039; research/experiments/E01-directive-duplicates/; packages/engine/test/frontend/quirks.test.ts).
- 2026-08-12 E03-S01-T01 done — DOM walker with context stack: `packages/engine/src/template/walk.ts` (`loneExpression`/`expressionUnits`/`parseDirectiveKey` for recognition, `TemplateFrame`/`rootFrame`/`childFrame`/`bindLoopVariable` for the context stack, `walkTemplate` with a per-directive-kind visitor seam), 54 tests. **Both of the task's Ground sources were insufficient, in opposite directions, and that is what set the size of this task**: the templates and template-expressions docs state *none* of the recognition rules and their one structural claim about where expressions expand is false, while the `actions/runner` object-templating engine knows exactly one directive — `TemplateConstants.cs` declares `InsertDirective = "insert"` and has no `if`/`elseif`/`else`/`each` counterpart anywhere (C-E03-115), so it is a design reference for the walk loop and for nothing about the directive set. 33 live probes (`pnpm template-walk-survey` → `research/experiments/E03-walk/`) measured the rules instead. **Directive keywords are case-sensitive** — the only such corner of a language whose names, functions and even boolean literals all fold case — and a wrongly cased keyword is not a mis-spelled directive but *not a directive at all*: `${{ IF eq(1, 1) }}` falls through to ordinary expression parsing and reports `Unexpected symbol: 'eq'` at position 4, i.e. C-E02-103's leftover-token rule applied to `IF` read as a name (C-E03-100). Directly beside it, loop-variable names *do* fold case (C-E03-107). **The finding that changed the implementation** is that directive parameters are top-level **expression units, not whitespace-split words**: `${{ else if eq(1, 1) }}` is rejected "Exactly 0 parameter(s) were expected following the directive 'else'. Actual parameter count: 2", so `eq(1, 1)` counts as one (C-E03-101, counts measured: `each` 3, `else` 0, `insert` 0). The consequence is that the `each` separator must be found by tokenizing, never by `indexOf(' in ')` — `${{ each item in split('a in b', ' in ') }}` legitimately iterates `a`,`b` and `${{ each item in split(format('{0}', in('b','b')), ',') }}` is not captured by the later `in(` either (C-E03-104); a text search gets one of those wrong **silently**, iterating the wrong collection with no error to notice. `expressionUnits` reproduces every count the service reported. Three more that shaped the API: loop variables share one **flat namespace with the contexts** and collide rather than shadow — `${{ each variables in … }}` → "The idenfifier 'variables' has already been defined within the current scope", the service's own typo preserved (C-E03-106), so `bindLoopVariable` returns a rejection instead of a nested scope; a directive is recognized only on a mapping key or a one-key sequence item, never on a scalar value (C-E03-112); and malformed-directive sentences come back from the service **without** the help link every expression error carries, which the tests assert rather than describe. **Docs correction (rule 5):** the template-expressions doc's "Expressions are only expanded for `stages`, `jobs`, `steps`, and `containers` … You can't … use an expression inside `trigger`" is false — `trigger: [${{ 'main' }}]` expands to `trigger.branches.include`, and expressions and `if` directives both expand in `pool.demands` and root `variables:` (C-E03-109). Position gating is nonetheless real, with its own sentence `A template expression is not allowed in this context`, and has exactly **one** measured member (`resources.repositories`), so it is modelled as a seam rather than a table extrapolated from the doc's list (C-E03-110). Scope line held deliberately: T01 detects, frames and accumulates (never throws — C-E02-110 hands document-wide collection to E03-S01), while `if`/`each`/`insert` semantics stay with T02/T03/T04, each of which owes committed oracle fixture pairs; the default walk is a faithful traversal that changes nothing, and `expect(result.node).toEqual(root)` is a real gate on provenance surviving the rebuild, which E03-S04-T02's provenance map depends on. Two gaps found and **filed rather than fixed**: our C-E01-023 duplicate-key quirk rejects two byte-identical `${{ if }}` keys at parse time while the service accepts the document and merges both bodies — E01-S01-T02 is right about all 13 cases it probed and never probed a `${{ }}` key (C-E03-111 → new **E01-S01-T04**) — and `Expected '(' to follow a function: '<name>'` is a general expression error kind missing from `ExprErrorCode`, so a bare `${{ eq }}` renders `Unrecognized value` where the service names the missing paren (C-E03-114 → new **E02-S01-T03**). Two late additions after review, both closing a claim-without-evidence: the fork's evaluation loop was actually **read and pinned** rather than cited by filename (`TemplateEvaluator.cs#L89-L203` — recursive `Evaluate(DefinitionInfo)` over `TemplateUnraveler` predicates; our walk is the same recursion over a materialized DOM, so it needs no unraveler, C-E03-116), and `loneExpression` now finds its closing `}}` with a **quote-aware scan** because the documented escape for a literal `${{` is to wrap it in an expression string — `${{ 'my${{value' }}` is the doc's own canonical spelling and the first implementation reported it as *not* a lone expression, a bug that would have surfaced in E03-S01-T05 rather than here (C-E03-117). `research/E03-template-engine.md` opens with the **claim-ID block table** the E02 merge collision taught us to write first; `research/E03-normalizer.md`'s 001–003 are recorded in it. One probe is recorded as **not evidence**: the multi-line directive key was rejected by the YAML layer before the template engine saw it, and the useful conclusion is that a directive is always a mapping key so the case cannot arise. Docs updated: docs/02 §4, docs/06 §5 decision 22, REFERENCES.md (templates + new template-expressions row with the false statement flagged, actions/runner object-templating) (evidence: research/E03-template-engine.md C-E03-100..117; research/experiments/E03-walk/ 33 probes; tests packages/engine/test/template/walk.test.ts 58 tests, suite 898 vitest + 10 bats green, build/typecheck/lint/prettier/VERIFY-guard clean)
- 2026-08-13 E02-S05-T02 done — dual-backend conformance harness: one row table (`packages/engine/test/expr/conformance.table.ts`) drives the evaluator through the E02-S02/S03 entry points and **generates** `packages/runtime/test/expr-conformance.bats`, 104 executed rows, committed with a freshness guard (`toMatchFileSnapshot`) so editing the compiler without regenerating is red rather than untested. **Building the runner falsified E02-S05-T01's output — none of it could have executed** (C-E02-145): status functions compiled to `[ azdo_status_succeeded = True ]`, a bare *word* compared against a string, so the function was never invoked and the docs' canonical condition was False for every run; `not(x)` nested `[ [ … ] != True ]`; comparisons double-wrapped an already-quoted literal and compared quoting syntax rather than value; dependency reads passed `azdo_output` two arguments where it takes three; and every emitted `azdo_expr_*` helper had **no definition anywhere**, because `packages/runtime/lib/expr.sh` — named in T01's own **Do** — was never written. All of it invisible to a test asserting the emitted string, which is what T01 shipped. The fix was structural, not cosmetic: mapping comparisons onto `test` operators discards the operand *kind* the conversion table needs, and `eq(1, true)` — True by Boolean→Number — compiles to `[ "1" = True ]` and is **silently False**. Values now cross into the shell as their String form plus a kind tag (`bool|num|str|ver`) and `azdo_expr_cmp` reproduces `compareValues()` in bash, including decimal comparison without an external tool, `1.2 < 1.2.0` version ordering, and ordinal-ignore-case strings. Status convention **0 True / 1 False / 2 error**, because `test` already reserves >1 for errors and a missing helper exits 127 (C-E02-135/136) — a harness reading "non-zero = False" would have gone green over a backend that never ran, which is the trap this task existed to avoid. `LC_ALL=C` is pinned in every string op: `[[ alpha < BETA ]]` is **false** under C and **true** under en_US.UTF-8, so a bare `[[ < ]]` would answer differently on different developer machines (C-E02-142, measured — neither the bash manual nor POSIX states it). Three divergences are **declared as rows carrying the measured shell answer**, so each is red if it widens *or* if it quietly goes away: the shell has no Null, so `lt(variables.Absent, 'x')` answers True where the evaluator raises (C-E02-138); an evaluation error is masked by `||` and discarded outright in value position, because command substitution keeps output and not status (C-E02-143/144) — handed to E06-S03-T03, which owns condition semantics, rather than fixed with a new runtime error protocol here; and non-ASCII case folding differs from .NET OrdinalIgnoreCase (C-E02-141). Five constructs now raise `BashCompileError` and are asserted to (never skipped): `split`/`join`/`convertToJson`/`containsValue` (no Object/Array in the shell), a dynamic index, and `counter` (convert-time state seam) — T01 had emitted `azdo_expr_split` etc. for these, which would have exited 127. **Structural finding filed rather than absorbed:** T02 says the tables run "through both the evaluator and the compiled bash", but E02 has **no AST evaluator** — the tables call `compareValues`/`evaluateLogicalMembershipFunction`/`evaluateGeneralFunction` directly and no task ever allocated a walker; building one inside a harness task would bury a missing story, so rows carry an explicit `evaluate()` thunk and the walker is new task **E02-S05-T03**. Three mutation checks confirm the gate is real: case-sensitive string compare → 6 bats rows red; letting the compiler emit `split` again → the unsupported row red; hand-editing the generated bats → the freshness snapshot red. Docs updated: docs/02 §6 (comparisons no longer compile to `[ ]`; divergence list) + docs/06 §5 decision 23; REFERENCES gains POSIX.1-2024 and a note that gnu.org rate-limits (evidence: research/E02-expressions.md C-E02-135..146; research/experiments/E02-conformance/shell-semantics.md 11 probes; packages/engine/test/expr/conformance.test.ts 223 tests + packages/runtime/test/expr-conformance.bats 104 rows; suite 1128 vitest + 118 bats green, build/typecheck/lint/shellcheck/VERIFY-guard clean)
- 2026-08-14 E02-S05-T02 follow-up — **the first CI run caught a bash-3.2 incompatibility whose failure mode was silent agreement** (C-E02-147). macOS runners execute bats under the system bash 3.2, where `${v^^}`/`${v,,}` do not exist; the bad substitution yields the empty string on *both* sides of a comparison, so the two empties compared **equal**: `lt('alpha','BETA')` reported "equal" (four of its six operators red) while `eq(lower('AB'), 'ab')` **passed for the wrong reason**, the helper having returned nothing at all. Ubuntu was green throughout. Case folding now goes through `LC_ALL=C tr`, the pattern `core.sh` already used for exactly this reason, and a new `packages/runtime/test/expr.bats` (7 cases) asserts the returned **text** of each helper rather than only a comparison's status — a comparison-only suite structurally cannot tell "both correct" from "both empty", which is the same lesson as the exit-status convention one level down (evidence: research/E02-expressions.md C-E02-147; GitHub Actions run 31776027219; packages/runtime/test/expr.bats)
2026-08-14 E02-S05-T03 in-progress — claimed by Codex on e02-s04-t01-expr-contexts (evidence: pending).
- 2026-08-14 E02-S05-T03 done — added the recursive `evaluateExpression` AST backend with slot-derived status scope and lazy family dispatch; replaced every conformance-table `evaluate()` thunk with parser→walker execution and filed the ungrounded filtered-array contract as E02-S05-T04 instead of guessing it (evidence: `research/E02-expressions.md` E02-S05-T03 composition map; `packages/engine/test/expr/evaluate.test.ts` 13 focused walker/laziness/state cases; `packages/engine/test/expr/conformance.test.ts` 225 evaluator/conformance tests).
2026-08-18 E02-S05-T04 in-progress — claimed by Codex `/root` on `codex/e02-s05-t04-filtered-arrays` (evidence: pending).
- 2026-08-18 E02-S05-T04 done — grounded filtered-array traversal with a 24-cell live preview matrix and pinned source, then implemented distinct mapped traversal for Array/Object wildcards, misses, nested filters, and later indices; the eval backend covers both spellings while shell collection traversal remains explicitly unsupported (evidence: `research/E02-expressions.md` C-E02-160..164; `research/experiments/E02-filtered-arrays/`; `packages/engine/test/expr/evaluate.test.ts`; 1,176 Vitest + 126 Bats).
2026-08-18 E03-S01-T03 done — grounded and implemented sequence/mapping `each`, nested loops, object/stepList/jobList structural iteration, exact authored mapping order, and no implicit index; corrected the design docs' mapping-binding claim (evidence: research/E03-template-engine.md C-E03-140..151; research/experiments/E03-each/; fixtures/oracle/directives/; packages/engine/test/template/each.test.ts; 1,193 Vitest + 126 Bats)
2026-08-18 E03-S01-T02 in-progress — claimed by Codex `/root` on `claude/e06-s01-t02` (evidence: pending).
2026-08-18 E03-S01-T02 blocked — official docs grounded sequence/mapping syntax and the documented `if`/`elseif`/`else` form, but the mandatory six-case preview-oracle matrix cannot run because all four `AZDO_*` variables and `.env.oracle` are absent; no implementation was attempted (evidence: research/E03-template-engine.md C-E03-120/121; research/oracle-setup.md).
2026-08-19 E03-S01-T02 in-progress — unblocked and reclaimed by Orchestrator on `claude/e06-s01-t02-v2`; the doc-only grounding commit `7459ab3` is integrated and `.env.oracle` is present, so the six-case oracle matrix can run (evidence: pending).
- 2026-08-19 E03-S01-T02 done — conditional insertion chains: `packages/engine/src/template/conditional.ts` (`conditionalVisitor`, `ConditionalChainError`) on T01's directive seam, plus `scripts/if-survey.ts` (`pnpm if-survey`) and the shared fixture harness `packages/engine/test/template/fixture-harness.ts` extracted from E03-S01-T03 so both directive suites share one parameter binding and one T05 scalar stand-in. **The 2026-08-18 `[!]` was wrong and is lifted** — it reported the four `AZDO_*` values absent, but they live in `.env.oracle`, which was merely missing from that worktree; the doc-only grounding commit `7459ab3` (C-E03-120/121) is cherry-picked in rather than left stranded. 22 live probes (`research/experiments/E03-if/`, C-E03-122..134) produced **18** committed input/`finalYaml` pairs against the six the task requires, plus 4 rejections asserted against their transcripts. **Two findings invert this task's own `Do` field**, and both are mutation-checked rather than merely asserted. Chain membership is **not adjacency-gated**, and the winning body splices at the **winning directive's own position** rather than the head's: with a false `if`, an ordinary step written between `${{ if }}` and `${{ else }}` came out *first* and the `else` body landed where the `else` was written — measured in both parent shapes — so "group the contiguous chain and emit the winner at the head's index", which is what **Do** describes, silently reorders that document (C-E03-128). And chain conditions evaluate in **document order**, stopping at the first winner: `if(true) / elseif parameters.missing / else` expands, so resolving the trailing `else` must not reach past the winner — a backwards *nearest-first* scan, the obvious way to write the backwards resolution the first finding forces, would evaluate the raising member and reject a document the service accepts (C-E03-132). Both laziness claims rest on a control proving the same `parameters.missing` read **is** a hard 400 `Key not found 'missing'` when actually reached (C-E03-134); without it, "it expanded" would have proved nothing. Three shapes the service separates and the code keeps separate: a losing `else` (silent), an orphan `elseif`/`else` (HTTP 400, two newline-joined sentences, **no help link** — C-E03-129), and an `elseif` after an `else`, rejected identically because `else` terminates its chain (C-E03-130). Conditions are **converted** to Boolean rather than required to be one — `${{ if 'text' }}` taken, `${{ if '' }}` not — reusing C-E02-020 instead of inventing a truthiness notion (C-E03-131). Also measured: a new `if` starts a new chain so a trailing `else` binds to the nearest one (C-E03-127); chains nest and a losing outer branch discards the nested structure unevaluated (C-E03-126); a chain with no `else` and every condition false is not an error (C-E03-125). Three mutations confirm the goldens gate the subtle rules rather than merely passing over them: adjacency-gating the mapping scan → 2 red, adjacency-gating the sequence scan → 4 red, nearest-first evaluation order → 2 red. **Open question filed rather than guessed:** no probe placed an `each`/`insert` between two chain members, so whether those siblings break a chain is unmeasured — the code skips them (the reading consistent with C-E03-128) and says so at that line, handed to E03-S01-T04. Docs updated per rule 5: docs/02 §4 (its one-sentence paragraph was silent on all of the above), docs/06 §5 decision 27, REFERENCES.md (evidence: research/E03-template-engine.md C-E03-120..134; research/experiments/E03-if/ 22 probes; fixtures/oracle/directives/if-* 18 pairs; packages/engine/test/template/conditional.test.ts 35 tests; suite 1,228 Vitest + 126 Bats green, build/typecheck/lint/prettier/VERIFY-guard clean)
- 2026-08-19 environment note (not a backlog task) — this session's worktree was created at `/tmp/wt-claude`, and `/tmp` is mounted **noexec**, so `pnpm install` fails at esbuild's postinstall and nothing can be built or tested there. Worktree relocated to `/root/wt-claude` (`git worktree repair`). One consequence is worth knowing before someone re-reports it as a regression: `packages/runtime/test/fixture-store.bats` "preserves the executable bit" fails whenever bats' tmpdir lands on a noexec filesystem, because `test -x` calls `access(X_OK)` and the kernel returns EACCES regardless of the mode bits. It fails identically on an untouched `main` checkout and passes under `TMPDIR=/root/batstmp`; it is environmental, not a code defect.
2026-08-18 E03-S01-T02 in-progress — claimed by Codex `/root` on `codex/e03-s01-t02-conditional-insertion` (evidence: pending).
- 2026-08-18 E03-S01-T02 done — grounded and implemented lazy, stateful conditional insertion across mapping/sequence/nested containers; 23 live probes produced 19 claim-tagged, hash-locked oracle pairs and four rejection controls (evidence: `research/E03-template-engine.md` C-E03-120..127; `research/experiments/E03-conditionals/`; `fixtures/oracle/directives/`; `packages/engine/test/template/conditionals.test.ts`; 1,213 Vitest + 126 Bats).
- 2026-08-19 E03-S01-T02 merge reconciliation — retained `conditionals.ts`: the original Side A implementation matched the 37-pair oracle union while Side B matched 34/37; preserved both surveys, deduplicated C-E03-120..137, and consolidated the mutation-backed ordering tests (evidence: `research/experiments/E03-conditionals/`; `research/experiments/E03-if/`; `fixtures/oracle/directives/`; `packages/engine/test/template/conditionals.test.ts` 62 tests; suite 1,255 Vitest + 130 Bats green).
2026-08-19 E06-S01-T03 in-progress — claimed by Codex leaf coder on codex/e06-s01-t03 (/root/wt-codex) (evidence: pending).
2026-08-19 E06-S01-T03 done — implemented the trusted Bash `.env` loader with lossless quoted/multiline values, same-process `--env-file` overlay, manifest-derived secret flags, and atomic syntax-error rejection (evidence: research/E06-runtime.md C-E06-013..017; docs/04 §10; packages/runtime/test/core.bats; 1,228 Vitest + 134 Bats; ShellCheck clean).
2026-08-19 E06-S02-T01 in-progress — claimed by Codex leaf agent on codex/e06-s01-t03 (/root/wt-codex) (evidence: pending).
2026-08-19 E06-S02-T01 blocked — hosted run 541 refutes the required end-to-end non-recursion (`a=$(b)` rendered `inner` in the next task); source tracing then stopped on GitHub code-search HTTP 401 per the explicit auth-error stop condition, so no implementation was written (evidence: research/E06-runtime.md C-E06-018..021; research/experiments/E06-macro-expansion/real-run.md; docs/04 §5; docs/06 §5 decision 28).
2026-08-19 E06-S02-T01 in-progress — resumed by Codex E06 runtime agent on codex/e06-s01-t03 (/root/wt-codex) for raw/API source tracing and corrected implementation (evidence: pending).
2026-08-19 E06-S02-T01 done — completed the commit-pinned agent phase trace and implemented recursive pre-step variable recalculation plus the non-recursive exact macro target scan, including secret propagation, nested misses, cycle/depth guards, and private expanded step files (evidence: research/E06-runtime.md C-E06-018..024; research/experiments/E06-macro-expansion/real-run.md; packages/runtime/test/core.bats; 1,255 Vitest + 136 Bats; ShellCheck clean).
2026-08-19 E06-S03-T01 in-progress — claimed by Codex leaf agent on codex/e06-s01-t03 (/root/wt-codex) (evidence: pending).
2026-08-19 E06-S03-T01 done — implemented the stable `run_step` contract with private macro-expanded execution, live-probed `System.DefaultWorkingDirectory` fallback, process-group timeout enforcement, and combined per-step log tee (evidence: `research/E06-runtime.md` C-E06-025..030; `research/experiments/E06-run-step/real-run.md`; `packages/runtime/test/core.bats`; 1,255 Vitest + 140 Bats; all linters clean).
2026-08-19 E06-S03-T02 in-progress — claimed by Codex leaf agent on codex/e06-s01-t03 (/root/wt-codex) (evidence: pending).
2026-08-19 E06-S03-T02 done — implemented atomic five-state result persistence, nonzero/stderr failure mapping with live streaming, Failed-only retries capped at ten with 1/4/…/100-second backoff under the step deadline, and post-retry `continueOnError` downgrade (evidence: `research/E06-runtime.md` C-E06-031..037; `packages/runtime/test/core.bats`; 1,255 Vitest + 146 Bats; ESLint/Prettier/ShellCheck/VERIFY guard clean).
2026-08-19 E06-S03-T03 in-progress — claimed by Codex /root on codex/e06-s01-t03 (/root/wt-codex) (evidence: pending).
2026-08-19 E06-S03-T03 done — implemented result-store-backed step status functions, implicit `succeeded()`, compiled condition invocation, `--no-condition`, exact false-condition skip flow, and status-preserving evaluation errors; hosted run 543 corrected the design's invented section-header log to the agent's plain skip line (evidence: `research/E06-runtime.md` C-E06-038..043; `research/experiments/E06-condition-flow/`; `packages/runtime/test/core.bats`; 1,255 Vitest + 151 Bats; build/typecheck/ESLint/Prettier/ShellCheck/VERIFY guard clean).
2026-08-19 E06-S04-T01 in-progress — claimed by Codex leaf agent on codex/e06-s01-t03 (/root/wt-codex) (evidence: pending).
2026-08-19 E06-S04-T01 done — implemented the live `##vso` stdout-line parser with case-insensitive command/property lookup, ordered `%AZP25`/structural-token decoding, handler dispatch, and grounded unknown/malformed warning passthrough wired into `run_step` (evidence: `research/E06-runtime.md` C-E06-044..049; `packages/runtime/test/core.bats`; 1,255 Vitest + 157 Bats; build/typecheck/ESLint/Prettier/ShellCheck/VERIFY guard clean).
2026-08-19 E06-S04-T02 in-progress — claimed by Codex E06 runtime leaf agent on codex/e06-s01-t03 (/root/wt-codex) (evidence: pending).
2026-08-19 E06-S04-T02 done — implemented `task.setvariable` dispatch with following-task timing, plain/output/read-only store routing, sticky secret metadata, default multiline-secret rejection, and immediate same-step/later-step log masking (evidence: `research/E06-runtime.md` C-E06-050..056; `research/experiments/E06-setvariable/real-run.md` run 544; `packages/runtime/test/core.bats`; 1,255 Vitest + 162 Bats; build/typecheck/ESLint/Prettier/ShellCheck/VERIFY guard clean).
2026-08-19 E03-S01-T04 in-progress — claimed by Orchestrator (Claude) on worktree `/root/wt-claude`, branch `claude/e03-s01-t04`; inherits E03-S01-T02's open question (an `each`/`insert` sibling between two conditional-chain members) to settle while it holds the `insert` oracle budget (evidence: pending)
- 2026-08-19 E03-S01-T04 done — the `${{ insert }}` merge directive: `packages/engine/src/template/insert.ts` (`insertVisitor`, `InsertValueError`) on T01's directive seam, plus `scripts/insert-survey.ts` (`pnpm insert-survey`) and `composeVisitors` in `walk.ts` (three directive modules now compose, and `{...a, ...b}` silently drops hooks wherever two visitors define the same one). 32 live probes (`research/experiments/E03-insert/`, C-E03-160..174) produced 13 committed input/`finalYaml` pairs, while **19 were rejected** and are asserted against their committed error transcripts — for this task the rejections carry most of the claims, because both headline answers are ones the service gives by refusing a document. **The task's named source does not exist:** there is no "Insertion" section on the templates page — `${{ insert }}` is documented on **template-expressions**, as one paragraph and one example, and the templates page's "Insert a template" is the unrelated `- template:` file include (C-E03-160/161). Unlike T02 and T03, the `actions/runner` fork genuinely implements this directive (C-E03-115), so for once it is a real second source; it was read and pinned (C-E03-162) and it predicted the collision rule correctly and the position rule **wrongly**, which is why every branch it suggested was still submitted to the oracle. **The flagged unknown is answered: collision is an error, not an overwrite** — `'FOO' is already defined`, HTTP 400, located at the **later** of the two occurrences, comparison folding case, message echoing the *later* spelling (`'foo'` when a literal `FOO` met an inserted `foo`); neither value wins (C-E03-169/170). The design consequence is bigger than the answer: **the rule belongs to the mapping, not to `insert`** — two colliding inserts reject, and so does an `each`-produced key colliding with a literal, with no insert involved (C-E03-171) — so the check lives on the mapping rebuild in `walk.ts`, accumulating a diagnostic and dropping the later entry rather than throwing (the service returned three such sentences in one response), with directive keys exempt so it agrees with the parse-time exemption E01-S01-T04 already made (C-E01-038/039). **The open question E03-S01-T02 filed and handed here is settled, and T02's guess was wrong** (C-E03-138): an `${{ each }}` or `${{ insert }}` written **between** two chain members orphans the member that follows — measured in both parent shapes, for both intervening directives, for a trailing `elseif` as well as `else`, with both truth values of the `if`, and against two controls placing the same insert immediately *before* and immediately *after* the chain, where the document expands; C-E03-128's ordinary-sibling control still holds, so the rule is specifically "directive sibling". `conditionals.ts` now ends the containing chain at any directive it does not own, exactly as `else` already ends one — which is why `conditionalVisitor` composes **first**. That fix exposed a second T02 defect: the orphan rejection hard-coded the wording T02 measured, but T02 probed only *sequence* position — in a mapping the second sentence is `A mapping was not expected`, located at the branch body, plus a third sentence dumping the engine's internal reader stack (C-E03-139); the diagnostics are now position-aware and the state dump is deliberately not reproduced. Also measured: the merge is **in place** at the directive's own position and preserves the source object's authored order, unsorted (C-E03-163); the value may be a literal mapping (164); an empty object contributes nothing (165); it works in mappings with well-known schema keys and keeps nested values whole (166); the source may be a loop binding (167); two `${{ insert }}` keys in one mapping both merge in document order (168); a string, sequence, bare scalar or empty value is `Expected a mapping` (172); outside key position the keyword is still *recognized* but cannot act and its text survives verbatim as `Unexpected value '${{ insert }}'` — proven not to be a failed expression parse by C-E03-151's differently-shaped `Unrecognized value: 'index'` control, and **handed to E03-S01-T05 as a rule: never evaluate a lone `${{ insert }}`** (173); and `- ${{ insert }}: <obj>` is still a mapping-key insertion, into the one-key mapping the *item* is, so it merges rather than splicing into the parent sequence (174). Six mutations turn the suite red — case-sensitive collision compare (1), collision-as-overwrite (5), splice-instead-of-merge (1), reverting the chain-break fix (5), position-independent orphan wording (6), append-instead-of-in-place (4) — and one of them is the reason an extra probe exists: the splice mutation initially left the whole suite **green**, because the single-key `sequence-position` object cannot distinguish merging from splicing, so `sequence-position-valid` supplies two keys forming one valid step and the service returned one step (C-E03-174). That pair is committed but is not a byte-golden, because the service desugars `script:` into `task: CmdLine@2` and the E03-S05-T01 normalizer deliberately does not desugar; the test asserts its structure and names the layer that owes the rest. One stale assumption corrected mid-task: the suite was first written to skip the two-insert fixture as blocked on E01-S01-T04, which had in fact landed on 2026-08-14. Docs updated per rule 5: docs/02 §4 (both the one-sentence `insert` paragraph and the conditional-chain list), docs/06 §5 decision 33 with decision 27's open question marked settled, REFERENCES.md (template-expressions "Insertion" row + the fork's `TemplateUnraveler`/`TemplateEvaluator` insert permalinks) (evidence: research/E03-template-engine.md C-E03-138/139 + C-E03-160..174; research/experiments/E03-insert/ 32 probes; fixtures/oracle/directives/insert-* 13 pairs (the other 19 probes are rejections, asserted against their transcripts); packages/engine/test/template/insert.test.ts 45 tests; suite 1,300 Vitest + 162 Bats green, build/typecheck/lint/prettier/shellcheck/VERIFY-guard clean)
- 2026-08-19 E03-S01-T04 rebase reconciliation — rebased `claude/e03-s01-t04` onto `main` after `b03c1f3` had reconciled the two parallel E03-S01-T02 implementations by **deleting** `conditional.ts` in favour of a differently-architected `conditionals.ts`. The branch was written against the deleted module, so the merge was a modify/delete on the file plus six textual conflicts. **`conditionals.ts` is kept as the trunk** and T04's two chain findings were ported onto it rather than reviving the backward-scan module — the trunk's per-container forward state machine matched 37/37 oracle pairs where the backward scan matched 34/37 (docs/06 §5 decision 27), so reviving it to preserve this branch's shape would have re-broken three fixtures to fix two. Ported: an intervening `each`/`insert` now **ends** the containing chain (previously only `else` did), and the orphan rejection's second sentence is parent-shape dependent. Three consequences the port forced, each mutation-checked: (1) `conditionalVisitor` is now a composable visitor and `expandConditionals` is that visitor composed with one that freezes the directives it does not own, so the standalone T02 pass and the composed T04 pipeline are one implementation; (2) it must be composed **first** — a pass that never reaches the `insert`/`each` site cannot know the chain was broken, and composing it last turns 5 tests red; (3) chain rejections became accumulated **diagnostics** rather than a thrown `ConditionalChainError`, matching `walk.ts`'s documented rule that the service reports every rejection in one response (C-E02-110), which retired that class. `TemplateVisitContext` gained `report()` so a visitor can reach the walk's diagnostic list. Conditions now parse with the frame's names and bindings in scope, which is a no-op at the root and is what lets a condition read an enclosing `each` variable once the two are composed. **Claim IDs reassigned:** the branch's `C-E03-135/136` collided with the reconciled T02 survey, which already held 135..137, so they are now `C-E03-138/139` — the T02 block is full. docs/06 §5 decision 28 → 33 for the same reason (28..32 were taken by E06). (evidence: `packages/engine/src/template/conditionals.ts`; `packages/engine/test/template/insert.test.ts` describe blocks `C-E03-138`/`C-E03-139`; mutations — no-op chain break 5 red, position-independent orphan wording 6 red, conditional visitor composed last 5 red; suite 1,300 Vitest + 162 Bats green, build/typecheck/eslint/prettier/shellcheck/VERIFY-guard clean)
2026-08-19 E03-S01-T05 in-progress — claimed by Orchestrator (Claude) on worktree `/root/wt-claude`, branch `claude/e03-s01-t04`; inherits E03-S01-T04's rule that a lone `${{ insert }}` is never evaluated (C-E03-173) and owns replacing the `fixtureScalars` stand-in in the shared fixture harness (evidence: pending)
- 2026-08-19 E03-S01-T05 done — scalar interpolation: `packages/engine/src/template/interpolate.ts` (`interpolationVisitor`) on T01's scalar seam, plus `scripts/interpolation-survey.ts` (`pnpm interpolation-survey`). 34 live probes (`research/experiments/E03-interpolation/`, C-E03-175..194) produced 27 committed input/`finalYaml` pairs and 7 rejections asserted against their transcripts. **The two rules the task's Ground field flags as needing probes turned out to be the documented ones** — the expressions page states `Null → ''` and Boolean → `'True'`/`'False'` verbatim (C-E03-175), so the casing question was answered by reading rather than measuring; the *float* half of the same field was worth every probe, because the Number row of that table is **false as written** ("no thousands separator and no decimal separator" — taken literally `0.5` renders `05`). Measured, rendering is shortest-round-trip invariant: `0.5`→`0.5`, `1.0`→**`1`**, `1000000`→`1000000`, `-1.25`→`-1.25`, all four reproduced by `convertValue(v,'string')`, so interpolation reuses `format`'s conversion exactly as C-E02-109 predicted rather than defining a second one (C-E03-182). Three undocumented findings decided the code. **(1) The lone/mixed boundary is whitespace-sensitive, and T01's `loneExpression` was wrong** (C-E03-180): `env: '  ${{ parameters.envVars }}  '` is mixed content and rejects `Unable to convert from Object to String. Value: Object`, while the *same expression double-quoted without padding* inserts structurally, and the positive control `"  ${{ 'x' }}  "` comes back `'  x  '` with the spaces intact — so loneness is a property of the raw text, not of YAML style and not of the text modulo whitespace. The trim is removed; it had been invisible for four tasks because YAML strips plain scalars itself, so only a quoted host can carry padding into the engine, which is why that probe had to be written that way. **(2) Every scalar result is converted to its String form; only Object and Array stay structural** (C-E03-183) — the proof is Null, which renders `''` in *lone* position, byte-identical to `${{ '' }}`, so a lone expression does not simply hand its typed result to the emitter. **(3) Keys run through the same split but have no structural option, and their spelling is `True`** (C-E03-190/191/192): a *lone* collection key rejects `Expected a scalar value` while the *same object in mixed key content* rejects with the conversion sentence — one rule cannot produce two sentences, which is what proves the split is shared rather than keys having a rule of their own. That closes the docs/02 §8 ambiguity list's "Boolean stringification casing in keys" entry, confirmed independently by the service's own `Unexpected value 'True'` when the key is written into a schema-checked mapping. Also measured: structural insertion is whole and typed at every depth (177/179); in sequence position an Array **splices** and an Object becomes **one item** (178), the only place a scalar's replacement splices, now in `walkSequence`; a collection in a string position is an accumulated diagnostic with an **empty-string substitution**, not a throw — the service's own response carried the conversion sentence *and* the schema's follow-on `Unexpected value ''` (187); the result is never re-parsed as YAML and the documented `${{` escape survives verbatim, so there is exactly one pass (185/188); a block scalar interpolates as one scalar (189); and a lone directive keyword in value position is never evaluated (194, inherited from T04's C-E03-173 rather than re-probed). Seams: `TemplateVisitor.scalar` gained a `TemplateScalarContext` carrying `position` and `report`, because key-vs-value is a real distinction and only the walk knows it; the unchecked `as ScalarNode` cast on the key path is gone. **The `fixtureScalars` stand-in is deleted** and all four E03-S01 suites now run the real pass — two of its shortcuts were exactly the bugs above. Eight mutations turn the suite red (trim restored 4, Null→null node 2, no sequence flatten 1, keys structural 3, directive keyword evaluated 2, lower-case Boolean 6, mixed content trimmed 5, no empty-string substitution 1). **A defect in a neighbouring layer was found and filed rather than patched here:** `normalizeExpandedYaml` compares scalar leaves *after* the YAML parser types them, so the service's `PROBE: 0123` reads back as the Number 123 while our `"0123"` stays four characters — two spellings of one value reported as drift, and the same for the key `True` (C-E03-193). The fix belongs to the normalizer, so it is added as **E03-S05-T03** with the two affected pairs asserted against raw `finalYaml` meanwhile, rather than quietly widening N7 from this branch. Docs updated per rule 5: docs/02 §3 rewritten and §8's ambiguity list pruned, docs/06 §5 decision 34, REFERENCES.md (the expressions page's false Number row; the template-expressions page's array-only structural sentence) (evidence: research/E03-template-engine.md C-E03-175..194; research/experiments/E03-interpolation/ 34 probes; fixtures/oracle/directives/interp-* 27 pairs; packages/engine/test/template/interpolate.test.ts 70 tests; suite 1,370 Vitest + 162 Bats green, build/typecheck/eslint/prettier/shellcheck/VERIFY-guard clean)
2026-08-19 E03-S02-T01 in-progress — claimed by Orchestrator (Claude) on worktree `/root/wt-claude`, branch `claude/e03-s01-t04`; builds on E03-S01-T01's `childFrame`/`depth` seam in `walk.ts`. Claim block C-E03-195..229 (evidence: pending)
- 2026-08-20 E03-S02-T01 done — reference resolution: `packages/engine/src/template/reference.ts` (path math, `(repository, commit, path)` identity, injected `TemplateFetcher` with a local-FS impl; E08 supplies the remote one), plus `scripts/reference-survey.ts` (`pnpm reference-survey`) and a second oracle repository created/authorized by `scripts/oracle-provision.ts`. 34 live probes (`research/experiments/E03-references/`, C-E03-195..218) against a **two-repository** fixture — 21 expanded, 13 rejected. The two repos are the whole point: every `@alias` question is "which repository was this path read from", and one repo cannot tell the answers apart. The named docs settle the syntax (absolute-vs-relative, `@alias`, `@self`, alias charset `[-_A-Za-z0-9]*`, `ref` default `refs/heads/main`, and "repositories are resolved only once") and **none** of the semantics the resolver has to encode. Three measured findings decided the code, all three contradicting the obvious implementation. **(1) A repository switch resets the base directory to the target repo's root (C-E03-215)** — the task's "per-file base dir" holds only *within* one repository. This took three purpose-built fixtures, because every spelling written from a root pipeline file passes under either model (at `/azure-pipelines.yml` the base *is* the root): `../leaf.yml@self` from a subdirectory resolves (same repo → base kept, so an explicit alias is not itself a switch), `../e03-refs/leaf.yml@self` from inside the templates repo is rejected `The file path /../e03-refs/leaf.yml is invalid` (switch → base discarded), and `cross/leaf.yml@templates` from `/e03-refs/dir/` reaches `/cross/leaf.yml` rather than `/e03-refs/dir/cross/leaf.yml`. The resolved repository decides, not the alias text. **(2) The service has no cycle detection (C-E03-208)** — a self-including and a mutual a→b→a template both die of `Maximum object depth exceeded`, which is *none* of the three documented limits (100 files / 100 nesting / 20 MB, C-E03-199). We cannot reproduce the mechanism (recursing until the stack blows is a hang, not a behavior), so the resolver detects the repeat over the **active** stack and emits that sentence at the file the service attributes it to — the *repeated* file, matching both probes. Active-stack rather than visited-set is load-bearing: a diamond is legal and expands twice (C-E03-209). Divergence is in the mechanism only, recorded deliberately; E03-S04-T01 inherits reconciling the documented limits with the emitted message. **(3) The alias splits on the *first* `@` (C-E03-210)** — `a.yml@self@self` asks for a repository named `self@self`, and a file genuinely named `we@ird.yml` is unreachable (probed with the file present: `No repository found by name ird.yml`). `lastIndexOf('@')` reproduces neither. Also measured: repository context stays switched inside a cross-repo template for bare *and* absolute paths (C-E03-216); `@self` is the definition's repo from anywhere, including from inside another repository (C-E03-214); alias lookup folds case while path lookup does **not** (C-E03-213/204); an empty alias means `self` (C-E03-212); the reference is never trimmed (C-E03-205); `./` is ordinary and `\` normalizes to `/` (C-E03-202/203); the escape check is on the *result* so a traversal may dip and recover (C-E03-206); the rejection prints the **unnormalized** join, the only reason `//../outside.yml` and `/../e03-refs/leaf.yml` differ (C-E03-200); the not-found sentence names repository URL, branch and pinned commit, and the same commit recurs across a run, which independently confirms "resolved only once" (C-E03-207); and a cross-repo file is *named* `<path>@<alias>` in diagnostics (C-E03-217). **The fixture trees are committed files** (`fixtures/oracle/references/repos/{self,templates}/`) that the survey script pushes and the tests mount as local repositories, so the suite replays all 33 in-scope probes against the service's own `finalYaml` instead of a copy of the tree — re-running the survey after editing a fixture reports the tree as changed, which is the drift alarm. The 34th probe (`alias-undeclared-repo`) is excluded with a reason: it fails while resolving *resources*, before any reference is read, and belongs to E08. Docs updated per rule 5: docs/02 §5 (the per-file bullet stated only half the rule; cycles added), docs/06 §5 decision 35, REFERENCES.md templates-page row re-verified (evidence: research/E03-template-engine.md C-E03-195..218; research/experiments/E03-references/ 34 probes; fixtures/oracle/references/ 21 pairs + 2 repo trees; packages/engine/test/template/reference.test.ts 69 tests; suite 1,439 Vitest + 162 Bats green, build/typecheck/eslint/prettier/shellcheck/VERIFY-guard clean)
2026-08-20 E03-S02-T02 in-progress — claimed by worker `claude` on worktree `/root/wt-claude`, branch `claude/e03-s01-t04`; typed parameter binding, claim block C-E03-300..339 (evidence: pending)
2026-08-21 E06-S04-T03 done — `task.prependpath`, `task.setsecret`, `task.complete`, `task.logissue`, `task.setprogress`, `task.debug` handlers plus `azdo_render_stream` ANSI rendering of the formatting commands; `run_step` gained per-attempt command state, the grounded result precedence (complete merges → exit status assigns → command failures merge → continueOnError downgrades), retry-while-exactly-Failed, and per-step issue counters under `results/issues/`. Corrected two docs/04 statements — the §6 table rows and, on re-verification, the §5 step-6 result-precedence summary that still echoed the old model (docs/06 §5 decision 36): issue counts alone do not change a step result, and `System.Debug` gates the debug *channel* rather than filtering echoed `##[debug]` lines — hiding them from the console is a recorded local decision, with logs kept verbatim. Fixed a T02 defect where a failing handler aborted the output reader and dropped the rest of a step's console/log output, and a `azdo__logging_fold` local-name shadowing bug. The command-failure message deliberately narrows the agent's shape — it names `area.action` instead of interpolating the wire line, because a handler that rejects a value fails before registering it with the masker, which runs downstream of dispatch. No hosted run: no oracle credentials in this environment. (evidence: research/E06-runtime.md C-E06-057..068; docs/04 §6 table rows + docs/06 §5 decision 36; packages/runtime/test/core.bats cases 42–55, all 55 green (C-E06-068 added on a reverse code→claim check: the `task.issue` dispatch arm was correct but uncited, and that alias appears only in the agent source, not the doc page); suite 175/176 Bats (the 176th case is C-E06-068’s, added after this line was first written), build/typecheck/eslint/prettier/shellcheck/VERIFY-guard clean. The one red case is **environmental, not ours**: `fixture-store.bats` "preserves the executable bit" (E12-S01-T01) asserts `[ -x ]` on a copy under `BATS_TEST_TMPDIR`, and this sandbox mounts `/tmp` `noexec`, so `access(X_OK)` fails while the mode bits are `755` — `findmnt /tmp` reports `noexec` and a two-line `chmod 755` repro under `/tmp` reproduces it with no runtime code loaded. Left unpatched: the mode-bit-vs-`access(X_OK)` distinction is E12's to own)
2026-08-21 E06-S04-T04 in-progress — claimed by autonomous Claude worker on worktree /root/wt-claude, branch claude/e06-s04-t03; artifact/attachment/build logging commands, claim block C-E06-069.. (evidence: pending)
2026-08-21 E06-S04-T04 done — the artifact/attachment/build logging commands: `artifact.upload`, `artifact.associate`, `task.addattachment`, `task.uploadfile`, `task.uploadsummary`, `build.uploadlog`, `build.uploadsummary`, `build.updatebuildnumber`, `build.addbuildtag`. Scope was widened past the task's named four on purpose: the three attachment commands are **one function** in the agent (`TaskAddAttachmentCommand.AddAttachment`, with `uploadfile`/`uploadsummary` as calls into it), so shipping a subset would leave the shared helper half-tested, and the two `build.upload*` commands are one case arm each on top of it. Three findings decided the code, each contradicting the obvious implementation. **(1) An empty directory is a counted warning and a success, not a failure** (C-E06-070) — `context.Warning` → `AddIssue(Warning)` then `return`, so the natural port of "nothing to upload" gets the agent exactly backwards. **(2) The container item path is relative to the source's *parent*** (C-E06-071): a file contributes its basename, a directory contributes its *contents* and its own name never appears. **(3) `build.updatebuildnumber` legitimately overwrites a read-only variable** (C-E06-081) — `build.buildNumber` is a member of `Constants.Variables.ReadOnlyVariables`, but the agent enforces read-only in `TaskSetVariableCommand`, not in `Variables.Set`, and the source comment names this command as the reason; `azdo__write_var` was therefore split into a checked front and an `azdo__write_var_unchecked` that still *preserves* the flag, and the test asserts both that a later step sees `BUILD_BUILDNUMBER` and that `task.setvariable` on the same name is still refused. **Two docs/04 §6 rows were corrected rather than implemented (rule 5):** there is no `task.uploadartifact` command on the doc page or in the agent, and `build.uploadlog` is a real attachment command (fixed name `CustomToolLog`), not "ignored with a debug note"; `build.uploadsummary` exists too, deprecated and undocumented. The layout decision is docs/06 §5 decision 37: **`.artifacts/` is keyed by artifact *name*** — what E06-S05-T01's `$(Pipeline.Workspace)/<name>` download asks for — with `containerfolder` recorded in `.artifacts/.meta/<artifactname>` rather than becoming a directory level, because a download of a container artifact yields the folder's contents, not the folder. Two smaller local decisions recorded there: attachment type segments use `Distributedtask.Core.Summary` (the only `CoreAttachmentType` value with a citable source — the doc's own shorthand, which makes the shorthand identity testable) and the C# member names `FileAttachment`/`Log` for the two whose wire values live in the closed WebApi assembly, rather than inventing spellings; and `artifact.associate` is accepted-validated-recorded on the `task.setprogress` precedent instead of the unknown-command path, since its message is a server-side coordinate with nothing to materialize. Also grounded and deliberately *not* reproduced: .NET's platform-dependent `Path.GetInvalidFileNameChars()` guard (the narrower `azdo__valid_store_segment` is reused, C-E06-076), the doc's server-side "no colon in a build tag" rule (C-E06-082), and four host-type/ArgUtil/async-queue preconditions (C-E06-083). Thirteen mutations turn the suite red — containerfolder default removed, artifacts keyed by container folder, empty-directory-as-failure, directory name included, checked write for updatebuildnumber, read-only flag dropped, trimming removed, case-sensitive tag dedup, uploadfile/uploadlog name derivations, attachment accepting a directory, and the directory branch's `TrimEnd(separator)` — the last of those found late, in review: without it `##vso[artifact.upload artifactname=drop]$(Build.ArtifactStagingDirectory)/` leaves the prefix strip unmatched and silently nests the whole absolute path inside the artifact, and the agent's own quoted line is a `TrimEnd` that belongs only to the directory branch (the file branch takes `Path.GetDirectoryName`, so trimming earlier would accept `file.txt/`) — with a no-op control (`cp file dir/` vs `cp file dir/name`) staying green. **Two red gates are inherited, not introduced, and both are filed:** (a) three `core.bats` cases are red **on `origin/main` too** — the merge of the two parallel E06-S04 implementations left duplicate test blocks with conflicting claim numbering (C-E06-057 means `setsecret` in one and `prependpath` in the other), two ANSI-rendering expectations the trunk renderer cannot satisfy, and two signatures of `azdo_step_issue_count`; filed as **E06-S04-T05** with the reconciliation precedent named. (b) `pnpm test:unit` fails its coverage thresholds on this branch only: crash-recovery commit `f76a47b` swept in an unfinished, unimported, untested 755-line `packages/engine/src/template/parameters.ts` from the abandoned 2026-08-20 E03-S02-T02 session, ~247 uncovered statements; noted on that task, left in place (deleting another session's WIP is not this task's call) and thresholds **not** lowered. The same sweep also brought `scripts/parameter-binding-survey.ts` plus ~60 committed E03-parameters probes and fixture pairs, and that script broke `pnpm lint:es` (4 `no-useless-escape`) and `pnpm typecheck` (`exactOptionalPropertyTypes` on an optional `templateParameters`) repo-wide; both are **fixed here** because they are provably semantics-free — `"\$"` and `"$"` are the same double-quoted string (the first blanket `sed` attempt was reverted after it also hit a template literal, where the escape is real), and the probe factory now spreads the optional field instead of writing `undefined` into it. Build, typecheck, eslint, prettier and shellcheck are green on the branch as a result; only the coverage thresholds and the three E06-S04-T05 cases remain red. Also recorded: E03's checkboxes understate the epic by five finished tasks on `main` as well as here, so a cold "next unchecked task" pick lands on finished work — noted at the top of the epic file. No hosted run: no oracle credentials in this environment. (evidence: research/E06-runtime.md C-E06-069..083; docs/04 §1 + §6 five rewritten rows; docs/06 §5 decision 37; research/REFERENCES.md agent + logging-commands rows re-verified; packages/runtime/test/core.bats 12 new cases, 69/72 green — the 3 reds are the inherited E06-S04-T05 set; 1,439/1,439 Vitest; shellcheck + prettier clean)
2026-08-21 E06-S04-T05 in-progress — claimed by Claude on claude/e06-s04-t03 (/root/wt-claude) (evidence: pending).
2026-08-21 E06-S04-T05 done — reconciled the two merged logging-command test suites in favour of the trunk implementation, deleting the five orphaned cases whose conflicting claim numbering (C-E06-057 as `setsecret` vs `prependpath`) and three reds came in through the two `accept theirs on conflicts` merges. The reconciliation is smaller than the task assumed and the check that proves it is the one a green suite cannot make: **only the tests were duplicated.** `packages/runtime/lib/core.sh` has no duplicate function definition, one renderer and one dispatch arm per command, so the deleted block was never a rival implementation — it was tests written against one that never landed, which is also why its `azdo_step_issue_count <step> <kind>` signature does not exist in trunk. The two reds resolve on grounds the epic's own **Ground** field names before any code is read: `path.d` newest-wins is C-E06-012/057 and the losing assertion (two files under `path.d`) measured a storage shape, not the resulting `PATH`; and C-E06-066 makes the formatting commands message-only with the hosted agent emitting tags alone, so **no source prescribes escape sequences** — the losing tests asserted a particular tag-preserving `\033[1;33m` sequence, i.e. a local decision (recorded in decision 36) rather than parity, against a renderer that strips the tag prefix and needs `AZDO_COLOR=always`. **Two assertions were ported, not dropped,** because they were the losing block's only non-redundant content: `task.complete result=SucceededWithIssues` alone yields `SucceededWithIssues` at exit 0, and execution continues past the `task.complete` line; both now live in the trunk precedence case. Claim-citation coverage was diffed mechanically (cited IDs before vs after) rather than eyeballed, which turned up a pre-existing gap unrelated to the merge — C-E06-067 (`task.setprogress`) had no test citing it by ID although the debug-channel case asserts its ignore note — and that citation is added. The task's "renumber the losing claim block" step is a no-op and is recorded as such so a future reader does not hunt for a phantom block: `research/E06-runtime.md` carries exactly one entry per ID and already used the trunk numbering, so the conflict lived only in the deleted test titles. **Two inherited reds remain, both out of this task's scope and both already filed:** `pnpm test:unit` passes 1,439/1,439 but fails its coverage thresholds on the unimported, untested 755-line `packages/engine/src/template/parameters.ts` swept in by crash-recovery commit `f76a47b` (noted on E03-S02-T02; thresholds not lowered), and `bats test` shows 187/188 because `/tmp` is mounted `noexec` in this environment, so the fixture-store exec-bit case fails on the mount, not the code. No hosted run: no oracle credentials in this environment; no new sources were consulted, so `research/REFERENCES.md` is unchanged by design — this task cites only existing claims. (evidence: docs/06 §5 decision 38; `packages/runtime/test/core.bats` 67/67 green, 5 cases deleted, 2 assertions ported, 1 citation added; 1,439/1,439 Vitest; build/typecheck/ESLint/Prettier/ShellCheck clean)
2026-08-21 E06-S04-T05 follow-up — closed two coverage holes the claim-ID diff structurally could not see, and corrected the docs/06 §5 decision 38 sentence that had leaned on it. Diffing cited claim IDs proves every claim still has a *reference* in some test title; it says nothing about a lib branch whose only exercising case was deleted while the ID stays cited by a different test. Two branches were dark after the delete: `azdo_step_issue_count` lost all three of its direct callers (all three lived in the deleted block; it survived only transitively via `azdo_step_issues`, so its arity and unknown-kind guards were untested), and `azdo_render_stream`'s `debug` arm lost the only input reaching it under color — the trunk formatting case feeds group/warning/error/section/command/endgroup/ordinary and no `##[debug]`, and the raw-debug case asserts uncolored output. Direct coverage added to `command state is per step and per job scope` and `formatting commands render with ANSI only when color is enabled`; the latter also pins the arm's real shape, which is that the debug tag is the one tag the renderer drops *entirely* when the channel is closed, independently of color. Both holes verified by mutation rather than by inspection: `\033[2m`→`\033[3m` and the `error` arm reading `warnings` each turn the suite red, and `lib/core.sh` is byte-identical after the reverts. (evidence: `packages/runtime/test/core.bats` 67/67 green with 2 mutations confirmed red; docs/06 §5 decision 38 amended)
2026-08-21 E06-S05-T01 in-progress — claimed by Claude on claude/e06-s04-t03 (/root/wt-claude); pipeline artifact publish/download, claim block C-E06-084.. (evidence: pending).
2026-08-21 E06-S05-T01 done — `azdo_artifact_publish` / `azdo_artifact_download` plus the deployment-job `azdo_artifact_auto_download` injection point, over the same run-level `.artifacts/<name>/` store `artifact.upload` writes (decision 37). The first finding is where the *implementation* is: both tasks are **`AgentPlugin` tasks**, so `task.json` carries only inputs and the behavior lives in `src/Agent.Plugins/PipelineArtifact/PipelineArtifactPlugin{V1,V2}.cs` — and specifically **not** in `PipelineArtifactPlugin.cs`, which holds the older V0 classes of the same shape. **Three findings contradicted the task as written.** (1) The **Do** field's download target `$(Pipeline.Workspace)/<name>` is the `download:` **keyword's** layout (C-E06-084), not the task's: `DownloadPipelineArtifact@2` defaults `path` to `$(Pipeline.Workspace)` and appends the artifact-name segment **only** on the no-name branch (C-E06-085/086/087), so building the segment into the runtime function would emit `$(Pipeline.Workspace)/<name>/<name>` for every pipeline that writes the task explicitly with its documented default. The function reproduces the task; the emitter passes the keyword layout through `--path`, and the test asserts both layouts side by side (docs/06 §5 decision 39; docs/04 §7 corrected per rule 5). (2) A relative `path`/`targetPath` resolves against **`System.DefaultWorkingDirectory`**, not the pipeline workspace — the plugin source against its own task.json help text, decided by BACKLOG §3's source hierarchy and observable because the two directories differ (C-E06-089). (3) Pattern lists are **order-sensitive**: the agent applies each pattern to an accumulating map, so `!*.md` followed by `**` selects *everything*, which the obvious "match all includes, then subtract all excludes" implementation cannot reproduce (C-E06-090); the list splits on newlines only, so a `;`-joined value is one literal pattern (C-E06-088), and `#` is read as a comment *before* the `!` prefix, with an even number of `!` an include. Also grounded and deliberately not implemented: `.artifactignore` and the implicit `.git` exclusion are server/BlobStore-side (C-E06-095), and the minimatch subset stops at `**`/`*`/`?` — braces and bracket classes are matched literally with a `degraded` note rather than guessed at. Three local decisions are recorded in decision 39: the artifact-name guard is the **union** of the agent's forbidden set and `azdo__valid_store_segment` (which additionally rejects `""`, `.` and `..` — names the agent accepts but a directory under `.artifacts/` cannot be); an associated artifact fails a *named* download and is skipped with a debug note on the multi-download branch, where "no files" is explicitly not an error; and `--source specific` is refused rather than silently served from the current run (E08 owns it). The file-source rule (a file contributes its basename) is an **inference** from the container rule C-E06-071, flagged in C-E06-094 because the pipeline-artifact path runs through the closed BlobStore client — a later oracle run can falsify it. Verified by mutation rather than by a green run: **12 mutations each turn the suite red** — `.default` fallback removal, `*` crossing separators, named download appending the name, unnamed download flattening it, `;` splitting, sticky excludes, `#` as an ordinary character, `!` without the toggle, the forbidden-character set dropped, relative paths resolved against the workspace, publish reusing `artifact.upload`'s empty-directory warning, and a missing artifact downloading silently. One process note worth keeping: an earlier mutation batch timed out mid-run and left a mutation applied, which a later batch then backed up as its baseline — every result in that batch was invalid. Mutation work was redone against a **committed** baseline restored with `git checkout`. No hosted run: no oracle credentials in this environment. (evidence: research/E06-runtime.md C-E06-084..096; docs/06 §5 decision 39; docs/04 §7 four rewritten bullets; research/REFERENCES.md pipeline-artifacts + steps-download + agent `42bde98b` + tasks `299572e2` rows; `packages/runtime/test/core.bats` 10 new cases, 77/77 green in that file and 197/198 for `bats test` — the one red is the environmental `/tmp` `noexec` exec-bit case; ShellCheck, ESLint and Prettier clean; `scripts/check-verify-markers.sh --all` clean; Vitest 1,439/1,439 pass — `pnpm test:unit` still exits non-zero on the **pre-existing** engine coverage-threshold shortfall, untouched by this task, which changed no TypeScript)
2026-08-21 E06-S05-T02 in-progress — claimed by Claude on claude/e06-s04-t03 (/root/wt-claude); checkout self modes (clone/copy/worktree) + option matrix, claim block C-E06-097.. (evidence: pending).
2026-08-22 E06-S05-T02 done — `azdo_checkout` for the `self` repository in three modes with the full option matrix and `Build.*` seeding. The first correction is to the task's own **Ground** field: git-scm.com tells you what `--depth`, `--tags` and `-ffdx` *do*, not which flags the agent emits for each YAML option, so the mapping was read from **`src/Agent.Plugins/GitCliManager.cs`** (each method carries its literal command line in a comment) and `GitSourceProvider.cs` at the `42bde98b…` pin already in REFERENCES, with git-scm cited for flag *meaning* — BACKLOG §3's source hierarchy, the same call as C-E06-089. **The headline finding is that `fetchTags: false` does not stop tags from being fetched.** The agent's fetch line carries `--no-tags` **and** `--prune-tags` (C-E06-097), and git documents `--prune-tags` as "a shorthand for providing the explicit tag refspec", "equivalent to having `refs/tags/*:refs/tags/*` declared in the refspecs" — an explicit refspec beats `--no-tags`' automatic-tag-following suppression — while the knob that would drop it, `DisableFetchPruneTags`, has a built-in default of `false` (C-E06-111). Reproduced locally: `--no-tags --prune --prune-tags` brings the tag down anyway. The `steps-checkout` page's entire "Sync tags" section therefore oversells the option; the runtime emits the agent's command line and states the discrepancy under `System.Debug` rather than inventing a divergence to match doc prose. **Three of the six options have no YAML-recoverable default at all:** `clean`, `fetchTags` and shallow fetch are *pipeline-settings-UI* values, and `fetchTags`/shallow additionally depend on whether the pipeline predates sprint 209 — facts a converter reading a file cannot know. Decision 40 uses the agent's own empty-input defaults (`clean=false`, `fetchDepth=0`, `fetchTags=true`, C-E06-099/100) because they are the only values with a citable source for the exact situation the emulator is in, and are also the conservative pair. **Two further **Do**-field corrections.** (1) `clone` is not a reference clone: the agent does `git init` → `remote add origin` → `fetch` → `checkout --force <commit>`, ending detached (C-E06-102), and git *silently ignores* `--depth` for a local-path remote — warns, exits 0, full history (experiment `research/experiments/E06-checkout/local-shallow-clone.md`, C-E06-109) — so a reference clone would have accepted `fetchDepth` and done nothing with it; the remote is `file://<source>`. (2) `copy` uses `tar`, not `rsync`: rsync is absent from this container and unguaranteed in the target images, and a two-arm fallback would have left the untestable arm untested in the one function that empties a directory. Also grounded: `ConvertToBoolean` recognizes only `1/true/$true` and `0/false/$false` and silently defaults everything else, so `clean: yes` does not clean while `fetchTags: yes` does sync tags (C-E06-100); `clean` is `git clean -ffdx` **then** `reset --hard HEAD` (C-E06-101); `lfs: false` exports `GIT_LFS_SKIP_SMUDGE=1` to every git command and a failing LFS fetch is a *warning* the agent continues past (C-E06-104) — which is what the "lfs skipped if unavailable w/ warning" criterion rests on; `Build.SourceBranchName` is the ref's **last path segment**, so `refs/heads/feature/tools` is `tools` (C-E06-106). Local decisions in 40: `copy` **refuses** `clean: true` because it would delete the uncommitted work the mode exists to run; every no-op cell in the mode×option table announces itself instead of staying silent; the target path must resolve inside `$(Pipeline.Workspace)` (local hardening beyond the agent's opt-in variable, because `clean` and `copy` both delete inside it); and `Build.*` are seeded-never-overwritten with `Build.SourceVersion` read *before* the checkout so an `.env` override actually selects the commit. **`submodules` turned out to be broken, not merely untested.** The first pass shipped a parser test and no integration test for it, and the option fails outright in `clone` mode: since CVE-2022-39253 git refuses the `file` transport for *submodule* clones, and 40(b) makes the origin `file://` by construction, so `git submodule update --init` dies with "transport 'file' not allowed" (measured on git 2.47.3, C-E06-112). The restriction is a consequence of the emulator's own `file://` decision — the hosted agent's submodule URLs are https and never meet it — so it is relaxed with `-c protocol.file.allow=always` on `submodule sync`/`update`, recorded as decision **40f** and labelled a deliberate widening rather than parity. `submodule foreach` clones nothing and is left alone. Two integration cases now cover it: `true` takes one level while `recursive` reaches a nested submodule (the only assertion separating the `--recursive` arms), and `clean` with submodules enters the `foreach` pair that `git clean -ffdx` on the outer repo cannot reach. Verified by mutation against a **committed** baseline: **23 mutations each turn the suite red**, including the `file://` URL, `--prune-tags`, the ConvertToBoolean fallback, a negative fetch depth, `--unshallow`, each half of `clean`, the copy-mode clean refusal and its note text, the seed-never-overwrite guard, `worktree --detach`, the workspace guard, its *ordering* against `mkdir -p` (the guard used to create the directory before refusing the path), `GIT_LFS_SKIP_SMUDGE`, the branch-name segment rule, the copy-target wipe, the read-before-checkout ordering, the `s/` default, and four on the submodule path — the `protocol.file.allow` relaxation, `--init`, `--recursive` and the `foreach` pair. No mutation is claimed on `submodule sync`: it is a no-op on a fresh clone and would survive. A first mutation pass left **three survivors** — the copy/clean note (the assertion only saw the shared `(degraded)` suffix), the seed guard (the override test re-read values the checkout would have written back byte-identically) and `worktree --detach` (redundant while the committish is a SHA) — each fixed by strengthening the test, the last with a case where `.env` sets `Build.SourceVersion` to a *branch name*, which is when `--detach` earns its place. Out of scope and reported rather than dropped: multi-checkout layout (T03), `persistCredentials` (E08), `fetchFilter` and the two `sparseCheckout*` inputs (C-E06-110). No hosted run: no oracle credentials in this environment. (evidence: research/E06-runtime.md C-E06-097..111; research/experiments/E06-checkout/local-shallow-clone.md; docs/06 §5 decision 40; docs/04 §8 rewritten with the mode×option table; research/REFERENCES.md steps-checkout + predefined-variables + agent `42bde98b` + git-scm `git-fetch` rows; docs/06 §5 decision 40f and C-E06-112/113; `packages/runtime/test/core.bats` 14 new cases, 92/92 green in that file and 212/213 for `bats test` — the one red is the environmental `/tmp` `noexec` exec-bit case; ShellCheck, ESLint and Prettier clean; `scripts/check-verify-markers.sh --all` clean; Vitest 1,439/1,439 pass — `pnpm test:unit` still exits non-zero on the **pre-existing** engine coverage-threshold shortfall, untouched by this task, which changed no TypeScript)
2026-08-22 E06-S05-T03 done — implemented the multi-checkout layout (single → `s`, multiple → `s/<repoName>` including self, `path:` overriding both and rooted at Pipeline.Workspace), the `GetCloneDirectory` port that names those folders, non-`self` repository checkouts, and the two path variables' non-obvious seeding: `Build.SourcesDirectory` and `Build.Repository.LocalPath` both stay at `s` under multi-checkout even though self's files moved one level down, and only a `path` differing from the default moves `LocalPath` (evidence: `research/E06-runtime.md` C-E06-114..121; `packages/runtime/test/core.bats` 11 new cases, 19 mutations confirmed red; docs/04 §8.1 + docs/06 §5 decision 41; 223/224 Bats — the one red is the pre-existing `/tmp`-noexec exec-bit fixture case; 1,438/1,439 Vitest — the one red is a pre-existing 5-second-timeout flake in `packages/engine/test/schema-vendor.test.ts`, confirmed on a stashed clean tree and untouched by this task; ESLint/Prettier/ShellCheck/VERIFY guard clean).
