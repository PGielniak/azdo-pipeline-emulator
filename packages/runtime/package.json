{
  "name": "@azdo-emu/runtime",
  "version": "0.0.0",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "description": "Bash runtime library sourced by emitted pipeline scripts (bash >= 4)",
  "files": [
    "lib"
  ],
  "scripts": {
    "test": "bats test",
    "lint": "\"${SHELLCHECK:-shellcheck}\" lib/*.sh test/helpers/*.bash"
  },
  "devDependencies": {
    "bats": "^1.13.0",
    "shellcheck": "^4.1.0"
  }
}
