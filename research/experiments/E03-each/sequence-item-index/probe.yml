parameters:
- name: items
  type: object
  default: [alpha, beta]
stages:
- stage: probe
  jobs:
  - job: probe
    steps:
      - ${{ each item in parameters.items }}:
        - task: CmdLine@2
          inputs:
            script: echo ${{ item }}:${{ item.index }}
