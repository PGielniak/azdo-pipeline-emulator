stages:
- stage: __default
  jobs:
  - job: Job
    steps:
    - task: CmdLine@2
      env:
        OUT: >-
          [
            "a",
            "b"
          ]
      inputs:
        script: echo probe

