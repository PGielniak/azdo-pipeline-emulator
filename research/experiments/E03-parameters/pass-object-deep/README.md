# oracle probe — pass-object-deep

The task's third named probe: a deep object — nested mappings, a sequence, and mixed scalar types — passed to an `object` parameter. Does every leaf keep its YAML type?

- Endpoint: `POST {org}/{project}/_apis/pipelines/{pipelineId}/preview?api-version=7.1`
- Outcome: **HTTP 200 · expanded**
- Outcome was **not** predicted by this script.
