# oracle probe — pass-missing-required

The other error the task names: a template parameter with no default, not passed by the caller — while the caller passes *something*, so the `parameters:` mapping exists.

- Endpoint: `POST {org}/{project}/_apis/pipelines/{pipelineId}/preview?api-version=7.1`
- Outcome: **HTTP 400 · rejected · typeKey=PipelineValidationException**
- Outcome was **not** predicted by this script.
