# oracle probe — pass-expression

Pass an expression that reads the caller's own parameter — the ordinary way a value crosses two files, and a check that the caller's frame is used, not the callee's.

- Endpoint: `POST {org}/{project}/_apis/pipelines/{pipelineId}/preview?api-version=7.1`
- Outcome: **HTTP 200 · expanded**
- Outcome was **not** predicted by this script.
