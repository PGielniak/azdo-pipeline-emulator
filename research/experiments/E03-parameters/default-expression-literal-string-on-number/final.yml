parameters:
- name: p
  type: number
  default: 42
stages:
- stage: __default
  jobs:
  - job: Job
    steps:
    - task: CmdLine@2
      env:
        OUT: 42
      inputs:
        script: echo probe

