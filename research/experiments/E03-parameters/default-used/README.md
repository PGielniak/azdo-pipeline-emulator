# oracle probe — default-used

Control: an unpassed parameter with a default binds the default, and `convertToJson` shows its type.

- Endpoint: `POST {org}/{project}/_apis/pipelines/{pipelineId}/preview?api-version=7.1`
- Outcome: **HTTP 200 · expanded**
