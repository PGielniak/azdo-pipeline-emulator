parameters:
- name: p
  type: object
  default: {}
stages:
- stage: __default
  jobs:
  - job: Job
    steps:
    - task: CmdLine@2
      env:
        OUT: >-
          {
            "a": 1
          }
      inputs:
        script: echo probe

