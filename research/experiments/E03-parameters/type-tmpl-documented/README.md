# oracle probe — type-tmpl-documented

The documented types inside a template (`deployment`/`stage` singletons omitted — a `steps:` template cannot carry their bodies).

- Endpoint: `POST {org}/{project}/_apis/pipelines/{pipelineId}/preview?api-version=7.1`
- Outcome: **HTTP 200 · expanded**
- Outcome was **not** predicted by this script.
