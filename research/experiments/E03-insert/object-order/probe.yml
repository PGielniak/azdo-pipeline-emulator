parameters:
- name: extra
  type: object
  default:
    ZETA: z
    ALPHA: a
    MIDDLE: m
variables:
  BASE: base
  ${{ insert }}: ${{ parameters.extra }}
stages:
- stage: probe
  jobs:
  - job: probe
    steps:
      - task: CmdLine@2
        inputs:
          script: echo probe
