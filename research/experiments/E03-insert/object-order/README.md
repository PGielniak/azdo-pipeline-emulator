# oracle probe — object-order

Keys authored out of lexical order. Records whether the merge preserves authored order the way `each` over a mapping does (C-E03-145) or sorts them.

- Endpoint: `POST {org}/{project}/_apis/pipelines/{pipelineId}/preview?api-version=7.1`
- Outcome: **HTTP 200 · expanded**
- Outcome was **not** predicted by this script.
