parameters:
- name: a
  type: boolean
  default: false
- name: extra
  type: object
  default:
    MID: mid
variables:
  ${{ insert }}: ${{ parameters.extra }}
  ${{ if parameters.a }}:
    PICK: from-if
  ${{ else }}:
    PICK: from-else
stages:
- stage: probe
  jobs:
  - job: probe
    steps:
      - task: CmdLine@2
        inputs:
          script: echo probe
