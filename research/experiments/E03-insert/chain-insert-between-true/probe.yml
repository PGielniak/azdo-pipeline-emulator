parameters:
- name: a
  type: boolean
  default: true
- name: extra
  type: object
  default:
    MID: mid
variables:
  ${{ if parameters.a }}:
    PICK: from-if
  ${{ insert }}: ${{ parameters.extra }}
  ${{ else }}:
    PICK: from-else
stages:
- stage: probe
  jobs:
  - job: probe
    steps:
      - task: CmdLine@2
        inputs:
          script: echo probe
