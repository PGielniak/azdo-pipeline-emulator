# oracle probe — position

A literal key on **both** sides of the directive. Fixes whether the merged keys land at the directive's own position or are appended to the end of the mapping.

- Endpoint: `POST {org}/{project}/_apis/pipelines/{pipelineId}/preview?api-version=7.1`
- Outcome: **HTTP 200 · expanded**
