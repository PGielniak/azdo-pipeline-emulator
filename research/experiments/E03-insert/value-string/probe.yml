parameters:
- name: extra
  type: string
  default: plain
variables:
  BEFORE: before
  ${{ insert }}: ${{ parameters.extra }}
stages:
- stage: probe
  jobs:
  - job: probe
    steps:
      - task: CmdLine@2
        inputs:
          script: echo probe
