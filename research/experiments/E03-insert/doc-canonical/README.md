# oracle probe — doc-canonical

The template-expressions doc's own example: literal keys plus `${{ insert }}` fed an `object` parameter, inside a `variables:` mapping.

- Endpoint: `POST {org}/{project}/_apis/pipelines/{pipelineId}/preview?api-version=7.1`
- Outcome: **HTTP 200 · expanded**
