parameters:
- name: a
  type: boolean
  default: false
- name: extra
  type: object
  default:
    MID: mid
variables:
- name: PICK
  value: from-else
- name: MID
  value: mid
stages:
- stage: probe
  jobs:
  - job: probe
    steps:
    - task: CmdLine@2
      inputs:
        script: echo probe

