parameters:
- name: extra
  type: object
  default: {}
variables:
- name: BEFORE
  value: before
- name: AFTER
  value: after
stages:
- stage: probe
  jobs:
  - job: probe
    steps:
    - task: CmdLine@2
      inputs:
        script: echo probe

