parameters:
- name: a
  type: boolean
  default: true
- name: b
  type: boolean
  default: false
stages:
- stage: probe
  jobs:
  - job: probe
    steps:
      - ${{ if parameters.missing }}:
        - task: CmdLine@2
          inputs:
            script: echo reached
