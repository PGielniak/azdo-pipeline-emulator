parameters:
- name: a
  type: boolean
  default: true
- name: b
  type: boolean
  default: false
stages:
- stage: probe
  jobs:
  - job: probe
    steps:
    - task: CmdLine@2
      inputs:
        script: echo before
    - task: CmdLine@2
      inputs:
        script: echo taken
    - task: CmdLine@2
      inputs:
        script: echo after

