resources:
  repositories:
  - repository: templates
    type: git
    name: azdo-emu-templates
stages:
- stage: __default
  jobs:
  - job: Job
    steps:
    - task: CmdLine@2
      inputs:
        script: echo cross-leaf

