parameters:
- name: obj
  type: object
  default:
    A: a
stages:
- stage: probe
  jobs:
  - job: probe
    steps:
      - task: CmdLine@2
        inputs:
          script: echo probe
        env:
          PROBE: pre-${{ parameters.obj }}
