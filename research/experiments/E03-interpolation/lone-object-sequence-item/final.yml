parameters:
- name: extraStep
  type: object
  default:
    script: echo from-object
    displayName: From Object
stages:
- stage: probe
  jobs:
  - job: probe
    steps:
    - task: CmdLine@2
      displayName: From Object
      inputs:
        script: echo from-object
    - task: CmdLine@2
      inputs:
        script: echo probe

