# oracle probe — mixed-two-expressions

Two expressions separated by literal text, and two written **adjacent** with nothing between them. The adjacent case is the boundary: each half looks lone, the scalar is not.

- Endpoint: `POST {org}/{project}/_apis/pipelines/{pipelineId}/preview?api-version=7.1`
- Outcome: **HTTP 200 · expanded**
- Outcome was **not** predicted by this script.
