parameters:
- name: suffix
  type: string
  default: TAIL
stages:
- stage: probe
  jobs:
  - job: probe
    steps:
      - task: CmdLine@2
        inputs:
          script: echo probe
        env:
          PRE_${{ parameters.suffix }}: value
