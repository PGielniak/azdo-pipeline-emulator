stages:
- stage: probe
  jobs:
  - job: probe
    steps:
      - task: CmdLine@2
        inputs:
          script: echo probe
        env:
          TRUE_MID: pre-${{ true }}-post
          FALSE_MID: pre-${{ false }}-post
