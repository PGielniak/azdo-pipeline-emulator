# oracle probe — lone-object-nested

The same, with a nested object and array inside. Fixes whether the inserted structure is kept whole and typed, or flattened/stringified at some depth.

- Endpoint: `POST {org}/{project}/_apis/pipelines/{pipelineId}/preview?api-version=7.1`
- Outcome: **HTTP 200 · expanded**
- Outcome was **not** predicted by this script.
