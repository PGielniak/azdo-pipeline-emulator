stages:
- stage: probe
  jobs:
  - job: probe
    steps:
      - task: CmdLine@2
        inputs:
          script: echo probe
        env:
          BEFORE: before
          ${{ variables.nosuchvariable }}: value
