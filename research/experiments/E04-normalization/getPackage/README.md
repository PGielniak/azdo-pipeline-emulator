# oracle probe — getPackage

The least-documented of the set. Included because the task names it, and because an unhandled shorthand is a silent hole rather than an error.

- Endpoint: `POST {org}/{project}/_apis/pipelines/{pipelineId}/preview?api-version=7.1`
- Outcome: **HTTP 400 · rejected · typeKey=PipelineValidationException**
- Step keys in the expansion: ``
- Verdict: HTTP 400 · rejected · typeKey=PipelineValidationException
- Not predicted by this script: every row is asking, not asserting.
