# oracle probe — three-scopes

The same name set at root, stage and job. Does the **expansion** already resolve precedence into one effective value, or do all three survive for us to layer?

- Endpoint: `POST {org}/{project}/_apis/pipelines/{pipelineId}/preview?api-version=7.1`
- Outcome: **HTTP 200 · expanded**
- Not predicted by this probe: it is asking, not asserting.
