# Canonical reference index

Status legend: `VERIFY` = written from knowledge, must be confirmed live and pinned before first
use. After verification, the row states the last-checked date (E00-S02-T02 pass: **2026-07-30**,
curl status + title per URL; GitHub rows pinned to HEAD commits of that day). GitHub references
must become commit-pinned permalinks when cited in claims.

## Official docs (learn.microsoft.com)

All rows below: **verified 2026-07-30** (HTTP 200; learn.microsoft.com redirects to `/en-us/` +
`?view=azure-pipelines` moniker — links kept moniker-free on purpose). The yaml-schema pages
carry their source commit in page metadata (`git_commit_id`); quotes taken 2026-07-30 are from
MicrosoftDocs/azure-devops-yaml-schema-pr @ `d089fd2d` — cite that pin alongside the URL. The
public mirror **MicrosoftDocs/azure-devops-yaml-schema** carries the same `content/*.md` sources
and is the permalink target when a rendered section must be quoted exactly (verified 2026-08-11:
`content/index.md` @ `d089fd2d` — the "See also" statement on unsupported YAML features, C-E01-021,
which is present in the rendered HTML but dropped by markdown converters).

| Area | URL |
|---|---|
| YAML schema reference (landing; per-keyword subpages → see index below) | https://learn.microsoft.com/azure/devops/pipelines/yaml-schema/ (C-E00-009) |
| Expressions | https://learn.microsoft.com/azure/devops/pipelines/process/expressions — **deep-verified 2026-08-18** (E02-S05-T04; page source `MicrosoftDocs/azure-devops-docs@1eeaa8de`). Includes logical/comparison/membership C-E02-028..032; general functions C-E02-040..051; member access C-E02-024..027; conversion C-E02-020..023; value model C-E02-018/019; grammar C-E02-002..009; context availability C-E02-080..091; dependency shapes C-E02-092..095; filtered-array public contract C-E02-160. The live run confirms same-stage `dependencies` is empty without a job dependency while cross-stage `stageDependencies` is stage→job→`outputs['step.var']` plus metadata. **Conversion table re-verified 2026-08-19 (E03-S01-T05)**: the Boolean→String (`'True'`/`'False'`) and Null→String (`''`) rows are the documented source for scalar interpolation's casing rules (C-E03-175), and the Version row for its dotted rendering. ⚠️ **One row measured false**: "### Number … To string: Converts the number to a string with no thousands separator and **no decimal separator**" — the decimal point is kept whenever the value has a fraction (`0.5` → `0.5`), while `1.0` → `1`; rendering is shortest-round-trip invariant (C-E03-182). |
| Conditions — deep-verified 2026-08-19 for implicit step `succeeded()`, first-step truth, and condition overwrite semantics (C-E06-038); hosted run 543 supplies skip/result/log behavior (C-E06-040/041) | https://learn.microsoft.com/azure/devops/pipelines/process/conditions |
| Templates | https://learn.microsoft.com/azure/devops/pipelines/process/templates — **verified 2026-08-12** (E03-S01-T01). Carries limits (100 files, 100 nesting levels, 20 MB), reference-path and `@alias`/`@self` rules; the directive *syntax* lives on the template-expressions page below, and neither page states any recognition rule (case, whitespace, parameter splitting) — all of those are C-E03-100..108, measured. The `${{` escape rule (`${{ 'my${{value' }}`) is on the template-expressions page and forces a quote-aware delimiter scan (C-E03-117). **Re-verified 2026-08-20** (E03-S02-T01) for §"Reference template paths" and §"Store templates in other repositories": both give syntax only — absolute-vs-relative, `@alias`, `@self`, and "repositories are resolved only once" (C-E03-195..197). The semantics the resolver needs are all measured instead: which repository a bare path inside a cross-repo template reads from (C-E03-216), that a repository switch **resets the base directory** (C-E03-215), that cycles die of `Maximum object depth exceeded` rather than any documented limit (C-E03-208), and that the alias splits on the *first* `@` (C-E03-210). **Re-verified 2026-08-23** (E03-S06-T01), page `ms.date: 2026-06-17`, source commit `be5c6557603d2e61bafaa70ec0d4e4ec1351d058` of `MicrosoftDocs/azure-devops-docs-pr` `docs/pipelines/process/templates.md`: it enumerates **four** container positions for a reference (`stages`/`jobs`/`steps`/`variables`) plus `extends.template`, shows the stage-level `variables:` form, and states that a template file must exist on the filesystem at run start and cannot come from an artifact — the sentence that makes the bundler necessary (C-E03-400..404). It does **not** mention `phases:`, which the vendored schema does carry; that position is schema-derived (C-E03-405) and awaits a live probe in E03-S06-T02. |
| Template expressions (directives: `if`/`elseif`/`else`/`each`/`insert`) | https://learn.microsoft.com/azure/devops/pipelines/process/template-expressions — **deep-verified 2026-08-18**, "Insertion" **re-verified 2026-08-19** (E03-S01-T02 + E03-S01-T03 + E03-S01-T04; independent page-source pins `MicrosoftDocs/azure-devops-docs@7ba9a9ac` and `@7d36475a`). Conditional sequence/mapping insertion and the documented chain spelling are C-E03-120/121; the union of two independent live matrices supplies the omitted chain-state, ordering, truthiness, body-shape, recursion, and rejection contract C-E03-122..137 (45 probes, 37 successful pairs under `research/experiments/E03-conditionals/`, `research/experiments/E03-if/`, and `fixtures/oracle/directives/`). "Iterative insertion" documents sequence/mapping iteration, the `pair.key`/`pair.value` idiom, `jobList` wrapping, and the `object` fallback for template list inputs (C-E03-140..143); live probes establish authored mapping order and no synthesized index (C-E03-145/151). ⚠️ **One statement measured false**: "Expressions are only expanded for `stages`, `jobs`, `steps`, and `containers` (inside `resources`). You can't … use an expression inside `trigger`" — refuted by C-E03-109 (`trigger` and `pool.demands` both expand). Real position gating has one measured member, `resources.repositories` (C-E03-110). **"Insertion" re-verified 2026-08-19 (E03-S01-T04)**: this — not the templates page the E03-S01-T04 Ground field names — is where `${{ insert }}` is documented, and it is one paragraph plus one example (C-E03-160), with the sequence form being lone-expression array flattening rather than this directive (C-E03-161). Collision behavior, ordering, position restrictions, non-mapping values and multiple inserts per mapping are all undocumented and were measured live (C-E03-163..174, 32 probes under `research/experiments/E03-insert/`); the same probe set refuted E03-S01-T02's guess that an `each`/`insert` sibling leaves a conditional chain intact (C-E03-138) and found the orphan wording to be parent-shape dependent (C-E03-139). **Insertion re-read once more 2026-08-19 (E03-S01-T05)**: its structural statement is the array case only — "When you insert an array into an array, you flatten the nested array" — and it is *correct* (C-E03-178). What it never states is the mapping case, the lone-expression-vs-mixed-content distinction the whole of docs/02 §3 rests on, or expressions in keys, although its own `each` example is built on `${{ pair.key }}: ${{ pair.value }}` (C-E03-176). The escape section's `${{ 'my${{value' }}` spelling is confirmed by execution, not only by recognition (C-E03-188). All of it measured under `research/experiments/E03-interpolation/` (34 probes, C-E03-175..194). |
| Template parameters | https://learn.microsoft.com/azure/devops/pipelines/process/template-parameters — **status recorded 2026-08-25** (E03-S02-T02, from the 2026-08-20 survey): its 13-name type list and its "`stringList` data type isn't available in templates" sentence are C-E03-300, and that sentence is **measured false** (C-E03-306). "You can only use literals for parameter default values" is real but far narrower than it reads — the slot admits exactly one expression form, a lone single-quoted string literal (C-E03-315). |
| Runtime parameters | https://learn.microsoft.com/azure/devops/pipelines/process/runtime-parameters — **status recorded 2026-08-25** (E03-S02-T02, from the 2026-08-20 survey): same 13-name list (C-E03-300); its "when there is no default the first available value is used" statement is **measured false** — `values:` does not supply a default and the parameter is simply required (C-E03-303/309). |
| Variables | https://learn.microsoft.com/azure/devops/pipelines/process/variables — **deep-verified 2026-08-19** (E06-S01-T02/T05, E06-S02-T01), **scope section re-verified 2026-08-25** (E05-S01-T05): root variables reach every job and job values override stage/root values (C-E04-082/083, C-E05-017). Non-secret variables enter the process environment with upper-case/dot→underscore names; secrets require explicit task `env:` mapping (C-E06-007/009). Macro syntax is processed before each task and an unmatched name stays literal (C-E06-018). Space→underscore, collision precedence, task-env ordering, and nested/chain scan behavior are agent/runtime claims instead (C-E06-008/010/011/019..021; hosted runs 540/541). |
| Set variables in scripts | https://learn.microsoft.com/azure/devops/pipelines/process/set-variables-scripts — **deep-verified 2026-08-19** (E06-S04-T02): newly set values are unavailable to their emitting task; later same-job tasks use plain macros or `step.variable` for `isOutput`; dependent jobs use `dependencies` mappings; `isSecret` values are masked (C-E06-050..056; hosted run 544). |
| Predefined variables | https://learn.microsoft.com/azure/devops/pipelines/build/variables — **verified 2026-08-22** for E06-S05-T02 (page `git_commit_id` `1eeaa8de39f8b7130d8eb45ec907d9e47d6f5a32`, `ms.date` 2026-02-13): `Build.SourceBranch` is the full ref while `Build.SourceBranchName` is only its **last path segment** (`refs/heads/feature/tools` → `tools`), `Build.SourceVersionMessage` is "the first line or 200 characters, whichever is shorter", `Pipeline.Workspace` "has the same value as `Agent.BuildDirectory`", and `Build.SourcesDirectory`/`Build.Repository.LocalPath` coincide for a single checkout — C-E06-105/106/107 **Vendored as data 2026-08-23** (E04-S02-T03): `packages/engine/data/predefined-vars.json`, 91 rows generated by `pnpm predefined-vars` from the **public docs repository** rather than the rendered page — the table reaches the page through an `[!INCLUDE]` and so has no version to pin, while `MicrosoftDocs/azure-devops-docs` has both (C-E04-093). Two commits are pinned because the content is split: the include `docs/pipelines/build/includes/variables-hosted.md` @ `8e41b654da0437cb473ea1c78cf3df6a36289237` for the tables, and the page `docs/pipelines/build/variables.md` @ `1eeaa8de39f8b7130d8eb45ec907d9e47d6f5a32` for `Build.Clean` and `System.Debug`, which live in their own sections **because** they are the only writable predefined variables (C-E04-094/095). Re-run with `pnpm predefined-vars --latest` to repin. |
| Logging commands | https://learn.microsoft.com/azure/devops/pipelines/scripts/logging-commands — **re-verified 2026-08-21** for the remaining per-command sections: `prependpath` subsequent-task scope, `setsecret` job-duration/forward-only masking, `complete` result values (whose "when result not provided" sentence the agent source contradicts), `logissue` properties and its `exit 1` tip, `setprogress`, and the formatting-command list (C-E06-057..059, C-E06-062/066/067); **re-verified again 2026-08-21** for E06-S04-T04 (page `ms.date` 2026-03-05, docs commit `1481c0d18812667ac57f38b2b70c34d924608ccc`): `artifact.upload`'s `containerfolder`/`artifactname` properties, `artifact.associate`'s required type list, `task.addattachment`'s required `type`/`name`, `task.uploadsummary`'s documented shorthand — the one citable `CoreAttachmentType` wire value — `task.uploadfile`, `build.uploadlog`, `build.updatebuildnumber` and `build.addbuildtag` including its server-side no-colon restriction (C-E06-069/073/075/077/079/080/082). The page has **no `task.uploadartifact` command**, and does not list `build.uploadsummary`, which the agent still installs; previously **re-verified 2026-08-19** for the physical-line command format and `%AZP25`/`%0D`/`%0A`/`%5D`/`%3B` escaping (C-E06-044..047), for `task.prependpath` newest-first subsequent-task behavior (C-E06-012; hosted run 540), and for `task.setvariable` following-task environment visibility plus secret masking (C-E06-051/053; hosted run 544). |
| Task types and usage / control options | https://learn.microsoft.com/azure/devops/pipelines/process/tasks — **deep-verified 2026-08-19** (E06-S03-T02): `continueOnError` ignores a final failed status for downstream control flow; task retries default to zero, cap at ten, warn in the same task log, and wait 1/4/…/100 seconds (C-E06-031/034/036). |
| Jobs (phases) | https://learn.microsoft.com/azure/devops/pipelines/process/phases — **verified 2026-08-24** (E04-S03-T01, page `git_commit_id` `1eeaa8de39f8b7130d8eb45ec907d9e47d6f5a32`, `ms.date` 2026-02-03): the `matrix`/`parallel`/`maxParallel` strategy sections — `matrix` dispatches a job with different variable sets and its names must match `[A-Za-z][A-Za-z0-9_]{0,99}`; `parallel: N` duplicates the job and adds `System.JobPositionInPhase`/`System.TotalJobsInPhase`; `parallel` and `matrix` are mutually exclusive and `maxParallel` is matrix-only; a `matrix` may be a runtime `$[ … ]` expression of stringified JSON; "Multi-configuration always generates at least one job" (C-E04-113..117). The naming/`System.JobName`/1-based-position facts are a real run, not this page (C-E04-119..121). **Dependencies § re-verified 2026-08-24** (E04-S03-T02): "By default Azure DevOps YAML pipeline jobs run in parallel unless the `dependsOn` value is set" and "Pipelines must contain at least one job with no dependencies" (C-E04-124/129). |
| Stages | https://learn.microsoft.com/azure/devops/pipelines/process/stages — **verified 2026-08-24** (E04-S03-T02, page `git_commit_id` `1eeaa8de39f8b7130d8eb45ec907d9e47d6f5a32`): §"Specify dependencies" — stages "run sequentially by default in the order you define them in the YAML file" unless `dependsOn`, "Pipelines must contain at least one stage with no dependencies", and the `dependsOn: [] # Runs in parallel` example (C-E04-123/125/128). |
| Deployment jobs | https://learn.microsoft.com/azure/devops/pipelines/process/deployment-jobs — **deep-verified 2026-08-24** (E04-S03-T03, page `git_commit_id` `1eeaa8de39f8b7130d8eb45ec907d9e47d6f5a32`, `updated_at` 2026-05-07): the runOnce hook order `preDeploy → deploy → routeTraffic → postRouteTraffic` then `on: success|failure` (C-E04-146); hooks inherit the job's `pool` by default (C-E04-147); the download-artifact task is auto-injected only in the `deploy` hook and `download: none` suppresses it (C-E04-149); the four output-variable namings — runOnce `<job>.<step>.<var>`, runOnce+resourceType `Deploy_<resourceName>.<step>.<var>`, canary `<hook>_<increment>`, rolling `<hook>_<resource>` (C-E04-151/152/154); "A deployment job doesn't automatically clone the source repo" (C-E04-155). The runOnce job-name quirk is **confirmed live** by run 548 (`research/experiments/E04-deployment/real-run.md`, C-E04-153). |
| Container jobs | https://learn.microsoft.com/azure/devops/pipelines/process/container-phases |
| Service containers | https://learn.microsoft.com/azure/devops/pipelines/process/service-containers |
| Pipeline resource metadata | https://learn.microsoft.com/azure/devops/pipelines/yaml-schema/resources-pipelines-pipeline — **deep-verified 2026-08-12** (E02-S04-T03, page `git_commit_id` d089fd2dbb54483ec611eeb478e3eff14be74393): §"Pipeline resource metadata as predefined variables" is the canonical 12-name list, and the word **variables** is load-bearing — two real runs proved the same names do *not* exist in the `resources` expression context (C-E02-120/121, superseding the doc-only C-E02-111/112). Same page carries the `projectName`-absent caveat (C-E02-122) and the printenv sample that fixes the env-name rule incl. hyphens (C-E02-127) |
| Resources | https://learn.microsoft.com/azure/devops/pipelines/process/resources — verified 2026-08-12 (page `git_commit_id` 1eeaa8de39f8b7130d8eb45ec907d9e47d6f5a32): `resources.repositories.<alias>.{name,ref,type,id,url,version}` confirmed live as **context** members, unlike the pipeline family (C-E02-123/125) |
| Multi-repo checkout | https://learn.microsoft.com/azure/devops/pipelines/repos/multi-repo-checkout — **deep-verified 2026-08-19** for `workspaceRepo`: it retargets the default working directory; hosted run 542 used that control to distinguish `System.DefaultWorkingDirectory` from `Build.SourcesDirectory` (C-E06-026/027). **Re-verified 2026-08-22 for E06-S05-T03** (`git_commit_id` `a9ee075785637d668c69557d66708bfa49943233`, `ms.date` 2026-04-22): the "Checkout path" section's single-vs-multiple rule and its `path`-is-relative-to-`$(Agent.BuildDirectory)` sentence (C-E06-114), the folder-name-is-the-`name`-not-the-alias note (C-E06-118) and the warning that adding a second checkout moves the first (C-E06-119). The page is **silent** on what `Build.SourcesDirectory`/`Build.Repository.LocalPath` become under multi-checkout — that half is agent source only (C-E06-116/117). |
| Pipeline artifacts | https://learn.microsoft.com/azure/devops/pipelines/artifacts/pipeline-artifacts — **deep-verified 2026-08-21** (E06-S05-T01, page `git_commit_id` `1eeaa8de39f8b7130d8eb45ec907d9e47d6f5a32`, `updated_at` 2026-05-07): named-download semantics (only that artifact, fails when absent, patterns relative to the artifact root) and the `$(Pipeline.Workspace)` default with a per-artifact subdirectory only when the name is omitted, C-E06-086/087; deployment-job auto-download restricted to the `deploy` lifecycle hook and suppressed by `download: none`, C-E06-096; the cross-stage `publish`/`$(Pipeline.Workspace)\drop\test.ps1` example that fixes the directory-contents rule, C-E06-094; `.artifactignore` and the implicit `.git` exclusion recorded as a deliberate non-implementation, C-E06-095 |
| Build artifacts | https://learn.microsoft.com/azure/devops/pipelines/artifacts/build-artifacts |
| Pipeline caching | https://learn.microsoft.com/azure/devops/pipelines/release/caching |
| Run (build) number | https://learn.microsoft.com/azure/devops/pipelines/process/run-number — **deep-verified 2026-08-25** (E05-S03-T01, page `git_commit_id` `1eeaa8de39f8b7130d8eb45ec907d9e47d6f5a32`, `ms.date` 2025-08-12, `updated_at` 2026-05-07): the full run-number token table and its example values (C-E05-005), the default format `$(Date:yyyyMMdd).$(Rev:r)` (C-E05-001), `Rev` reset-on-any-other-change including the version example (C-E05-009/010), zero-padding via extra `r`s (C-E05-011), UTC time values (C-E05-013), the 255-character/forbidden-character limits (C-E05-014) and the `Build.BuildId`-is-the-Run-ID note (C-E05-016). The page is **silent** on the Classic `$(Rev:.r)` spelling and on the `Date:` format grammar beyond two examples — both recorded as deliberate non-implementations (C-E05-024/025). |
| Tasks reference (landing) | https://learn.microsoft.com/azure/devops/pipelines/tasks/reference/ — **deep-verified 2026-08-19** for Bash@3, CmdLine@2, and PowerShell@2 working-directory prose. Their `Build.SourcesDirectory` default sentence is incomplete under `workspaceRepo`; hosted run 542 measured all three shortcuts starting in `System.DefaultWorkingDirectory` instead (C-E06-026/027). |
| Service connections | https://learn.microsoft.com/azure/devops/pipelines/library/service-endpoints |
| Hosted agents / vmImage labels | https://learn.microsoft.com/azure/devops/pipelines/agents/hosted |
| PATs — **deep-verified 2026-07-30** (E00-S03-T01): Basic auth w/ empty username, org-scoped creation UI, 84-char format w/ `AZDO` at 76–80, rotation/revocation guidance (C-E00-020/021) | https://learn.microsoft.com/azure/devops/organizations/accounts/use-personal-access-tokens-to-authenticate |
| OAuth scopes table (display names ↔ `vso.*`; `vso.build` = "Build (read)", C-E00-019). Page also documents ADO-OAuth deprecation | https://learn.microsoft.com/azure/devops/integrate/get-started/authentication/oauth |
| Auth methods overview (old `…/authentication/` landing **404s**; moved) | https://learn.microsoft.com/azure/devops/integrate/get-started/authentication/authentication-guidance |
| Entra OAuth for ADO — **confirms resource GUID `499b84ac-1321-427f-aa17-267ca6975798`** + resource URI + `.default` scope (C-E00-011). Note: ADO's own OAuth is deprecated (no new registrations since 2025-04; full deprecation announced for 2026) → E08 uses Entra/MSAL | https://learn.microsoft.com/azure/devops/integrate/get-started/authentication/entra-oauth |
| REST API landing + versioning | https://learn.microsoft.com/rest/api/azure/devops/ |
| REST: Pipelines Preview — **deep-verified 2026-07-30** (E00-S03-T01): route/api-version 7.1, body `previewRun`+`yamlOverride`, response `PreviewRun.finalYaml`, scope `vso.build` (C-E00-017..019). **Body re-read 2026-08-11** (E12-S01-T02): the full `RunPipelineParameters` also carries `variables`, `stagesToSkip` and `resources.repositories[].refName/token/tokenType` — the escape hatch for previewing a non-default branch (C-E12-013). **Confirmed against the live service 2026-07-31** (E00-S03-T02): 200 body carries exactly one field `finalYaml`; failure modes recorded, three of which contradict the documented-status intuition — 302 (not 401) on a bad PAT, 500 (not 404) on an unknown pipelineId, 200 (not an error) on an empty `yamlOverride` (C-E00-022..027; transcripts under `research/experiments/oracle-spike/`) | https://learn.microsoft.com/rest/api/azure/devops/pipelines/preview/preview |
| REST: Pipelines Runs / Artifacts | https://learn.microsoft.com/rest/api/azure/devops/pipelines/ |
| REST: Git Items / Refs | https://learn.microsoft.com/rest/api/azure/devops/git/ |
| REST: Git Pushes — Create — **deep-verified 2026-08-11** (E12-S01-T02): `refUpdates[{name, oldObjectId}]` + `commits[{comment, changes[{changeType, item.path, newContent{content, contentType: rawtext}}]}]`; used live to mirror `fixtures/corpus/` into the oracle repo (C-E12-014) | https://learn.microsoft.com/rest/api/azure/devops/git/pushes/create |
| REST: Build (definitions, artifacts) | https://learn.microsoft.com/rest/api/azure/devops/build/ |
| REST: Variablegroups — Add — **verified 2026-08-11** (E12-S01-T02): **org-scoped** POST route (no project segment) needing `variableGroupProjectReferences[].projectReference`, scope `vso.variablegroups_manage`; used live to create the corpus group (C-E12-015) | https://learn.microsoft.com/rest/api/azure/devops/distributedtask/variablegroups/add |
| REST: Environments — Add — **verified 2026-08-11** (E12-S01-T02): project-scoped POST, body `{name, description}`, scope `vso.environment_manage`; used live to create the corpus environments (C-E12-017) | https://learn.microsoft.com/rest/api/azure/devops/distributedtask/environments/add |
| REST: Pipeline Permissions — Update — **verified 2026-08-11** (E12-S01-T02): `PATCH …/_apis/pipelines/pipelinepermissions/{resourceType}/{resourceId}?api-version=7.1-preview.1`, body `{pipelines:[{id, authorized}]}`; the *authorization* half of C-E12-015/017 (creating the object is not enough) | https://learn.microsoft.com/rest/api/azure/devops/approvalsandchecks/pipeline-permissions/update-pipeline-permisions-for-resource |
| REST: Yamlschema — Get — **deep-verified 2026-08-11** (E01-S02-T03): org-scoped route (no project segment), optional `validateTaskNames`, scope `vso.agentpools`; confirmed live (HTTP 200, 611 KB, draft-07 document, C-E01-029/033/036) | https://learn.microsoft.com/rest/api/azure/devops/distributedtask/yamlschema/get |
| Task contribution / task.json (extension docs) | https://learn.microsoft.com/azure/devops/extend/develop/integrate-build-task |
| dotnet-install scripts | https://learn.microsoft.com/dotnet/core/tools/dotnet-install-script |

### yaml-schema per-keyword subpages (all 73 verified 2026-07-30, HTTP 200)

`steps-bash`, `steps-script`, `steps-powershell`, and `steps-pwsh` were deep-re-verified
2026-08-19 for E06-S03-T01: each exposes `workingDirectory` and `timeoutInMinutes`, and each states
that the enclosing job timeout terminates a longer-running step (C-E06-025). `steps-task`,
`steps-bash`, and `steps-powershell` were re-verified for E06-S03-T02: the common controls expose
`continueOnError`/`retryCountOnTaskFailure`, while the shell shortcuts expose `failOnStderr`
(C-E06-031).

Pattern: `https://learn.microsoft.com/azure/devops/pipelines/yaml-schema/<slug>`. Slugs:

`pipeline` · `extends` · `jobs` · `jobs-deployment` · `jobs-deployment-environment` ·
`jobs-deployment-strategy` · `jobs-deployment-strategy-canary` · `jobs-deployment-strategy-rolling` ·
`jobs-deployment-strategy-run-once` · `jobs-job` · `jobs-job-container` · `jobs-job-strategy` (**verified 2026-08-24** for E04-S03-T01, page `git_commit_id` `d089fd2dbb54483ec611eeb478e3eff14be74393`, `ms.date` 2026-07-29: "the name *string1* is the copy's name and is appended to the name of the job", the `Build Python35` example, the matrix-key charset `[A-Za-z][A-Za-z0-9_]{0,99}`, and `maxParallel` 0/absent = unlimited — C-E04-110..113; **`jobs-deployment`, `jobs-deployment-environment`, `jobs-deployment-strategy-run-once` verified 2026-08-24** for E04-S03-T03 at the same pin: deployment-name charset + `deploy`-keyword ban (C-E04-140), the environment `string` vs `name, resourceName, resourceId, resourceType, tags` forms with `resourceType ∈ virtualMachine, Kubernetes` (C-E04-145), and the hook definitions carrying `steps` + optional `pool` (C-E04-146/147)) ·
`jobs-job-uses` · `jobs-template` · `parameters` · `parameters-parameter` · `pool` · `pool-demands` ·
`pr` · `resources` · `resources-builds` · `resources-builds-build` · `resources-containers` ·
`resources-containers-container` · `resources-containers-container-trigger` · `resources-packages` ·
`resources-packages-package` · `resources-pipelines` · `resources-pipelines-pipeline` ·
`resources-pipelines-pipeline-trigger` · `resources-pipelines-pipeline-trigger-branches` ·
`resources-repositories` · `resources-repositories-repository` (**verified 2026-08-25** for E03-S02-T01, page `git_commit_id` `d089fd2dbb54483ec611eeb478e3eff14be74393`, `ms.date` 2026-07-29: the `ref` default, "ref name to checkout; defaults to 'refs/heads/main'" (C-E03-198), and the "Variables" section's per-run `resources.repositories.<Alias>.version` (C-E03-196)) · `resources-webhooks` ·
`resources-webhooks-webhook` · `resources-webhooks-webhook-filters` ·
`resources-webhooks-webhook-filters-filter` · `schedules` · `schedules-cron` · `stages` ·
`stages-stage` · `stages-template` · `steps` · `steps-bash` · `steps-checkout` (**verified 2026-08-21** for E06-S05-T02, page `git_commit_id` `d089fd2dbb54483ec611eeb478e3eff14be74393`, `ms.date` 2026-07-29: every option's text plus the Shallow-fetch/Clean-property/Sync-tags remarks, the `$(Pipeline.Workspace)` root for `path`, and the implicit `self`/`none` default — C-E06-105/108, and the sprint-209 pipeline-setting defaults the agent contradicts at C-E06-099) · `steps-download` (**verified 2026-08-21** for E06-S05-T01, same `git_commit_id`: the keyword's `$(Pipeline.Workspace)/<artifact name>` layout and its status as a shorthand for the download task — C-E06-084) ·
`steps-download-build` · `steps-get-package` · `steps-powershell` · `steps-publish` · `steps-pwsh` ·
`steps-review-app` · `steps-script` · `steps-task` · `steps-template` · `target` ·
`target-settable-variables` · `trigger` · `variables` · `variables-group` · `variables-name` ·
`variables-template` — supporting: `deploy-hook` · `include-exclude-filters` ·
`include-exclude-string-filters` · `mount-read-only` · `on-failure-hook` · `on-success-hook` ·
`on-success-or-failure-hook` · `post-route-traffic-hook` · `pre-deploy-hook` · `route-traffic-hook` ·
`workspace`

## GitHub source (behavior references — always pin commits)

All rows: **verified + pinned 2026-07-30** (HEAD of that day; paths confirmed via contents/trees API).

| Repo (pin) | What we use it for — confirmed paths |
|---|---|
| microsoft/azure-pipelines-agent @ `c59f46aa`; `ExpressionManager.cs` re-pinned @ `9d00422e` for E02-S03-T03; variable/runtime environment source re-pinned @ `15ee11cd728d630f9c9905485449e3359da0a493` for E06-S01; complete macro, step-lifecycle, and logging-handler traces re-pinned @ `4571a73531e1ea6342ed46723dd39a115b92843b` for E06-S02-T01/E06-S03/E06-S04 | Worker step lifecycle: `src/Agent.Worker/StepsRunner.cs` (deep-verified 2026-08-19: sequential start → variable recalculation → condition → timeout-bound run → result merge/complete, C-E06-028/030; result state, retry/`continueOnError`, false-condition skip, evaluation-error failure, and accumulated job-status updates, C-E06-036..043); retry policy: `src/Agent.Worker/{TaskRunner,RetryHelper}.cs` (exact-Failed eligibility, ten-retry cap, square-second delay, C-E06-034/035); runtime condition evaluation: `src/Agent.Worker/ExpressionManager.cs` (step status arities/truth table and implicit `succeeded()` deep-verified 2026-08-19, C-E02-061/062 and C-E06-038/039; consumes the **closed** `Microsoft.TeamFoundation.DistributedTask.Expressions` NuGet — engine sources are *not* in this repo, C-E00-012); variable state and output wiring: `src/Agent.Worker/{Variables,ExecutionContext,TaskCommandExtension}.cs` plus `src/Misc/layoutbin/en-US/strings.json` (deep-verified 2026-08-19, C-E06-003..006/C-E06-052..056; hosted runs 539/544 establish enforcement and timing); environment materialization: `TaskRunner.cs`, `Util/VarUtil.cs`, `Handlers/{Handler,NodeHandler}.cs`, and `TaskCommandExtension.cs` (deep-verified 2026-08-19, C-E06-008/010..012; hosted run 540); macro expansion phase boundary: raw logging-command write in `TaskCommandExtension`/`ExecutionContext`/`Variables.Set`, recursive pre-step `StepsRunner` → `Variables.RecalculateExpanded`, then one-pass task target scan in `TaskRunner` → `Variables.ExpandValues` → `Util/VarUtil.cs` (deep-verified 2026-08-19, C-E06-019/022..024; hosted run 541 supplies C-E06-020/021); `TaskSetVariableCommand` parses flags, registers secrets, rejects unsafe multiline values, preserves secret status, and routes output aliases through `ExecutionContext.SetVariable` (C-E06-052..056; hosted run 544); **pipeline-artifact plugins re-pinned @ `42bde98bea7bb3b9e186d693e3b1554249e93a38` for E06-S05-T01** (deep-verified 2026-08-21): `src/Agent.Plugins/PipelineArtifact/PipelineArtifactPluginV1.cs` (the `System.JobIdentifier` name fallback and its `[^a-zA-Z0-9 - .]` + `.default` normalization, the neither-file-nor-directory failure, and the `system.defaultworkingdirectory` base for a relative path — C-E06-089/091/092), `…/PipelineArtifactPluginV2.cs` (input defaults and target-directory creation — C-E06-085), `…/PipelineArtifactPluginUtil.cs` (the forbidden artifact-name characters — C-E06-093), `src/Agent.Plugins/Artifact/ArtifactItemFilters.cs` (comment/negation parsing and the **ordered accumulating map** that makes `!*.md` followed by `**` select everything — C-E06-090) and `…/Artifact/FileContainerProvider.cs` (`AppendArtifactNameToTargetPath` consulted only on the multi-download branch — corroboration for C-E06-086; the pipeline-artifact path itself runs through the **closed** BlobStore `DedupManifestArtifactClient`, so the file-source rule of C-E06-094 is an inference flagged for a later oracle run); **git source provider read at the same `42bde98b…` pin for E06-S05-T02** (deep-verified 2026-08-22): `src/Agent.Plugins/GitSourceProvider.cs` (input parsing and its defaults — `fetchTags` defaulting to **true** against the doc page's sprint-209 `false`, the `int.TryParse` fetch-depth collapse, the `''`/`true`/`recursive` submodule triple, `GIT_LFS_SKIP_SMUDGE=1` when LFS is off, the warn-and-continue LFS fetch, the `clean -ffdx` → `reset --hard HEAD` order, and the init/remote/fetch/checkout sequence ending at a detached HEAD — C-E06-098..104), `src/Agent.Plugins/GitCliManager.cs` (the literal flag strings for `fetch`/`checkout`/`clean`/`reset`/`submodule`, each carried in a comment above its method — C-E06-097/101/103), `src/Agent.Sdk/Util/StringUtil.cs` (`ConvertToBoolean`'s five literals and its silent fallback for everything else — C-E06-100) and `src/Agent.Sdk/Knob/AgentKnobs.cs` (`DisableFetchPruneTags` defaulting to `false`, which is why `--prune-tags` defeats `fetchTags: false` — C-E06-111); **multi-checkout layout read at the same `42bde98b…` pin for E06-S05-T03** (deep-verified 2026-08-22): `src/Agent.Plugins/RepositoryPlugin.cs` L152-182 (the three-branch layout decision — explicit `path` first, then `s/<repoName>` when the job has multiple checkouts, then `s` — and the `maxRootDirectory` guard on `path` — C-E06-114), `src/Agent.Sdk/Util/RepositoryUtil.cs` L32-45 + L190-252 (`HasMultipleCheckouts` as a *job setting* rather than a per-step derivation, and `GetCloneDirectory`'s port of git's clone-directory algorithm with its `name ?? url ?? alias` fallback chain — C-E06-115/118), `src/Test/L0/Util/RepositoryUtilL0.cs` L396-451 (the 33-case conformance table for that algorithm, ported wholesale into `packages/runtime/test/core.bats`), `src/Agent.Worker/Build/BuildJobExtension.cs` L187/L361-446 (`Build.SourcesDirectory` seeded from the tracking config, `GetDefaultRepoLocalPathValue`'s backward-compatibility branch that keeps `Build.Repository.LocalPath` at `s` under multi-checkout, `IsCheckoutToCustomPath`'s compare-against-the-default test, and `GetDefaultWorkingDirectoryRepoLocalPathValue`'s whole-step-list `workspaceRepo` scan — C-E06-116/117/120), `src/Agent.Worker/Build/BuildDirectoryManager.cs` L174-183/L316-344 (`UpdateDirectory` rewriting the tracking `SourcesDirectory` **only** when exactly one repository is tracked, and `GetDefaultRepositoryPath`'s matching two branches — C-E06-116) and `src/Agent.Worker/Build/TrackingConfig.cs` L84-101/L242-260 (`SourcesDirectory` = `<buildDir>/s`, per-repo tracking = `<sources>/<cloneDir>`); containers: `src/Agent.Worker/ContainerOperationProvider.cs` (+`Enhanced`); secret masker: `src/Agent.Sdk/SecretMasking/`; pipeline-cache plugin: `src/Agent.Plugins/PipelineCache/` |
| microsoft/azure-pipelines-agent logging parser @ `4571a73531e1ea6342ed46723dd39a115b92843b` (checked 2026-08-19; command handlers, issue/result flow, and debug gating deep-verified 2026-08-21 for E06-S04-T03: `TaskCommandExtension.cs` prependpath/setsecret/complete/logissue/setprogress/debug handlers, `ExecutionContext.cs` `AddIssue`/`WriteDebug`/`Complete`/`WellKnownTags`, `StepsRunner.cs` result precedence, `Util/TaskResultUtil.cs` worst-wins merge, `WorkerCommandManager.cs` command-failure path and alias registration (`TaskCommandExtension.cs` L363-364 `Aliases => { "issue" }` + the `_commands[alias]` store, which is why `task.issue` dispatches to the `task.logissue` handler — C-E06-068, a source-only behavior the doc page never mentions), `src/Misc/layoutbin/en-US/strings.json` message text — C-E06-057..068) | `src/Microsoft.VisualStudio.Services.Agent/Command.cs`, `src/Agent.Sdk/CommandStringConvertor.cs`, `src/Agent.Sdk/Knob/AgentKnobs.cs`, `src/Agent.Worker/WorkerCommandManager.cs`, and `src/Agent.Worker/Handlers/ProcessHandler/ProcessHandler.cs`: line discovery, grammar, case-insensitive properties, one-pass decoding, malformed/unknown warnings, and output consumption (C-E06-046..048). Artifact/attachment/build handlers deep-verified 2026-08-21 for E06-S04-T04: `src/Agent.Worker/Build/ArtifactCommandExtension.cs` (`artifact.upload` name/containerfolder rules, file-or-directory source, the empty-directory **warning-and-succeed** path, container-path-to-artifact association; `artifact.associate` as a link to existing bytes — C-E06-069/070/072/073), `src/Agent.Worker/Build/FileContainerServer.cs` L83-93/L238 (`_sourceParentDirectory`: a file contributes its basename, a directory contributes its contents — C-E06-071), `src/Agent.Worker/TaskCommandExtension.cs` L249-357 (`uploadfile`/`uploadsummary` as two calls into the single `AddAttachment` helper, both naming the attachment `Path.GetFileName(data)` — C-E06-074/075/076), `src/Agent.Worker/Build/BuildCommandExtension.cs` (`uploadlog`'s fixed `CustomToolLog` name, the deprecated-but-installed `build.uploadsummary`, `updatebuildnumber`'s untrimmed synchronous `Variables.Set`, `addbuildtag`'s `Trim()` and set semantics — C-E06-077/078/080/082), plus `src/Agent.Worker/Variables.cs` L416-453/L650-658 with `src/Microsoft.VisualStudio.Services.Agent/Constants.cs` L427/L623 (`build.buildNumber` is a member of `ReadOnlyVariables`, yet `Variables.Set` does not enforce read-only — the enforcement is in `TaskSetVariableCommand`, which is why `updatebuildnumber` legitimately overwrites it — C-E06-081). The `CoreAttachmentType` constants are **not** in this repo: they live in the closed `Microsoft.TeamFoundation.DistributedTask.WebApi` assembly, so only `Distributedtask.Core.Summary` (stated on the doc page) has a citable value (C-E06-079). |
| **actions/runner @ `34ef7f24`** (added 2026-07-30) | Open behavioral reference for the DistributedTask expressions + templating engine: `src/Sdk/DTExpressions2/`, `src/Sdk/DTObjectTemplating/`, `src/Sdk/DTPipelines/` (forked from Azure DevOps; divergence possible → oracle D6 decides, C-E00-013). Error-location format `(Line: {0}, Col: {1})`: `src/Sdk/Resources/TemplateStrings.g.cs` + `DTObjectTemplating/ObjectTemplating/TemplateContext.cs` GetErrorPrefix (C-E01-007). Expression grammar (E02-S01-T01, verified 2026-08-11): `DTExpressions2/Expressions2/Tokens/LexicalAnalyzer.cs` (scan loop L120-L246, legality-by-previous-token table L317-L467), `ExpressionConstants.cs` (operators L50-L59, `MaxDepth`/`MaxLength` L30-L31), `ExpressionParser.cs` (name resolution L126-L146, arity L344-L354, depth L394-L404), `Tokens/TokenKind.cs`, `ParseExceptionKind.cs`. Value shape (E02-S02-T01, verified 2026-08-12): `ValueKind.cs`, `EvaluationResult.cs` `GetKind`, and `Sdk/IReadOnlyArray.cs` corroborate six tagged kinds/read-only collections but omit Azure's Version kind (C-E02-019). Object templating (E03-S01-T01, verified 2026-08-12): `DTObjectTemplating/ObjectTemplating/TemplateConstants.cs#L21` declares `InsertDirective = "insert"` and there is **no** `if`/`elseif`/`else`/`each` counterpart anywhere in that folder — the fork is usable for the walk-loop shape and for nothing about the directive set (C-E03-115). Evaluation loop pinned and read: `TemplateEvaluator.cs#L89-L203` — recursive `Evaluate(DefinitionInfo)` consuming children through `TemplateUnraveler` predicates (`AllowSequenceEnd` L116, `AllowScalar` L196) rather than an index; our walker is the same recursion over an already-materialized DOM and needs no unraveler because T01 expands nothing (C-E03-116). Object templating `insert` directive (E03-S01-T04, verified 2026-08-19): `DTObjectTemplating/ObjectTemplating/TemplateUnraveler.cs#L434-L466` (entered only when the token's parent is a `MappingState` with `IsKey`, else `DirectiveNotAllowed`) and `#L640-L703` (`StartMappingInsertion`: mapping token used directly, expression evaluated via `EvaluateMappingToken`, anything else `ExpectedMapping`; zero entries contribute nothing), with duplicate keys caught one layer up in `TemplateEvaluator.cs#L193-L219` against a `HashSet<String>(StringComparer.OrdinalIgnoreCase)` raising `ValueAlreadyDefined` and skipping the later value. Azure agrees on the merge, the `Expected a mapping` rejection and the case-insensitive `'{0}' is already defined` collision error, and **diverges on non-key positions**, where it emits no `DirectiveNotAllowed` sentence (C-E03-162/173). This is the one directive the fork actually implements (C-E03-115), so it is the only one for which it is a real second source. Member access (E02-S02-T03, verified 2026-08-12): `Sdk/Operators/Index.cs` null-propagation + object/array conversion; `DictionaryContextData.cs` and `CaseSensitiveDictionaryContextData.cs` establish per-object ordinal-ignore-case vs ordinal policies (C-E02-024..027). **Caveat now measured, not theoretical:** this fork is the *GitHub Actions* dialect — use it for shape only where live Azure probes agree |
| **actions/runner @ `258d6c85`** (E02-S05-T04, checked 2026-08-18) | Filtered-array evaluator reference: `DTExpressions2/Expressions2/Sdk/Operators/Index.cs#L51-L225`. It keeps `FilteredArray` distinct from ordinary `IReadOnlyArray`, maps later Object/Array access over every child, skips misses/non-collections, flattens nested wildcards, and returns an empty filtered array for a wildcard on a non-collection. The 24-call Azure preview matrix in `research/experiments/E02-filtered-arrays/` agreed in every cell (C-E02-160..164), so these particular fork branches are adopted. |
| microsoft/azure-pipelines-tasks @ `6485321954dafb296697763c54c30a70840154f8` (HEAD checked 2026-08-19) · snapshot pin tag **v277** = `8ba25cfb` (2026-07-30) | Per-task `Tasks/<Name>V<n>/task.json` + implementation. Bash@3 and CmdLine@2 temp-script creation, cwd binding, and ordered live-output wiring deep-verified for E06-S03-T01 (C-E06-029); Bash@3 and PowerShell@2 exit/fail-on-stderr state handling deep-verified for E06-S03-T02 (any stderr `Buffer` flags failure while output remains live, C-E06-032/033); shared modules `Tasks/Common/` (Deployment, Sanitizer, TlsHelpers, …). `PublishPipelineArtifactV1`/`DownloadPipelineArtifactV2` `task.json` pinned @ `299572e25b6cf14b21c7b60e5228603cbb5ffb42` for E06-S05-T01 (checked 2026-08-21): both are `AgentPlugin` tasks, so the implementation to read is `Agent.Plugins.PipelineArtifact.*TaskV1/V2_0_0` in the agent repo rather than a task `main.ts`; input defaults C-E06-085/091, and the `targetPath` help text that C-E06-089 records as **wrong** against the plugin source. Sprint-cadence release tags `v<sprint>`; versioning rules `docs/taskversionbumping.md` (C-E00-014..016). tasks-meta snapshots vendor from the tag pin via `scripts/refresh-tasks-meta.ts` |
| microsoft/azure-pipelines-task-lib @ `c377a1115fdc0e5aea896df36219b59c181d9bc4` | `node/task.ts` (INPUT_/env encodings, getBoolInput, findMatch), `node/taskcommand.ts` + `node/internal.ts` (`##vso` emission; escape/decode order deep-verified 2026-08-19, C-E06-045), `node/toolrunner.ts` — all confirmed present |
| microsoft/azure-pipelines-vscode @ `2f4500cf` | Official machine-readable YAML schema: `service-schema.json` at repo root (C-E00-006..008, C-E00-010); vendored in `packages/engine/vendor/schema/`. Also (verified 2026-08-11, E01-S02-T03) `src/schema-association-service.ts`: how the extension obtains the *per-org* schema — `taskAgentApi.getYamlSchema()`, session-only cache, and the note that the service offers no version to bust a cache on (C-E01-029/035) |
| **microsoft/azure-devops-node-api @ `cdf57a1407df00ed9465eeaed6c90c7777b74bb1`** (added 2026-08-11, E01-S02-T03) | The REST client the VS Code extension calls through: `api/TaskAgentApiBase.ts` `getYamlSchema()` — area `distributedtask`, locationId `1f9990b9-1dba-441f-9c2e-6485888c42b6`, empty `routeValues` (⇒ organization-scoped route), `validateTaskNames` query parameter (C-E01-029) |
| **microsoft/azure-pipelines-language-server @ `543ceeec`** (added 2026-07-30, E01-S02-T01) | Reference semantics for the schema's non-standard keywords and for pipeline-value typing — `language-service/src/parser/jsonParser.ts`: `firstProperty` branch selection + message (C-E01-009/018), `ignoreCase`/`aliases` (C-E01-017), boolean/number/null→string and `${{ }}`/`$( )`/`$[ ]` exemptions (C-E01-015/016). This is the validator the VS Code extension actually runs over the vendored schema |
| actions/runner-images @ `4055b521`; **macOS image re-pinned @ `8d3ea005fa2d87f3cbc9255c27fdfed9e901a043`** (2026-08-23, E11-S01-T03) | Hosted image contents: `images/ubuntu/` (`Ubuntu2204/2404/2604[-Arm64]-Readme.md`, `toolsets/`) for doctor/E2E/sandbox-image design. The macOS pin covers `images/macos/macos-26-arm64-Readme.md` — the image `macos-latest` resolves to — for what CI can assume is present: **Bash 3.2.57 only**, Homebrew 6.0.13, no second bash (C-E00-028/029) |
| bats-core/bats-core @ `ae4b94d7` | Runtime test framework (invocation/report claims C-E00-003..005) + https://bats-core.readthedocs.io/en/stable/usage.html |
| eemeli/yaml — npm **2.9.0** = tag v2.9.0 = `ddb21b04` (pinned 2026-07-30, E01-S01-T01; earlier HEAD check `bf03c0cb`) | CST/source-position APIs verified in `docs/` at the pin: `range=[start,value-end,node-end]`, `lineCounter.linePos` 1-indexed, `keepSourceTokens`→`srcToken`, Scalar.type styles (C-E01-001..006); `docs/07_parsing_yaml.md` CST token table — `&`→`anchor` SourceToken with `offset`/`source`, carried in the `start`/`sep`/`end` arrays (C-E01-027, verified 2026-08-11); rendered docs https://eemeli.org/yaml/ (200) |
| qetza/replacetokens-task @ `3b06eec6` | Marketplace task ground truth (repo name resolved 2026-07-30). Confirmed 2026-08-11 as the only non-`builtIn` extension installed in the oracle org, contributing `replacetokens@3..@7` to the org schema — the live evidence that marketplace task **inputs** validate only under the org document (C-E01-031) |
| **tj/commander.js @ `ba6d13ddb4243e5913367734f8c159089ffe7834`** (added 2026-08-11, E13-S01-T01) | The CLI framework's real behaviour, as depended on by `packages/cli`: `lib/command.js` — `_exit`/`exitOverride` (the override must throw; commander exits afterwards otherwise), `error()` defaulting to exit 1, `unknownOption()` message + did-you-mean, `--help`/`--version` exiting *through* the error path with code 0, and the `getOutHelpWidth`/`useColor` environment dependencies that make help snapshots flaky; `lib/help.js` — the width-80 fallback (C-E13-003..006). npm dist-tag `latest` = 15.0.0, `engines: node >=22.12.0` (C-E13-001/002) |
| git-scm.com/docs · gnu.org/software/bash/manual/bash.html | git flag + shell semantics citations (both 200; **`git-fetch` deep-checked 2026-08-22** for E06-S05-T02: `--prune-tags` is "a shorthand for providing the explicit tag refspec", "equivalent to having `refs/tags/*:refs/tags/*` declared in the refspecs", which is what overrides `--no-tags` — C-E06-111; deep-checked 2026-08-12 for quoting and exit status, C-E02-129/130; 2026-08-13 for the 127/126 special statuses and `&&`/`||` short-circuiting, C-E02-136/137 — `html_node/Exit-Status.html` and `html_node/Lists.html`; 2026-08-19 for assignment names/expansions, single/double/backslash quoting, multiline continuations, and comments — `html_node/{Definitions,Shell-Parameters,Single-Quotes,Double-Quotes,Escape-Character,Comments}.html`, C-E06-014..017). **Rate-limits aggressively:** repeated fetches return 429 and then time out, so pull the pages you need in one pass | **`Exit-Status` deep-verified 2026-08-26** (E10-S02-T02, Bash Reference Manual v5.3): the 128+N fatal-signal convention (C-E13-020), 127/126 for not-found/not-executable (C-E13-021), and the 0–255 range with values above 125 reserved (C-E13-022).
| POSIX.1-2024 (Open Group Base Specifications Issue 8) — https://pubs.opengroup.org/onlinepubs/9799919799/ | The shell facts the GNU manual leaves unstated: `utilities/test.html` EXIT STATUS "0 true / 1 false / >1 An error occurred" (C-E02-135 — the reason the conformance harness asserts an exact status per row) and `utilities/V3_chap02.html` §2.6.3 trailing-newline removal in command substitution (C-E02-140). Verified 2026-08-13 |
| *(measured, not cited)* `research/experiments/E02-conformance/shell-semantics.md` | Locale-collation of `[[ < ]]` and `${v^^}`, and the masking of a status-2 error by `||`, are answered by neither the bash manual nor POSIX; measured per BACKLOG §3.3 and regenerated with `pnpm expr-shell-survey` (C-E02-141/142/143) |

## Tooling (converter runtime & test frameworks)

| Area | URL | Status |
|---|---|---|
| Node.js release schedule (LTS windows for engines floor) | https://nodejs.org/en/about/previous-releases · pinned JSON: https://github.com/nodejs/Release/blob/e4bf922d83b877a116763e2f83d2d9b6701871f9/schedule.json | verified 2026-07-30 (claims C-E00-001/002) |
| bats test-authoring semantics (helper loading, scratch dirs, `run` flags) — the L4 harness | https://github.com/bats-core/bats-core/blob/ae4b94d7cc35f62468297791aa4ab8c3af7377ba/docs/source/writing-tests.md · `docs/source/warnings/BW02.rst` · `docs/CHANGELOG.md` (same pin) | verified 2026-08-11 (claims C-E12-001..004) |
| vitest 4.1.10 projects + v8 coverage thresholds — the L1/L2 runner | installed copy: `node_modules/vitest/dist/chunks/reporters.d.DtoKVV2s.d.ts` (option types), `node_modules/vitest/dist/chunks/coverage.DM_a_rWm.js` (`resolveThresholds`/`checkThresholds`) · https://registry.npmjs.org/@vitest/coverage-v8 (exact peer pin) | verified 2026-08-11 (claims C-E12-005..010); behaviour the sources don't state is measured in `research/experiments/E12-test-harness/` |

## Experiment archives (grow under `research/experiments/`)

- `oracle-spike/` — first preview-API request/response (E00-S03-T02)
- `E12-test-harness/` — vitest project/threshold and bats tmpdir probes (E12-S01-T01)
- `E01-quirks/` — anchors/dup-keys/multi-doc service behavior
- `E02-grammar/` — **survey.md**: 74 live probes settling the expression grammar (literals, operators,
  access, parse-time validation, `$[ ]`) — E02-S01-T01
- `E02-coercion/`, `E02-errors/` — expression edge cases & error shapes
- `E03-visibility/` — compile-time variable visibility matrix
- `E06-env-materialization/` — hosted collision/overlay/secret/PATH environment matrix (run 540)
- `E06-condition-flow/` — hosted implicit/explicit condition, skip-log, and continued-failure flow (run 543)
- `E06-setvariable/` — hosted current/subsequent task timing, output routing, and masking (run 544)
- `E08-rest/<endpoint>/` — redacted live samples per REST endpoint
- `E10-<task>/` — live parity transcripts for the priority task set
