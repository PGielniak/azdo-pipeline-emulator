# E04 — Semantic model (expanded pipeline)

Phase: P2 · Depends on: **E00-S04** (the service expansion supplies `finalYaml`), E01 · Design: docs/01 §3–§6
> *Dependency corrected 2026-08-22 (E12-S01-T02): this line still read `E01, E03` — a reference to a
> locally-computed expansion. E03 is now the offline fallback and this epic's input is the service's
> `finalYaml` (see the re-scope note below); E03 is a dependency only when `--offline-expand` is used.*

Primary grounding set: yaml-schema per-keyword pages (stages/jobs/steps/pool/strategy) · …/process/phases (jobs) · …/process/stages · …/process/deployment-jobs · …/build/variables (predefined) · …/process/variables.

> **Re-scoped by the simplification (docs/07).** The model is now built from the **service's
> normalized `finalYaml`**, not a locally-expanded DOM. The preview endpoint already desugars
> shorthand and wraps implicit structure (measured: `steps:` → `stages: __default` → `job: Job` →
> `task: CmdLine@2`), so part of the old normalization work is delegated. What remains here is the
> **runtime** model: canonical steps, variable scoping, matrix/parallel expansion, and the
> dependency graph.

## E04-S01 — As an engine developer, the expanded pipeline becomes a typed model with invariants, so emission works from guaranteed-clean input.
Acceptance: model types + builder with all invariants from docs/01 §6 enforced and tested.

- [x] **E04-S01-T01 — Model types & builder** *(done 2026-08-23: `packages/engine/src/model/{types,build}.ts` — the docs/01 §6 shape plus `buildPipeline(ParseResult)` returning `{pipeline, diagnostics}`. **The input contract was measured rather than assumed, and the corpus was already on disk:** all **147** inherited `final.yml` transcripts are rooted at `stages:` (C-E04-001), with `__default`/`Job` appearing in exactly the 78 whose input was a bare `steps:` (C-E04-002, confirming C-E00-017/018 holds beyond the original spike). The corpus had **one gap** — no probe is rooted at `jobs:` — so two probes were run to close it (`research/experiments/E04-model/`), and the second returned a surprise: an authored but unnamed `- job:` expands to **`job: ''`**, an empty name, *not* the synthetic `Job` (C-E04-004). The synthetic name is what the service supplies when it invents the job, not a default it applies to one the author wrote — so **job identity may be the empty string**, the builder preserves it, and E05 owns inventing a filename rather than having that hidden from it. **Why the builder still wraps** although the default path never needs it: `--offline-expand`'s retained engine makes no such guarantee, so wrapping keeps the fallback a conversion instead of a crash, and makes both arms produce the *same* model. Fields later tasks own (`disposition` E07-S03-T01, `fidelity` E05-S02-T02, `matrixKey` E04-S03) are typed and marked rather than omitted, and asserted unset so "not built yet" cannot be mistaken for "absent". A deployment job carries **no** steps rather than borrowing its strategy's — that flattening is E04-S03's. Evidence: `research/E04-semantic-model.md` C-E04-001..005 (new file + block table); `research/experiments/E04-model/` 2 redacted probes; `packages/engine/test/model/build.test.ts` (30, two of which rebuild from the captured `final.yml` byte-for-byte); 1,589 Vitest + 241 Bats green.)*
  **Do:** `packages/engine/src/model/*`: Pipeline/Stage/Job/Step per docs/01 §6; builder from the service's `finalYaml`; single-job/single-stage shorthand → full tree (any case the service leaves unwrapped).
  **Ground:** yaml-schema `pipeline` page (which shorthands exist at root); C-E00-017/018 (the preview's `__default`/`Job`/`CmdLine@2` wrapping).
  **Done:** builder tests: shorthand forms produce identical models to explicit forms.
- [x] **E04-S01-T02 — Normalization boundary: what the service already desugars** *(done 2026-08-23: 9 probes (`research/experiments/E04-normalization/`, `matrix.md`) and **the task's own instruction resolves in an unexpected way — the remainder is empty in the sense it meant and non-empty in a sense it did not anticipate.** *Empty:* every documented shorthand is desugared by the service — `script`→`CmdLine@2`, `bash`→`Bash@3`, `pwsh`/`powershell`→`PowerShell@2`, and `publish`/`download`/`checkout`→bare GUIDs — with the canonical-`task:` control passing through unchanged, so we desugar nothing (C-E04-030/036). *Non-empty:* three of them lose the authored keyword to a GUID, and **PLAN D4 emits `checkout` natively while docs/04 §8.1 tells the emitter to count `checkout` steps** — on the default path that count is **zero**, silently, giving every job the single-checkout layout. So the model recovers the keyword into `Step.origin` (`packages/engine/src/model/shorthand.ts`), docs/04 §8.1 gained a banner saying to count that, and docs/06 §5 decision **57** records it. Boundary respected: this renames nothing — `normalize.ts` keeps its single grounded GUID→name entry and its deliberate refusal to name the agent-internal ones (C-E12-019), `task.name` keeps the GUID, and a test asserts that table was not quietly extended (C-E04-033/034). Two details carried forward: desugaring **renames inputs** (`publish:`'s `artifact:`→`artifactName:`, `download:`'s scalar→`alias:`), and `pwsh` vs `powershell` is decided by a `pwsh: true` **input**, not by the task reference (C-E04-037). `getPackage` is HTTP 400 without a package resource and is recorded as **unmeasured** in code rather than silently omitted (C-E04-035). Evidence: `research/E04-semantic-model.md` C-E04-030..037; `packages/engine/test/model/shorthand.test.ts` (18, table-driven off the transcripts); 1,607 Vitest + 241 Bats green.)*
  **Do:** probe `finalYaml` for a matrix of step shorthands (`script`, `bash`, `pwsh`, `powershell`, `publish`, `download`, `checkout`, `getPackage`) and record which become canonical tasks; implement a `normalize.ts` pass for **only** the remainder.
  **Ground:** the preview expansion is the authority — one redacted probe set under `research/experiments/E04-normalization/`; each row a claim.
  **Done:** table-driven tests; every shorthand either provably desugared by the service or normalized by us with a claim.
- [x] **E04-S01-T03 — Common step fields normalization** *(done 2026-08-23: 6 probes (`research/experiments/E04-step-fields/`) plus the `steps.task` schema page, and **two of the fields this task's Do calls "common step fields" are not step fields at all**. `workingDirectory` and `failOnStderr` are task **inputs** — the schema page omits both from the common list and the expansion puts them in `inputs` for `script`→`CmdLine@2` and `bash`→`Bash@3` alike (C-E04-061, the same fact C-E06-033 records from the runtime side). A model reading them off the step mapping finds nothing, silently, for every pipeline — **which is exactly what E04-S01-T01 shipped this morning**, so its reader and its test are corrected here. **The `VERIFY` resolves to "no scheme exists":** two unnamed steps expand with no `name:`, and across all **157** captured expansions no step-level `name:` appears that the author did not write — the 84 files matching `name:` are all `resources.repositories[].name` (C-E04-063). A corpus test asserts that continuously. Also measured: `target:` always arrives as the **object** form, the scalar shorthand having been normalized to `{container: host}` (C-E04-062); `enabled: false` **survives** expansion, so skipping the step is the runner's job (C-E04-064); `displayName` is *not* defaulted by the service, so our fallback is ours and is labelled as such (C-E04-065); and `bash` gains a `targetType: inline` input the author never wrote (C-E04-067). **The Ground field's real-run method was not used and that is stated rather than glossed:** it prescribed "a real-run output-variable reference experiment", and the preview corpus answers the question the model can actually observe — the model is built from `finalYaml`, so a name the *server* might invent in its timeline is unobservable to it and changes nothing here (E05 names files from ordinal + `displayName`, docs/04 §1). No pipeline run was spent. Evidence: `research/E04-semantic-model.md` C-E04-060..067; `packages/engine/test/model/step-fields.test.ts` (21); 1,628 Vitest + 241 Bats green.)*
  **Do:** `name` auto-generation (verify the scheme), `displayName` defaulting, `enabled`, `timeoutInMinutes`, `retryCountOnTaskFailure`, `continueOnError`, `workingDirectory`, `failOnStderr`, `target` parse.
  **Ground:** yaml-schema `steps-task` page field list; auto-name scheme `VERIFY` via a real-run output-variable reference experiment.
  **Done:** field defaulting tests incl. the verified auto-name scheme.

## E04-S02 — As a pipeline developer, variables resolve with the exact scoping/precedence rules of the service, so `$(var)` values match cloud runs.
Acceptance: scoping engine + classification, proven against docs and probes.

- [x] **E04-S02-T01 — Scope resolution & precedence** *(done 2026-08-23: `packages/engine/src/model/variables.ts` — `resolveVariables(pipeline, stage?, job?)` layering root → stage → job with last-wins inside a layer, plus group-name collection. **The load-bearing finding is that this is ours at all:** unlike the step shorthands, which the service desugars completely, the expansion resolves **no** variable precedence — a document setting `a` at all three levels comes back with all three blocks intact (C-E04-080) — and collapses **no** duplicates within a scope (C-E04-081). The documented order is quoted (C-E04-082) and the resolver layers exactly the three YAML levels: queue-time and the settings UI are levels 4–5 and sit outside the model, being the `.env` boundary of PLAN D7. Scope isolation is asserted in the negative too — a job never sees a sibling stage's or a sibling job's (C-E04-083). Consequential design change: `variables` on Pipeline/Stage/Job moved from a flat `Record<string,string>` to a **declaration list**, because a flat map cannot represent the duplicates the expansion keeps or carry `readonly`; E04-S01-T01's two assertions were updated to match. Also measured: the mapping shorthand **is** normalized to the list form by the service (C-E04-084, the mapping branch kept for `--offline-expand`), `readonly: true` survives verbatim so the runtime's C-E06-005/006 enforcement can be told which names are locked (C-E04-085), and names fold case to match the agent dictionary (C-E06-003) so a differently-cased override replaces rather than duplicates. **Gap recorded, not guessed:** an unauthorized `group:` is rejected by the service outright (C-E04-086) and what an *authorized* group expands to is unmeasured — it needs a group provisioned in the test org, and D7 forbids fetching values regardless; the resolver carries a group as a named marker contributing no value, which is what E04-S02-T02 and docs/04 §10 expect. Evidence: `research/E04-semantic-model.md` C-E04-080..086; `research/experiments/E04-variables/` 5 probes; `packages/engine/test/model/variables.test.ts` (22); 1,650 Vitest + 241 Bats green.)*
  **Do:** root→stage→job layering, list-order override semantics, mapping vs list forms, `readonly` flag carried to manifest/runtime.
  **Ground:** variables doc "Variable scope"/precedence sections (quote); oracle probes for list-order overrides mixing `group`/`template`/inline entries.
  **Done:** precedence test matrix; claims cited.
- [x] **E04-S02-T02 — Variable classification** *(done 2026-08-23: `packages/engine/src/model/classify.ts` classifies every referenced variable as `inline` / `group-member` / `predefined` / `setvariable-produced` / `env-required`, with reference sites and warnings. **The dependency runs backwards and is handled by a port rather than a duplicate table:** the Ground field names the predefined list, but building it is **E04-S02-T03**, which is scheduled *after* this task — so the set is injected and empty by default (C-E04-092), and a **prefix heuristic** (`Build.`/`System.`/`Agent.`/`Pipeline.`/`Environment.`/`Release.`) keeps the gap visible by warning instead of silently demanding a service-owned name in `.env`. That heuristic is ours and is labelled as not-a-claim. **A bug the tests caught, and the reason the scanner is hand-rolled:** the first version used a regex, which on `$(a$(b))` consumed the closing paren and reported **nothing** — the runtime resolves the *inner* macro (C-E06-024), so the scan now restarts at the innermost opener and mirrors `azdo__macro_scan` exactly (C-E04-087). Macro positions are step `inputs` and `env` values only, per docs/01 §4 — a `$(x)` in a `displayName` is not one (C-E04-088). Classification is per **referencing scope**, so a name declared only in a sibling job is `env-required` here (C-E04-083); names fold case (C-E06-003); the `setvariable` scan is best-effort by construction and a miss degrades to `env-required`, which prompts rather than fails (C-E04-091); and a group makes an unaccounted name `group-member` — a *possibility* carrying the declared group names, never a resolved membership (C-E04-090, PLAN D7). No new probe was run, deliberately: both syntaxes are already measured in E06, and re-probing would record the same facts twice. Evidence: `research/E04-semantic-model.md` C-E04-087..092; `packages/engine/test/model/classify.test.ts` (32, incl. the corpus snapshot over 100+ captured expansions and the warning path in both directions); 1,682 Vitest + 241 Bats green.)*
  **Do:** classify every referenced variable: inline / group-member (name-only) / predefined / `.env`-required / setvariable-produced (best-effort static scan).
  **Ground:** docs/01 §4; predefined list from …/build/variables page (vendored as data with provenance).
  **Done:** classification snapshots on corpus; unknown-predefined warning path tested.
- [x] **E04-S02-T03 — Predefined variable table as data** *(done 2026-08-23: `packages/engine/data/predefined-vars.json` — **91 rows**, each with its doc section, a deep anchor and the first sentence of its description — generated by `scripts/predefined-vars.ts` (`pnpm predefined-vars`), plus a reader in `packages/engine/src/model/predefined.ts` that fills the port E04-S02-T02 left injectable. **The source is the docs *repository*, not the rendered page, and that is what makes the Done criterion achievable:** the page builds its table through an `[!INCLUDE]`, so the rendered HTML has no version to fetch, while `MicrosoftDocs/azure-devops-docs` is public and addressable by commit — re-running at the same pin produces byte-identical output, verified by regenerating and diffing (C-E04-093). **Two pins, because the content is split**, and the coverage check is what caught it: fetching only the include silently loses `Build.Clean` and `System.Debug`, which docs/01 §5 requires — they live in their own page sections **because** they are the page's stated exceptions to "automatically set … and read-only", i.e. the only *writable* predefined variables, now flagged as such in the data (C-E04-094/095). All 31 names docs/01 §5 references resolve, with `System.PullRequest.*` expanded to its eight members and `Build.SourceBranch(Name)` to its two spellings — asserted by a test rather than checked once (C-E04-096). Two packaging bugs fixed before they shipped: the loader's fixed `../../data` would have resolved wrongly from the bundled `dist/index.js`, so it uses the same walk-up resolver as `vendoredSchemaPath()`; and `data` was missing from the engine's `package.json` `files`, so the table would not have been published — both verified by loading from `dist`. Evidence: `research/E04-semantic-model.md` C-E04-093..096; `packages/engine/test/model/predefined.test.ts` (11); `research/REFERENCES.md` row updated; 1,693 Vitest + 241 Bats green.)*
  **Do:** `packages/engine/data/predefined-vars.json` generated from the docs page, with local-mapping strategy per docs/01 §5.
  **Ground:** …/build/variables page (pin); every row carries the doc anchor.
  **Done:** table covers docs/01 §5 set; scraper re-run produces stable output.

## E04-S03 — As a pipeline developer, matrices, slicing and dependency graphs expand/validate exactly like the service, so job topology is identical locally.
Acceptance: matrix/parallel expansion + graph validation with service-matching defaults.

- [x] **E04-S03-T01 — Matrix & `parallel` expansion** *(done 2026-08-24: `packages/engine/src/model/strategy.ts` — `expandJobStrategy()` multiplies a job with `strategy:` into its concrete jobs, wired through `buildJob` so the builder now returns a leg set. **The naming was measured, and two of the three answers contradict the docs.** A matrix leg is `<JobName> <key>` — a **space**, not the `Job_<key>` underscore docs/01 §2 wrote (C-E04-110/119, timeline records `Build Alpha`/`Build Beta` from run 546); inside a leg `System.JobName` is the **key alone** (`Alpha`) while `System.JobDisplayName`/`Agent.JobName` carry `<JobName> <key>` (C-E04-120) — a runtime-seeding fact recorded for E06, not a model-shape fact; and `parallel: N` slices are `<JobName> <position>` with a **1-based** position and `System.JobPositionInPhase`/`System.TotalJobsInPhase` = `{1..N}`/`N` (C-E04-121). Implemented: matrix keys expand in authored order, each key's mapping becomes job-level variables (C-E04-112/122), `maxParallel` recorded verbatim (C-E04-113), `parallel` slices inject the two slice variables (C-E04-114), a runtime-expression `matrix: $[ … ]` warns `strategy-matrix-runtime-expression` and leaves the job unexpanded (C-E04-116), and an empty matrix yields one job (C-E04-117). docs/01 §2/§6 corrected and docs/06 §5 decision **58** records the whole finding. Evidence: `research/E04-semantic-model.md` C-E04-110..122; `research/experiments/E04-strategy/real-run.md` (run 546) + `scripts/matrix-parallel-realrun.ts`; `packages/engine/test/model/strategy.test.ts` (11 + 1 snapshot); 1,704 Vitest green, typecheck/eslint/prettier/VERIFY-guard clean.)*
  **Do:** matrix → concrete jobs (naming scheme `JobName multiplier` — verify exact format), per-job injected variables; `maxParallel` recorded; `parallel` slicing with `System.JobPositionInPhase`/`System.TotalJobsInPhase`.
  **Ground:** jobs doc (…/process/phases) strategy sections; verify generated job names via a real matrix run in the test org.
  **Done:** expansion tests incl. runtime-expression matrix → warning path per docs/01.
- [x] **E04-S03-T02 — Dependency graphs & defaults** *(done 2026-08-24: `packages/engine/src/model/graph.ts` — `resolveStageGraph()`/`resolveJobGraph()` compute the effective dependency lists and validate them, wired into `buildPipeline` so the docs/01 §6 invariant ("graphs acyclic & references valid") is enforced at model build. **The two defaults are the one thing the docs settle, and they are the asymmetry the module exists for** (C-E04-123/124): a stage without `dependsOn` runs after the stage before it, a job without `dependsOn` has no dependency, and `dependsOn: []` is the explicit opt-out that breaks the sequential stage default (C-E04-125) — so `Stage.dependsOn` became `readonly string[] | undefined` (absent vs explicit-empty) while `Job.dependsOn` stays a plain list. **Everything about a broken graph is measured, not guessed** — 12 live preview probes (`research/experiments/E04-dependency-graph/`, `pnpm dependency-survey`) — and the validation reproduces the service's sentences verbatim: a missing target is `Stage B depends on unknown stage NoSuchStage.` / `Stage A job A2 depends on unknown job NoSuchJob.` (C-E04-126/127); a cycle is reported **edge-by-edge** (`… which creates a cycle in the dependency graph.`), one sentence per participating edge in source-declaration order (C-E04-130/131/132); a graph with no dependency-free node is a single sentence (`The pipeline must contain at least one stage with no dependencies.`) that **shadows** the other checks (C-E04-128/129/133); and the precedence is no-dependency-free-node → cycle edges → missing targets (C-E04-134), with a self-loop reported as a cycle edge only when a root exists elsewhere (C-E04-135). **The finding with reach beyond this task is C-E04-136:** `dependsOn` references the *authored job name*, not a matrix/parallel leg id, so the job graph keys on a new `Job.referenceName` (the base name, shared by all legs) rather than on the `Build Alpha`/`Build Beta` ids E04-S03-T01 renamed them to. Recorded-but-deferred (not in the **Do**): duplicate stage/job names are rejected by the service with their own sentences (C-E04-137/138), out of scope here because the service rejects them before expansion so the default path never sees them. Evidence: `research/E04-semantic-model.md` C-E04-123..138; `research/experiments/E04-dependency-graph/` (6 committed transcripts); `packages/engine/test/model/graph.test.ts` (20); 1,724 Vitest green, typecheck/eslint/prettier/VERIFY-guard clean.)*
  **Do:** stage graph (default **sequential** in YAML order without `dependsOn`) and job graph (default **parallel**), cycle/missing-target errors, empty-`dependsOn: []` semantics.
  **Ground:** stages + jobs doc dependency sections (quote the differing defaults); error phrasing via oracle probes with a missing dependency.
  **Done:** graph tests for defaults, cycles, missing refs; claims cited.
- [x] **E04-S03-T03 — Deployment job model (runOnce)** *(done 2026-08-24: `packages/engine/src/model/deployment.ts` (the `runOnceOutputVariableKey` naming-quirk helper) + `environment`/`strategy`/`autoDownloadArtifacts` on `Job` in `types.ts` and parsed in `build.ts`. **The naming quirk refutes docs/01 §3's `outputs['<lifecycle-hook>…']` description** — run 548 reads both spellings and the **job-name** key resolves while the hook-name key is empty (C-E04-151/153), so runOnce keys by `<job>` or `Deploy_<resourceName>` (C-E04-152) and only canary/rolling use the hook name (C-E04-154, reserved). **The environment resource forms are doc-grounded, not measured**, because preview rejects them outright when the resource is absent (`"Resource <r> does not exist in environment <e>."`, C-E04-143/144) and no task provisions a VM/K8s resource; the scalar `{name}` promotion and runOnce hook sequence are measured (`research/experiments/E04-deployment/`). **The auto-download flag's detection is a measured surprise**: `- download: none` desugars to the `download` GUID task with `condition: false` + `inputs.alias: none` (C-E04-150), so the flag keys off `origin === 'download'` + `alias === 'none'` rather than a key the expansion does not carry. `rolling`/`canary` parse to a bare `{kind}` marker (E08). Evidence: `research/E04-semantic-model.md` C-E04-140..155; `research/experiments/E04-deployment/` (5 preview probes + run 548); `packages/engine/test/model/deployment.test.ts` (14); docs/01 §3/§6 corrected + docs/06 §5 decision **59**; 1,752 Vitest + 241 Bats green.)*
  **Do:** `deployment` kind, `environment` parse (name, resourceName, resourceType), runOnce hook sequence model, implicit artifact auto-download flag, output-variable naming quirks recorded on the model.
  **Ground:** deployment-jobs doc (quote hook order, auto-download, output-variable naming); the naming quirk verified by real-run experiment reading `stageDependencies`.
  **Done:** model tests; quirk claims cited; rolling/canary reserved but unimplemented (E08).
- [ ] **E04-S03-T04 — `manifest.json` serializer**
  **Do:** versioned schema per docs/04 §11; includes fidelity/warnings/env/tools aggregation hooks (filled by E07/E08).
  *Pointer added 2026-08-22 (E12-S01-T02): the manifest is also where the **expansion mode** is recorded — E12-S01-T01 already returns a typed `ExpansionManifestEntry` (`service` + api-version/pipelineId/hashes, or `offline` + `degraded: true`) from `resolveExpansion()` with no writer to serialize it. Serializing it is this task's, not new scope; see docs/06 §5 decision 42(b) for the one interaction to handle — an offline re-convert of a service-expanded project leaves a stale `expansion` lockfile entry under the same request hash.*
  **Ground:** docs/04 §11 shape as spec; JSON-schema for the manifest committed.
  **Done:** golden manifests for corpus; schema validation in tests.
