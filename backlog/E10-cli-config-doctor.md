# E10 — CLI, config & doctor

Phase: P1–P3 (incremental) · Depends on: E00 · Design: docs/06 §1–§2
Primary grounding set: docs/06 (internal spec) · consuming epics' manifests · vendor docs for version-detection commands in doctor.

> **Trimmed by the simplification (docs/07).** The `preview-diff` command is gone (there is no
> local expansion to diff against — the service *is* the expansion, PLAN D3); `--min-coverage` and
> `--target-os` are dropped with the coverage metric and the deferred Windows host.

## E10-S01 — As a user, the CLI skeleton exists early with stable UX conventions, so every feature lands behind a consistent interface. (P1)
- [x] **E10-S01-T01 — CLI framework & command scaffold**
  **Do:** `packages/cli` (commander): `auth login|status`, `convert`, `doctor`, `fetch-artifacts`, `run` registered with help text per docs/06 §1; global `--json`, exit-code policy (0/1/2/3) centralized.
  **Ground:** docs/06 §1 as spec; chosen CLI lib docs pinned.
  **Done:** `--help` snapshots; exit-code unit tests; unknown-flag behavior defined.
- [x] **E10-S01-T02 — Config loader & precedence**
  **Do:** `azdo-emu.yaml` schema (typed, validated with friendly errors), precedence CLI > config > defaults, `--parameter` repeatable + `@file.json` complex values.
  **Ground:** docs/06 §2 example as schema source; JSON-schema for the config committed.
  **Done:** precedence matrix tests; schema published in repo.

## E10-S02 — As a user, `convert` exposes the full P2 surface honestly. (P2)
- [x] **E10-S02-T01 — `convert` wiring & flags** *(done 2026-08-26: `packages/cli/src/convert/convert.ts` — the keystone. `convert`'s body threw `NotImplementedError` while seven epics' worth of machinery sat behind it; it now runs **config → bundle → expand → validate → model → emit → write**, and the order is forced: bundle before expand (the service reads *committed* bytes, so a local template edit reaches it only by being spliced into the override), expand before validate (the expansion is a different dialect with its own validator, C-E03-258), validate before model. Flags: the whole docs/06 §1 surface — `-o`, `--parameter`, `--org`, `--project`, `--target-os`, `--checkout-mode`, `--exec-env`, `--sandbox-image`, `--group-names`/`--no-group-names`, `--only-stage` (repeatable), `--frozen`, `--update`, `--offline`, `--offline-expand`, `--strict`, `--no-bundle`. Five behaviours are ours and are recorded as docs/06 §5 decision **69**: `--only-stage` **drops** a dependency on a stage it left out and warns (keeping it would make `run.sh` name a stage that is not there; refusing would make the flag useless for the huge pipelines it exists for); `--exec-env sandbox` and `--sandbox-image` are **refused**, not ignored, because accepting them writes a `manifest.json` claiming an execution environment the project does not have; `--offline` alone is a usage error, since docs/06 §1 makes it satisfiable only via `--frozen` or `--offline-expand`; `--strict` writes the project first and *then* exits 2, so the user has the README's ranked list to read; and `--json` prints a **versioned** summary rather than a schema-validated document. **Three deferred fills landed here, each by name:** `lib/runtime.sh` + `lib/expr.sh` copied from the runtime (decision 62), `.shellcheckrc` with `SC2005,SC2046` (decision 61) and `SC2016,SC2071` (decision 62(d)), and the manifest's per-step `file` + per-job `targetOs` (decision 64) — filled *after* `serializeManifest` because both are facts about the generated project, with `manifest.schema.json` gaining the two properties docs/04 §11 always specified. Also: `expandDocument` is now bound as the `OfflineExpander` port, so `--offline-expand` works end to end; `run()` became async and commander is driven through `parseAsync`, which also fixed two existing tests that were leaking unhandled rejections. Done met: **e2e per flag** — every flag has a case observing a *different* generated project or exit code, on both arms (the service arm hermetic through the `fetchImpl` seam, the offline arm for real) — and `--json` is snapshot-pinned. The strongest test is not about a flag: the generated project is **run with bash** and its first step's log read back. 27 convert tests; full suite 2171 green; `cli/src` back above its 97% line gate.)*
  **Do:** `-o`, `--checkout-mode`, `--only-stage`, `--group-names`, `--offline`, `--frozen`/`--update`, `--strict`, `--no-bundle`; progress output; a final one-line summary (steps emitted, warnings count).
  **Ground:** docs/06 §1 flag list; each flag's behavior test cites the epic that implements it (cross-link table in a research note).
  **Done:** e2e per flag on fixtures; `--json` output schema stable.
- [x] **E10-S02-T02 — `run` convenience proxy** *(done 2026-08-26: `packages/cli/src/run/run.ts` — `runProject(outdir, args)` spawns `bash run.sh` with `stdio: 'inherit'` and reports the child's status as its own. **The guard the Do field asks for is the first test in the file:** the module's source, with comments stripped, is asserted to contain **none** of `run.sh`'s flag names (`--list`, `--env-file`, `--resume`, `--from-step`, `--to-step`, `--only-step`). A proxy that learned them would be a second place to update whenever the emitter changes, and would diverge silently from the script it fronts. **Exit-code transparency is the contract, not a nicety:** `run.sh` ends with `exit "$(azdo_run_exit_code)"`, so its status *is* the pipeline's verdict, and collapsing it onto the CLI's own 0/1/2 would make `azdo-emu run` and `bash run.sh` disagree about whether the pipeline passed. The status therefore passes through untouched — including 126/127 from the shell and **128+N** for a fatal signal, both pinned to the GNU Bash Reference Manual v5.3 §3.7.5 (C-E13-020/021/022, newly deep-verified). A `ProxiedExit` carries it out through the one exit path, and `run()`'s return type widened from `ExitCode` to `number` for exactly that reason. `os.constants.signals` supplies N rather than a hard-coded table, because `SIGUSR1` is 10 on Linux and 30 on macOS. Commander needed `enablePositionalOptions()` + `passThroughOptions()` so `azdo-emu run out --list` hands `--list` to the script instead of rejecting it here. **Δ recorded, not hidden:** Node has no `exec(3)`, so an intermediate process exists where a shell proxy would have replaced its image — the observable status is identical, but a signal delivered to the proxy is not automatically delivered to the child's group. Done met: **e2e parity** compares proxy vs direct `bash run.sh` on a real converted project — success, a non-zero verdict, `--list`, an unknown flag (`run.sh`'s exit 2, not ours) and a fatal signal — each on both exit code and behaviour. 18 tests; full suite **2188 green**.)*
  **Do:** thin passthrough to `<outdir>/run.sh` forwarding args; zero logic duplication (guard test: proxy adds nothing but exec).
  **Ground:** docs/04 §2 entry-point contract (the proxy must forward exactly the documented flags; cite the flag claims from E05-S01-T03); POSIX `exec`/arg-forwarding semantics from the GNU bash manual (pin).
  **Done:** e2e parity: proxy vs direct invocation identical, including exit codes and signal handling.

## E10-S03 — As a user, auth commands make sign-in state obvious. (P3)
- [ ] **E10-S03-T01 — `auth login`/`auth status` UX**
  **Do:** device-code display flow (code + URL + spinner), mode display, org/identity/expiry table, `--github` variant; failure hints (firewall, conditional access).
  **Ground:** E09-S01 implementations + their pinned sources; UX copy reviewed against the real `az login` device flow wording.
  **Done:** manual walkthrough recorded; non-tty behavior defined & tested.

## E10-S04 — As a user, `doctor` tells me exactly what my machine is missing for a given generated project. (P3)
- [!] **E10-S04-T01 — Doctor engine** *(**The probe table, version comparison, remediation and report are done 2026-09-02 in `packages/cli/src/doctor/probe.ts`, with both Done items met — probe table tests over canned outputs, and a live run of the shipped engine. What is left is the `--json` flag and the CLI wiring that reads `manifest.json` `tools[]`, which belongs with `program.ts` and E10-S04-T02's `ToolRequirement` contract.** `runDoctor` already takes exactly that array, so the wiring is a call site, not a design. **Scope note (C-E10-006):** `az` and `docker` probes are confirmed against live installations; `kubectl`/`helm`/`pwsh` are doc-grounded only, because they are not installed here.)*
  **Do:** read `manifest.json` `tools[]`; probe PATH + versions (per-tool version-command table), compare semver ranges; remediation strings (install hints per OS); `--json`.
  **Ground:** each probe command cited from the tool's official docs (`az version`, `docker version --format`, `helm version --template`, `kubectl version --client -o json`, `pwsh -v` — pin each vendor page).
  **Done:** probe table tests with canned outputs; fixture manifest → doctor snapshot.
- [ ] **E10-S04-T02 — Doctor↔task contract**
  **Do:** `ToolRequirement` schema finalized (cmd, min, neededBy, remediation); aggregation in manifest; CI check: every task declaring CLI calls declares requirements.
  **Ground:** requirement values cite the task research notes (E07/E08) — doctor never invents versions.
  **Done:** contract test over all registered tasks.
