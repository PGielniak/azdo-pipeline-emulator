# E11 — Testing & parity harness

Phase: cross-cutting (starts P1) · Depends on: E00 · Design: docs/06 §3
Primary grounding set: the preview API and real runs in the test org are themselves the grounding instruments; this epic builds and operates them. Layer numbering = docs/06 §3.

> **Re-scoped by the simplification (docs/07).** There is no local expansion engine to diff, so the
> old "expansion golden" and "`preview-diff`" layers are replaced: the **service's `finalYaml` is the
> golden**, and the nightly job re-expands the corpus and converts + runs it as a regression check
> against service drift. Real-run verification (L6) remains, because runtime claims still need it.

## E11-S01 — As a contributor, test infrastructure exists from day one, so every epic lands with its tests.
- [x] **E11-S01-T01 — Test layout & runners**
  **Do:** vitest projects per package; bats harness for `packages/runtime` with fixture-store helpers; `pnpm test` orchestrates all; coverage thresholds per package.
  **Ground:** bats-core docs (pin) for helper patterns; internal spec docs/06 §3.
  **Done:** skeleton suites run in CI (E00-S01-T02).
- [x] **E11-S01-T02 — Fixture corpus v1 (authoring)**
  **Do:** author the first 10 corpus pipelines patterned on docs/06 §3 shapes; each with a README stating what it exercises.
  **Ground:** every corpus file must be **accepted by the real service** — submit via preview; store `finalYaml`; a corpus entry without its oracle pair is invalid.
  **Done:** 10 pairs committed under `fixtures/corpus/` + `fixtures/oracle/`.
- [x] **E11-S01-T03 — Run the bats suite under bash ≥ 4 on macOS CI** *(done 2026-08-23: `brew install bash` on the macOS runner plus Homebrew's bin prepended via `$GITHUB_PATH` — that is what actually selects the interpreter, since bats' shebang is `#!/usr/bin/env bash` and resolves through PATH. CI logs the choice (`bats will run under /opt/homebrew/bin/bash — GNU bash, version 5.3.15`) and a guard step in front of bats fails with one message naming PLAN D2 if the major version is < 4, because bats itself is content on 3.2 (C-E00-005) so the symptom is otherwise ~30 `bad substitution` failures that read as runtime bugs. **This was a known requirement nobody had built:** C-E00-005 said on 2026-07-30 that "E06 tests must ensure a bash ≥ 4 interpreter on macOS CI"; the claim was right and unimplemented, and unobservable until E03-S02-T05 made the macOS vitest step green. Claims C-E00-028/029 (`macos-latest` = macOS 26 arm64, Bash 3.2.57 only, Homebrew 6.0.13, no second bash) with the image commit-pinned in REFERENCES.md; docs/06 §3 states the L4 interpreter rule. The runtime was **not** downgraded to 3.2 syntax. Done criterion — "all four CI jobs green" — verified by run **32605819867** on PR #24, which also carried E11-S01-T04; the bash fix alone took macOS from ~30 failures to 9.)*
  **Do:** `.github/workflows/ci.yml` invokes `bats test` directly, so on `macos-latest` the suite runs under the system `/bin/bash` — **3.2**, from 2007. `packages/runtime/lib/core.sh` uses bash-4 syntax (`${value,,}` at line ~3057 and `${repository,,}` at ~3605), which 3.2 rejects outright: `bad substitution`, ~30 failing tests across the `.env` loader, artifacts and every `azdo_checkout` mode. Install a modern bash on the macOS runner (`brew install bash`) and put it ahead of `/bin/bash` for the bats step (or point bats at it), so the suite runs on the version the project targets. **Do not** rewrite the runtime to bash 3.2: `bash ≥ 4` is a documented convention (CLAUDE.md tech conventions, docs/04) and lowering it silently would be a decision, not a fix — if it is ever revisited it needs a docs/06 §5 entry.
  **Ground:** CLAUDE.md tech conventions + docs/04's runtime prerequisites for the `bash >= 4` floor; the GitHub-hosted macOS runner image manifest (`actions/runner-images`, commit-pinned) for which bash versions are present and at what paths; bats-core docs for how the suite selects its interpreter. Failure evidence is on file: CI run 32603458075, macOS jobs.
  **Done:** all four CI jobs green on a PR — `macos-latest` bats included; the workflow states which bash it runs and why, so the next reader does not re-derive it; no runtime source is downgraded to 3.2 syntax.
- [x] **E11-S01-T04 — Bats scratch paths must be physical, so macOS's `/var` symlink stops failing path assertions** *(done 2026-08-23: `prepare_checkout()` normalizes its scratch root once and `azdo_emu_scratch_dir()` hands back a physical path, so a test comparing against runtime output is right by default. **No runtime source changed** — the runtime's `pwd -P` is deliberate and correct; the expectations were wrong. **Two further faults surfaced only once the suite ran that far, and neither is named by this task's Do — recorded rather than folded in silently:** (1) a regression from this task's own first commit — `fixture-store.bats` asserted `[[ "$first" == "$BATS_TEST_TMPDIR"/* ]]`, which stops matching once the helper returns the physical path, so the assertion now compares against the physical tmpdir (the helper's contract is the stronger one); (2) `core.bats:1950` compared `wc -l` output with `=`, and **BSD `wc` pads its count with leading spaces** — a portability bug unrelated to paths, fixed with numeric `-eq`, matching line 554's existing form. Done criterion verified by run **32605819867**: all four jobs green, macOS bats included; Linux unaffected (231/231 locally).)*
  **Do:** `prepare_checkout()` in `packages/runtime/test/core.bats` builds its tree from `$BATS_TEST_TMPDIR` verbatim, and on macOS that is `/var/folders/…`, where `/var` is a symlink to `/private/var`. The runtime resolves physically (`cd … && pwd -P`, `core.sh` ~3143/3326/3702 — deliberate, and correct), so every `[ "$(azdo_var 'Build.SourcesDirectory')" = "$checkout_workspace/s" ]` compares `/private/var/…` against `/var/…` and fails. **The expectation is what is wrong, not the runtime:** normalize the scratch root once (`root="$(cd "$root" && pwd -P)"`) and do the same in `azdo_emu_scratch_dir()` (`test/helpers/fixture-store.bash`) so the next test to compare a path against runtime output is right by default. A no-op on Linux.
  **Ground:** internal — no Azure DevOps behavior is involved; the sources are the runtime's own `pwd -P` calls and macOS's documented `/var` → `/private/var` symlink. Failure evidence on file: CI run 32604368463, macOS jobs, tests 80/85/95–98/100–102 (C-E06-114..121).
  **Done:** those 9 tests pass on macOS CI; no runtime source changes (the fix is in the harness); Linux stays green.

## E11-S02 — As an engine developer, table-driven conformance suites encode every grounded claim, so regressions name the claim they broke.
- [x] **E11-S02-T01 — Claim-linked test convention & tooling**
  **Do:** test-name convention embedding claim IDs (`[C-E05-014]`); script listing claims ↔ tests coverage (claims without tests reported).
  **Ground:** BACKLOG §3; the tool output itself becomes part of review.
  **Done:** report runs in CI; E06/E02 tables adopt the convention first.
- [x] **E11-S02-T02 — Runtime-project golden harness (L2)** *(done 2026-09-02: `fixtures/golden/MANIFEST.json` pins one `treeDigest` + `stepCount` + `finalYamlSha256` per corpus entry — ten entries, 103 emitted step scripts — computed from the committed `fixtures/oracle/<entry>.final.yml` and never from a local expansion. `packages/emit/test/golden.ts` holds the single emit-and-digest and the freshness gate, shared by the assertions and by `--update` so a golden cannot confirm itself; `scripts/golden.ts` (`pnpm golden [--update]`) is the CLI. **Three design choices are recorded as docs/06 §5 decision 74:** digests rather than a committed tree of scripts (the step snapshots already pin the shape of every emitted kind, and two suites committing the same bytes drift apart rather than reinforce); `--update` **requires** a fresh oracle pair rather than fetching one, because `scripts/corpus.ts`'s header makes `corpus-oracle.ts` the only thing that talks to the service and a second caller would give the corpus two ways to go stale; and the harness runs under vitest rather than bare node, because node cannot resolve the packages' `.js` specifiers to `.ts` and a golden verified against a stale build would go green over a broken emitter. All three Done criteria measured, not asserted: **corpus goldens green** (10/10, and the digest is bound to its expansion three ways — the oracle manifest row, the committed pair's own hash, and the golden row — which catches a re-fetch that was never followed by a regeneration); **mutation fails loudly** — a real one-line emitter change (an extra blank line before the body in `emitStepScript`) was injected and `node scripts/golden.ts` failed naming all ten entries with both digests and the fix, then reverted, and a *no-op* injection was checked first to prove the harness is not simply always red; **update-without-oracle proven to fail** by running the real `updateGoldens` against temporary repo roots — no pair, no manifest row, a hand-edited pair, and a moved-on corpus input each refuse with their own message, and nothing is written when they do. Also: `packages/emit/tsconfig.json` gained `allowImportingTsExtensions` (the harness consumes the corpus catalogue from `scripts/`), and because that flag cannot be scoped to a directory, `test/test-layout.test.ts` gained the guard that no `packages/*/src` file may import a `.ts` path. 59 harness tests; full suite 2670 Vitest + 292 Bats green.)*
  **Do:** snapshot runner: pinned `finalYaml` in → emitted project out; assert deterministic, shellcheck-clean output; update flow gated behind `--update` with mandatory re-fetch of `finalYaml`.
  **Ground:** goldens are only valid when their oracle pair exists (E11-S01-T02 rule); the `--update` path must re-fetch `finalYaml` via the preview endpoint (E00-S04) — a golden without a fresh oracle pair is rejected by the harness.
  **Done:** corpus goldens green; mutation test (emitter bug injection) fails loudly; update-without-oracle path proven to fail.

## E11-S03 — As the project owner, nightly runs detect service drift within a day, so service changes never surprise users.
- [x] **E11-S03-T01 — Nightly re-expansion + convert smoke (L3)** *(done 2026-09-03: `scripts/drift.ts` + `.github/workflows/oracle-nightly.yml` enabled — schedule uncommented, `ORACLE_ENABLED=true` set, the four secrets already provisioned. **Phase A** re-`preview`s every corpus entry and byte-compares against the committed pair; all ten were **stable** against the live service, three weeks after they were fetched, so the corpus evidence is confirmed current. **Phase B** converts each entry and runs the generated project. Both Done criteria met: *first run green* (dispatch-triggered; the first cron firing has not happened yet) and *simulated drift alerts correctly*, proved offline in `test/drift.test.ts` through `preview()`'s `fetchImpl` seam rather than by hand-editing a committed fixture — a one-byte change, a mid-document change, and a change that is *only* the organization name (which must **not** alert) are each asserted. **The phase found a ship-blocking bug on its first real run:** the built `azdo-emu convert` could not convert anything — `cannot locate the runtime library (lib/core.sh)` — because `@azdo-emu/runtime` was not a dependency of `@azdo-emu/cli` and the module-relative fallback was written for the `src/` depth, not `dist/`. Every test in the package imports `../src/…`, so a green suite sat over a broken binary. Fixed, with `packages/cli/test/packaging.test.ts` guarding both facts. Scope call recorded as docs/06 §5 decision 75: exit codes are **recorded, not pinned** (a pinned per-entry code would encode the runner's toolset — no `helm`/`kubectl`/`pwsh` on a GitHub runner — and go red for reasons unrelated to drift); the job fails on drift, on a refusal, on a `convert` failure, and on nothing running clean. Redaction happens **before** the comparison and on everything the job writes, since the report is an uploaded artifact and the organization is real. 20 drift tests + 3 packaging tests; full suite 2693 Vitest + 292 Bats green. **Operational note for the owner:** the PAT was created 2026-08-11 with a 30-day expiry, so it lapses around 2026-09-10 — a lapsed token shows up here as every entry `rejected` with a 302, not a 401 (C-E00-025); rotation steps are in research/oracle-setup.md.)*
  **Do:** enable the E00 CI job: re-expand the corpus against the test org, assert `finalYaml` byte-stability, then convert + run each fixture and compare exit codes; failure artifact = the diff + logs.
  **Ground:** preview endpoint page (pinned in E00-S03); org PAT rotation documented in the runbook.
  **Done:** first scheduled run green; simulated drift (hand-edited fixture) alerts correctly.
- [x] **E11-S03-T02 — Drift triage runbook** *(done 2026-09-03: `research/drift-runbook.md` — classify → fixture-first → claim update, written against the nightly's report shape and reachable from the failure message the job prints. **The grounding is the substance:** the Ground field says a service-change verdict must link the release notes page checked, and the obvious URL is the wrong page. `learn.microsoft.com/azure/devops/release-notes/` canonicalizes to `features-timeline`, the **roadmap** — forward-looking plans, not a changelog (C-E12-025). The record of what shipped is the per-sprint series with its `Azure Pipelines` section (C-E12-026). And the trap worth the whole section: that section announces **features** — sprint 275's three items are a PR-validation comment policy, a new Azure DevOps service connection with `AzureCLI@3`, and Apple Silicon agents — and says nothing about how the preview endpoint expands templates or normalizes a document, so **absence there does not refute a service-change verdict** (C-E12-027); "nothing relevant" is the expected result and the link records that the check was done. Both pages pinned by `git_commit_id`. **Done met — the runbook was exercised on a synthetic drift and produced a real fixture and a real claim update.** The exercise filed `fixtures/corpus/11-implicit-wrapping/` with its live oracle pair: bare `steps:` in, `stage: __default` → `job: Job` → `task: CmdLine@2` out, pinning C-E04-002 — a behaviour every scaffold path depends on that **no corpus entry asserted**, because all ten originals declare their stages and jobs explicitly. C-E04-002 gained a dated addendum recording the live re-verification; C-E04-003 (the `jobs:`-rooted variant) is left transcript-only and recorded as a gap, since one drift produces one fixture. The 11th entry flowed additively into four corpus-driven snapshot suites (0 lines removed). **A measurement defect surfaced and was fixed:** `scripts/claim-coverage.sh` scanned only `packages/*/test`, so repo-level tests citing claims counted as gaps; it now scans `test/` too — **excluding the coverage meta-test by name**, because that file names uncovered claim ids as *data* and counting them would let the report certify itself. Coverage 75% → **76%**, floor raised. 11 runbook tests + 2 new coverage-scan tests; full suite 2725 Vitest + 292 Bats green; all 11 entries stable against the live service.)*
  **Do:** `research/drift-runbook.md`: classify (our bug vs service change), fixture-first fix rule (every drift becomes a permanent fixture), claim updates.
  **Ground:** the classification step requires re-running the oracle (preview endpoint); service-change verdicts must additionally link the Azure DevOps release notes page (verify & pin) checked for the change.
  **Done:** runbook exercised once on a synthetic drift, producing a filed fixture + claim update.

## E11-S04 — As a pipeline developer, the generated projects are proven end-to-end in clean environments, so "works on the author's machine" can't ship.
- [x] **E11-S04-T01 — Docker E2E images & harness (L5)** *(done 2026-09-04. Two images (base + node, layered so they cannot drift), three samples, `pnpm test:e2e`, and a CI job. **The tier found five defects on its first three samples, three of them fixed here** — see `research/experiments/E12-l5-e2e/first-run.md`. (1) `Build.ArtifactStagingDirectory` was never seeded, nor `Build.StagingDirectory`/`Build.BinariesDirectory`/`Common.TestResultsDirectory`, so the commonest idiom in real pipelines reached bash as a command substitution (C-E12-031). (2) The emitter never passed a step's authored `name:` to `run_step`, which had no flag for it — so **every** `##vso[task.setvariable isOutput=true]` in every generated project failed, while the runtime implemented output variables correctly (C-E12-032). (3) `set -euo pipefail` aborted `run.sh` before `azdo_run_summary` and `exit "$(azdo_run_exit_code)"`, so a **failing** pipeline printed no summary and exited with the failing step's raw status instead of the runner's verdict (C-E12-035). **Two more are recorded and filed as E11-S04-T03, not guessed at:** pipeline/stage/job `variables:` are seeded nowhere (C-E12-033, confirmed against the corpus's own `04-variable-layers`), and `publish`/`download` are not native so an artifact task needs a fetched package (C-E12-034). **One is open** (C-E12-036): a `condition: failed()` step ran after a tolerated failure when C-E06-040 says it should not, and the cause is not located — so it is recorded with its evidence rather than pinned in either direction. **What L5 adds over `drift.ts` Phase B is the pinned exit code** (C-E12-029): Phase B only *records* them because a hosted runner's toolset decides them (decision 75), and inside a controlled image the code is a fact about the emitter. Samples live in `fixtures/e2e/`, **not** the oracle-bound corpus, and are template-free so the suite needs no PAT (C-E12-028). The harness converts on the host and runs in the container, which proves the generated project is the dependency-free bash PLAN promises (C-E12-030). Hosted toolset pinned at `cbb8df97e1dd32af7cb23a90590f12734ec11d0b` with the gap documented (C-E12-037).)*
  **Do:** minimal images approximating hosted toolsets (base + dotnet, + node, + docker-in-docker variant); harness: convert fixture pipelines → run → assert artifacts/exit codes/log markers.
  **Ground:** actions/runner-images `images/ubuntu` manifest (pin) as the reference for what "hosted" contains; gaps documented.
  **Done:** 3 sample-app pipelines green in CI containers.
- [x] **E11-S04-T02 — Runtime conformance suite growth (L4 umbrella)** *(done 2026-09-03: the bats suite is tagged and standalone — `pnpm test:runtime` (whole suite) and `pnpm test:conformance` (`--filter-tags conformance`), with every bats flag passing through; **CI now invokes the shipped script instead of re-spelling the `bats` command**, because an entry point only contributors run is unverified — decision 75's lesson, and the reason this task found `test:runtime` did not exist at all. All three Done items met: CI-status badge in the README with a paragraph on what it covers; **measured on every CI leg**, not just locally — bats alone takes 131 s / 132 s (ubuntu node 22/24), 161 s (macOS node 22) and **218 s** (macOS node 24, the slowest), against the 5-minute budget; 135 s locally; and **zero orphans**. **Defining "orphan" was the substance (decision 77).** The gap direction — a claim with no test — cannot be the criterion: `claim-coverage.sh`'s own header marks it deliberately un-zeroable and there are 181. So an orphan is the *other* direction, a test citing a claim `research/` does not define, which is always a defect; it is now a hard `--check` failure with no floor, while gaps keep their ratchet. **The gate found two on its first run:** `doctor-requirements.test.ts` cited `C-E08-042/043`, both inside an *unallocated* block, because `checkToolContract` matches `/C-E\d{2}-\d{3}/` — a **shape** check, not an existence check — so E10-S04-T02's "doctor never invents versions" was only half-enforced. Fixtures repointed to the real `C-E10-002`/`C-E10-009`. **One scope call recorded rather than quietly dropped:** the Ground field's "every test carries its claim ID" was measured at **46 of 292** bats tests citing nothing — argument-count checks, error shapes and harness self-tests with no Azure DevOps behavior to cite. Gating that would buy green with fabricated citations (BACKLOG rule 1), so it is reported, not enforced; `fixture-store.bats` is tagged `harness` and a meta-test asserts it is the **only** file allowed that exemption. The macOS leg already existed (E11-S01-T03) and now runs the new entry point too. `expr-conformance.bats` is generated, so its tag is emitted by the generator rather than hand-added. 18 meta-tests (memoized — the scan is repo-wide and the un-memoized version starved a parallel 5 s end-to-end test); 2,770 Vitest + 292 Bats green.)*
  **Do:** consolidate E06 bats into a tagged conformance suite runnable standalone (`pnpm test:runtime`); macOS leg in CI.
  **Ground:** suite inherits the E06 claim links (every test carries its claim ID per E11-S02-T01); the claims↔tests report is the gate.
  **Done:** suite badge in README; runs < 5 min; claims↔tests report shows zero orphans.

## E11-S05 — As the project owner, selected behaviors are verified against real cloud runs, so runtime claims (not just expansion) are proven.
- [ ] **E11-S04-T03 — Close the three gaps L5 found** *(added 2026-09-04 by E11-S04-T01, which found them by running converted projects rather than by reading code.)*
  **Do:** (a) **seed pipeline/stage/job `variables:` into the generated project** — the expansion keeps them and the emitter drops every one, so `$(anyVariable)` is unresolvable and a `$[ dependencies… ]` job variable evaluates to nothing (C-E12-033); this touches classification, precedence, secret marking and the `.env` interaction, which is why E11-S04-T01 did not attempt it. (b) **make `publish`/`download` runtime-performed** like `checkout`, so an artifact step needs no fetched task package — the runtime already has `azdo_artifact_publish`/`azdo_artifact_download` (C-E12-034). (c) **explain C-E12-036**: a `condition: failed()` step ran after a `continueOnError: true` failure, though the step results record `SucceededWithIssues` and `azdo__job_status_from_results` downgrades only from `Succeeded`.
  **Ground:** (a) and (b) are internal design — docs/04 §8 for variable seeding, docs/04 §12 + docs/03 §6 for step disposition — so the grounding is the existing claims they must not contradict (C-E06-013..017 for `.env` precedence, C-E06-002/005 for output variables). (c) is a *behaviour* question and its answer must cite C-E06-036/037/039/040 or correct them.
  **Done:** the two L5 samples that had to avoid these idioms use them instead — a pipeline `variables:` block read from a script step, and an artifact asserted through `PublishPipelineArtifact@1`; C-E12-036 either becomes a fixed defect with a regression test or a corrected claim.

- [ ] **E11-S05-T01 — Real-run harness (L6)**
  **Do:** scripts to queue a fixture pipeline in the test org, wait, download logs + artifacts (REST), and extract comparable facts (step results sequence, produced variables via a dump step, artifact hashes); comparator vs local run of the same fixture.
  **Ground:** Build/Pipelines REST pages for queue+logs (pin + live samples); this harness is the instrument behind many `VERIFY` items across epics (E02 contexts, E06 macro/env experiments).
  **Done:** one fixture compared end-to-end with report; manual-trigger CI job wired.
- [ ] **E11-S05-T02 — Release gate definition**
  **Do:** document the release checklist: L1–L4 green, L3 nightly green ≥ 3 consecutive days, L5 green, L6 spot-check on majors; encode as a checklist template.
  **Ground:** each checklist line links the CI job/artifact that proves it; the gate is evidence-based by construction.
  **Done:** first release candidate walks the checklist with all evidence links resolving.
