# E05 — Emitter: generated project & scripts

Phase: P2 · Depends on: E04, E06 (runtime contract) · Design: docs/04 §1–§2, §10–§12
Primary grounding set: docs/04 as internal spec · variables doc (env-var name transform) · logging-commands doc · GNU bash manual for emitted-code semantics.

> **Re-scoped by the simplification (docs/07).** The emitter has **no per-task handler registry**.
> Script steps (`script`/`bash`/`pwsh`/`powershell`) are emitted natively; every other task emits a
> dispatch to real-task mode or a stub (E07). The weighted coverage report is dropped — the generated
> README carries a plain warnings/unsupported list (PLAN D10).

## E05-S01 — As a pipeline developer, `convert` writes a navigable project mirroring my pipeline, so I can find any step by eye.
Acceptance: output tree per docs/04 §1 generated deterministically.

- [x] **E05-S01-T01 — Project scaffolder** *(done 2026-08-24: `packages/emit/src/scaffold.ts` — `scaffold(pipeline) → Scaffold`, a pure deterministic plan of the `stages/` subtree (docs/04 §1) plus `GITIGNORE`, owning the three things the tree layout is made of and nothing else: `NNN-` numbering (1-based ordinal ×10, zero-padded, gaps left for hand-inserted steps), the fs-safe `slugify` (lower-case, non-`[a-z0-9]` runs → `-`, trim) with a `displayName → id → kind-token` fallback, and collision handling (`uniquify` → `-2`/`-3`/…). Jobs are numbered by *group* (`referenceName`) so the legs of one matrix/`parallel:` job share a number and differ only by `__<suffix>` (docs/04 §1's `010-BuildJob__linux_x64/`); a runOnce deployment job flattens its lifecycle hooks into one `steps/` dir with the hook recorded per step (C-E04-146), and `rolling`/`canary` carry none. A shorthand-desugared step (`checkout`/`download`/`publish`, C-E04-031) names its file from `origin` + distinguishing input (`010-checkout-self.sh`) rather than the defaulted GUID displayName, unless the author wrote one. **The rules are ours, not ADO's** — the service has no filesystem to copy, so nothing here is grounded in a primary source; docs/06 §5 decision **60** records the four rules, and the "validated against real pipelines" half of the Ground field is met by snapshotting the whole `stages/` tree of all ten corpus entries built from the captured `final.yml`s. Done met: `scaffold.test.ts` (22) snapshots the golden tree over the corpus and asserts determinism (`scaffold(p) === scaffold(p)`, the T01-level form of "re-convert produces zero diff"); emit 25 tests green, typecheck/eslint/prettier clean. `@azdo-emu/engine` added as the emit package's first dependency.)*
  **Do:** `packages/emit/src/scaffold.ts`: tree creation, `NNN-` numbering with gaps, slug rules (displayName → fs-safe), collision handling, `.gitignore`, deterministic ordering (stable across runs for clean diffs).
  **Ground:** docs/04 §1 tree as spec; slug edge cases validated against real pipelines in the corpus.
  **Done:** golden-tree tests on corpus; re-convert produces zero diff.
- [x] **E05-S01-T02 — Step script emission** *(done 2026-08-24: `packages/emit/src/step.ts` — `emitStepScript(step, number)` returns the full `.sh` file: the docs/04 §12 header block (shebang, `Step NNN · "display" · kind` rule line, `from: file:line` provenance, `condition/continueOnError/timeout` line, `fidelity` line, an optional macro NOTE, and any `# warning:` lines) plus `set -euo pipefail` and `source "$AZDO_EMU_LIB/runtime.sh"` (with the `SC1091` directive). **Native** (`script`/`bash`/`pwsh`/`powershell`, classified by the task name before `@` — `CmdLine`/`Bash`/`PowerShell` — with `pwsh` vs `powershell` from the `pwsh:` input, C-E04-037): `script`/`bash` bodies are the authored `inputs.script` **verbatim, macros intact** (C-E06-018/024); `pwsh`/`powershell` run through a quoted heredoc `pwsh -NoLogo -NoProfile -Command -` that reproduces `errorActionPreference` → `$ErrorActionPreference` (docs/03 §75, degraded fidelity). **Everything else** (incl. the desugared `checkout`/`download`/`publish` GUIDs) emits a **stub** that logs the task and dumps its resolved inputs — the `run_task.sh` dispatch and native checkout/artifact dispatch are E07-S03-T01's disposition work. The header's `Step NNN` matches the scaffolder's `NNN-` prefix (`ScaffoldStep` gained a `number` field, and the header's displayName reuses the scaffolder's `originStepLabel` so a defaulted-GUID shorthand reads `checkout-self`, not the GUID). **The finding that needed a decision:** "macros left intact" and "pass shellcheck" conflict on the corpus — `$( )` macros are shellcheck command-substitution false positives (`SC2005` for `echo "$(cmd)"`, `SC2046` for unquoted `$(…)`), so the emitted-project gate excludes exactly those two; docs/06 §5 decision **61** records why. Done met: `step.test.ts` (10) snapshots every native kind + the stub and runs shellcheck over **all emitted corpus scripts** with zero findings beyond the two macro excludes; emit 35 tests green, typecheck/eslint/prettier clean.)*
  **Do:** script steps emit a header block (displayName, provenance, condition source, fidelity label, warnings) + body with `$( )` macros left intact + `set -euo pipefail` policy + `source runtime.sh` preamble. Non-script tasks emit a `run_task.sh` dispatch (E07) or a stub-dump body.
  **Ground:** docs/04 §12 sample as spec; macro-preservation requirement traced to the macro claim (E06-S02-T01).
  **Done:** emitted corpus scripts pass shellcheck; headers snapshot-tested.
- [x] **E05-S01-T03 — `run-job.sh` / `run-stage.sh` / `run.sh` emission** *(done 2026-08-24: `packages/emit/src/entrypoints.ts` — `emitEntrypoints()` emits `run.sh`, `run-stage.sh`, `run-job.sh` and `conditions.sh`. **`conditions.sh`** (per stage) compiles the stage/job/step conditions through the E02-S05 compiler (`compileBash` over `parseExpression(…, registryForSlot(slot))`) into `cond_stage`/`cond_job_<slug>`/`cond_step_<NNN>` functions; absent → `azdo_status_succeeded`. **`run-job.sh`** sets the per-job scope/`AZDO_LOG_DIR`/`AZDO_RESULT_DIR`/`AZDO_OUTPUT_DIR`/`AZDO_HAS_MULTIPLE_CHECKOUTS`, seeds the five predefined dir vars, and runs each step via `run_step --id --file --cond --display --wd --continue-on-error --fail-on-stderr --retries --timeout`, with the `--from-step`/`--to-step`/`--only-step`/`--no-condition` range filter as an `if` guard (not `continue` — steps are flat). **`run-stage.sh`** runs jobs in E04-S03-T02 topological order (stable Kahn over `resolveJobGraph`). **`run.sh`** sets up `.work/run-<N>` with a monotonic counter, loads `.env`, and runs stages in `resolveStageGraph` order, plus `--list`/`--env-file`/`--resume`. **The layout/flags/seeding are ours** (the agent has no shell entry points); docs/06 §5 decision **62** records the four choices (run layout, scope/output naming, flag surface, the shellcheck `SC2016`/`SC2071` excludes that extend decision 61). Done met: `entrypoints.test.ts` (8) snapshots each entry point, runs shellcheck over the whole generated `run.sh` family + step scripts (zero findings beyond the documented excludes), and **actually runs a generated project under bash** — full run (`Result: Succeeded` + step log asserted), `--only-step`, `--from-step`/`--to-step` range, and a `--list` snapshot. 45 emit tests green, coverage 100% statements/lines/functions, typecheck/eslint/prettier/VERIFY-guard clean.)*
  **Do:** per-job sequencer invoking `run_step` with metadata args (docs/04 §5 signature); per-job `conditions.sh` from the E02-S05 compiler; root orchestrator with topological order from E04 graphs; flags per docs/04 §2 (`--from-step --to-step --only-step --resume --no-condition --list`).
  **Ground:** docs/04 §2–§3 as spec; ordering-semantics claims from E04-S03-T02 (cite IDs).
  **Done:** bats E2E on a fixture project: full run, partial run, `--only-step`, `--list` output snapshot.

## E05-S02 — As a pipeline developer, everything unresolvable lands in a documented `.env.example`, so I know exactly what to fill and why.
Acceptance: synthesis per docs/04 §10 with per-entry provenance comments.

- [x] **E05-S02-T01 — `.env.example` synthesizer** *(done 2026-08-24: `packages/emit/src/env-example.ts` — `synthesizeEnvExample(pipeline)` returns `{content, manifestEnv}`. Sections per docs/04 §10 with one recorded ordering choice (docs/06 §5 decision **63**): run identity overrides → `SYSTEM_ACCESSTOKEN` → **Pipeline variables (unresolved)** (`env-required` from E04-S02-T02's `classifyVariables`, each with a `# used by <stage>/<job>/step <ordinal> (macro in input|env)` provenance comment) → variable groups (`group-member`, names-only with `# from group '<name>'`, PLAN D7) → runtime parameters (prefilled with defaults) → a placeholder for E08's service connections/secure files. The `.env` spelling is the runtime's `azdo__env_name` transform (`.`/space → `_`, upper); `SYSTEM_ACCESSTOKEN` is the single secret `manifestEnv` entry (`secret: true` for masking) and its minting note cites the Entra resource `499b84ac-1321-427f-aa17-267ca6975798` (C-E00-011). Done met: `env-example.test.ts` (4) snapshots the synthesized `.env.example` for all ten corpus pipelines and asserts the lint invariant structurally — every `NAME=` line is preceded by a `#` provenance comment. 49 emit tests green, coverage 100% statements/lines/functions, typecheck/eslint/prettier clean.)*
  **Do:** sections in docs/04 §10 order; entries fed by E04 classification + task env requirements; provenance comments ("used by <stage/job/step>", "from group '<name>'"); secret flags to manifest for masking.
  **Ground:** docs/04 §10; `SYSTEM_ACCESSTOKEN` filling instructions grounded in the OAuth/PAT docs (verify the Entra resource GUID from the official page).
  **Done:** corpus `.env.example` snapshots; lint: no entry without a provenance comment.
- [ ] **E05-S02-T02 — Generated README + warnings report**
  **Do:** README generator: conversion summary, per-step fidelity labels, warnings/unsupported list, `.env` how-to, run instructions, tool prereqs (from manifest).
  **Ground:** docs/04 §12; content assembled only from manifest data (no free text at emit time).
  **Done:** README snapshot for corpus; broken-link check over generated links.

## E05-S03 — As a pipeline developer, run-number and identity variables behave like the service, so `Build.BuildNumber`-dependent logic works.
Acceptance: `name:` format evaluation + counters local semantics.

- [ ] **E05-S03-T01 — Run-number formatter**
  **Do:** `name:` format evaluation at run start (emitted into runtime init): `$(Date:yyyyMMdd)`, `$(Rev:.r)`, variable tokens, literal text; `Build.BuildId` monotonic local counter.
  **Ground:** run-number doc (…/process/run-number) — quote token table incl. `Rev` reset semantics; local deviation (Rev per local state) documented as a delta claim.
  **Done:** formatter tests per token; runtime integration test shows `Build.BuildNumber` set before first step.
